{
  "name": "@thalon/db",
  "version": "0.1.0",
  "private": true,
  "description": "Data layer: drizzle schema, forward-only migrations, tenant-scoped repositories (SPINE §2.5–2.6). Raw DB handles never leave this package — the repos are the API.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "generate": "drizzle-kit generate",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.3.14",
    "@thalon/contracts": "*",
    "@thalon/platform": "*",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.6"
  }
}
