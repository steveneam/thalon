{
  "name": "@thalon/platform",
  "version": "0.1.0",
  "private": true,
  "description": "Dev→prod seams: env config (the only place process.env is read), embedded-Postgres db client, object store, queue, AI gateway, auth (SPINE §2.2).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/gateway": "^1.0.41",
    "@aws-sdk/client-s3": "^3.1095.0",
    "@electric-sql/pglite": "^0.3.14",
    "ai": "^5.0.209",
    "zod": "^4.1.0"
  }
}
