# Thalon build charter

> **Approved by the founder 2026-07-02 23:59 +10:00.** This is the dependency-ordered bucket list the build follows. One bucket at a time; every bucket ends at a checkpoint requiring founder review before the next begins. Amendments to this charter require founder approval.

## Goal

A **standalone, generic, multi-tenant content/social-automation engine**: one sentence or source in → fan-out into platform-native drafts → every draft passes the shared judge harness (G1 denylist + G3 grounding-to-provided-sources) → human **Approve** gate → (Sprint 3+ only) publish via official platform APIs. Brand/voice, grounding sources, and compliance rules are **per-tenant runtime config, never code**. The repo ships a generic self/demo tenant only and contains zero forbidden upstream brand tokens in any tracked file (enforced by `scripts/ci-grep-guard.ps1` in CI).

## Ratified decisions (founder interview, 2026-07-02)

1. **AWS isolation:** Thalon gets its **own AWS sub-account** (AWS Organizations). Clean blast-radius, own billing line; OIDC deploy role + `cdk bootstrap` land inside it. Region `ap-southeast-2`.
2. **G3 grounding judge:** **two-tier** — a cheap model pre-screens every draft; a **stronger model (per-call gateway routing) is the final gate**; any disagreement → block + queue for the operator. Never uniform-cheap on the safety gate.
3. **Demo tenants:** tenant #0 = **Thalon-markets-Thalon** (dogfood from Sprint 1); one **fictional generic tenant #2** lands in Sprint 2 purely as runtime config to prove config-not-code.
4. **Operational mirror:** stood up at **Sprint 0** (external documentation agent's job; build agents in this repo never write outside it).
5. **Eval gate:** `edit_diff`/override capture wired **before the first draft ever generates**; the green-suite ship-gate **arms at Sprint-1 exit**.
6. **First platform targets:** **LinkedIn + X.** These shape the Sprint-1 fan-out profiles now and which OAuth apps/audits are filed first in Sprint 3.

## Standing discipline (every bucket)

- Checkpoint = founder review before the next bucket starts.
- `scripts/ci-grep-guard.ps1` passes before every commit; the same guard is a required CI check.
- Tests ship with the code they verify; small, verifiable steps.
- Every founder override/correction becomes one eval row in the same change.
- **No publish path is wired anywhere in Sprints 0–2.** A fan-out can never emit an ungated draft.
- Dev runs on local seams (embedded Postgres/PGlite · local object store · inline queue); cloud stacks land only when a bucket needs them.

## Buckets

### Sprint 0 — foundation

| Bucket | Deliverable | Depends on | Founder-supplied |
|---|---|---|---|
| **B0.1 — Remote + CI guard** (first action) | Private GitHub repo created from this directory; push; GitHub Actions workflow running the grep guard as a **required check**; branch protection on `main`. | — | `gh` auth |
| **B0.2 — App skeleton, dev-seamed** | Next.js (App Router, TypeScript) + shadcn/ui; health endpoint; smoke test; `.env.example` (placeholders only); dev = SQLite + local object store + inline queue behind `sqlite→aurora`, `local→s3`, `inline→sqs` seams; Clerk dev-seamed; own AI-gateway wiring (`ai` ≥5.0.36 + `@ai-sdk/gateway`, v5 `createGateway`, per-call routing). | B0.1 | `AI_GATEWAY_API_KEY` (Clerk keys may trail) |
| **B0.3 — Schema table 1 + grounding index** | Multi-tenant schema: `tenants` · `brand_profiles` · `sources` · `source_chunks` · `drafts` · `judge_results` · `approvals` · `publish_queue` · `eval_cases`/`edit_diffs` **+ `fanout_runs` · `events` · `usage_ledger` (amendment A2)**. `tenant_id` on every row; composite hot-path indexes (`(tenant_id,status)`, `(tenant_id,platform,scheduled_at)`, `(tenant_id,created_at)`); grounding index behind the seam (**dev = embedded Postgres/PGlite with pgvector — amendment A1**; prod = Aurora Postgres pgvector HNSW); content-addressed caches (LLM-generation keyed on prompt+model+params+input-hash; grounding-retrieval keyed on source-set-hash+query-hash); per-tenant daily budget caps enforced in the gateway wrapper; **package extraction + boundary/tenancy/state-machine ratchets (amendment A3)**; draft lifecycle state machine per `docs/SPINE.md` §1.1. | B0.2 | — |
| **B0.4 — Eval scaffold** (before any draft exists) | $0/self-hostable eval stack (Langfuse self-host + promptfoo + DeepEval — MIT/Apache only); `edit_diff`/override capture wired end-to-end and proven by test; golden-set seed file. | B0.3 | Langfuse keys/host |
| **B0.5 — AWS bootstrap (own sub-account)** | Organizations sub-account; GitHub-OIDC deploy role (no static keys); `cdk bootstrap` ap-southeast-2; CDK-Python skeleton with Thalon-named stacks. Aurora/S3 stacks land only when a bucket needs them. | B0.1 | AWS sub-account + one-time OIDC/bootstrap; `AWS_DEPLOY_ROLE_ARN` |

### Sprint 1 — the safe vertical slice (no publish)

| Bucket | Deliverable | Depends on |
|---|---|---|
| **B1.1 — source-ingest** | URL / prompt / dropped doc → `sources` + chunked/embedded `source_chunks`. | B0.3–B0.4 |
| **B1.2 — fan-out** | One source → N drafts from tenant #0's runtime config; LinkedIn + X niche profiles as **data, not code**. | B1.1 |
| **B1.3 — judge harness** (`proprietary/judge/`) | G1 denylist + G3 grounding-to-provided-sources; **two-tier** per ratified decision 2; structurally impossible for an ungated draft to reach the queue — **test-proven**. | B1.2 |
| **B1.4 — Approve queue** | 3-zone layout (feed → per-platform fan-out grid → approve panel with per-variant judge badge); `edit_diff` captured on every touch → eval rows; batch-approve **schema** now, UX later. | B1.3 |
| **B1.5 — Sprint-1 exit gate** | Dogfood on Thalon-markets-Thalon; green eval suite **arms as the ship gate**; exit criteria below verified. | B1.4 |

### Sprint 2 — seam proof + content intelligence (amendment A5)

| Bucket | Deliverable | Depends on |
|---|---|---|
| **B2.1 — Fictional tenant #2** | Added purely as runtime config/data. Exit criterion: **zero code changes**. | B1.5 |
| **B2.2 — Timed ingest + contract window** | `source_chunks.start_ms/end_ms`; `sources.kind` += `video_transcript\|exemplar\|voice_sample\|site_crawl`; `sources.modality` + `visual_ref` (visual-tier seam); generic per-source metrics; `drafts.format` += `clip_plan\|demo_plan`; unique `sources (tenant_id, content_hash)`. Caption-file ingest (SRT/VTT/plain) as deterministic core; transcript fetch behind one `TranscriptProvider` seam (**in-house**: self-hosted Whisper is the strategic driver; hosted APIs = optional adapters). **The sprint's only contract window** — contract re-freezes at merge. | B2.1 |
| **B2.3 — Waterfall clip plans** | Pillar transcript → deterministic candidate windows (core) → highlight-select (shell) → `clip_plan` drafts (start/end + hook + captions + platform copy), G3-grounded to the transcript, same Approve queue. No rendering (stays B3.3/B3.4). | B2.2 |
| **B2.4 — Exemplar library** | `exemplar`/`voice_sample` sources + generic metrics → grounding index → top-k retrieval into fan-out context; exemplar ids recorded per run (provenance). **Invariants:** exemplars are grounding-only — deterministic overlap gate blocks verbatim reuse; PII stripped at ingest. | B2.2 |
| **B2.5 — Demo-plan slice** | Front half of B3.4: site crawl (robots.txt + rate limits in core) → flow map → storyboard draft judged against the crawl → approved plan → deterministic Playwright drive → raw capture (video + synthetic cursor + event trace, content-addressed). Fails loudly at capture before any render spend. No composition/render. | B2.2 |
| **B2.6 — Queue UI for new formats** | Approve-queue rendering for clip plans / exemplars / demo plans (vs contract mocks); closes queued UI follow-ups (panel refresh after save-edit, aborted-run rows, operator re-judge action). | B2.2 |

### Sprint 3 — origination + profiles (amendment A6)

> The engine's missing first stage: the operator's content does not exist yet — Thalon generates it. Prompt + active profile (+ optional site/GitHub) in → pillar video **with caption/SRT** out; the waterfall then closes on Thalon's own output. Decision record: `docs/adr/0003-sprint3-origination.md`.

| Bucket | Deliverable | Depends on |
|---|---|---|
| **B3.8 — Profile spine (lean)** | Rich per-tenant profile data shape on the existing `tenants`/`brand_profiles` spine (company facts, brand voice, philosophy, audience, offers, links) — **data first; editor UI deferred to B3.11**. Seed demo tenant + fictional tenant #2 in-repo; founder's real companies enter as runtime data ([you]). Every generation resolves the active profile automatically — the operator never re-types company context. | Sprint 2 |
| **B3.9 — Pillar origination** | Prompt + active profile + optional site/GitHub crawl (reuses B2.5 crawl core; adds GitHub ingest) → new `pillar_script` draft format (beats, narration, on-screen text, timing) → G1+G3 judged against crawl + profile sources → same Approve queue. Topic/hook selection grounded by the trend/viral research doc + operator-dropped exemplars (B2.4). | B3.8 |
| **B3.10 — Remotion render seam** | Approved script → Remotion composition (brand styling from profile) → MP4 + **deterministic SRT from the authored script** (no ASR for own content); TTS behind a seam (first cut may be on-screen text + music); content-addressed render cache. Remotion licence = **growth gate** (free for ≤3-person companies incl. for-profit; swap path behind the seam) — downgrades A5's launch-gate note. | B3.9 |
| **B3.11 — Loop closure + profile UX** | B2.3 dogfood on the first **generated** pillar's SRT → clip plans (Sprint-2 exit review completes here; its B2.5 half — pinned docs-search demo plan — runs as sprint opener). Profile editor/switcher UI, shaped by B3.9/B3.10 usage. Overrides → eval rows; green suite gates sprint exit. | B3.10 |
| **B3.12 — Trend-intel intake (amendment A7: promoted from pull-trigger — founder direction: the operator never curates trends manually; finding them IS the product's feature 2)** | Per-tenant watchlists (accounts/queries/niches, runtime config) → official-API pollers behind one `TrendSource` driver seam (YouTube Data API first — free quota; AT Protocol/Bluesky next — open; X API v2 when its paid tier is funded) → timestamped engagement snapshots into the existing generic `source_metrics` → **deterministic outlier detection in core** (velocity vs account baseline · share-to-view · bookmark-efficiency — the operator blueprint's ratio formulas as tested math, never model vibes) → outliers auto-ingest as exemplars (PII-stripped, denylist-screened) feeding the existing B2.4 retrieval into every generation. Extraction tiers, honestly bounded: text platforms = full text via official APIs; video platforms = title/description/tags/stats + thumbnail (vision tier arrives with B3.7); spoken-hook transcripts only via permitted paths (owner-authorized captions, licensed sources, operator-supplied files) — **never audio ripping or proxy-cluster scraping** (A5/ADR-0002 rejection stands; the blueprint's §6–7 scraper architecture is explicitly not built). Disjoint from B3.8–B3.11 — parallel-lane candidate. | B2.4 |
| **B3.13 — Transcription engine (amendment A8: promoted from pulled — founder direction: it feeds BOTH core features)** | The full transcription capability behind the existing B2.2 `TranscriptProvider` seam, with a video-URL ingest surface (paste a URL → timed `video_transcript` source, the kind that has existed since B2.2). Two drivers: **(1) self-hosted Whisper** (whisper.cpp / faster-whisper, MIT, word timestamps) for operator-owned/permitted media → feeds B2.3 waterfall of the operator's own videos; **(2) hosted third-party-transcript adapter** (commercial transcript API, keyed runtime config with a swap path — the vendor absorbs platform-ToS risk, exactly the pattern A5 ratified as "hosted APIs = optional adapters") for third-party videos → feeds B3.12 spoken-hook extraction from trending videos, with timestamps for hook-vs-body mapping per the operator blueprint. The engine itself never rips captions/audio from platform endpoints. | B2.2 seam |

Standing dogfood flywheel: origination runs across all three tenants → eval rows; trend intel is B3.12's job (manual exemplar drops stay possible as an interim convenience, never the product). Parallel [you] track: LinkedIn + X OAuth developer apps (B3.1 long pole) · gateway credit top-up (judge tier needed this sprint) · profile content for the two real companies (runtime data — the first company's name is a guarded token, so its config lives only in gitignored/vault space).

### Sprint 4 — hardening pass (amendment A10; build pass 2 of 3)

> Pass 1 (A9 breadth) materialized the spine; this pass makes it robust and consistent before pass 3 adds the live "meat and gravy". **Scope rule (ratified):** harden only what pass 1 proved by repetition (3+ instances), store only what the skeletons already compute, install tooling — all NEW features wait for pass 3; zero live spend. **Standing invariant for every refactor here:** generation keys and content hashes stay byte-stable, test-pinned BEFORE code moves (idempotency + every content-addressed cache depend on them).

| Bucket | Deliverable | Depends on |
|---|---|---|
| **B4.1 — One origination engine** | Extract the shared single-draft spine (origination / webpage / storyboard are near-clones): one deterministic core parameterized by format config; ONE bounded shell repair loop (3 copies today); one post-approval artifact-stage helper behind the render/deploy/capture trio (approved-only gate → seam driver → content-addressed artifact → status/ref meta patch, optimistic-concurrency). Behavior-preserving. | key-stability pins |
| **B4.2 — Format contract registry** | One declared registry in contracts: per-format pinned meta schema + capabilities (renderable/deployable/capturable, artifact-ref fields, judged-body derivation). Scattered `format === "x"` branches route through it. Ratchet: property test that every persisted draft's meta parses against its registered schema. | B4.1 |
| **B4.3 — Trend durable storage** | `watchlists` table (tenant-scoped runtime config) + dedicated **`trend_snapshots` intermediate table** (ratified) so ALL watched items accrue timestamped engagement history without polluting `sources`; longitudinal outlier math (Δ-velocity between snapshots vs the account's stored baseline) as pure tested math; hot-path indexes. | — |
| **B4.4 — Audit + metering coverage ratchets** | Events-coverage test: every state-changing write emits an `events` row (close gaps — `updateMeta`, render/deploy/capture stages, trend ingests); boundary test that no module reaches `getGateway` outside `withGatewayGuard`. | B4.1 |
| **B4.5 — Error taxonomy** | Shared error classes (NotFound / InvalidState / ArtifactMissing / …) + consistent loud-failure conventions; run rows record `lastError` for operator triage. | B4.1 |
| **B4.6 — Object-store hygiene** | One key-scheme helper (three conventions exist today); content-address verification on read; orphan-sweep script + documented GC stance. | — |
| **B4.7 — Toolchain doctor** | Install the full pass-3 toolchain now (ratified; all free/keyless): Remotion (licence = growth gate, recorded) · whisper.cpp/faster-whisper + model · ffmpeg · Vercel CLI. Executable ratchet: `npm run doctor` reports per-seam live-capability (fake-only vs live-ready). | — |
| **B4.8 — Transcription thin (B3.13 pass-2 cut)** | Video-URL ingest surface + `TranscriptProvider` driver registry (env-selected); Whisper driver + hosted-vendor adapter as seam skeletons with fakes; optional $0 local Whisper smoke on operator-owned media. Live transcription runs = pass 3. | B4.7 |
| **B4.9 — CI ratchet** | Promote the existing CI `test` + `eval-gate` jobs to **required** branch-protection checks beside `ci-grep-guard` (ratified); monthly documented-command execution pass. | — |

Parallel-lane candidates (disjoint file sets): B4.3 · B4.6 · B4.7 are independent of the B4.1→B4.2 chain. Deliberately OUT of this pass: S3 object-store driver (no local consumer yet), any publish path, UI format panels (pass 3), speculative abstractions for pulled buckets (B3.14 BYOK, B3.7 visual tier — one instance each, no repetition proof).

### Sprint 5 — deterministic video creation (amendment A11; build pass 2.5)

> Founder direction at the Sprint-4 exit checkpoint (2026-07-05): before live verification, the video family gets its real creation model — staged, interactive, and **deterministic-first** ("a good video never gets generated in one prompt"; the engine as code-driven/schema-structured as possible, AI freed for edge cases/elaboration/creativity — SPINE §1 doctrine applied and pinned). Ratified decisions: **(1) Hyperframes** (HeyGen's Apache-2.0 HTML→video framework) becomes the **default** video render driver — Remotion demoted to the swap-path alternative behind the same seam (its growth gate is thereby retired); **(2) direction.md is strict-schema markdown** (front-matter + pinned per-scene sections, byte-identical round-trip), never freeform; **(3) the staged-flow UI ships in this pass** so the 3-prompt experience is felt on fake drivers before any live spend. Discipline unchanged from pass 2: zero live spend (fake/keyless drivers; a local render smoke is $0), generation keys and content hashes byte-stable test-pinned, scope limited to this video-creation surface — everything else stays pass 3.

| Bucket | Deliverable | Depends on |
|---|---|---|
| **B5.1 — Hyperframes render driver** | New `RenderTarget` implementation behind the existing B3.10 seam: render manifest → deterministic HTML composition (template + data; brand styling from the active profile) → Hyperframes engine (headless chromium + ffmpeg, both already toolchain) → MP4 + artifacts under the existing content-addressed render cache. Version-pinned (pre-1.0); env-selected default with Remotion as the recorded swap path (decision record `docs/adr/0004-render-driver-default.md`; licence ledger updated — Apache 2.0, no gate); `npm run doctor` gains the per-seam check; optional $0 local render smoke on the demo tenant. | — |
| **B5.2 — Staged video pipeline** | Stage registry in contracts (per-format **ordered stage list — count is config, default 3**: structure → scenes/effects → polish; export is deterministic core, never a prompt): new pinned formats/schemas for `storyboard` and `direction_doc`; deterministic direction.md renderer/parser (front-matter + per-scene sections) with a **byte-identical round-trip ratchet**; prefill deterministic-first (aspect/pacing/brand from profile + platform config), AI fills creative slots only; **one-prompt mode = the same stages auto-advanced on prefill defaults** — one code path, two modes; every stage output judged (G1+G3) before it advances; stage prompt chains live in `proprietary/prompts/`. `pillar_script` keys stay byte-stable — staging lands as new formats around it. **The sprint's only contract window.** | — |
| **B5.3 — Shell-inventory ratchet** | SPINE §1's documentary shell inventory promoted to an executable pin: a test over the `withGatewayGuard` operation set (six sites today) so any new LLM call-site is a deliberate, review-visible addition; SPINE §1 list refreshed; audit of the existing shell sites for computable leakage (whatever can be computed moves to core). | — |
| **B5.4 — Staged-flow UI** | The advanced-mode experience on fake drivers, in the approve-queue surface: storyboard cards (reorder/accept/tweak), pick-from-2-3-candidates per stage, per-beat accept/tweak chips, style presets from the profile, Hyperframes low-res preview per stage, direction editor (form view + raw strict-md view, same schema). Never a chain of blank prompt boxes — the operator reacts to visible artifacts. Every interaction → `edit_diff` → eval row (advanced-mode telemetry is what trains one-prompt mode's defaults). | B5.2 contract |

**Pass 3 — live verification + meat (was "pass 2" in A9):** *superseded by amendment A12 — pass 3 is chartered as Sprint 6 below (same substance, founder-shaped 2026-07-06: Thalon-first dogfood, frontend first, intel v2; decision record `docs/adr/0005-pass3-recharter.md`).*

### Sprint 6 — live verification + frontend (amendment A12; build pass 3)

> Founder direction at the pass-3 checkpoint (2026-07-06 planning session; shape delegated to lead, ratified same day). Decision record: `docs/adr/0005-pass3-recharter.md` · frontend design home: `docs/FRONTEND.md`. Ratified: **(1) Thalon-first dogfood** — tenant #0 (Thalon-markets-Thalon, ratified decision 3) gets the real deep profile, pillar #1 is a Thalon video, the landing page is Thalon's own web presence; the founder's first company onboards later as a pure runtime-config tenant proof; **(2) frontend first, hand-built shell, dogfooded content** — landing copy becomes B3.15 grounding, demo clips are real Hyperframes renders; **(3) intel v2** — operator-described monitored **areas** (the un-built B3.12 "niches" slot) + deterministic two-stage ranking (candidate generation → EdgeRank-shaped config-weighted score: embedding relevance × engagement ratios × velocity/freshness), never neural machinery; **(4) transcript live drivers elevated** as a learning/dogfood input channel. Pollers are quota-shaped from day one (per-driver budgets as config: YouTube `search.list` ≈100/day own-bucket since June 2026, `videos.batchGetStats` cheap; Bluesky keyless/abundant). **One opening contract window** (lead terminal, before any lane): `waitlist` + monitored-areas + search-intel tables + the format-registry SEO-meta capability — all additive; contract re-freezes at merge (A13, superseding A12's two-window note). **No publish path in Sprint 6** — B3.1 stays pulled; the OAuth apps remain a parallel [you] paperwork track. Lane plan: `COORDINATION.md` (Sprint 6 — launches require fresh founder go per protocol).

| Bucket | Deliverable | Depends on |
|---|---|---|
| **B6.1 — Brand + landing page** | Design tokens (dark-first, shared landing↔workspace); falcon/talon mark concepts (founder picks); landing `/` per `docs/FRONTEND.md` §2 — hero + waitlist (additive `waitlist` table + rate-limited API; queue position + referral), 3-feature side-scroll with popout demo clips (placeholder loops until B6.3), early-access pricing/CTA (tiers [you], non-blocking), objection-driven FAQ; landing copy written against a **manual B6.8-style keyword pass** (dogfooded content; honest-claims rule per ADR 0006); static-first performance budget. | contract window |
| **B6.2 — Workspace shell + dashboard** | `/app/*` shell: sidebar + top bar (tenant/profile switcher, needs-you badge); Cmd-K palette + dashboard omnibox; dashboard v1 (pulse counts · needs-you · quick actions · `events` activity feed · seam-status card); approve queue + staged flow restyled into the shell; keyboard triage + batch approve; first-run guided flow + empty-states-as-tutorials on fake drivers; profile editor/switcher UI (from B3.11). | B6.1 tokens |
| **B6.3 — Self-tenant deep profile + demo content** | Tenant #0 gets the real Thalon identity/voice/topics (tracked — the self tenant carries no guarded tokens); pillar-#1 topic locked (Thalon itself); feature-demo clips rendered via the live Hyperframes driver, replacing the landing placeholders ("rendered by Thalon" captions). | B6.1 |
| **B6.4 — Intel v2: monitored areas + ranker** | Per-tenant areas config (name + free-text description, durable); deterministic two-stage pipeline per ADR 0005: area→query expansion in core + config-weighted multi-signal score (embedding relevance vs area description · existing engagement ratios · velocity/freshness), reason strings preserved; area-tagged exemplar ingest; intel workspace surface (trend cards with %-badges + reasons; *generate from this* / dismiss → eval row) proven on the fake TrendSource. | contract window · B6.2 shell (UI) |
| **B6.5 — Live drivers wave 1** | YouTube + Bluesky `TrendSource` drivers (official APIs, per-driver quota budgets as config); transcript live drivers — local Whisper (owned/permitted media) + hosted vendor adapter (thin per-vendor response mapping) — with a video-URL ingest surface in the workspace; gateway dogfood for the judge tier. [you]: transcript-vendor key · gateway top-up. | B6.4 seams |
| **B6.6 — Origination live loop** | Pillar #1 generated end-to-end: `compositionSpecFromDirectionExport` wired into a live direction_doc→render path; `/api/staged/*` swapped onto `startVideoStages`/`advanceVideoStage` + the real judge lane; `approvals.record` accepts caller-supplied `{kind, patch}`; B2.5 pinned docs-search chromium drive (the sprint-opener item); B2.3 waterfall dogfood on the generated pillar's SRT; approve-queue format panels (`pillar_script`/`direction_doc`/`web_page`). Closes B3.11 + the Sprint-5 carried follow-ups. | B6.3 · B6.5 gateway |
| **B6.7 — Deploy + exit gate** | *(re-chartered 2026-07-08, amendment A14 / ADR 0007: VPS-first, Vercel = swap path)* Container deploy to the portfolio's self-managed VPS — prep first (Dockerfile w/ `output:"standalone"` · CI image build → GHCR, never local · `next build` CI job · **workspace auth gate before anything public** · app-level DB dump hook), deploy at box-ready (landing live once the domain cuts over [you]); site verification arms the B6.8 GSC flywheel; revalidate-on-publish at the own-site door; hyperframes temp-jobDir cleanup follow-up; eval-row refinement + exit reviews across all three families; green suite as the sprint-exit gate. | all |
| **B6.8 — Search intel v1 (SEO/AEO/GEO, amendment A13)** | `search_targets` + `search_snapshots` (mirrors of the proven intel tables; schema in the opening contract window); profile-seeded keyword compilation (deterministic expansion in core + judged AI expansion, profile-grounded); on-page optimization pack — deterministic core checks (meta lengths · question-shaped headings · JSON-LD `Organization`/`FAQPage`/`Product`/`VideoObject` · `llms.txt`) + an SEO/AEO quality lens in the judge; format-registry SEO-meta capability (`web_page` + video formats); `SearchIntelSource` driver seam — fake driver first, **GSC driver** (official API, operator-OAuth, free ≈50k rows/day) live once B6.7 verifies the site; "peering over the horizon" opportunity math as tested core math (position 8–20 × rising impressions × below-expected CTR, reason strings); Intel surface gains **Trends · Search** tabs. Paid SEO tools = recorded keyed-vendor swap path, not built. Decision record: `docs/adr/0006-search-intel.md`. [you]: optional Trends-API alpha application. | contract window · B6.2 shell (UI) · B6.7 (GSC live) |

### Sprint 7 — visual uplift + asset engine + template portfolio + leads interleave (amendments A15+A16)

> Founder ratification 2026-07-13 (session-26 checkpoint; decision record `docs/adr/0008-sprint7-charter-and-leads-interleave.md`; full phase detail stays in the two ratified proposals — link, don't copy). Method rules bind every bucket: **every vendor asset pinned at mint** (own storage + provenance manifest: model, prompt, credits, license tier — never hotlinked); **≥3 iteration passes + browser-verify per surface**; judge/guard/test gates on top; stealth rules hold (neutral hosting, no real domain until the launch call); **no free-tier vendor asset ever reaches a shipped surface** (watermark + vendor promo/training license).
>
> **Re-sequenced same day (founder direction, session-26 evening): the ENTIRE vendor-visual phase (B7.2 · B7.3 · B7.4) defers as one coherent block** until the vendor tier lands — the build-method recipe (the founder-shared video distilled in the proposal §Method) anchors its planning, code, and look, so it runs together, not piecemeal. Sprint 7 therefore opens with **B-crm.1+2 + B7.a/e** (nothing in them touches the vendor). Contract-window consequence, flagged honestly: the window opens at sprint start with the **leads tables + ICP block only**; AssetSource contracts land in a second, scoped window when the visual phase starts — a deliberate exception to the one-window-per-sprint opinion (it is an opinion, not an invariant; two phases → two windows).

| Bucket | Deliverable | Depends on |
|---|---|---|
| **B7.1 — Asset pinning + provenance** | Mint-time pinning module: vendor asset → own content-addressed storage + provenance manifest (model, prompt, credits, license tier, mint date); the Phase-1 ratchet — no vendor URL survives into any surface. Lead, small PR, no vendor account needed. | — |
| **B7.2 — Thalon uplift** | Order: images → pages → workspace → video-if-credits. Landing hero/section imagery to the Grip II warm-sails voice (stays dark, 4 sections + demo popouts kept); blog per-post AI heroes wired as a *product feature* (auto at draft time, judge-gated); workspace light-first paper/navy illustration set; vendor video-analysis learning track folded into direction.md conventions. | B7.1 · paid vendor tier |
| **B7.3 — AssetSource seam** | Generic `AssetSource` driver interface (request → pinned asset + provenance), vendor driver #1 behind per-tenant config; commercial dep flagged as launch gate with swap path; mirrors the TrendSource pattern. **Opens the sprint's contract window.** | B7.1 |
| **B7.4 — Template portfolio factory** | One meta-prompt, per-vertical variation as data; **pilot wave: 3 verticals (tech · trades · food) → founder review on quality bar + factory economics** → remaining ~20 in waves. Generic template code tracked in `proprietary/templates/`; client instantiations = gitignored data; neutral-named preview hosting. Lane fan-out possible but each launch needs fresh founder go; default sequential. | B7.2 quality bar · B7.3 |
| **B7.a–e — Competitor-informed features** | Cadence gate (a) · autopilot graduation ladder (b) · persona-brief upgrades (c) · programmatic SEO/AEO page families (d) · routing table (e) — per the ratified proposal §Phase 4. | B7.2 |
| **B-crm.1 — Leads spine** | `leads` + `lead_scores` tables + repos, tenant-scoped; intake: waitlist auto-bridge (idempotent on contact hash) + CSV import (dedupe on `(tenant_id, email_hash)`); official-API/operator-supplied sources only — no scraping, no purchased lists. *Late sprint; consumes the frozen contract.* | contract window |
| **B-crm.2 — Profile scoring + ranked queue** | ICP block on the profile schema; deterministic config-weighted scoring with readable reasons (embedding relevance vs ICP description × structured-field matches × completeness/recency — the intel-v2 ranker pattern, never black-box); ranked lead cards in the workspace with thermal heat pills; dismiss/promote → eval rows. Scores intake-provided fields only (enrichment = B-crm.3, next checkpoint). | B-crm.1 |

Phase-5 outreach enablement stays founder-led in parallel (portfolio index page, pitch economics, publish-paperwork applications — multi-week lead times, start early). B-crm.3–5 (enrichment seam · gated outreach · learn loop) queue for the next checkpoint per A16.

### Pulled, not pushed — B3.1–B3.7 (each chartered at its own checkpoint)

| Bucket | Deliverable |
|---|---|
| B3.1 | Publisher (LinkedIn + X first): OAuth token store + refresh worker; MSW-mocked → live = one env flip. Platform OAuth apps + audits are founder-supplied and filed early — they are the long pole. |
| B3.2 | **G5 AI-disclosure gate — hard precondition for any public post** (EU AI Act Art. 50, in force 2026-08-02). |
| B3.3 | Multi-platform preview/edit (canonical item → per-platform variants; render N ratios from one composition). |
| B3.4 | Demo-video pipeline (drives any operator-pointed web app; Fargate render rig + content-addressed render cache). |
| B3.5 | Analytics MVP (generation-metadata ↔ performance join) **+ trend radar / outlier detection — official platform APIs only (A5)**. |
| B3.6 | Studio shell. |
| B3.7 | **Visual-ingest tier (A5):** Apache-2.0 visual-RAG sidecar behind the ingest seam (screenshot-tile rendering + visual retrieval for exemplars/site design); schema seam (`modality`/`visual_ref`) lands at B2.2; pulled once B2.4 proves exemplar value. |
| B3.14 | **Per-tenant model-provider choice (founder idea 2026-07-05):** each tenant picks how generation/judge calls are backed — Thalon's gateway (default, metered), **BYOK** (tenant-supplied provider API keys for GPT/Claude/Gemini/Grok, encrypted per-tenant runtime config — the dependable cross-provider mechanism), or **local-CLI** (self-hosted operators: shell to the provider's own CLI on the operator's machine — the pattern the `claude-cli/*` dev transport already proves). Provider-OAuth adapters slot behind the same seam where providers actually offer them (rare today). Invariant: the two-tier judge's final-gate quality floor is per-tenant config but never silently downgraded — disagreement still blocks. |
| B3.15 | **Web / landing-page generation (founder direction 2026-07-05 — third output family: not just social posts and videos, but the tenant's web presence):** brand profile + prompt → a generated landing page / site (self-contained HTML/CSS, or a componentized build) → **judged on the same spine** (G1 denylist + G3 grounding of every on-page claim to profile/site sources; a new brand-safety/accessibility lens is the natural extension) → approve queue → **ship seam** (preview artifact first; Vercel deploy adapter as the natural target — the tenants already live there). New `web_page` draft format; design quality leans on the available design systems (frontend-design / ui-ux skills) as generation context, never hard-coded brand. Fits the existing origination→judge→approve→render spine exactly — the only genuinely new surface is the web output format + a deploy target seam alongside the publisher (B3.1) and Remotion (B3.10) render seams. Dogfood-rich: the tenants' own sites (e.g. the founder's #1 company site) are the first targets. Chartered at its own checkpoint. |

## Exit criteria (Sprint 0–1 = the MVP gate)

- [ ] Charter approved before any feature code (this file).
- [ ] Off-machine remote exists; grep guard is a required CI check; zero forbidden-token hits in tracked files.
- [ ] Multi-tenant schema from table 1; own sub-account, own DB, own gateway key; no cross-project infra dependency.
- [ ] Sprint-1 vertical: one source → generic-profile fan-out → G1+G3 two-tier gate → Approve queue; no ungated draft can reach the queue (test-proven); no publish path wired.
- [ ] Eval harness live; `edit_diff`/override captured from the first draft; green suite armed as the ship gate at Sprint-1 exit.
- [ ] Operational mirror stood up (external to this repo).

## Amendments

- **2026-07-03 — Spine adoption (founder-approved).** Architecture home: `docs/SPINE.md`; decision record: `docs/adr/0001-spine-adoption.md`.
  - **A1** — dev DB seam driver = **embedded Postgres (PGlite)** replacing SQLite: one SQL dialect dev→prod, dev-side pgvector for the grounding index, per-worktree DB isolation preserved.
  - **A2** — B0.3 adds `fanout_runs` (idempotency + fan-out batch anchor) · `events` (append-only audit spine) · `usage_ledger` + per-tenant daily budget caps enforced in the gateway wrapper.
  - **A3** — package extraction at B0.3 (`packages/{contracts,db,platform,engine}`, `proprietary/{judge,prompts,profiles}` skeleton) + three enforcement ratchets: import-boundary lint, tenant-id schema test, draft-state-machine property test.
  - **A4** — parallel-ready plumbing: `COORDINATION.md` lane board, `.worktreeinclude`, tracked worktree settings, and `agent_handoff/CURRENT.md` (single-file session handoff, overwritten each wrap).
- **2026-07-04 — A5 Sprint-2 expansion (founder-approved).** Decision record: `docs/adr/0002-sprint2-expansion.md`. Adds B2.2–B2.6 (timed ingest + single contract window · waterfall clip plans · exemplar library · demo-plan slice · queue UI) and B3.7 (visual-ingest sidecar, pulled); folds trend radar into B3.5 (official APIs only); transcript capability built in-house (self-hosted Whisper as strategic driver; hosted APIs = optional adapters behind the seam); fine-tuning parked indefinitely (the eval corpus keeps the option open); rejected: ToS-evading scraper architectures. Rendering stays B3.3/B3.4 (Remotion licence = launch gate; superseded by A6 — growth gate).
- **2026-07-05 — A6 Sprint-3 re-charter: origination + profiles (founder-approved; shape delegated to lead recommendation).** Decision record: `docs/adr/0003-sprint3-origination.md`. Founder correction: no pillar video/caption/SRT exists or will be supplied — **generating it is the product**. Adds B3.8–B3.11 (profile spine → pillar origination → Remotion render seam → loop closure + profile UX) + pull-trigger B3.12 (lean official-API trend intake). Sprint-2 exit review completes inside Sprint 3 (B2.5 dogfood = opener; B2.3 dogfood = first generated pillar). Downgrades A5's Remotion note: free licence covers ≤3-person companies **including for-profit** → growth gate with swap path behind the render seam, not a launch gate. B3.1–B3.7 remain pulled; no publish path wired in Sprint 3.
- **2026-07-05 — A7 Trend-intel promotion (founder direction at the B3.8 checkpoint).** The operator must never have to find viral/trending content manually — automated acquisition is the product's second core feature, informed by the operator-supplied viral-content pipeline blueprint (runtime research input, never committed). B3.12 is promoted from pull-trigger to chartered bucket (watchlists → official-API pollers behind a `TrendSource` seam → `source_metrics` snapshots → deterministic outlier ratios → auto-exemplar ingest); B3.13 (Whisper driver behind the B2.2 `TranscriptProvider` seam) added as pulled. Blueprint mapping recorded honestly: its RAG-injection, HITL-correction, metric-snapshot, and brand-safety pieces already exist in Thalon (B2.4 · edit-diffs→eval · source_metrics · judge/denylist); its fine-tuning stays parked (A5) and its §6–7 proxy-cluster/DOM-evasion scraper architecture stays rejected (ADR 0002). Extraction is tiered: text via official APIs, video via metadata+thumbnail, transcripts only via permitted paths.
- **2026-07-05 — A8 Transcription engine promoted (founder direction).** B3.13 goes from pulled to chartered as a core capability feeding both main features: video-URL ingest surface + two `TranscriptProvider` drivers — self-hosted Whisper (MIT; own/permitted media → B2.3 waterfall) and a hosted third-party-transcript adapter (vendor-absorbed ToS risk, keyed runtime config; trending-video spoken hooks → B3.12 intel). The A5 line survives intact: the engine never fetches third-party captions/audio from platform endpoints itself — operator media, Whisper, or a hosted vendor, always behind the one seam.
- **2026-07-05 — A9 Breadth-first build strategy (founder direction).** Sprint 3+ runs **breadth-first, two-pass**: pass 1 materializes all three output families end-to-end (social — done; video — B3.9 done, B3.10/B3.11 build thin; web — B3.15) plus supporting skeletons (trend intel B3.12, profiles B3.8 done), using fake/keyless drivers so **pass 1 needs zero gateway spend**; render/deploy seams may be thin stubs. Pass 2 does the live verification once the surface is stable: gateway dogfood, real-tenant (the founder's #1 company) setup + runs, chromium drives, exit reviews, eval-row refinement, UI polish, TTS, full Remotion, full trend pollers. **Retained from standing discipline:** cheap inline unit tests ship with each module (the green-suite ratchet, AGENTS.md rules 7–8) — they enable safe breadth and are not the deferred churn. **Relaxed for pass 1:** per-bucket live dogfood + human verification + real-tenant setup + founder checkpoints-to-proceed (buckets still land as reviewable commits, but the build does not stop for a live-verify gate between them). Rationale: sequential build→verify→dogfood per bucket re-verifies a surface that is still changing; materialize once, verify once. *(Pass numbering superseded by A10: A9's "pass 2" is now pass 3.)*
- **2026-07-05 — A10 Hardening pass inserted (founder direction at pass-1 completion; shape delegated to lead recommendation).** With the spine materialized, a robustness/consistency pass runs BEFORE live verification: passes renumber to pass 1 (breadth, complete: B3.8/B3.9/B3.10/B3.15/B3.12) → **pass 2 = Sprint 4 hardening (B4.1–B4.9)** → **pass 3 = A9's live-verification pass** (+ B3.11 and the real drivers). Ratified decisions: (1) **strict pass-2 scope** — harden only repetition-proven patterns, store only already-computed data, install tooling; all new features wait for pass 3; zero live spend; (2) trend history gets dedicated **`watchlists` + `trend_snapshots` tables** for longitudinal baselines; (3) the **full pass-3 toolchain installs now** (Remotion · whisper.cpp/faster-whisper · ffmpeg · Vercel CLI) behind an executable `npm run doctor` readiness check; (4) **CI `test` + `eval-gate` become required branch-protection checks**. Standing invariant: every B4 refactor keeps generation keys and content hashes byte-stable, test-pinned before code moves. B3.13's thin cut rolls into B4.8; B3.11 lands in pass 3.
- **2026-07-05 — A11 Pass 2.5 inserted: deterministic video creation (founder direction at the Sprint-4 exit checkpoint; shape delegated to lead recommendation, founder-ratified same day).** Sprint 4 closed complete (B4.1–B4.9 + the lastError residue via PR #20). Before pass 3, **Sprint 5 (B5.1–B5.4)** gives the video family its real creation model: Hyperframes (Apache 2.0) as default render driver behind the existing seam with Remotion demoted to swap path — retiring the A6 growth gate; a data-driven stage registry (default 3 stages: structure → scenes/effects → polish, export deterministic) powering **one-prompt and advanced modes as one code path**; storyboard + strict-schema direction.md (round-trip ratcheted) as the driving artifacts, prefilled deterministic-first; SPINE §1's shell inventory promoted to an executable pin; the staged-flow UI included in-pass (founder decision 3) so the experience is felt on fake drivers. Zero live spend; keys/hashes byte-stable; pass-3 scope otherwise unchanged and follows Sprint 5.
- **2026-07-06 — A12 Pass-3 re-charter: Thalon-first dogfood + frontend-first + intel v2 (founder direction at the pass-3 checkpoint; shape delegated to lead recommendation, founder-ratified same day).** Decision record: `docs/adr/0005-pass3-recharter.md`; frontend design home: `docs/FRONTEND.md`. Pass 3 charters as **Sprint 6 (B6.1–B6.7)**: brand + landing page (4 founder-specified sections; waitlist with referral; Hyperframes-rendered feature demos — "hand shell, dogfood content") → workspace command-center shell (10-second-rule invariant; tenant switcher; keyboard triage; first-run on fake drivers) → self-tenant deep profile (tenant #0 is the real dogfood; pillar #1 = a Thalon video; the founder's first company defers to a later config-only onboarding proof) → intel v2 (operator-described monitored areas fill B3.12's "niches" slot; deterministic two-stage ranking informed by the founder-supplied recommender-frameworks summary — embeddings + tested math, never neural machinery; quota-shaped pollers per the June-2026 YouTube granular-quota change) → live drivers wave 1 (YouTube/Bluesky pollers · local Whisper + hosted transcript-vendor adapter, elevated per founder as a learning/dogfood input channel · judge-tier gateway dogfood) → origination live loop (closes B3.11 and the Sprint-5 carried follow-ups) → Vercel deploy + green-suite exit gate. No publish path in Sprint 6 (B3.1 stays pulled). [you]: domain + pricing tiers (non-blocking) · gateway top-up · transcript-vendor key · OAuth apps (parallel paperwork).
- **2026-07-06 — A13 Search intel v1: SEO/AEO/GEO as Intel's second half (founder direction, same planning session; lead recommendation "integrate, don't add a fourth family" ratified same day).** Decision record: `docs/adr/0006-search-intel.md`. Adds **B6.8** to Sprint 6: profile-seeded keyword compilation (deterministic core + judged AI expansion) · on-page optimization pack (deterministic checks + JSON-LD + `llms.txt` in core; SEO/AEO quality lens in the judge; format-registry SEO-meta capability) · `SearchIntelSource` driver seam (GSC first — official API, operator-OAuth, free; paid tools = recorded keyed-vendor swap path; Trends-API alpha = optional [you] application) · "peering over the horizon" opportunity math as tested core math · Intel surface gains Trends/Search tabs. GSC's live half is deploy-gated by design (no data until the site is verified + indexed) — nothing in B6.8 blocks on it. B6.1's landing copy gets the manual keyword pass now (hand shell, dogfooded content; honest-claims rule — optimization is claimable, rankings are not). Consolidates Sprint 6's schema needs into **one opening contract window** (waitlist + areas + search tables + SEO-meta capability; supersedes A12's two-window note). Dogfood loop closes on Thalon itself: own profile → own keywords → own landing page → own GSC → own opportunities.
- **2026-07-08 — A14 Deploy re-charter: self-managed VPS first (founder direction at the session-21 server-side briefing).** Decision record: `docs/adr/0007-vps-deploy-recharter.md`. B6.7's web deploy moves off Vercel-first onto the portfolio's self-managed VPS (agent-operated infra engine, its own repo; coordination via founder) — Thalon is its **first dogfood tenant**, web container first, render worker second (the Linux box supersedes the Lambda render-farm story as the render fast path). Vercel = documented swap path behind the B3.15 `DeployTarget` seam; AWS parks at ~$0 with OIDC kept; DNS co-location accepted. New invariant: **the workspace never faces the internet ungated** (app-level auth gate, fail-closed in production). New joint design item: app-level PGlite dump hook for the box's dump-before-snapshot backup invariant. Infra specifics stay gitignored in `.context/` per protocol.
- **2026-07-13 — A15 Sprint 7 chartered: visual uplift + asset engine + template portfolio (founder ratification at the session-26 checkpoint; Sprint-6 exit ratified the same turn — suite of record 1013/3/0 deterministic).** Decision record: `docs/adr/0008-sprint7-charter-and-leads-interleave.md`; ratified proposal: `docs/proposals/2026-07-11-visual-uplift-and-template-portfolio.md`. Buckets B7.1–B7.4 + B7.a–e per the Sprint-7 table. Standing method rules: assets pinned at mint with provenance manifests (never hotlinked); ≥3 iteration passes + browser-verify per surface; free-tier vendor assets never on a shipped surface; stealth holds until the launch call. Vendor-tier decision deferred to 2026-07-14 — imagery gates on it, B7.1 proceeds now.
- **2026-07-13 — A16 Leads-engine interleave (founder approval, same checkpoint, per lead recommendation).** Ratified proposal: `docs/proposals/2026-07-13-leads-engine-gated-crm.md` (detailed B-crm.1+2 build plan appended there). B-crm.1 (leads spine) + B-crm.2 (profile scoring + ranked queue) land **late in Sprint 7** so the Phase-5 portfolio outreach dogfoods them; their tables join the sprint's ONE contract window (opened at B7.3). B-crm.3–5 (enrichment seam · gated outreach · learn loop) queue for the next checkpoint. Invariants inherited: no scraping/purchased lists · no ungated contact, ever (any send path is judge+approve-gated and is NOT in this sprint) · scoring is deterministic-with-reasons, never black-box.
