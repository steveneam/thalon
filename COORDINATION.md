# COORDINATION — lane board

> Parallel-lane ledger (protocol: `docs/SPINE.md` §5). **One writer per row** — the lead owns assignments + merge-order; each owner writes only its own `status`. Messages append-only. Status vocab: `pending · in_progress · blocked:<what> · review · merged`. **Contract** = `packages/contracts` + the drizzle schema — frozen per sprint once committed; a lane needing to edit it mid-flight = re-plan, not an ad-hoc edit. Merges serialize through `main` in merge-order: rebase → CI green (guard + lint + tests) → review → merge; never on red.

## Sprint 0 — proposed lanes (activate on founder go)

| lane | owner | owns (glob) | branch | status | depends-on | merge-order |
|------|-------|-------------|--------|--------|------------|-------------|
| a-core | lead terminal | `packages/**`, `proprietary/**`, `apps/web/**`, `eval/**` (B0.3 → B0.4) | `agent/a/b03-schema` → `agent/a/b04-eval` | merged | — | 1 |
| b-aws | second terminal (`claude --worktree b05-aws`) | `infra/**` (B0.5) | `worktree-b05-aws` | merged | AWS sub-account ([you]) | 2 |

## Sprint 1 — lanes (cut 2026-07-04, founder go)

| lane | owner | owns (glob) | branch | status | depends-on | merge-order |
|------|-------|-------------|--------|--------|------------|-------------|
| engine | lead (worktree subagent) | `packages/engine/**`; additive-only: embedding tier in `packages/platform/src/**`, new repo fns in `packages/db/src/repos/*` | `agent/engine/b11-ingest` → `agent/engine/b12-fanout` | merged | contract (frozen ✅); `AI_GATEWAY_API_KEY` for live runs ([you] — tests keyless) | 1 |
| judge | lead (worktree subagent) | `proprietary/judge/**`, `proprietary/prompts/judge-*`; sanctioned surgical edit: I1 check in the `packages/db` transition fn if missing | `agent/judge/b13-harness` | merged | contract (frozen ✅); fixture drafts, no engine import | 2 |
| ui | lead (worktree subagent) | `apps/web/**` | `agent/ui/b14-approve-queue` | merged | contract (frozen ✅); MSW mock, no engine/judge dep | 3 |
| eval | lead terminal | `eval/**` (B1.5 arming) | `agent/eval/b15-exit-gate` | merged | B1.2 merged | 4 |

## Sprint 2 — planned lanes (cut after B2.2 contract re-freeze; launch requires fresh founder go)

> B2.1 (code-free seam proof) and B2.2 (the contract window — one owner) run solo on the lead terminal, in that order, **before** any lane launches. Default execution: **two waves of 2** (waterfall+exemplar, then demo+ui) as in-session worktree subagents; all-4-concurrent = Mode B (founder-opened terminals) if pulled.

| lane | owner | owns (glob) | branch | status | depends-on | merge-order |
|------|-------|-------------|--------|--------|------------|-------------|
| waterfall | lead (worktree subagent) | `packages/engine/waterfall/**`, `proprietary/prompts/highlight-*` | `agent/waterfall/b23-clip-plans` | merged | B2.2 merged ✅ | 1 |
| exemplar | lead (worktree subagent) | `packages/engine/exemplar/**`, `proprietary/prompts/fanout-exemplar-*`, `proprietary/profiles/**` | `agent/exemplar/b24-library` | merged | B2.2 merged ✅ | 2 |
| demo | lead (worktree subagent) | `packages/engine/demo/**`, `proprietary/prompts/storyboard-*` | `agent/demo/b25-plan-capture` | merged | B2.2 merged ✅ | 3 |
| ui | lead (worktree subagent) | `apps/web/**` | `agent/ui/b26-queue-formats` | merged | B2.2 contract mocks ✅ | 4 |

## Sprint 4 — hardening lanes (cut 2026-07-05; launch = founder's paste of the session-9 resume prompt, which names exactly these runs)

> A10 pass-2 rules bind every lane: no new features, zero live spend, generation keys + content hashes byte-stable (pin before moving code). **Sprint-4 contract window** = B4.2's registry (`packages/contracts`, core lane) + B4.3's new tables (drizzle, intel lane) — disjoint files, additive-only, freeze after both merge. 2 concurrent subagents = in-session mode (Mode B not needed). B4.6 was pulled INTO the core chain (its key-scheme helper refactors the same render/deploy/capture files as B4.1 — not lane-safe).

| lane | owner | owns (glob) | branch | status | depends-on | merge-order |
|------|-------|-------------|--------|--------|------------|-------------|
| core | lead terminal | `packages/contracts/**`, `packages/engine/src/{origination,webpage,demo,render,waterfall,fanout,exemplar}/**`, `packages/platform/src/object-store.ts`, `packages/db/src/repos/drafts.ts` (B4.4 events gap), `.github/**` + branch protection (B4.9 first, quick) | `agent/core/b41-engine` (single branch, 7 bucket commits) | merged | — | 1 |
| intel | lead (worktree subagent → lead-finished) | `packages/engine/src/trend/**`; `packages/db`: new schema file (`schema/intel.ts`), new repos (`watchlists`/`trend-snapshots`) + additive `repos/index.ts`, migration 0003, db tests incl. tenant-id table list | `agent/intel/b43-trend-storage` | merged | contract window co-owner (additive drizzle only) | 2 |
| tooling | lead (worktree subagent → lead-finished) | `scripts/doctor*`, root `package.json` (additive `doctor` script), machine-global installs (Remotion · whisper.cpp/faster-whisper · ffmpeg · Vercel CLI), `packages/engine/src/ingest/**` (B4.8 transcript providers), engine `package.json` additive deps | `agent/tooling/b47-doctor` → `agent/tooling/b48-transcription` | merged | — | 3 |

Sanctioned out-of-glob, additive-only (established pattern): both subagent lanes may add export lines to `packages/engine/src/index.ts`. Standing kickoff caveats: skip lint in lanes (lead re-runs at merge); `@thalon/*` vitest aliasing (engine config = reference); contract friction ⇒ stop-and-report, never edit; git identity re-set per worktree.

## Sprint 5 — proposed lanes (cut 2026-07-05 at the A11 charter; activate on fresh founder go next session — each subagent launch needs it)

> A11 rules bind every lane: zero live spend, keys/hashes byte-stable, video-creation scope only. **Required kickoff reading for every lane: `docs/research/hyperframes-integration.md`** (founder-directed docs read-around, 2026-07-05) — it pins the composition contract + producer API for B5.1/B5.2, the lint-before-render core gate, and the player/SDK surfaces B5.4 builds on (live preview needs NO render; operator edits arrive as RFC-6902 patches → store verbatim in `edit_diffs`). **Sprint-5 contract window** = B5.2's stage registry + `storyboard`/`direction_doc` schemas in `packages/contracts` — ONE owner (pipeline lane), freeze at its merge; the ui lane consumes contract mocks after the freeze. B5.1/B5.3 are disjoint from the window and from each other.
>
> **Execution = Mode B (founder preference, 2026-07-05):** pipeline solo on the lead terminal FIRST (window discipline, mirrors B2.2); then render + ui in **founder-opened terminals**, each on a **lead-prepped worktree** — lead runs `git worktree add` + **`npm run worktree:setup -- .claude/worktrees/<lane>`** (junctions node_modules, copies env/vault includes, verifies identity+eslint+guard; ratchet landed 2026-07-05) and authors the kickoff prompt per window; ratchet lane closes on the lead terminal. **Caveat changes vs prior sprints:** "skip lint in lanes" is RETIRED — eslint works in a prepped worktree (lead still re-runs at merge); NEW hard rule: **never `npm install` inside a worktree** (npm replaces the junction with a real folder — root `preinstall` guard blocks it; dep additions = edit package.json in the lane, install runs in main). `@thalon/*` vitest aliasing is now complete across db/eval too (was engine/judge/web only).

| lane | owner | owns (glob) | branch | status | depends-on | merge-order |
|------|-------|-------------|--------|--------|------------|-------------|
| pipeline | lead terminal | `packages/contracts/**` (stage registry + new format schemas), `packages/engine/src/direction/**` (new), `packages/engine/src/pipeline/**` (additive), `proprietary/prompts/direction-*` + `storyboard-stage-*` | `agent/pipeline/b52-stages` | merged | — (contract-window owner) | 1 |
| render | founder terminal (Mode B, lead-prepped worktree) | `packages/engine/src/render/**` (hyperframes driver + fake + composition templates), engine `package.json` additive deps (install runs in main), `scripts/doctor*`, `docs/adr/0004-render-driver-default.md` | `agent/render/b51-hyperframes` | pending | — | 2 |
| ui | founder terminal (Mode B, lead-prepped worktree) | `apps/web/**` | `agent/ui/b54-staged-flow` | pending | B5.2 merged (contract freeze) | 3 |
| ratchet | lead terminal (quick) | `packages/engine/src/__tests__/shell-inventory.test.ts`, `docs/SPINE.md` §1 refresh | `agent/ratchet/b53-shell-pin` | pending | — | 4 |

## Messages (append-only)

- 2026-07-03 lead: board created. B0.3→B0.4 vs B0.5 are file-disjoint per the charter; Lane B is inert until the founder opens the second terminal (or we fall back to sequential). Worktree prereqs landed: `.worktreeinclude`, `.claude/settings.json` symlinks `node_modules`, dev DB/store/queue isolate per worktree via the local seams.
- 2026-07-03 lead: B0.3 complete on `agent/a/b03-schema` → at the founder checkpoint (status `review`). Contract (`packages/contracts` + drizzle schema) is now committed — **frozen for the sprint**. Lane B unaffected (`infra/**` untouched).
- 2026-07-03 b-aws: B0.5 credential-free scaffold at review — CDK-Python skeleton (`infra/`), `ThalonGithubOidc` stack (OIDC provider + `thalon-github-deploy` role, assume-cdk-roles-only, main-branch-scoped trust), 7 synth-time tests green, `cdk synth` verified. Blocked on founder one-time steps (sub-account → bootstrap → hand-deploy OIDC stack → set `AWS_DEPLOY_ROLE_ARN`): runbook in `infra/README.md`. For the lead at merge: move `infra/github-workflow/deploy-infra.yml.example` → `.github/workflows/` and add an infra pytest job to CI (both outside this lane's glob). Handoff note: `agent_handoff/CURRENT.md` left untouched — lead-owned during parallel lanes; this message is Lane B's wrap record.
- 2026-07-03 b-aws: AWS side of B0.5 is live — founder supplied the sub-account; `cdk bootstrap` done (ap-southeast-2), `ThalonGithubOidc` deployed by hand (the one-time chicken-and-egg deploy), deployed trust policy read back and verified against the tests, repo variable `AWS_DEPLOY_ROLE_ARN` set. Account id + local access path recorded outside the repo (agent memory / founder), per the never-commit rule. Only remaining B0.5 item: lead activates `deploy-infra.yml.example` at merge — the first green OIDC-assumed run from `main` is the final proof.
- 2026-07-03 b-aws: credential-door correction (founder): local AWS access now goes through IAM Identity Center only — the `thalon-admin` profile was repointed from an unrelated IAM-user hop to SSO, and identity + deployed stack were re-verified through the new path (resources were already in the correct account; nothing redeployed). Ratchet: `infra/scripts/sso-login.py` — CLI-free device-flow login (the AWS CLI installer is blocked on the dev machine); it writes the standard token cache so both boto3 and the CDK CLI work. Portal URL / account ids stay out of the repo — the script reads them from `~/.aws/config`.
- 2026-07-03 b-aws → lead: your SSO directive executed (one design deviation: the helper writes the shared SSO token cache instead of minting temp keys into the profile — boto3 AND the CDK CLI then resolve/re-mint automatically, no plaintext keys on disk). Proven on SSO creds: sts identity, describe-stacks, and `cdk diff` (exercises the CDK CLI's credential chain end-to-end). The static-key IAM user is **deleted** — founder authorized doing it from here instead of the console: access key + PowerUserAccess/IAMUserChangePassword + console login profile removed, `NoSuchEntity` verified, zero IAM users remain in the management account. Human door = Identity Center; CI door = GitHub OIDC role. Lane B is clear-safe; B0.5 awaits your merge (workflow rename + infra pytest job in CI).
- 2026-07-03 lead: merge train complete (founder-delegated): PR #2 (B0.3) and PR #3 (B0.5, rebased onto post-B0.3 main; one COORDINATION.md reconcile — the expected shared-file overlap) merged in order; deploy workflow activated + infra pytest job added to CI; **first OIDC-assumed `deploy-infra` run from `main` green — B0.5 closed**. Both lanes `merged`; board idle until the next lanes are cut. Standing founder directive: lead drives merge mechanics end-to-end; founder handles cloud-console/terminal-only steps.
- 2026-07-04 lead: Sprint-1 lanes cut per SPINE §5 on founder go. Three worktree subagents launched (engine B1.1 · judge B1.3 · ui B1.4-vs-MSW); eval lane stays pending until the train reaches B1.5. Contract stays frozen — a lane hitting contract friction stops and reports (re-plan, not ad-hoc edit). Merge train serializes engine → judge → ui → eval; lead drives rebase/PR/merge per standing directive. Housekeeping done: `b05-aws` worktree dir + local branch GC'd. [you] outstanding: `AI_GATEWAY_API_KEY` (Vercel dashboard → AI Gateway → API keys → into `apps/web/.env.local`) — needed for B1.1 live embeddings only; all lane tests run keyless.
- 2026-07-04 lead: **Sprint-1 merge train complete — B1.1 (PR #5) → B1.3 (PR #6) → B1.4 (PR #7) merged in order, CI green each time.** Lead integration commits during the train: (a) judge G3 calls now metered through the `withGatewayGuard` choke point (the lane's real driver had bypassed budget/tracing; `BudgetExceededError` is an operational halt, not a verdict); (b) approve-queue selection resets moved out of effect bodies (2 eslint errors the lane's worktree didn't surface). Ratchets picked up along the way: PGlite boot-test timeout 30s (flaky under lane disk contention); root vitest projects now include `proprietary/*` (judge suite silently never ran under `npm test` before); worktree lesson — `@thalon/*` imports in worktrees resolve to the MAIN checkout via the node_modules symlink; lanes must alias `@thalon/*` to their own worktree src in vitest/tsconfig (engine's configs are the reference). Known follow-ups: `packages/db` bulk `runs.list()`/`drafts.listByRun()` (UI hydrates via events spine meanwhile); unique index on `sources (tenant_id, content_hash)` at next contract window. All lane worktrees GC'd. Remaining Sprint 1: B1.2 fan-out (engine lane, kickoff drafted, awaiting founder go per standing approval rule) → B1.5 exit gate.
- 2026-07-04 lead: **B1.2 founder go received** — engine-lane worktree subagent launched on `agent/engine/b12-fanout` (single lane, so in-session worktree per the mode rule; Mode B reserved for >2 concurrent lanes per founder directive today). Founder has obtained `AI_GATEWAY_API_KEY`; `apps/web/.env.local` created from the template for the paste (gitignored, verified) — unblocks B1.5 dogfood, not needed by B1.2 tests.
- 2026-07-04 lead: **B1.2 merged (PR #8) — Sprint 1 code complete; only B1.5 exit gate remains.** Lane delivered fan-out + profiles-as-data + versioned prompt in one pass; lead review caught one correctness bug pre-merge (idempotent fast-path replayed a *partial* draft set after a mid-run irrecoverable failure — silent, and permanently stuck behind the generation key), lane fixed it with self-healing backfill (replay generates only the missing platforms; clean replay stays zero-call). 109/109 tests + typecheck + lint verified on the integration checkout; CI green; worktree + branches GC'd. **Follow-up (ratchet candidate, structural):** `.claude/settings.json` declares `worktree.symlinkDirectories: ["node_modules"]` but orchestrator-spawned (Agent-tool) worktrees get a plain empty dir instead of a link — imports still resolve by walking up to the main checkout (the known `@thalon/*` aliasing hazard) but npm-run binaries (eslint) silently don't; until fixed, lane kickoffs must say "skip lint, lead verifies at merge" and the lead always re-runs lint before merging.
- 2026-07-04 lead: **B1.5 merged (PR #9) — SPRINT 1 COMPLETE.** Dogfooded live on tenant #0: ingest → fan-out → judge-block → operator edits (5 eval rows captured transactionally, exported) → re-judge → queued through the I1 gate → human-approved; golden-G3 live run 6/6; CI `eval-gate` job armed (keyless deterministic suite = ship gate; model-graded `golden:g3` stays out of CI by invariant). The two-tier judge proved itself editorially: sonnet final failed a draft for an added rationale the source never stated, which the haiku screen had passed — disagreement blocked, exactly per ratified decision 2. Ratchets landed in-change: `lastError` surfaced through both bounded-retry loops (a gateway 400 and free-tier 429s all read as "malformed shell output" before); eval CLIs refuse the shared dev DB while the dev server holds it (PGlite single-writer — a server-side edit was invisible to the CLI and lost on shutdown); export CLI now targets the web app's data dir (silently exported an empty cwd-relative DB before); db client migrations lookup no longer uses `new URL(rel, import.meta.url)` (Turbopack bundles the pattern — first real apps/web boot against the DB path failed on it). **Post-sprint, founder-requested: `claude-cli/<alias>` dev model transport (PR #10)** — build/test tiers run on the founder's Claude subscription via headless Claude Code; production stays on the gateway; embeddings excluded. Follow-ups for a future window: approve-panel doesn't refresh after save-edit; aborted fan-out runs show as empty runs in the feed; no operator "re-judge" action for operationally-blocked drafts (edit is the only path back to judging); gateway credit top-up restores the ratified sonnet final tier ([you], optional until Sprint 3).
- 2026-07-04 lead: **B2.2 merged (PR #12) — the Sprint-2 contract window is closed and re-frozen; wave-1 lanes are cuttable.** One migration carried the whole window: `source_chunks.start_ms/end_ms` · 4 new source kinds · `modality`/`visual_ref` (the B3.7 seam, column-only) · `source_metrics` (generic name/value, append-only) · the `sources (tenant_id, content_hash)` unique index (Sprint-1 follow-up closed). Timed ingest live: fail-loud SRT/VTT/plain caption parser, segment-atomic timed chunking (chunk media windows stay honest), `TranscriptProvider` seam with caption-file as the shipped zero-dep driver. Founder-prompted ratchet in the same PR: root eslint over packages/proprietary/eval + `npm run lint` as a CI step — lint now gates the PR, not the lead's merge pass (B1.4 lesson made structural). Direction pinned from founder Q&A: the Whisper transcript driver will be in-house and Windows-native (whisper.cpp / faster-whisper, MIT) behind the existing seam; B3.7 visual-ingest is a port-the-pattern job, not a sidecar adoption (Apache-2.0 — no clean room needed; render tiles via Playwright → object store via `visual_ref` → vectors in pgvector → ONNX/DirectML or torch inference; WSL2 as interim dev bridge; verify the fine-tuned weights' licence at bucket time). Next: **founder go to cut wave 1** (waterfall + exemplar lanes per the board above).
- 2026-07-04 lead: **B2.1 merged (PR #11, rebase-merge so the proof commit stays legible on main) — the Sprint-2 seam proof is in.** Recon surfaced two Sprint-1 hardcodes that would have blocked any pure-data tenant: the dogfood CLI could only run built-in tenant #0, and apps/web hardcoded slug `self`. Prep commit made both runtime config (dogfood takes a Zod-validated JSON input file; `DEMO_TENANT_SLUG` env via the platform env module) and left a ratchet: an eval test validates every `proprietary/profiles/tenants/*.json` — landed before the first tenant file existed. Proof commit = `fernwood.v1.json` + README lines, **zero code** (exit criterion met literally). Live slice on tenant #2: x pass/pass → queued; linkedin **blocked on a real g3 tier disagreement** (screen=pass, final=fail) — invariant I3 proven on a second tenant; replay run idempotent (same ids, zero re-judge spend). Both profile-resolution paths exercised (tenant override for linkedin, shipped default for x). Founder triage: `DEMO_TENANT_SLUG=fernwood npm run dev` → /approve. Next: B2.2 timed ingest + contract window (lead, solo) at founder go, then wave-1 lanes per the Sprint-2 board above.
- 2026-07-04 lead: **Amendment A5 landed (founder-approved at the Sprint-1 exit review)** — Sprint 2 expands to B2.1–B2.6; see `CHARTER.md` + `docs/adr/0002-sprint2-expansion.md`. Sprint-2 lane plan added above (pending; launch needs fresh founder go at cut time per standing rule). Known worktree caveat carries over: orchestrator-spawned worktrees miss the node_modules symlink — lane kickoffs say "skip lint, lead verifies at merge". Next: B2.1 (lead, solo, zero-code proof) → B2.2 contract window → wave 1 (waterfall+exemplar).
- 2026-07-04 lead: **Wave-1 lanes launched on founder go** — waterfall (B2.3) + exemplar (B2.4) as in-session worktree subagents (2 concurrent = in-session mode; Mode B not needed). Kickoffs carry the standing caveats: skip lint (lead re-runs at merge), `@thalon/*` vitest aliasing (engine config is the reference), contract frozen (friction ⇒ stop-and-report, never edit). Sanctioned out-of-glob edits, additive-only: both lanes may add export lines to `packages/engine/src/index.ts` + new repo fns in `packages/db/src/repos/*`; exemplar additionally gets one surgical additive edit to `fanout.ts`/shell generator to thread optional exemplar context + provenance (waterfall doesn't touch fanout, so no cross-lane conflict; exemplar-off path must stay byte-identical, proven by untouched existing tests). Merge train: waterfall → exemplar; rebase → CI green → review → merge, per SPINE §5.
- 2026-07-04 lead: **Wave-1 merge train complete — B2.3 (PR #13) → B2.4 (PR #14) merged in order, CI green each time.** Lead review caught one correctness bug pre-merge on waterfall (same class as B1.2's): the highlight-select boundary accepted duplicate `windowIndex` selections — schema-valid, in-range — which would collide on the content-addressed draft generation key mid-persist and leave a half-persisted platform that platform-granularity backfill skips forever; fixed at the validation boundary (duplicate = schema-invalid, consumes a repair attempt) + 2 tests, including proof that an always-duplicating shell persists nothing. Exemplar review clean — the three risk points all verified: PII strip runs before content-hash (nothing raw ever stored), `stableStringify` drops `undefined` so plain-run generation keys are hash-stable across the upgrade (exemplar-off = byte-identical, existing fanout tests unmodified), and the overlap gate returns an outcome instead of throwing (a breach blocks only that draft via generated→judging→blocked through the one transition fn). Integrated verification: 187/187 root suite, typecheck, lint 0 errors, guard — first run with both modules together. Micro-cleanup noted for whoever's next in `fanout.ts` (likely B2.6): the exemplar-aware path reads `sourceChunks.listBySource` twice (second read's result unused when precomputed) — harmless local read. Worktrees + branches GC'd. Next: **wave 2 (demo B2.5 + ui B2.6) needs fresh founder go**; B2.3 dogfood unblocked the moment the founder supplies a pillar caption/SRT file.
- 2026-07-04 lead: **Wave-2 lanes launched on founder go** — demo (B2.5) + ui (B2.6) as in-session worktree subagents (2 concurrent = in-session mode; Mode B not needed). Standing caveats carried: skip lint (lead re-runs at merge), `@thalon/*` vitest aliasing (engine config is the reference), contract frozen (friction ⇒ stop-and-report, never edit). **Cross-lane pin:** `demo_plan` draft body/meta stub authored verbatim into both kickoffs — B2.5 emits it (additive extensions allowed), B2.6 renders exactly the pinned core; body = step narrations joined `\n\n`, mirroring clip_plan's body convention (I1 body_hash binding). Sanctioned out-of-glob, additive-only: both lanes may add export lines to `packages/engine/src/index.ts` + new repo fns in `packages/db/src/repos/*` (B2.6 may land the queued bulk `runs.list()`/`drafts.listByRun()` follow-up this way); demo may add the `playwright` dep (Apache-2.0) to `packages/engine/package.json` + lockfile — tests stay browser-free behind a driver seam so CI never needs a browser install. Founder decisions this session: B2.5 dogfood target = a known brand's **public docs-search flow** (Anthropic docs first) — runtime input only, never committed; auth-walled chat products (claude.ai / chatgpt.com) are out per A5's no-ToS-evasion line; robots.txt + rate-limit checks in core make disallowed targets fail loudly by construction. Merge train: demo → ui.
- 2026-07-05 lead: **Wave-2 merge train complete — B2.5 (PR #15) → B2.6 (PR #16) merged in order, CI green each time. SPRINT 2 IS CODE-COMPLETE (B2.1–B2.6).** Review rounds pre-merge (all fixed on-branch, test-proven): demo — capture failure-path teardown ordering (a throwing `driver.finish()` could leave a stale prior-success `captureRef` as a durable lie; the failed-meta write now always lands), robots.txt fetch moved behind the rate limiter, `drafts.updateMeta` gained optimistic concurrency (`expectedUpdatedAt` in the UPDATE's WHERE; typed `ConcurrentUpdateError`, never silent last-write-wins), robots rules now merge across ALL matching UA groups (was first-group-only, fail-open). ui — the substantive one: re-judge/save-edit flipped drafts to `judging` but **nothing in apps/web ever ran the judge** (only the dogfood CLI called the pipeline; a re-judged draft was an unrecoverable operator dead end). Lead-sanctioned fix: apps/web now depends on `@thalon/judge`; `judge-runner.ts` wires `runJudgePipeline` exactly like dogfood (metering stays inside the pipeline's `withGatewayGuard` choke point), both paths judge in-request, failures rethrow loudly (`role="alert"`) with the draft honestly at `judging`; plus a stale-meta indicator when `body` diverges from meta-derived text post-edit. Lint rounds (the known lane blind spot, lead-fixed on-branch): 2 errors on demo, 1 on ui — the ui one re-tripped the B1.4 `set-state-in-effect` class; `loadDraftDetail` rewritten promise-chain-form (the analyzer can't see through async fn boundaries — pattern note now in the file). Rebase reconcile: both lanes added additive fns to `packages/db/src/repos/drafts.ts` (`updateMeta` + `reJudge`) — kept both. Integrated verification on the rebased ui branch pre-merge: 300 passed / 2 skipped (browser-gated), typecheck, lint 0 errors, guard. Worktrees + local/remote lane branches GC'd. **Founder process directive (recorded in agent memory): an approval covers exactly the named agent runs — reviewer agents and fix-round resumes each count; any extra agent run = pause for approve-or-defer.** Follow-ups carried: `fanout.ts` double `sourceChunks.listBySource` read (harmless; next fanout window), pre-existing `_dataDir` lint warning in `apps/web/src/app/api/health/route.ts`, B2.5 capture discards the partial event trace on a failed drive (debuggability nicety). Next: **Sprint-2 exit review** — dogfood B2.3 ([you]: pillar caption/SRT) + B2.5 live against the founder-pinned target (a known brand's public docs-search flow, runtime input only), then Sprint-3 re-charter at its own checkpoint.
- 2026-07-05 lead: **Sprint-4 lane board cut** (pass-1 breadth complete `77820b3`/`c3ac987`/`cc692f9`; A10 chartered `fd16c79`). Founder approved parallel workflow in-session; the session-9 resume prompt IS the launch go for exactly the two named subagent lanes (intel B4.3 · tooling B4.7→B4.8) alongside the lead's core chain (B4.9 → B4.1 pins → extraction → B4.2 → B4.4/B4.5 → B4.6). Any agent run beyond those named (reviewers, fix-round resumes) = pause for approve-or-defer per the standing founder rule. Merge train: core → intel → tooling; contract freezes after B4.2 + B4.3 merge.
- 2026-07-05 lead: **SPRINT 4 CODE-COMPLETE in one session (B4.1–B4.9 minus one deferred column) — lanes launched per the founder's resume-prompt go, then finished lead-inline on founder direction after the tooling subagent died mid-run on a session limit ("do everything yourself").** Sequence: B4.9 first (CI `test` + `eval-gate` now REQUIRED checks — they gated every merge below). Core (PR #17, rebase-merged): B4.1 key-stability pins FIRST (hash vectors, all three generation-key field sets, artifact key schemes, judged-body derivations) → the three extractions (`pipeline/repair-loop.ts` · `pipeline/single-draft.ts` · `pipeline/artifact-stage.ts`, behavior-preserving, pins prove byte-stability) → B4.2 format registry in contracts (apps/web's hand-synced schema mirrors DELETED — the registry is why web can stay engine-free) → B4.4 ratchets (events coverage + getGateway boundary scan) + B4.5 taxonomy (InvalidState/ArtifactMissing/IrrecoverableGeneration) → B4.6 store hygiene (one key helper, verified reads, `npm run -w @thalon/eval sweep`). Intel (PR #18): subagent landed schema+migration 0003 at its pause boundary; lead finished repos (events-audited, tenancy-walled), pure longitudinal Δ-velocity math, and intake history wiring (EVERY polled item accrues a `trend_snapshots` row; exemplar content hash byte-pinned first). Tooling (PR #19): salvaged the dead subagent's uncommitted doctor + installs (all four runtimes verified live-ready — ffmpeg was already installed, just PATH-stale), finished `npm run doctor`, then B4.8 (TranscriptProvider registry env-selected; whisper-local skeleton REFUSES remote URLs per A5/A8; hosted-vendor keyed adapter; video-URL ingest surface hashing the fetched transcript). **Deferred, deliberately:** `fanout_runs.lastError` (B4.5's last item) waits for migration 0004 so the drizzle journal stayed linear behind intel's 0003 — next session's first act. The ratchets earned their keep on the tooling PR: two red CI rounds caught the lead's own gaps (flat-config `no-undef` on `scripts/*.mjs` node globals; the repo-ratchet boundary test rejecting direct `process.env` reads in the new providers — both fixed at the choke points, on-branch). Final verified state on merged main: root suite 362 → **424 passed / 2 skipped**, lint 0 errors, guard green on every commit, `npm run doctor` all-green from the main checkout (the B4.9 documented-command pass ran `npm install` · `npm run guard` · `npm run doctor` · `npm test` · `npm run lint` verbatim); all lane worktrees + branches GC'd.
- 2026-07-05 lead: **B4.5 residue closed (PR #20, rebase-merged, `ea3f41c`) + A11 chartered — SPRINT 4 FULLY COMPLETE; pass 2.5 (Sprint 5) is next.** Residue delivered beyond the handoff's minimum on the charter's general wording: `fanout_runs.last_error` (migration 0004, additive) + single-writer `fanoutRuns.recordLastError` (tenant-scoped, events-audited `last_error_recorded`/`_cleared`) wired into ALL THREE generation paths (single-draft spine + fanout + waterfall — the latter two's plain irrecoverable `Error`s upgraded to `IrrecoverableGenerationError`), recorded before the throw, cleared on a successful backfill; `FeedRun` wire type carries it to the queue for free. Suite 424 → 428 / 2 skipped; typecheck + lint + guard green; key pins untouched. Then the Sprint-4 exit checkpoint ran in-chat: founder directed **pass 2.5 — deterministic video creation** (ratified: Hyperframes default render driver · strict-schema direction.md · staged-flow UI in-pass) — chartered as **A11 / Sprint 5 (B5.1–B5.4)**, lane board cut above (pending; activation needs fresh founder go next session per the standing rule). Hyperframes verified real before chartering: HeyGen's Apache-2.0 HTML→video framework, 33k stars, active (v0.7.33 2026-07-04), headless-chromium+ffmpeg engine — both already in the toolchain.
- 2026-07-05 lead: **Hyperframes docs read-around done (founder-directed) → `docs/research/hyperframes-integration.md` landed as required Sprint-5 kickoff reading.** Load-bearing finds: (1) `@hyperframes/lint` is browser-free static analysis → a CORE validation gate between template generation and any render spend (SPINE §1); (2) `@hyperframes/player` gives live in-queue preview with NO render — B5.4's stage previews are free; (3) the SDK emits RFC-6902 patches with inverses → operator edits become machine-readable `edit_diffs` rows verbatim, undo included; (4) **keyless local audio stack** (Kokoro TTS · Whisper word-level caption alignment · MusicGen · bundled SFX; "no key configured is a normal state") → pass-3 TTS gets a $0 local default, hosted voices stay optional keyed adapters per A5; (5) variables = typed template slots with `--strict-variables` (root w/h/duration/fps are compile-time — the deterministic generator bakes them from the direction doc); (6) their own website-to-video pipeline ships DESIGN.md → SCRIPT.md → STORYBOARD.md → per-beat compositions — independent validation of the A11 storyboard+direction.md design (ours adds schemas/judge/tenancy); (7) scale paths fit existing rails (CDK `HyperframesRenderStack` for the sub-account; Apache-2.0 Vercel render-API template). Platform caveat recorded: BeginFrame determinism is Linux-only, Windows dev falls back to screenshot capture — harmless (render cache keys on manifest hash), Docker/Lambda for prod artifacts.
- 2026-07-05 lead: **Mode B unblocked (founder direction: fix the worktree node_modules issue before Sprint 5).** Root cause diagnosed on-machine: `worktree.symlinkDirectories` needs TRUE symlinks, which Windows denies without admin/dev-mode → Claude-created worktrees silently got an EMPTY node_modules (the B1.2-era "skip lint in lanes" caveat). Fix = **junctions** (no privileges needed) via new `scripts/worktree-setup.ps1` (`npm run worktree:setup -- <path>`): junctions every node_modules main has (root · apps/web · eval · packages/db · packages/engine), copies the `.worktreeinclude` gitignored set (env files, `.context/` vault pointer, settings.local.json) for worktrees Claude Code did NOT create, then VERIFIES identity+eslint+guard — fail-loud, idempotent, repair-capable (proven: rebuilt a deliberately-broken lane; eslint v10.6.0 + contracts 9/9 + engine taxonomy 4/4 incl. PGlite all green inside the junctioned worktree). Second ratchet: root `preinstall` (`scripts/guard-worktree-install.mjs`) BLOCKS `npm install` inside any worktree (npm v7+ deletes a linked node_modules and forks the lane's dep tree — founder-flagged hazard; installs run in main, junction propagates instantly; verified blocking in-lane and passing in main). Third fix: `@thalon/*` vitest aliasing audit — **packages/db and eval had NO aliases** (their worktree tests would silently exercise MAIN's sources); both now alias sibling packages to worktree-local src, completing the engine/judge/web pattern. settings.json symlinkDirectories extended to all five node_modules (harmless where symlinks work; the script is the guarantee). Git identity confirmed inherited in worktrees (shared repo config); `.data` dev-DB isolation per-worktree confirmed by construction (cwd-relative). Sprint-5 board updated: render+ui lanes = Mode B founder terminals on lead-prepped worktrees; "skip lint" caveat retired.
- 2026-07-03 lead: **B0.4 merged (PR #4) — Sprint 0 complete.** Override capture proven end-to-end by test; golden seed committed; promptfoo + DeepEval proven keyless; Langfuse tracing seam live. Cleanup note: `.claude/worktrees/b05-aws` + local `worktree-b05-aws` branch could not be removed (directory still held open — close the Lane B terminal, then `git worktree remove .claude/worktrees/b05-aws` and `git branch -D worktree-b05-aws`); remote branch already deleted. Next: Sprint-0 exit review, then cut Sprint-1 lanes per SPINE §5 (engine → judge → ui → eval, merge in that order).
- 2026-07-05 lead: **B5.2 merged (PR #21, rebase-merged, `a5ed7f1`) — the Sprint-5 contract window is CLOSED and FROZEN; the Mode B wave is cuttable.** Delivered: `storyboard` + `direction_doc` formats with pinned meta schemas; stage registry (count is config, default 3: structure → scenes_effects → polish; first stage produces storyboard, later stages direction_doc — schema-enforced; export is deterministic core by construction, no plan can even declare an export stage); **direction.md renderer/parser lives in CONTRACTS** (deliberate: B5.4's direction editor gets the form-view ↔ raw-md round-trip without an engine dependency) with the byte-identical round-trip ratchet pinned by a literal wire fixture; deterministic-first prefill (platform-config-or-default, loud on invalid config) + deterministic export (timeline/SRT/compile-time dims; SRT round-trips B2.2 ingest); staged pipeline where **the stage gate is structural** (advance requires queued/approved — reachable only via the judge's I1 transition) and **one-prompt mode = the same start/advance functions auto-advanced** (replay proof: same drafts, zero shell + zero judge calls, call-counted). Three new guarded shell ops for B5.3's inventory pin: `staged_video.structure/.scenes/.polish`. Sanctioned out-of-glob edits (the pin-before-move mandate, review-visible): key-stability pins extended for both new key compositions, format-registry ratchet extended, gateway-boundary allowlist += the direction shell. Suite 428 → **489 passed / 2 skipped**; pillar_script pins untouched. Worktrees for render+ui prepped next; **wave launch awaits fresh founder go** per the standing rule.
- 2026-07-05 lead: **Mode B wave LAUNCHED on founder go** — render (B5.1) + ui (B5.4) live in founder-opened terminals on the lead-prepped worktrees (`.claude/worktrees/render` @ `agent/render/b51-hyperframes`, `.claude/worktrees/ui` @ `agent/ui/b54-staged-flow`, both cut from post-B5.2 main `a7c91e2`). Lead watching from the main terminal for lane commits/messages; standing reminder to lanes: dep additions = edit package.json then STOP-and-report — `npm install` runs in MAIN only (preinstall guard; junction propagates). Merge train on lane completion: render → ui, then B5.3 closes the sprint on the lead terminal.
