# COORDINATION — lane board

> Parallel-lane ledger (protocol: `docs/SPINE.md` §5). **One writer per row** — the lead owns assignments + merge-order; each owner writes only its own `status`. Messages append-only. Status vocab: `pending · in_progress · blocked:<what> · review · merged`. **Contract** = `packages/contracts` + the drizzle schema — frozen per sprint once committed; a lane needing to edit it mid-flight = re-plan, not an ad-hoc edit. Merges serialize through `main` in merge-order: rebase → CI green (guard + lint + tests) → review → merge; never on red.

## Sprint 0 — proposed lanes (activate on founder go)

| lane | owner | owns (glob) | branch | status | depends-on | merge-order |
|------|-------|-------------|--------|--------|------------|-------------|
| a-core | lead terminal | `packages/**`, `proprietary/**`, `apps/web/**`, `eval/**` (B0.3 → B0.4) | `agent/a/b03-schema` → `agent/a/b04-eval` | merged | — | 1 |
| b-aws | second terminal (`claude --worktree b05-aws`) | `infra/**` (B0.5) | `worktree-b05-aws` | merged | AWS sub-account ([you]) | 2 |

## Sprint 1 — lanes (cut 2026-07-04, founder go)

| lane | owner | owns (glob) | branch | status | depends-on | merge-order |
|------|-------|-------------|--------|--------|------------|-------------|
| engine | lead (worktree subagent) | `packages/engine/**`; additive-only: embedding tier in `packages/platform/src/**`, new repo fns in `packages/db/src/repos/*` | `agent/engine/b11-ingest` → `agent/engine/b12-fanout` | merged | contract (frozen ✅); `AI_GATEWAY_API_KEY` for live runs ([you] — tests keyless) | 1 |
| judge | lead (worktree subagent) | `proprietary/judge/**`, `proprietary/prompts/judge-*`; sanctioned surgical edit: I1 check in the `packages/db` transition fn if missing | `agent/judge/b13-harness` | merged | contract (frozen ✅); fixture drafts, no engine import | 2 |
| ui | lead (worktree subagent) | `apps/web/**` | `agent/ui/b14-approve-queue` | merged | contract (frozen ✅); MSW mock, no engine/judge dep | 3 |
| eval | lead terminal | `eval/**` (B1.5 arming) | `agent/eval/b15-exit-gate` | merged | B1.2 merged | 4 |

## Sprint 2 — planned lanes (cut after B2.2 contract re-freeze; launch requires fresh founder go)

> B2.1 (code-free seam proof) and B2.2 (the contract window — one owner) run solo on the lead terminal, in that order, **before** any lane launches. Default execution: **two waves of 2** (waterfall+exemplar, then demo+ui) as in-session worktree subagents; all-4-concurrent = Mode B (founder-opened terminals) if pulled.

| lane | owner | owns (glob) | branch | status | depends-on | merge-order |
|------|-------|-------------|--------|--------|------------|-------------|
| waterfall | lead (worktree subagent) | `packages/engine/waterfall/**`, `proprietary/prompts/highlight-*` | `agent/waterfall/b23-clip-plans` | merged | B2.2 merged ✅ | 1 |
| exemplar | lead (worktree subagent) | `packages/engine/exemplar/**`, `proprietary/prompts/fanout-exemplar-*`, `proprietary/profiles/**` | `agent/exemplar/b24-library` | merged | B2.2 merged ✅ | 2 |
| demo | lead (worktree subagent) | `packages/engine/demo/**`, `proprietary/prompts/storyboard-*` | `agent/demo/b25-plan-capture` | merged | B2.2 merged ✅ | 3 |
| ui | lead (worktree subagent) | `apps/web/**` | `agent/ui/b26-queue-formats` | merged | B2.2 contract mocks ✅ | 4 |

## Sprint 4 — hardening lanes (cut 2026-07-05; launch = founder's paste of the session-9 resume prompt, which names exactly these runs)

> A10 pass-2 rules bind every lane: no new features, zero live spend, generation keys + content hashes byte-stable (pin before moving code). **Sprint-4 contract window** = B4.2's registry (`packages/contracts`, core lane) + B4.3's new tables (drizzle, intel lane) — disjoint files, additive-only, freeze after both merge. 2 concurrent subagents = in-session mode (Mode B not needed). B4.6 was pulled INTO the core chain (its key-scheme helper refactors the same render/deploy/capture files as B4.1 — not lane-safe).

| lane | owner | owns (glob) | branch | status | depends-on | merge-order |
|------|-------|-------------|--------|--------|------------|-------------|
| core | lead terminal | `packages/contracts/**`, `packages/engine/src/{origination,webpage,demo,render,waterfall,fanout,exemplar}/**`, `packages/platform/src/object-store.ts`, `packages/db/src/repos/drafts.ts` (B4.4 events gap), `.github/**` + branch protection (B4.9 first, quick) | `agent/core/b41-engine` (single branch, 7 bucket commits) | merged | — | 1 |
| intel | lead (worktree subagent → lead-finished) | `packages/engine/src/trend/**`; `packages/db`: new schema file (`schema/intel.ts`), new repos (`watchlists`/`trend-snapshots`) + additive `repos/index.ts`, migration 0003, db tests incl. tenant-id table list | `agent/intel/b43-trend-storage` | merged | contract window co-owner (additive drizzle only) | 2 |
| tooling | lead (worktree subagent → lead-finished) | `scripts/doctor*`, root `package.json` (additive `doctor` script), machine-global installs (Remotion · whisper.cpp/faster-whisper · ffmpeg · Vercel CLI), `packages/engine/src/ingest/**` (B4.8 transcript providers), engine `package.json` additive deps | `agent/tooling/b47-doctor` → `agent/tooling/b48-transcription` | merged | — | 3 |

Sanctioned out-of-glob, additive-only (established pattern): both subagent lanes may add export lines to `packages/engine/src/index.ts`. Standing kickoff caveats: skip lint in lanes (lead re-runs at merge); `@thalon/*` vitest aliasing (engine config = reference); contract friction ⇒ stop-and-report, never edit; git identity re-set per worktree.

## Sprint 5 — proposed lanes (cut 2026-07-05 at the A11 charter; activate on fresh founder go next session — each subagent launch needs it)

> A11 rules bind every lane: zero live spend, keys/hashes byte-stable, video-creation scope only. **Required kickoff reading for every lane: `docs/research/hyperframes-integration.md`** (founder-directed docs read-around, 2026-07-05) — it pins the composition contract + producer API for B5.1/B5.2, the lint-before-render core gate, and the player/SDK surfaces B5.4 builds on (live preview needs NO render; operator edits arrive as RFC-6902 patches → store verbatim in `edit_diffs`). **Sprint-5 contract window** = B5.2's stage registry + `storyboard`/`direction_doc` schemas in `packages/contracts` — ONE owner (pipeline lane), freeze at its merge; the ui lane consumes contract mocks after the freeze. B5.1/B5.3 are disjoint from the window and from each other.
>
> **Execution = Mode B (founder preference, 2026-07-05):** pipeline solo on the lead terminal FIRST (window discipline, mirrors B2.2); then render + ui in **founder-opened terminals**, each on a **lead-prepped worktree** — lead runs `git worktree add` + **`npm run worktree:setup -- .claude/worktrees/<lane>`** (junctions node_modules, copies env/vault includes, verifies identity+eslint+guard; ratchet landed 2026-07-05) and authors the kickoff prompt per window; ratchet lane closes on the lead terminal. **Caveat changes vs prior sprints:** "skip lint in lanes" is RETIRED — eslint works in a prepped worktree (lead still re-runs at merge); NEW hard rule: **never `npm install` inside a worktree** (npm replaces the junction with a real folder — root `preinstall` guard blocks it; dep additions = edit package.json in the lane, install runs in main). `@thalon/*` vitest aliasing is now complete across db/eval too (was engine/judge/web only).

| lane | owner | owns (glob) | branch | status | depends-on | merge-order |
|------|-------|-------------|--------|--------|------------|-------------|
| pipeline | lead terminal | `packages/contracts/**` (stage registry + new format schemas), `packages/engine/src/direction/**` (new), `packages/engine/src/pipeline/**` (additive), `proprietary/prompts/direction-*` + `storyboard-stage-*` | `agent/pipeline/b52-stages` | merged | — (contract-window owner) | 1 |
| render | founder terminal (Mode B, lead-prepped worktree) | `packages/engine/src/render/**` (hyperframes driver + fake + composition templates), engine `package.json` additive deps (install runs in main), `scripts/doctor*`, `docs/adr/0004-render-driver-default.md` | `agent/render/b51-hyperframes` | merged | — | 2 |
| ui | founder terminal (Mode B, lead-prepped worktree) | `apps/web/**` | `agent/ui/b54-staged-flow` | merged | B5.2 merged (contract freeze) | 3 |
| ratchet | lead terminal (quick) | `packages/engine/src/__tests__/shell-inventory.test.ts`, `docs/SPINE.md` §1 refresh | `agent/ratchet/b53-shell-pin` | merged | — | 4 |

## Sprint 6 — proposed lanes (cut 2026-07-06 at the A12+A13 charter; activate on fresh founder go next session — each launch needs it)

> A12/A13 rules bind every lane: no publish path; official APIs only; honest-claims rule on landing copy (ADR 0006); guard hygiene — founder reference imagery stays outside the repo (the brand folder path contains guard token A). **Required kickoff reading:** `docs/FRONTEND.md` + `docs/adr/0005-pass3-recharter.md` + `docs/adr/0006-search-intel.md`; web lanes additionally read `node_modules/next/dist/docs/` (apps/web pins a breaking-changes Next.js — per `apps/web/AGENTS.md`). **Sprint-6 contract window** = ONE owner (contract lane, lead terminal, opens the sprint): `waitlist` + monitored-areas + `search_targets`/`search_snapshots` tables (mirror `watchlists`/`trend_snapshots`) + format-registry SEO-meta capability — all additive; freeze at its merge; every other lane consumes the frozen contract. **One web writer per wave** (`apps/web` globs never overlap across live lanes — Sprint-1/2 lesson).
>
> **Execution = Mode B (standing founder preference):** contract solo on the lead terminal FIRST; then wave 1 in founder-opened terminals on lead-prepped worktrees (`git worktree add` + `npm run worktree:setup -- .claude/worktrees/<lane>`; lead authors each kickoff; **never `npm install` inside a worktree** — dep additions edit package.json in the lane, install runs in main). Wave 2 cuts after `landing` merges (design tokens are the dependency); wave-3 work (B6.5 live drivers · B6.6 origination loop · B6.7 deploy) is integration-heavy and defaults to the lead terminal, re-planned at the wave-2 merge checkpoint.

| lane | owner | owns (glob) | branch | status | depends-on | merge-order |
|------|-------|-------------|--------|--------|------------|-------------|
| contract | lead terminal | `packages/contracts/**` (areas + search-target configs, SEO-meta capability), `packages/db/src/schema/**` + `drizzle/**` (waitlist · areas · search tables), `packages/db/src/repos/*` (new repos, additive) | `agent/contract/b6-window` | merged | — (contract-window owner) | 1 |
| landing | founder terminal (Mode B, lead-prepped worktree) | `apps/web/**` (design tokens · `/` landing · waitlist API · mark SVGs), `proprietary/prompts/keyword-manual-pass` notes | `agent/landing/b61-brand-landing` | merged | contract freeze | 2 |
| intel-engine | founder terminal (Mode B, lead-prepped worktree) | `packages/engine/src/trend/**` (area→query expansion · config-weighted ranker · area-tagged ingest) | `agent/intel/b64-areas-ranker` | merged | contract freeze | 3 |
| search-engine | founder terminal (Mode B, lead-prepped worktree) | `packages/engine/src/search/**` (NEW: seed compiler · horizon math · on-page checks · `SearchIntelSource` seam + fake + GSC skeleton), `proprietary/judge/**` (SEO/AEO lens), `proprietary/prompts/keyword-*` | `agent/search/b68-search-intel` | merged | contract freeze | 4 |
| workspace (wave 2) | founder terminal (Mode B, lead-prepped worktree) | `apps/web/**` (shell · dashboard · queue/staged restyle · profile editor · Intel Trends/Search tabs vs contract mocks) | `agent/workspace/b62-shell` | merged | landing merged (tokens) | 5 |
| self-tenant (wave 2) | lead terminal (quick) | `proprietary/profiles/**` (tenant #0 deep profile), demo-clip render scripts | `agent/tenant/b63-self-profile` | merged | landing merged | 6 |

## Sprint 6 — wave 3.5 lanes (cut 2026-07-07, session 20; the session-19 resume prompt carries the launch go for exactly these three + the lead inline work)

> Step-0 assessment (lead, 2026-07-07): **NO contract window this wave** — see the session-20 message below. Contract stays FROZEN at PR #25. Mode B per the standing preference: lead-prepped worktrees (`npm run worktree:setup`), founder-opened terminals, kickoffs in gitignored `.context/kickoffs/{origination,landing-v2,composition-v2}.md` (both mini-contracts — the session-19 `sources.meta` keys and the new posts-bundle shape — are pinned verbatim in the kickoffs). **One web writer** = landing-v2. **Never `npm install` inside a worktree.** Merge train: origination → composition-v2 → landing-v2 (lead train commits: FeatureLoop swap w/ lane-3 clips · live blog-read arming · any one-line env/inventory integrations).

| lane | owner | owns (glob) | branch | status | depends-on | merge-order |
|------|-------|-------------|--------|--------|------------|-------------|
| origination | founder terminal (Mode B, lead-prepped worktree) | `packages/engine/src/{origination,webpage,ingest}/**`, `proprietary/prompts/` page-loop prompts; sanctioned additive out-of-glob: engine `index.ts` exports · new repo fns · `eval/src/dogfood-page-loop.ts` + one `eval/package.json` script line · `PROTECTED_PREFIXES` += `"posts/"` one-liner | `agent/origination/b66-loop` | merged | — | 1 |
| composition-v2 | founder terminal (Mode B, lead-prepped worktree) | `packages/engine/src/{render,demo}/**`, `proprietary/profiles/demo-clips/**`, `eval/**` (minus origination's three named sanctioned edits); sanctioned additive: engine `index.ts` exports | `agent/render/composition-v2` | merged | — | 2 |
| landing-v2 | founder terminal (Mode B, lead-prepped worktree) | `apps/web/**` (THE web writer: §8 uplift + §9 blog v1 + Library rider) | `agent/landing/v2-blog` | merged | — | 3 |

## Messages (append-only)

- 2026-07-03 lead: board created. B0.3→B0.4 vs B0.5 are file-disjoint per the charter; Lane B is inert until the founder opens the second terminal (or we fall back to sequential). Worktree prereqs landed: `.worktreeinclude`, `.claude/settings.json` symlinks `node_modules`, dev DB/store/queue isolate per worktree via the local seams.
- 2026-07-03 lead: B0.3 complete on `agent/a/b03-schema` → at the founder checkpoint (status `review`). Contract (`packages/contracts` + drizzle schema) is now committed — **frozen for the sprint**. Lane B unaffected (`infra/**` untouched).
- 2026-07-03 b-aws: B0.5 credential-free scaffold at review — CDK-Python skeleton (`infra/`), `ThalonGithubOidc` stack (OIDC provider + `thalon-github-deploy` role, assume-cdk-roles-only, main-branch-scoped trust), 7 synth-time tests green, `cdk synth` verified. Blocked on founder one-time steps (sub-account → bootstrap → hand-deploy OIDC stack → set `AWS_DEPLOY_ROLE_ARN`): runbook in `infra/README.md`. For the lead at merge: move `infra/github-workflow/deploy-infra.yml.example` → `.github/workflows/` and add an infra pytest job to CI (both outside this lane's glob). Handoff note: `agent_handoff/CURRENT.md` left untouched — lead-owned during parallel lanes; this message is Lane B's wrap record.
- 2026-07-03 b-aws: AWS side of B0.5 is live — founder supplied the sub-account; `cdk bootstrap` done (ap-southeast-2), `ThalonGithubOidc` deployed by hand (the one-time chicken-and-egg deploy), deployed trust policy read back and verified against the tests, repo variable `AWS_DEPLOY_ROLE_ARN` set. Account id + local access path recorded outside the repo (agent memory / founder), per the never-commit rule. Only remaining B0.5 item: lead activates `deploy-infra.yml.example` at merge — the first green OIDC-assumed run from `main` is the final proof.
- 2026-07-03 b-aws: credential-door correction (founder): local AWS access now goes through IAM Identity Center only — the `thalon-admin` profile was repointed from an unrelated IAM-user hop to SSO, and identity + deployed stack were re-verified through the new path (resources were already in the correct account; nothing redeployed). Ratchet: `infra/scripts/sso-login.py` — CLI-free device-flow login (the AWS CLI installer is blocked on the dev machine); it writes the standard token cache so both boto3 and the CDK CLI work. Portal URL / account ids stay out of the repo — the script reads them from `~/.aws/config`.
- 2026-07-03 b-aws → lead: your SSO directive executed (one design deviation: the helper writes the shared SSO token cache instead of minting temp keys into the profile — boto3 AND the CDK CLI then resolve/re-mint automatically, no plaintext keys on disk). Proven on SSO creds: sts identity, describe-stacks, and `cdk diff` (exercises the CDK CLI's credential chain end-to-end). The static-key IAM user is **deleted** — founder authorized doing it from here instead of the console: access key + PowerUserAccess/IAMUserChangePassword + console login profile removed, `NoSuchEntity` verified, zero IAM users remain in the management account. Human door = Identity Center; CI door = GitHub OIDC role. Lane B is clear-safe; B0.5 awaits your merge (workflow rename + infra pytest job in CI).
- 2026-07-03 lead: merge train complete (founder-delegated): PR #2 (B0.3) and PR #3 (B0.5, rebased onto post-B0.3 main; one COORDINATION.md reconcile — the expected shared-file overlap) merged in order; deploy workflow activated + infra pytest job added to CI; **first OIDC-assumed `deploy-infra` run from `main` green — B0.5 closed**. Both lanes `merged`; board idle until the next lanes are cut. Standing founder directive: lead drives merge mechanics end-to-end; founder handles cloud-console/terminal-only steps.
- 2026-07-04 lead: Sprint-1 lanes cut per SPINE §5 on founder go. Three worktree subagents launched (engine B1.1 · judge B1.3 · ui B1.4-vs-MSW); eval lane stays pending until the train reaches B1.5. Contract stays frozen — a lane hitting contract friction stops and reports (re-plan, not ad-hoc edit). Merge train serializes engine → judge → ui → eval; lead drives rebase/PR/merge per standing directive. Housekeeping done: `b05-aws` worktree dir + local branch GC'd. [you] outstanding: `AI_GATEWAY_API_KEY` (Vercel dashboard → AI Gateway → API keys → into `apps/web/.env.local`) — needed for B1.1 live embeddings only; all lane tests run keyless.
- 2026-07-04 lead: **Sprint-1 merge train complete — B1.1 (PR #5) → B1.3 (PR #6) → B1.4 (PR #7) merged in order, CI green each time.** Lead integration commits during the train: (a) judge G3 calls now metered through the `withGatewayGuard` choke point (the lane's real driver had bypassed budget/tracing; `BudgetExceededError` is an operational halt, not a verdict); (b) approve-queue selection resets moved out of effect bodies (2 eslint errors the lane's worktree didn't surface). Ratchets picked up along the way: PGlite boot-test timeout 30s (flaky under lane disk contention); root vitest projects now include `proprietary/*` (judge suite silently never ran under `npm test` before); worktree lesson — `@thalon/*` imports in worktrees resolve to the MAIN checkout via the node_modules symlink; lanes must alias `@thalon/*` to their own worktree src in vitest/tsconfig (engine's configs are the reference). Known follow-ups: `packages/db` bulk `runs.list()`/`drafts.listByRun()` (UI hydrates via events spine meanwhile); unique index on `sources (tenant_id, content_hash)` at next contract window. All lane worktrees GC'd. Remaining Sprint 1: B1.2 fan-out (engine lane, kickoff drafted, awaiting founder go per standing approval rule) → B1.5 exit gate.
- 2026-07-04 lead: **B1.2 founder go received** — engine-lane worktree subagent launched on `agent/engine/b12-fanout` (single lane, so in-session worktree per the mode rule; Mode B reserved for >2 concurrent lanes per founder directive today). Founder has obtained `AI_GATEWAY_API_KEY`; `apps/web/.env.local` created from the template for the paste (gitignored, verified) — unblocks B1.5 dogfood, not needed by B1.2 tests.
- 2026-07-04 lead: **B1.2 merged (PR #8) — Sprint 1 code complete; only B1.5 exit gate remains.** Lane delivered fan-out + profiles-as-data + versioned prompt in one pass; lead review caught one correctness bug pre-merge (idempotent fast-path replayed a *partial* draft set after a mid-run irrecoverable failure — silent, and permanently stuck behind the generation key), lane fixed it with self-healing backfill (replay generates only the missing platforms; clean replay stays zero-call). 109/109 tests + typecheck + lint verified on the integration checkout; CI green; worktree + branches GC'd. **Follow-up (ratchet candidate, structural):** `.claude/settings.json` declares `worktree.symlinkDirectories: ["node_modules"]` but orchestrator-spawned (Agent-tool) worktrees get a plain empty dir instead of a link — imports still resolve by walking up to the main checkout (the known `@thalon/*` aliasing hazard) but npm-run binaries (eslint) silently don't; until fixed, lane kickoffs must say "skip lint, lead verifies at merge" and the lead always re-runs lint before merging.
- 2026-07-04 lead: **B1.5 merged (PR #9) — SPRINT 1 COMPLETE.** Dogfooded live on tenant #0: ingest → fan-out → judge-block → operator edits (5 eval rows captured transactionally, exported) → re-judge → queued through the I1 gate → human-approved; golden-G3 live run 6/6; CI `eval-gate` job armed (keyless deterministic suite = ship gate; model-graded `golden:g3` stays out of CI by invariant). The two-tier judge proved itself editorially: sonnet final failed a draft for an added rationale the source never stated, which the haiku screen had passed — disagreement blocked, exactly per ratified decision 2. Ratchets landed in-change: `lastError` surfaced through both bounded-retry loops (a gateway 400 and free-tier 429s all read as "malformed shell output" before); eval CLIs refuse the shared dev DB while the dev server holds it (PGlite single-writer — a server-side edit was invisible to the CLI and lost on shutdown); export CLI now targets the web app's data dir (silently exported an empty cwd-relative DB before); db client migrations lookup no longer uses `new URL(rel, import.meta.url)` (Turbopack bundles the pattern — first real apps/web boot against the DB path failed on it). **Post-sprint, founder-requested: `claude-cli/<alias>` dev model transport (PR #10)** — build/test tiers run on the founder's Claude subscription via headless Claude Code; production stays on the gateway; embeddings excluded. Follow-ups for a future window: approve-panel doesn't refresh after save-edit; aborted fan-out runs show as empty runs in the feed; no operator "re-judge" action for operationally-blocked drafts (edit is the only path back to judging); gateway credit top-up restores the ratified sonnet final tier ([you], optional until Sprint 3).
- 2026-07-04 lead: **B2.2 merged (PR #12) — the Sprint-2 contract window is closed and re-frozen; wave-1 lanes are cuttable.** One migration carried the whole window: `source_chunks.start_ms/end_ms` · 4 new source kinds · `modality`/`visual_ref` (the B3.7 seam, column-only) · `source_metrics` (generic name/value, append-only) · the `sources (tenant_id, content_hash)` unique index (Sprint-1 follow-up closed). Timed ingest live: fail-loud SRT/VTT/plain caption parser, segment-atomic timed chunking (chunk media windows stay honest), `TranscriptProvider` seam with caption-file as the shipped zero-dep driver. Founder-prompted ratchet in the same PR: root eslint over packages/proprietary/eval + `npm run lint` as a CI step — lint now gates the PR, not the lead's merge pass (B1.4 lesson made structural). Direction pinned from founder Q&A: the Whisper transcript driver will be in-house and Windows-native (whisper.cpp / faster-whisper, MIT) behind the existing seam; B3.7 visual-ingest is a port-the-pattern job, not a sidecar adoption (Apache-2.0 — no clean room needed; render tiles via Playwright → object store via `visual_ref` → vectors in pgvector → ONNX/DirectML or torch inference; WSL2 as interim dev bridge; verify the fine-tuned weights' licence at bucket time). Next: **founder go to cut wave 1** (waterfall + exemplar lanes per the board above).
- 2026-07-04 lead: **B2.1 merged (PR #11, rebase-merge so the proof commit stays legible on main) — the Sprint-2 seam proof is in.** Recon surfaced two Sprint-1 hardcodes that would have blocked any pure-data tenant: the dogfood CLI could only run built-in tenant #0, and apps/web hardcoded slug `self`. Prep commit made both runtime config (dogfood takes a Zod-validated JSON input file; `DEMO_TENANT_SLUG` env via the platform env module) and left a ratchet: an eval test validates every `proprietary/profiles/tenants/*.json` — landed before the first tenant file existed. Proof commit = `fernwood.v1.json` + README lines, **zero code** (exit criterion met literally). Live slice on tenant #2: x pass/pass → queued; linkedin **blocked on a real g3 tier disagreement** (screen=pass, final=fail) — invariant I3 proven on a second tenant; replay run idempotent (same ids, zero re-judge spend). Both profile-resolution paths exercised (tenant override for linkedin, shipped default for x). Founder triage: `DEMO_TENANT_SLUG=fernwood npm run dev` → /approve. Next: B2.2 timed ingest + contract window (lead, solo) at founder go, then wave-1 lanes per the Sprint-2 board above.
- 2026-07-04 lead: **Amendment A5 landed (founder-approved at the Sprint-1 exit review)** — Sprint 2 expands to B2.1–B2.6; see `CHARTER.md` + `docs/adr/0002-sprint2-expansion.md`. Sprint-2 lane plan added above (pending; launch needs fresh founder go at cut time per standing rule). Known worktree caveat carries over: orchestrator-spawned worktrees miss the node_modules symlink — lane kickoffs say "skip lint, lead verifies at merge". Next: B2.1 (lead, solo, zero-code proof) → B2.2 contract window → wave 1 (waterfall+exemplar).
- 2026-07-04 lead: **Wave-1 lanes launched on founder go** — waterfall (B2.3) + exemplar (B2.4) as in-session worktree subagents (2 concurrent = in-session mode; Mode B not needed). Kickoffs carry the standing caveats: skip lint (lead re-runs at merge), `@thalon/*` vitest aliasing (engine config is the reference), contract frozen (friction ⇒ stop-and-report, never edit). Sanctioned out-of-glob edits, additive-only: both lanes may add export lines to `packages/engine/src/index.ts` + new repo fns in `packages/db/src/repos/*`; exemplar additionally gets one surgical additive edit to `fanout.ts`/shell generator to thread optional exemplar context + provenance (waterfall doesn't touch fanout, so no cross-lane conflict; exemplar-off path must stay byte-identical, proven by untouched existing tests). Merge train: waterfall → exemplar; rebase → CI green → review → merge, per SPINE §5.
- 2026-07-04 lead: **Wave-1 merge train complete — B2.3 (PR #13) → B2.4 (PR #14) merged in order, CI green each time.** Lead review caught one correctness bug pre-merge on waterfall (same class as B1.2's): the highlight-select boundary accepted duplicate `windowIndex` selections — schema-valid, in-range — which would collide on the content-addressed draft generation key mid-persist and leave a half-persisted platform that platform-granularity backfill skips forever; fixed at the validation boundary (duplicate = schema-invalid, consumes a repair attempt) + 2 tests, including proof that an always-duplicating shell persists nothing. Exemplar review clean — the three risk points all verified: PII strip runs before content-hash (nothing raw ever stored), `stableStringify` drops `undefined` so plain-run generation keys are hash-stable across the upgrade (exemplar-off = byte-identical, existing fanout tests unmodified), and the overlap gate returns an outcome instead of throwing (a breach blocks only that draft via generated→judging→blocked through the one transition fn). Integrated verification: 187/187 root suite, typecheck, lint 0 errors, guard — first run with both modules together. Micro-cleanup noted for whoever's next in `fanout.ts` (likely B2.6): the exemplar-aware path reads `sourceChunks.listBySource` twice (second read's result unused when precomputed) — harmless local read. Worktrees + branches GC'd. Next: **wave 2 (demo B2.5 + ui B2.6) needs fresh founder go**; B2.3 dogfood unblocked the moment the founder supplies a pillar caption/SRT file.
- 2026-07-04 lead: **Wave-2 lanes launched on founder go** — demo (B2.5) + ui (B2.6) as in-session worktree subagents (2 concurrent = in-session mode; Mode B not needed). Standing caveats carried: skip lint (lead re-runs at merge), `@thalon/*` vitest aliasing (engine config is the reference), contract frozen (friction ⇒ stop-and-report, never edit). **Cross-lane pin:** `demo_plan` draft body/meta stub authored verbatim into both kickoffs — B2.5 emits it (additive extensions allowed), B2.6 renders exactly the pinned core; body = step narrations joined `\n\n`, mirroring clip_plan's body convention (I1 body_hash binding). Sanctioned out-of-glob, additive-only: both lanes may add export lines to `packages/engine/src/index.ts` + new repo fns in `packages/db/src/repos/*` (B2.6 may land the queued bulk `runs.list()`/`drafts.listByRun()` follow-up this way); demo may add the `playwright` dep (Apache-2.0) to `packages/engine/package.json` + lockfile — tests stay browser-free behind a driver seam so CI never needs a browser install. Founder decisions this session: B2.5 dogfood target = a known brand's **public docs-search flow** (Anthropic docs first) — runtime input only, never committed; auth-walled chat products (claude.ai / chatgpt.com) are out per A5's no-ToS-evasion line; robots.txt + rate-limit checks in core make disallowed targets fail loudly by construction. Merge train: demo → ui.
- 2026-07-05 lead: **Wave-2 merge train complete — B2.5 (PR #15) → B2.6 (PR #16) merged in order, CI green each time. SPRINT 2 IS CODE-COMPLETE (B2.1–B2.6).** Review rounds pre-merge (all fixed on-branch, test-proven): demo — capture failure-path teardown ordering (a throwing `driver.finish()` could leave a stale prior-success `captureRef` as a durable lie; the failed-meta write now always lands), robots.txt fetch moved behind the rate limiter, `drafts.updateMeta` gained optimistic concurrency (`expectedUpdatedAt` in the UPDATE's WHERE; typed `ConcurrentUpdateError`, never silent last-write-wins), robots rules now merge across ALL matching UA groups (was first-group-only, fail-open). ui — the substantive one: re-judge/save-edit flipped drafts to `judging` but **nothing in apps/web ever ran the judge** (only the dogfood CLI called the pipeline; a re-judged draft was an unrecoverable operator dead end). Lead-sanctioned fix: apps/web now depends on `@thalon/judge`; `judge-runner.ts` wires `runJudgePipeline` exactly like dogfood (metering stays inside the pipeline's `withGatewayGuard` choke point), both paths judge in-request, failures rethrow loudly (`role="alert"`) with the draft honestly at `judging`; plus a stale-meta indicator when `body` diverges from meta-derived text post-edit. Lint rounds (the known lane blind spot, lead-fixed on-branch): 2 errors on demo, 1 on ui — the ui one re-tripped the B1.4 `set-state-in-effect` class; `loadDraftDetail` rewritten promise-chain-form (the analyzer can't see through async fn boundaries — pattern note now in the file). Rebase reconcile: both lanes added additive fns to `packages/db/src/repos/drafts.ts` (`updateMeta` + `reJudge`) — kept both. Integrated verification on the rebased ui branch pre-merge: 300 passed / 2 skipped (browser-gated), typecheck, lint 0 errors, guard. Worktrees + local/remote lane branches GC'd. **Founder process directive (recorded in agent memory): an approval covers exactly the named agent runs — reviewer agents and fix-round resumes each count; any extra agent run = pause for approve-or-defer.** Follow-ups carried: `fanout.ts` double `sourceChunks.listBySource` read (harmless; next fanout window), pre-existing `_dataDir` lint warning in `apps/web/src/app/api/health/route.ts`, B2.5 capture discards the partial event trace on a failed drive (debuggability nicety). Next: **Sprint-2 exit review** — dogfood B2.3 ([you]: pillar caption/SRT) + B2.5 live against the founder-pinned target (a known brand's public docs-search flow, runtime input only), then Sprint-3 re-charter at its own checkpoint.
- 2026-07-05 lead: **Sprint-4 lane board cut** (pass-1 breadth complete `77820b3`/`c3ac987`/`cc692f9`; A10 chartered `fd16c79`). Founder approved parallel workflow in-session; the session-9 resume prompt IS the launch go for exactly the two named subagent lanes (intel B4.3 · tooling B4.7→B4.8) alongside the lead's core chain (B4.9 → B4.1 pins → extraction → B4.2 → B4.4/B4.5 → B4.6). Any agent run beyond those named (reviewers, fix-round resumes) = pause for approve-or-defer per the standing founder rule. Merge train: core → intel → tooling; contract freezes after B4.2 + B4.3 merge.
- 2026-07-05 lead: **SPRINT 4 CODE-COMPLETE in one session (B4.1–B4.9 minus one deferred column) — lanes launched per the founder's resume-prompt go, then finished lead-inline on founder direction after the tooling subagent died mid-run on a session limit ("do everything yourself").** Sequence: B4.9 first (CI `test` + `eval-gate` now REQUIRED checks — they gated every merge below). Core (PR #17, rebase-merged): B4.1 key-stability pins FIRST (hash vectors, all three generation-key field sets, artifact key schemes, judged-body derivations) → the three extractions (`pipeline/repair-loop.ts` · `pipeline/single-draft.ts` · `pipeline/artifact-stage.ts`, behavior-preserving, pins prove byte-stability) → B4.2 format registry in contracts (apps/web's hand-synced schema mirrors DELETED — the registry is why web can stay engine-free) → B4.4 ratchets (events coverage + getGateway boundary scan) + B4.5 taxonomy (InvalidState/ArtifactMissing/IrrecoverableGeneration) → B4.6 store hygiene (one key helper, verified reads, `npm run -w @thalon/eval sweep`). Intel (PR #18): subagent landed schema+migration 0003 at its pause boundary; lead finished repos (events-audited, tenancy-walled), pure longitudinal Δ-velocity math, and intake history wiring (EVERY polled item accrues a `trend_snapshots` row; exemplar content hash byte-pinned first). Tooling (PR #19): salvaged the dead subagent's uncommitted doctor + installs (all four runtimes verified live-ready — ffmpeg was already installed, just PATH-stale), finished `npm run doctor`, then B4.8 (TranscriptProvider registry env-selected; whisper-local skeleton REFUSES remote URLs per A5/A8; hosted-vendor keyed adapter; video-URL ingest surface hashing the fetched transcript). **Deferred, deliberately:** `fanout_runs.lastError` (B4.5's last item) waits for migration 0004 so the drizzle journal stayed linear behind intel's 0003 — next session's first act. The ratchets earned their keep on the tooling PR: two red CI rounds caught the lead's own gaps (flat-config `no-undef` on `scripts/*.mjs` node globals; the repo-ratchet boundary test rejecting direct `process.env` reads in the new providers — both fixed at the choke points, on-branch). Final verified state on merged main: root suite 362 → **424 passed / 2 skipped**, lint 0 errors, guard green on every commit, `npm run doctor` all-green from the main checkout (the B4.9 documented-command pass ran `npm install` · `npm run guard` · `npm run doctor` · `npm test` · `npm run lint` verbatim); all lane worktrees + branches GC'd.
- 2026-07-05 lead: **B4.5 residue closed (PR #20, rebase-merged, `ea3f41c`) + A11 chartered — SPRINT 4 FULLY COMPLETE; pass 2.5 (Sprint 5) is next.** Residue delivered beyond the handoff's minimum on the charter's general wording: `fanout_runs.last_error` (migration 0004, additive) + single-writer `fanoutRuns.recordLastError` (tenant-scoped, events-audited `last_error_recorded`/`_cleared`) wired into ALL THREE generation paths (single-draft spine + fanout + waterfall — the latter two's plain irrecoverable `Error`s upgraded to `IrrecoverableGenerationError`), recorded before the throw, cleared on a successful backfill; `FeedRun` wire type carries it to the queue for free. Suite 424 → 428 / 2 skipped; typecheck + lint + guard green; key pins untouched. Then the Sprint-4 exit checkpoint ran in-chat: founder directed **pass 2.5 — deterministic video creation** (ratified: Hyperframes default render driver · strict-schema direction.md · staged-flow UI in-pass) — chartered as **A11 / Sprint 5 (B5.1–B5.4)**, lane board cut above (pending; activation needs fresh founder go next session per the standing rule). Hyperframes verified real before chartering: HeyGen's Apache-2.0 HTML→video framework, 33k stars, active (v0.7.33 2026-07-04), headless-chromium+ffmpeg engine — both already in the toolchain.
- 2026-07-05 lead: **Hyperframes docs read-around done (founder-directed) → `docs/research/hyperframes-integration.md` landed as required Sprint-5 kickoff reading.** Load-bearing finds: (1) `@hyperframes/lint` is browser-free static analysis → a CORE validation gate between template generation and any render spend (SPINE §1); (2) `@hyperframes/player` gives live in-queue preview with NO render — B5.4's stage previews are free; (3) the SDK emits RFC-6902 patches with inverses → operator edits become machine-readable `edit_diffs` rows verbatim, undo included; (4) **keyless local audio stack** (Kokoro TTS · Whisper word-level caption alignment · MusicGen · bundled SFX; "no key configured is a normal state") → pass-3 TTS gets a $0 local default, hosted voices stay optional keyed adapters per A5; (5) variables = typed template slots with `--strict-variables` (root w/h/duration/fps are compile-time — the deterministic generator bakes them from the direction doc); (6) their own website-to-video pipeline ships DESIGN.md → SCRIPT.md → STORYBOARD.md → per-beat compositions — independent validation of the A11 storyboard+direction.md design (ours adds schemas/judge/tenancy); (7) scale paths fit existing rails (CDK `HyperframesRenderStack` for the sub-account; Apache-2.0 Vercel render-API template). Platform caveat recorded: BeginFrame determinism is Linux-only, Windows dev falls back to screenshot capture — harmless (render cache keys on manifest hash), Docker/Lambda for prod artifacts.
- 2026-07-05 lead: **Mode B unblocked (founder direction: fix the worktree node_modules issue before Sprint 5).** Root cause diagnosed on-machine: `worktree.symlinkDirectories` needs TRUE symlinks, which Windows denies without admin/dev-mode → Claude-created worktrees silently got an EMPTY node_modules (the B1.2-era "skip lint in lanes" caveat). Fix = **junctions** (no privileges needed) via new `scripts/worktree-setup.ps1` (`npm run worktree:setup -- <path>`): junctions every node_modules main has (root · apps/web · eval · packages/db · packages/engine), copies the `.worktreeinclude` gitignored set (env files, `.context/` vault pointer, settings.local.json) for worktrees Claude Code did NOT create, then VERIFIES identity+eslint+guard — fail-loud, idempotent, repair-capable (proven: rebuilt a deliberately-broken lane; eslint v10.6.0 + contracts 9/9 + engine taxonomy 4/4 incl. PGlite all green inside the junctioned worktree). Second ratchet: root `preinstall` (`scripts/guard-worktree-install.mjs`) BLOCKS `npm install` inside any worktree (npm v7+ deletes a linked node_modules and forks the lane's dep tree — founder-flagged hazard; installs run in main, junction propagates instantly; verified blocking in-lane and passing in main). Third fix: `@thalon/*` vitest aliasing audit — **packages/db and eval had NO aliases** (their worktree tests would silently exercise MAIN's sources); both now alias sibling packages to worktree-local src, completing the engine/judge/web pattern. settings.json symlinkDirectories extended to all five node_modules (harmless where symlinks work; the script is the guarantee). Git identity confirmed inherited in worktrees (shared repo config); `.data` dev-DB isolation per-worktree confirmed by construction (cwd-relative). Sprint-5 board updated: render+ui lanes = Mode B founder terminals on lead-prepped worktrees; "skip lint" caveat retired.
- 2026-07-03 lead: **B0.4 merged (PR #4) — Sprint 0 complete.** Override capture proven end-to-end by test; golden seed committed; promptfoo + DeepEval proven keyless; Langfuse tracing seam live. Cleanup note: `.claude/worktrees/b05-aws` + local `worktree-b05-aws` branch could not be removed (directory still held open — close the Lane B terminal, then `git worktree remove .claude/worktrees/b05-aws` and `git branch -D worktree-b05-aws`); remote branch already deleted. Next: Sprint-0 exit review, then cut Sprint-1 lanes per SPINE §5 (engine → judge → ui → eval, merge in that order).
- 2026-07-05 ui: **B5.4 staged-flow surface at review on `agent/ui/b54-staged-flow` (2 commits, pushed).** The advanced-mode experience is live in the approve queue on fake drivers, engine-free per the pin: selecting a stage-artifact draft (storyboard/direction_doc) swaps zones 2+3 for the staged surface — stage rail (plan-driven, count stays config), pick-from-2–3 candidates, storyboard cards with reorder + per-beat accept/tweak chips, direction editor with FORM and RAW strict-md views over the same schema (contracts' renderDirectionMd/parseDirectionMd; parse errors surface verbatim with line numbers), style presets read from the active profile's platform config, per-stage low-res preview, and a visible capture strip; the classic 3-zone flow is untouched (test-pinned). **Capture wiring:** every interaction round-trips as a verbatim RFC-6902 patch — pick = whole-doc add, tweak = add/remove/replace on exactly the changed fields, reorder = move op + the contract's sceneIndex repairs (the patch alone yields a schema-valid doc), preset = style-field replaces incl. per-scene motion, accept = explicit empty patch (reviewed-as-is is signal too), raw-md = structured diff — recorded as CapturedEdit rows (the edit_diff payload shape). **Fake-driver honesty:** the in-memory store parses every artifact against the FROZEN pinned schemas at seed/mutation, derives bodies via DRAFT_FORMAT_REGISTRY.expectedBody (I1), and enforces the engine's advance gate (queued/approved only, engine wording); the fake judge always passes — blocked-path UX already exists in the classic queue and stayed out of scope. `/api/staged/*` routes and the MSW handlers wrap the SAME store, and the staged fixture run rides the three real read routes behind loudly-commented merges, so `npm run dev` → /approve feels the whole 3-stage walk on an empty dev DB (founder decision 3 honored literally). Verified: root suite on the lane branch **533 passed / 2 skipped** (was 489/2 at the freeze — +44 lane tests, zero regressions; one earlier red run was dev-server/PGlite contention, clean on re-run), typecheck + lint 0 errors, guard green per commit, plus a live dev-server drive of every staged endpoint AND a browser walk (pick → form tweak → advance → polish pick → final-stage doctrine badge; raw-md view byte-exact; loud 400 on an invalid patch; captures counted). **Follow-ups for the lead:** (1) `@hyperframes/player` dep NOT added (dep add = stop-and-report, and there is nothing real to play until B5.1 emits compositions) — the preview is a deterministic stub behind a clean seam; when B5.1 merges and the dep installs in main, only `components/staged/stage-preview.tsx` internals change, and the SDK's commit patches drop into the same capture field. (2) Pass-3 real-wiring point (noted in route comments): swap the `/api/staged` route internals for startVideoStages/advanceVideoStage + the real judge lane, remove the three fake-seam merges, and land staged captures through approvals.record — `edit_diffs.diff` is free JSON text but the repo fn currently hardcodes `{before, after}`, so that window needs approvals.record to accept a caller-supplied structured payload (`{kind, patch}` verbatim). (3) Approve/reject/render handoff for the final direction_doc stays with the classic panel/artifact stage in pass 3 — not part of this surface per the charter row. Handoff note: `agent_handoff/CURRENT.md` untouched (lead-owned); this message is the ui lane's wrap record.
- 2026-07-05 lead: **B5.2 merged (PR #21, rebase-merged, `a5ed7f1`) — the Sprint-5 contract window is CLOSED and FROZEN; the Mode B wave is cuttable.** Delivered: `storyboard` + `direction_doc` formats with pinned meta schemas; stage registry (count is config, default 3: structure → scenes_effects → polish; first stage produces storyboard, later stages direction_doc — schema-enforced; export is deterministic core by construction, no plan can even declare an export stage); **direction.md renderer/parser lives in CONTRACTS** (deliberate: B5.4's direction editor gets the form-view ↔ raw-md round-trip without an engine dependency) with the byte-identical round-trip ratchet pinned by a literal wire fixture; deterministic-first prefill (platform-config-or-default, loud on invalid config) + deterministic export (timeline/SRT/compile-time dims; SRT round-trips B2.2 ingest); staged pipeline where **the stage gate is structural** (advance requires queued/approved — reachable only via the judge's I1 transition) and **one-prompt mode = the same start/advance functions auto-advanced** (replay proof: same drafts, zero shell + zero judge calls, call-counted). Three new guarded shell ops for B5.3's inventory pin: `staged_video.structure/.scenes/.polish`. Sanctioned out-of-glob edits (the pin-before-move mandate, review-visible): key-stability pins extended for both new key compositions, format-registry ratchet extended, gateway-boundary allowlist += the direction shell. Suite 428 → **489 passed / 2 skipped**; pillar_script pins untouched. Worktrees for render+ui prepped next; **wave launch awaits fresh founder go** per the standing rule.
- 2026-07-05 lead: **Mode B wave LAUNCHED on founder go** — render (B5.1) + ui (B5.4) live in founder-opened terminals on the lead-prepped worktrees (`.claude/worktrees/render` @ `agent/render/b51-hyperframes`, `.claude/worktrees/ui` @ `agent/ui/b54-staged-flow`, both cut from post-B5.2 main `a7c91e2`). Lead watching from the main terminal for lane commits/messages; standing reminder to lanes: dep additions = edit package.json then STOP-and-report — `npm install` runs in MAIN only (preinstall guard; junction propagates). Merge train on lane completion: render → ui, then B5.3 closes the sprint on the lead terminal.
- 2026-07-05 render: **B5.1 complete at review — branch fast-forwarded onto main `0aacb0c` (thanks for the live install; junction propagated instantly, so my duplicate package.json edit was dropped rather than committed — zero rebase conflict left on that file).** Delivered behind the byte-untouched B3.10 seam: deterministic composition core (`composition.ts` — one template, two mappings: pillar manifest + B5.2 direction export; root w/h/duration compile-time in markup, fps baked into the render job; brand styling = data via a strict-character-policy `identity.style` extension; judged text HTML-escaped, forbidden patterns unemittable by construction) → **two-belt lint gate before any chromium spend** (`composition-lint.ts`: own forbidden-pattern scan blind to judged text, then real `@hyperframes/lint` behind an injectable seam; error findings throw, `lintMediaUrls` deliberately skipped to keep the gate offline) → `hyperframes-target.ts` (`@hyperframes/producer` via non-literal dynamic import so nothing resolves at build time; `RenderCancelledError` reasons mapped onto typed `HyperframesRenderError`; engine failures carry failedStage + diagnostics for `lastError` triage) → env-selected registry (`RENDER_DRIVER`, default hyperframes; **remotion = named entry that fails loud as the recorded swap path**). Doctor render seam now probes `npx --no-install hyperframes doctor` (telemetry opted out; essentials FFmpeg/FFprobe/Chrome gate readiness) — live-ready on this machine. ADR-0004 records the default, the exact 0.7.33 lockstep pin + checkpoint-only bump policy, the telemetry opt-out (executed machine-wide + `HYPERFRAMES_NO_TELEMETRY=1` for CI), the GSAP CDN licence note, and two API corrections vs the research doc verified from shipped types (`executeRenderJob` returns void; timeout = AbortSignal). **Verified:** root suite 489→**533 passed / 3 skipped** (44 new + 1 gated), typecheck, lint 0 errors (the known `_dataDir` warning only), guard green every commit; **real $0 render smoke ran green TWICE** — scratchpad probe and the committed `RUN_RENDER_SMOKE=1` vitest path (real lint pass 0 errors, 258 frames @30fps 1080p → playable MP4 via the Windows screenshot fallback; cache keys on manifest hash so the fallback costs nothing). Sanctioned-out-of-glob, review-visible: `RENDER_DRIVER` in platform `env.ts` (the one process.env choke point — mirrors B4.8's `TRANSCRIPT_PROVIDER`; boundary ratchet forbids reading it anywhere else). Key pins untouched (key-stability suite green). One advisory from the real linter worth B5.2/B5.4 ears: it suggests sub-compositions (`data-composition-src`) per scene group once timelines grow — natural fit for per-scene stage previews. Lane clear-safe; merge train is yours.
- 2026-07-06 lead: **SPRINT 5 COMPLETE (B5.1–B5.4 all merged) — pass 2.5 done; pass 3 is next.** Merge train ran render → ui → ratchet, lead-driven, CI green each time: **B5.1** (PR #22, rebase-merged `61a12af`) — Hyperframes 0.7.33 render driver behind the untouched B3.10 seam, deterministic HTML composition (compile-time dims, judged text HTML-escaped, brand style as data behind a strict char policy), two-belt lint gate (own forbidden-pattern scan + real @hyperframes/lint) BEFORE any chromium spend, env-selected registry (default hyperframes, remotion = loud recorded swap path), doctor render seam live-ready, ADR-0004; proven with a real $0 258-frame chromium+ffmpeg render. **B5.4** (PR #23, rebased onto post-B5.1 main, one COORDINATION status-cell conflict resolved, merged `63b3119`) — the advanced-mode staged-flow UI on fake drivers, engine-free (contracts-only), every interaction captured as a verbatim RFC-6902 patch (the edit_diff payload shape), fake store honours the frozen schemas + expectedBody + the queued/approved advance gate. **B5.3** (PR #24, merged `4b5d804`) — the shell-inventory executable pin over the withGatewayGuard operation set (10 sites: 9 literals + the judge template → g3_screen/g3_final), SPINE §1 refreshed + computable-leakage audit recorded; the sibling gateway-boundary ratchet CAUGHT the pin's own comment mentioning the accessor token (reworded, re-green — the ratchet earned its keep). Dep install: `@hyperframes/{producer,lint}` + `hyperframes` 0.7.33 exact-pinned in main (`0aacb0c`) on the render lane's stop-and-report; junction propagated to both lanes. Sanctioned out-of-glob (review-visible): B5.1's additive `RENDER_DRIVER` in platform env.ts (mirrors B4.8's TRANSCRIPT_PROVIDER). **Final verified state on main `4b5d804`:** root suite 489 → **580 passed / 3 skipped** (browser + render-smoke gated), typecheck clean, lint 0 errors (pre-existing `_dataDir` warning only), guard green, `npm run doctor` render seam LIVE-READY (hyperframes essentials FFmpeg/FFprobe/Chrome green). All Sprint-5 worktrees removed (junction-safe; main node_modules verified intact) + three orphaned prior-session subagent worktree dirs (real node_modules, dead lanes) swept. **Carried follow-ups for pass 3** (from the lane wraps, all non-blocking): (1) `@hyperframes/player` dep + real preview in stage-preview.tsx (now that B5.1 emits compositions); (2) swap `/api/staged/*` route internals for startVideoStages/advanceVideoStage + the real judge lane, and make approvals.record accept a caller-supplied `{kind, patch}` so staged captures land as real edit_diffs (currently hardcodes `{before, after}`); (3) direction_doc → render wiring (compositionSpecFromDirectionExport exists, unwired into a live render path); (4) hyperframes target's throwaway temp jobDir isn't rm'd after render.ts reads the video (minor OS-temp accrual). Next: pass 3 (live verification + real drivers) at the founder's direction.
- 2026-07-06 lead: **PASS-3 RE-CHARTER COMPLETE (A12 + A13) — Sprint 6 chartered (B6.1–B6.8) and lanes cut; wave launch awaits fresh founder go next session.** Planning/research session, docs-only. Founder decisions ratified: Thalon-first dogfood (tenant #0 real profile; pillar #1 = a Thalon video; first-company onboarding deferred to a later config-only proof) · frontend first, hand-built shell + dogfooded content (landing spec + workspace command-center IA in `docs/FRONTEND.md`, synthesized from founder references; 10-second-rule invariant; waitlist w/ referral; keyboard triage; first-run on fake drivers) · intel v2 = operator-described monitored areas + deterministic two-stage ranker (founder-supplied recommender-frameworks summary adopted as math+embeddings, never neural — ADR 0005) · transcript live drivers elevated (learning/dogfood input channel; vendor-adapter design re-validated by the IP-block research) · **A13: search intel v1 (B6.8)** — SEO/AEO/GEO as Intel's second half + a judge lens, NOT a fourth family (ADR 0006): profile-seeded keyword compilation, on-page pack (JSON-LD/llms.txt/deterministic checks), `SearchIntelSource` seam (GSC free official API live after B6.7 deploy; paid tools = recorded swap path), "peering over the horizon" as tested core math, Intel gains Trends/Search tabs. Research verified against official docs: YouTube June-2026 granular quota (search.list ≈100/day own bucket; videos.batchGetStats cheap) → per-driver quota budgets as config; Bluesky keyless/abundant; GSC ≈50k rows/day free. **Sprint-6 contract window consolidated to ONE opener** (waitlist + areas + search tables + SEO-meta capability, lead terminal, freeze at merge — supersedes A12's two-window note). Lane board above: contract → wave 1 (landing · intel-engine · search-engine, Mode B) → wave 2 (workspace · self-tenant) → wave 3 re-planned at the wave-2 checkpoint (B6.5/B6.6/B6.7, lead-heavy). [you] consolidated: domain + pricing tiers (non-blocking) · gateway top-up (B6.5+) · transcript-vendor key (B6.5) · optional Trends-API alpha application · OAuth apps (parallel paperwork). Main clean at this wrap; suite carries 580/3 (no code touched).
- 2026-07-06 lead: **B6 CONTRACT WINDOW MERGED (PR #25, rebase-merged `570ce1e`) — the Sprint-6 contract is CLOSED and FROZEN; the wave-1 Mode B lanes are cuttable.** One migration (0005, purely additive) carried the whole window: `waitlist` (idempotent join on tenant+email, monotonic position assigned once — effective "skip the line" ordering is core math over `referred_by`, never a rewrite) · `monitored_areas` (free-text description as query-expansion seed + relevance-embedding anchor; zod-validated config with ranker-weight overrides + per-sweep query ration; paused-never-deleted; area provenance on exemplars rides the existing `sources.meta` — zero intel-table changes) · `search_targets`/`search_snapshots` (mirrors of watchlists/trend_snapshots: first-origin-wins provenance, durable dismissals; append-only history keyed tenant+source+query+page+capturedAt with page='' as the site aggregate so the structural key stays total under PG NULL semantics). Contracts: area/rankerWeights configs (an explicit overrides schema — zod 4's `.partial()` still fills defaults, which would silently pin unset signals to 1; test-pinned), searchTarget enums, seoMetaSchema; the format registry gained the `seoMeta` capability (`web_page`/`pillar_script`/`direction_doc` carry an optional `meta.seo` block; every pre-window draft parses unchanged, test-pinned). Ratchets extended in-change: tenancy table list += 4; new repos events-audited + tenancy-walled, pinned in `sprint6-repos.test.ts` per the B4.4 pattern. Suite 580 → **596 passed / 3 skipped**; typecheck + lint + guard green; CI green (guard 9s · eval-gate 1m49s · infra 37s · test 6m31s). Wave-1 kickoffs authored (gitignored `.context/kickoffs/`: landing · intel-engine · search-engine); worktree prep next; **launch awaits fresh founder go** per the standing rule.
- 2026-07-06 landing: **B6.1 complete at review on `agent/landing/b61-brand-landing` (8 commits, pushed).** Delivered per the kickoff: (1) **design tokens** — dark-first charcoal+amber over the shadcn variable names (light-capable `:root` kept; `.dark` on the html root), fonts wired to Geist (the template's `--font-sans` self-reference was dead), WCAG-AA pinned EXECUTABLE — `tokens-contrast.test.ts` re-derives contrast ratios from the oklch tokens in globals.css (7 pairs × both themes; body text held at AAA). (2) **Brand mark** — iterated live with the founder to a PICK: **Grip II, the warm sails reading** ("Opera-House sails, less sharp", one sail off-centre, right sail fattened to survive favicon scale — three founder feedback rounds applied in-lane); `BrandMark` alias is the one swap point, `/brand` (noindex) records the pick + the two alternates; literal twins in `app/icon.svg` + the OG image are comment-linked. (3) **Waitlist** — `POST /api/waitlist` thin per doctrine over the frozen repo; effective-position math in `lib/waitlist/position.ts` (5 spots/referral, floor 1), per-IP fixed-window limiter (5/min; honest per-instance note, WAF at B6.7 is the upgrade), referral-code collision-retry, unknown `ref` degrades to direct signup (a broken share link never blocks the conversion moment); 17 tests incl. route-level idempotent-resignup/429/503 against per-test PGlite. (4) **Landing `/`** — all four §2 sections + every [+]: pure-CSS 12s hero vignette (ZERO client JS, reduced-motion halted), popout `<dialog>` demos playing placeholder loops behind a `FeatureLoop` seam (B6.3 swaps ONE file), sticky mini-CTA observing stable anchors, `<details>` FAQ (zero JS), honest early-access pricing ($— placeholders, no fake anchors). **`/` prerenders STATIC in `next build`** — client JS is exactly forms+modal+sticky per the budget. (5) **AEO/GEO (A13)** — FAQPage+Organization JSON-LD generated from the SAME copy module the visible accordion renders, `llms.txt` route (force-static), robots/sitemap, build-time OG image; presence + honest-claims pins in tests. (6) **Keyword pass** — `proprietary/prompts/keyword-manual-pass.md`: 8 targets with placements, question-forms→FAQ map, provenance notes so the B6.8 seed compiler can treat it as its first fixture. Verified: root suite 596→**632 passed / 3 skipped**, typecheck clean, lint 0 errors (known `_dataDir` warning only), guard green per commit, production `next build` green with `/` static, full browser walk (signup → welcome-back → referral link · modal · accordion · sticky bar). **Sanctioned out-of-glob (review-visible): `proprietary/judge/src/shell/prompt-file.ts`** — the FIRST-EVER `next build` of apps/web failed on the judge's `new URL(rel, import.meta.url)` prompts-dir lookup (the exact Turbopack class B1.5 fixed in db's client.ts; latent since B1.3 because no CI job builds the app); fixed with the identical source-relative + cwd-walk pattern, judge suite green. **Follow-ups for the lead:** (a) consider a `next build` CI job — this class stays invisible until deploy otherwise (B6.7 will hit it); (b) deploy-time: the prompts dir + `NEXT_PUBLIC_SITE_URL` must reach the serverless bundle (outputFileTracing / env — B6.7); (c) founder may still fine-tune the picked mark — swap points are `BrandMark` + the two literal twins. Handoff note: `agent_handoff/CURRENT.md` untouched (lead-owned); this message is the landing lane's wrap record.
- 2026-07-06 intel-engine: **B6.4 engine half at review on `agent/intel/b64-areas-ranker` (3 feature commits + this board update, pushed).** Intel v2's two-stage pipeline is live in `packages/engine/src/trend/` behind the frozen contract, all keyless on the fake TrendSource + hash-embedding fake. **Candidate generation** (`area-expansion.ts`, pure): a monitored area expands deterministically into priority-ordered queries — name → operator-quoted phrases verbatim → description-clause keywords (stopword-only clauses drop, long clauses collapse to 6 significant words) — case-insensitively deduped, rationed by `config.maxQueriesPerSweep` over a tenant-config default (4), cut counts reported (`droppedQueries`), paused areas leave zero residue; merged into `watchlist.queries` ahead of the poll, accounts polling untouched. **Ranking** (`ranker.ts`, pure — vectors passed in): per (item × area) score = weight-normalized sum over ARMED signals only, each in [0,1] — relevance `(cosine+1)/2` vs the area description; engagement + velocity as the saturating form `x/(x+threshold)` (0.5 exactly AT the configured threshold, no cap cliff — a 10× outlier still outranks a 2×) reusing the outlier config's thresholds; velocity = mean of single-sweep signal + **stored-history Δ-velocity (longitudinal.ts finally wired into live sweeps, as its pass-3 note promised)**; freshness = publish-time half-life decay (config, default 24h). Weights resolve tenant default ← area override; unset override fields keep the tenant value (the contract's overrides-schema semantics, test-pinned). Reason strings on every row, one per armed signal, e.g.: `relevance 0.81 to area "AI video tooling" (embedding cosine 0.62)` · `engagement 0.71 (shares/views 0.025 vs threshold 0.01; bookmarks/views 0.042 vs threshold 0.02)` · `velocity 0.85 (sweep 4615.38 views/h vs 3× account baseline 202.5 views/h; Δ 15000 views/h between sweeps vs 3× stored baseline 125 views/h)` · `freshness 0.5 (published 24h ago, half-life 24h)`. **Provenance:** ingested outliers carry best-RELEVANCE area as additive `areaId`/`areaName` keys inside the existing `sources.meta.trend` payload — no schema change; content hashes byte-stable (existing pin untouched). Intake extension is strictly additive: `areas`/`expansionConfig`/`rankerConfig` optional on the request, `areas`+`ranked` on the result — the no-areas path is byte-identical, proven by the untouched pre-B6.4 intake tests. **No new gateway site:** ranking embeddings ride the EXISTING metered `embedChunks` choke point (budget + usage ledger + content-addressed cache — an unchanged area description is a cache hit on every sweep after its first; shell-inventory pin untouched). Verified in-worktree: root suite 596 → **629 passed / 3 skipped** (+33 lane tests, zero regressions), typecheck clean, lint 0 errors (known `_dataDir` warning only), guard green per commit. **Notes for the lead/next lanes:** (1) `sweepAreaSchema` accepts `monitoredAreas` repo rows directly — `status` pipes string→enum so drizzle's `string` column type passes the TYPE door (test-pinned); B6.5's poller is `list(ctx,{status:"active"}) → runTrendIntake({areas: rows})`. (2) Item→query attribution is deliberately embedding-relevance-based, NOT query-tag-based — `TrendSource.poll` shape unchanged for B6.5 drivers. (3) zod-4 gotcha re-confirmed: `.default()` short-circuits the inner parse; `rankerConfigSchema.weights` uses a full output-shape default (the contract lane's `.partial()` note, same family). (4) `result.ranked` is per (item × area) — wave-2's Trends-tab area filter is a client-side filter, no extra engine call. Handoff note: `agent_handoff/CURRENT.md` untouched (lead-owned); this message is the intel-engine lane's wrap record.
- 2026-07-06 search-engine: **B6.8 search intel v1 at review on `agent/search/b68-search-intel` (6 commits, pushed).** All five ADR-0006 decision-4 deliverables landed against the frozen contract, zero contract friction: (1) **seed compiler** (`search/seed-compiler.ts`) — deterministic topics×offers×audience×question-form expansion in pure core, forms/caps as config, normalization byte-stable, persists via `searchTargets.add` origin `profile_seed`; recompiles replay cleanly and first-origin/dismissals proven durable in tests. (2) **Judged AI expansion** (`search/expansion.ts` + `shell/expander.ts` + `proprietary/prompts/keyword-expand.v1.md`) — **the shell-inventory addition is `search.keyword_expand`** (pin extended 10→11 sites; sibling gateway-boundary allowlist += the expander shell — the two edits travel together per the B5.2 precedent); every candidate is gated in core before persist: G1 denylist over keyword+rationale, then **deterministic grounding-to-the-profile** (content-token overlap vs the rendered identity, ratio+stopwords as config, rejections returned with reasons — keywords are queries not prose claims, so grounding is checkable; prose stays with G3). Design point for review: this reads ADR-0006's "judged (G1 + grounding)" as the deterministic gates, mirroring trend-intake's G1 screen — no extra model spend per candidate. (3) **On-page pack** (`search/onpage.ts` + `llms-txt.ts`) — question-shaped H2s w/ answer-first openers, JSON-LD presence/parse + declared-vs-embedded type honesty, meta windows composed from the judge's `checkSeoMeta` (defined once), plus deterministic `renderLlmsTxt` from identity + shape check. (4) **SEO/AEO judge lens** (`proprietary/judge/src/seo-lens.ts`) — pure advisory `seo_aeo` gate (gate column is open text by design) wired into `runJudgePipeline` **opt-in by data**: runs only when a seoMeta-capable format actually carries `meta.seo`, so every pre-B6.8 draft and test judges byte-identically (proven — judge suite green untouched); warns NEVER block (I1 stays g3_final-only, test-pinned). (5) **`SearchIntelSource` seam** (`search/search-source.ts` + fake + `gsc-source.ts` + `intake.ts`) — registry fake|gsc|paid-vendor (paid-vendor fails loud = the recorded swap path), fake ships a demo dataset so `runSearchIntake`→`runHorizonScan` dogfoods keyless on an empty checkout, GSC skeleton speaks the official Search Analytics shape (keys-per-dimension, 25k/request cap, ≈50k/day quota AS CONFIG) and REFUSES unconfigured — operator OAuth + live wiring stays B6.7; **horizon math** (`search/horizon.ts`, outliers.ts mold) = position 8–20 × rising impressions × below-expected-CTR (band table as config), reason string per rule, missing metrics disarm rules. **Lead one-liners at merge:** (a) `SEARCH_INTEL_SOURCE` in platform env.ts (default `"fake"`) — the registry already reads it through `readEnv()` record-lookup, so the env selection activates with that line alone, zero edits in-lane; (b) SPINE §1 shell-inventory sentence += `search.keyword_expand` (docs/ out of glob). **Other review-visible out-of-glob edits (additive):** engine `vitest.config.ts` + `tsconfig.json` alias `@thalon/judge` worktree-local — engine's alias set was missing judge (apps/web already had it), and this lane is the first to CHANGE judge while testing through engine (the B1.2-class hazard, caught live when new lens exports came back undefined from MAIN's judge); engine `src/index.ts` export lines (sanctioned). **Verified in the worktree:** full suite green as a complete per-project sweep under heavy 3-lane disk contention — engine+judge 415/3 (new search 55 across 5 files; judge 41), contracts+db+platform+eval 126, web 114, repo-ratchets 3 — plus typecheck clean, lint 0 errors (known `_dataDir` warning only), guard green per commit; three contention flakes (worker-terminate/PGlite-30s-boot class, all in untouched suites) each green on isolated re-run. Zero live spend; no deps added; key pins untouched. Handoff note: `agent_handoff/CURRENT.md` untouched (lead-owned); this message is the search-engine lane's wrap record.
- 2026-07-06 lead: **WAVE-1 MERGE TRAIN COMPLETE — B6.1 (PR #27) → B6.4 (PR #28) → B6.8 (PR #29) merged in order, CI green each time (required checks: guard · test · eval-gate · infra).** Lead review findings, all resolved pre-merge: (a) landing copy honesty — "one-click publishing is rolling out" softened to "is planned for" in copy.ts + llms.txt (ADR 0006; no publish path exists this sprint; lead commit on-branch, propagates to JSON-LD/llms.txt via the shared copy module); (b) landing's out-of-glob judge `prompt-file.ts` fix REVIEWED AND BLESSED retroactively (the B1.5 Turbopack class, first-ever `next build` of apps/web surfaced it; behavior-preserving, fails loud, zero overlap with search's judge files — but note for future lanes: out-of-glob fixes should stop-and-report first); (c) search's "judged (G1 + grounding)" reading RATIFIED as deterministic gates (keywords are queries, not prose claims — mechanically checkable, zero per-candidate model spend); (d) intel's positional embedding recovery verified sound against embedChunks' one-result-per-input-chunk contract (no dedupe across duplicate contentHashes — checked, holds). Lead integration commit on the search branch: `SEARCH_INTEL_SOURCE` in platform env.ts (default "fake", mirrors RENDER_DRIVER/TRANSCRIPT_PROVIDER) + SPINE §1 inventory sentence += `search.keyword_expand` (pin 10→11). Board-conflict resolutions at each rebase were the expected status-cell/message reconciles (one PowerShell Get-Content/Set-Content round-trip mojibake'd the file mid-resolve — caught by diff inspection, redone with byte-safe edits; lesson: never round-trip UTF-8 repo files through PS 5.1 cmdlets). **Final verified state on main `b1c4a33`:** root suite 596 → **728 passed / 3 skipped** (each train car verified in its worktree pre-merge: 633 → 666 → 728), typecheck clean, lint 0 errors (known `_dataDir` warning only), guard green throughout. **Follow-ups carried:** (1) `next build` CI job — ratchet candidate from landing's latent-since-B1.3 find, sized for the next tooling window; (2) B6.7 deploy notes: prompts dir + `NEXT_PUBLIC_SITE_URL` must reach the serverless bundle (outputFileTracing/env); (3) founder may fine-tune the Grip II mark (swap points: `BrandMark` + icon.svg/OG twins); (4) landing's per-IP limiter is per-instance (WAF at B6.7 is the upgrade). Wave-1 worktrees + lane branches GC next; wave-2 prep (workspace worktree + kickoff) follows — **launch awaits fresh founder go per the standing rule.**
- 2026-07-06 lead: **Wave-2 LAUNCHED on the founder's pasted resume-prompt go (session 15) — exactly the two named runs.** Housekeeping first: leftover landing worktree dir removed + `git worktree prune` (founder closed the old terminal). workspace (B6.2) live in a founder-opened VS Code terminal on the lead-prepped worktree (`.claude/worktrees/workspace` @ `agent/workspace/b62-shell`, cut from `d19d09c`); lead monitor armed on the branch ref. self-tenant (B6.3) running lead-inline on `agent/tenant/b63-self-profile`: **phase 1 code-complete** — tenant #0's real deep profile as TRACKED data (`proprietary/profiles/tenants/self.v1.json`: full Thalon identity/voice/topics, honest-claims-checked facts, pillar-#1 prompt locked to Thalon itself, `identity.style` = the landing brand literals so renders wear the brand; `TENANT_ZERO` now loads the file — tenant #0 can never drift back into code, and the B2.1 tenants-JSON CI ratchet validates it for free) + the three feature demo-clip specs as data (`proprietary/profiles/demo-clips/`) + `npm run -w @thalon/eval render:demos` (spec → pillar timeline → manifest → env-selected RenderTarget registry). **Proven with a real $0 triple render** (h264 1080p@30, ~15s, ~0.6MB each) — and the B5.1 lint gate earned its keep live: it refused `Geist` (no font-face in the composition) before any render spend; tenant #0's render font is now `Inter` (auto-resolved, deterministic font-face injection), web app keeps Geist. Verified: eval 16/16 + typecheck + lint + guard per commit; root suite 732+1-flake/3 skipped (the boundary-ratchet scan timed out under render+lane disk contention, 3/3 green isolated — the known wave-1 flake class). **Phase 2 (deliberately after workspace merges, one-web-writer rule):** re-render into `apps/web/public/demos/` + the one-file `FeatureLoop` swap with the "this demo was rendered by Thalon" caption. Merge train on lane completion: workspace → self-tenant.
- 2026-07-06 workspace: **B6.2 workspace command-center complete at review on `agent/workspace/b62-shell` (6 commits, pushed).** All eight charter deliverables landed, [+] mechanics included. (1) **Shell** — left rail + top bar around every `/app` surface from ONE nav registry (sidebar, mobile strip, Cmd-K palette, omnibox all route from it); tenant/profile switcher honest about one-tenant-per-process (`DEMO_TENANT_SLUG`); needs-you badge (queued+blocked) from a shared pulse context, re-read per navigation and nudged by every operator action. (2) **Dashboard** — omnibox (deterministic family heuristic → Create, transparent and changeable there), pulse row, needs-you card, engine-attributed activity feed over the events spine with per-entity surface links, seam-status card (resolveSeams/readEnv NAMES only), first-run tutorial state with linked steps. (3) **Intel, two tabs** — Trends: monitored-areas manager over the REAL repo (add/edit/pause; paused-never-deleted), ranked demo cards carrying the B6.4 ranker's reason-string grammar verbatim (outlier badge, engagement ratios, per-signal reasons), client-side area filters (chips include zero-card real areas — the B6.5 seam visible), dismiss/generate-from-this captured in an in-memory fake-driver store as the exact payload pass 3 lands durably (B5.4 precedent). Search: keyword targets over the REAL repo (operator adds origin-tagged; dismissal durable across recompiles, route-test-pinned), horizon cards mirroring `HorizonScore` with reasons verbatim (opportunity = all three rules), target-this → Create with keyword context. Both demo datasets ride behind VISIBLE banners naming what arms the real read (B6.5 pollers / B6.7 GSC). (4) **Approve in the shell** — Sprint-1's placeholder button is now real batch approve (queued drafts sequentially through the single-draft endpoint, stop-loud-on-failure); [+] keyboard triage (j/k select · a/r act, queue-owned; e/Escape edit, panel-owned; one shared typing guard); [+] zero-inbox banner off the cross-run pulse; classic 3-zone + staged surfaces test-pinned untouched (restyle only — zones were already token-clean from B6.1). (5) **Profiles (B3.11)** — identity/denylist/topics as friendly fields (copy nudges short checkable statements — they double as judge grounding), voice/platform-profiles as contract-validated JSON; save = NEW active version; FIRST save seeds the demo tenant (idempotent ensure) = first-run step 1; version history read from the events spine. (6) **Runs** — history with `lastError` VERBATIM as triage evidence + incomplete badge. (7) **Settings** — budget cap + driver/seam/model readout, read-only by design. (8) **Create** — prompt-first family picker receiving Intel/omnibox handoffs (`?prompt/?keyword/?family`); video routes into the live staged demo; post/page state the B6.6 seam honestly. **Verified:** root suite 728 → **792 passed / 3 skipped** (+64, zero regressions; 4 PGlite-boot contention flakes green in isolation — the known wave-1 class), typecheck all workspaces, lint 0 errors (known `_dataDir` warning), guard green per commit, production `next build` green, and a full live browser walk: profile v1 save → tenant seeded → area add + card dismiss → target add → target-this → Create handoff → staged surface inside the shell → inbox-zero banner → Cmd-K → runs/settings → dashboard showing the walk's real activity. **Ratchets landed in-lane:** (a) `next build`→`next dev` on one `.next` served the stale prod route manifest (every new route 404'd) and the restart dance wedged Turbopack's cache behind an orphaned process — `distDir` now forks on NODE_ENV (`.next-dev` for dev), failure sequence replayed green (executable); ops note: kill the PORT's listener, not the wrapping shell. (b) `components/shell` tripped the `**/shell/**` eslint quarantine — renamed `components/workspace`; the ratchet earned its keep. **Zero contract friction; zero out-of-glob edits; no deps added.** Follow-ups for the lead/next windows: (1) events repo needs a desc-ordered/paged read (activity feed tails a 500-row window); (2) `brandProfiles.list()`/`activate(version)` for true version switching (editor states the limit honestly); (3) the dismiss→eval-row write door is missing — `eval_cases` is written only by `approvals.record` (draft-scoped), so trend-card dismissals are captured in-memory as the payload shape and search-target dismissals are durable via status only; (4) horizon/trend real reads arm at B6.5+ behind the commented route seams (engine-free doctrine keeps the horizon math out of apps/web — the poller or a job persists results, the route swaps its card source). Handoff note: `agent_handoff/CURRENT.md` untouched (lead-owned); this message is the workspace lane's wrap record.
- 2026-07-07 self-tenant: **B6.3 complete at review on `agent/tenant/b63-self-profile` (3 commits, pushed, rebased onto post-research main).** Delivered: (1) **tenant #0 deep profile as tracked data** — `proprietary/profiles/tenants/self.v1.json` (real Thalon identity/voice/topics; every fact a short, checkable, TRUE statement per the honest-claims rule; pillar-#1 prompt locked to Thalon itself; linkedin/x platform overrides with the human-review disclosure line; `identity.style` carries the landing brand literals `#161411`/`#ece7dd`/`#eeb64b` — the B5.1 `deriveBrandStyle` seam, exercised for real for the first time); `TENANT_ZERO` in eval now LOADS the file (tenant #0 can never drift back into code; the B2.1 tenants-JSON CI ratchet validates it for free; load-bearing pins added). (2) **Demo clips as data + render CLI** — `proprietary/profiles/demo-clips/{intel,create,everywhere}.v1.json` (honest copy incl. "publishing is planned") + `npm run -w @thalon/eval render:demos` (spec → pillar timeline → manifest → env-selected RenderTarget registry; injectable target; copies MP4s to an outDir). **Proven with a real $0 triple render** (h264 1080p@30, ~15s, ~0.6MB each); the B5.1 lint gate refused `Geist` pre-spend (no font-face in the composition) — render font pinned to auto-resolved `Inter`, web keeps Geist. (3) **FOUNDER DECISION (2026-07-07, ratified in-session): the FeatureLoop swap is DEFERRED to wave 3** — first renders judged visually stale (floor-grade B5.1 template: one fade-up tween, no transitions/captions/audio/depth); landing keeps its CSS placeholder loops; `docs/research/engaging-clips.md` (main `56b29f2`) records the diagnosis, the craft evidence, the 0.7.33 catalog/media toolkit we already ship, the **MusicGen CC-BY-NC licensing gate**, and the **composition-v2 proposal** for the wave-3 checkpoint (charter row's "replacing the landing placeholders" moves there — the render capability itself is proven and shipped). Verified: eval 16/16 (4 suites +2 new files), typecheck, lint 0 errors, guard green per commit; root suite 732/3 + one boundary-ratchet contention flake green in isolation (known wave-1 class). Zero apps/web edits (one-web-writer honored absolutely); no deps; no contract friction.
- 2026-07-07 lead: **WAVE-2 MERGE TRAIN COMPLETE — B6.2 (PR #30) → B6.3 (PR #31) merged in order, CI green each time (required checks: guard · test · eval-gate · infra). SPRINT-6 WAVES 1+2 ARE ON MAIN; wave 3 re-plans at the next-session checkpoint (founder direction).** Lead review pass on B6.2, all clean at the doctrine points: apps/web engine-free (grep-proven), routes thin + tenancy-scoped through `tenantCtx` (`tenants.ensure` pre-existed — zero-db-edits claim held), batch approve = sequential single-draft endpoint calls with stop-loud-on-failure (no new write door), distDir/robots/test-setup changes sound, zero out-of-glob lane edits. **One real find, lead-fixed on-branch:** `.next-dev` (the lane's own distDir-fork ratchet) was gitignored but NOT eslint-ignored — flat config does not inherit .gitignore, so any prior `next dev` run made lint scan Turbopack build output; one-line ignore + explanatory comment. **Two train lessons became an executable ratchet in the same train (`tests/board-hygiene.test.ts`):** (a) the lead's own conflict resolve once committed merge markers (a CRLF-blind resolver chained unconditionally into `rebase --continue` — history repaired pre-push; the gated-verify sequence is now the recorded pattern and agent memory); (b) the board file had lost its trailing newline, which concatenated the lane's append-only wrap onto the previous message's line — so repo-ratchets now pin: no conflict markers in ANY tracked file, and append-prone board files (COORDINATION.md, agent_handoff/CURRENT.md) end with exactly one newline. **Verification note for worktree merges on this box:** full-pool `npm test` stalls under load (vitest fork workers time out at START — 15 errors, files never run; port-3000/zombie-node ruled out); the reliable local pattern is the wave-1 search-lane per-project sweep (contracts 54 · db 40 · platform 21 · engine 408/3 · judge 41 · eval 11 · web 214 · repo-ratchets 5, all green sequentially), with the PR's CI full suite as the authoritative gate. **Founder decision recorded (B6.3):** first brand renders judged stale → FeatureLoop swap deferred to wave-3 **composition v2** (`docs/research/engaging-clips.md`: catalog transitions/kinetic captions/depth/pacing-density in the deterministic generator; keyless TTS+caption stack; **MusicGen weights are CC-BY-NC — licensing gate on any music bed**; real-product-footage via B2.5 captures as the non-slop end-game). **Final verified state on main `d969f78`:** suite 728 → **799 passed / 3 skipped** (CI-verified on PRs #30/#31), typecheck clean, lint 0 errors (known `_dataDir` warning only), guard green throughout; workspace worktree + both lane branches GC'd (main node_modules verified intact). **Follow-ups carried to the wave-3 checkpoint:** (1) events repo desc-ordered/paged read (activity feed tails a 500-row window); (2) `brandProfiles.list()`/`activate(version)`; (3) the dismiss→eval-row write door (`eval_cases` written only by draft-scoped `approvals.record`); (4) horizon/trend real reads arm at B6.5+ behind the commented route seams; (5) `next build` CI job (wave-1 carry, now two waves old); (6) B6.7 deploy notes (prompts dir + `NEXT_PUBLIC_SITE_URL` into the serverless bundle); (7) composition v2 + demo-clip re-cut + FeatureLoop swap (the B6.3 deferral).
- 2026-07-07 lead: **WAVE-3 RE-PLAN CHECKPOINT COMPLETE (session 16, planning on the lead terminal — zero agent launches, docs-only).** Founder opened the checkpoint with new reference material (34 screenshots + two video-essay transcripts + three landing catalogs, all outside the repo per the standing rule) and two directives: the intel→create workflow connection ("from a subject in intel, go to create video/post with the context carried — never retype it") and a colour-direction change (workspace beside white-background platforms must not be near-black). Study + gap analysis vs the merged B6.2 workspace → `docs/research/workspace-ux-v2.md`; **founder ratified all recommendations 2026-07-07**: (a) **dossier card + context spine** — intel cards gain ready titles/angles/hook + per-family exits; the promote/target-this handoff upgrades from `?prompt=<text>` to a structured context object behind a capture id, rendered as removable context chips on Create; symmetric promote/dismiss capture door (absorbs carried follow-up 3); cadence stamp on Intel; (b) **workspace goes light-first** — white/paper base · navy-ink structure · blue = act · **amber narrowed to the signal channel** (= the engine found heat); landing stays dark-cinematic for now, both themes in the one token set, `tokens-contrast.test.ts` re-derives AA at the retune; (c) **UX-psych mechanics, honest variants only** (smart defaults at seams · goal-gradient first-run · reciprocity via demo tenant · IKEA-effect profile onboarding · truthful freshness loss-framing; dark patterns explicitly recorded as not-to-copy); (d) **landing uplift** (founder-added mid-session): hero signature moment + honest stats band + old-vs-new strip + feature-card pull, sources MIT template set + ReactBits (MIT+Commons Clause recorded, per-component dep/license gate at adoption), client-JS budget consciously amended to "+ one hero moment"; (e) **ratified wave-3 order**: 1 light-theme flip/retune → 2 dossier+context spine (fixture-backed) → 3 B6.5 live drivers → 4 B6.6 origination loop → 5 landing-v2 moment (composition v2 + demo re-cut + FeatureLoop swap + landing uplift) → B6.7 deploy. Execution: steps 1–2 lead-terminal (integration-heavy, per the board default); any lane/subagent launch at later steps needs fresh founder go at cut time. Docs landed this checkpoint: `docs/research/workspace-ux-v2.md` (the full readout, written portable on founder direction) + `docs/FRONTEND.md` amendments (§1 palette superseded · §2 landing-v2 uplift · §3 wave-3 revision note). [you] unchanged (top-up + transcript key gate step 3; domain gates B6.7). No code touched; suite stays 799/3.
- 2026-07-07 lead: **Checkpoint addendum — lead-machine research tooling installed on explicit founder approval (same session; machine-global installs, zero repo deps).** (1) **last30days agent skill** (mvanhorn, MIT, ~10.6k stars) via `npx skills add -g` → `~/.agents/skills/last30days`, symlinked into the agent's skill registry. Pre-bless audit done: full egress grep — every endpoint is a recognizable platform/vendor API (Reddit public JSON/RSS + arctic-shift, HN Algolia, Bluesky, GitHub, Polymarket, job boards; optional keyed vendors); the registry's "high risk" rating maps to its opt-in browser-cookie X auth + subprocess use, not exfil. Two recorded cautions: the HTML-brief *publish* path posts content to an external share service (avoid for sensitive topics); prefer an xAI key over cookie mode for X. Value: lead-terminal trend research for content/keyword passes AND reviewable prior art for the B6.5 driver design (keyless endpoints, engagement-weighted scoring, per-source doctor) — noted in `workspace-ux-v2.md` §7 step 3; product drivers stay official-APIs-only, scrape-vendor APIs recorded as the paid swap path. (2) **yt-dlp** (user-site pip, both Pythons) — unlocks the skill's YouTube source; product-side YouTube caption/audio fetching remains the recorded ToS policy line. (3) **Python 3.12.10** (per-user, founder-offered mid-session; 3.10 hits EOL 2026-10) — the `py` launcher now defaults 3.12; readies the machine for the B6.6 Crawl4AI spike + the future PixelRAG tier without an EOL runtime. Install note: the box's IT policy denies elevation (two machine-wide attempts blocked); **per-user scope installs cleanly without elevation** — the recorded pattern for tooling on this machine (the pre-existing 3.10 in Program Files shows the runtime itself is IT-sanctioned). Crawl4AI itself deliberately NOT installed yet — its playwright/browser payload waits for the B6.6 bucket so nothing rots uninstalled-but-half-configured.
- 2026-07-07 lead: **Checkpoint addendum 2 + SESSION-16 WRAP — the blog surface adopted (founder-proposed, lead-assessed good, wrapped into the plan).** `/blog` on the site (nav/footer link; landing keeps its 4 sections): regular posts on AI/social-media/marketing tooling + Thalon pieces, for SEO/AEO/GEO. The assessment that makes it more than marketing (`workspace-ux-v2.md` §9): it gives the **Page family its destination**, it is **B6.6's dogfood target** (intel → →Page → judge → approve → publish to OUR OWN site — no platform APIs, the social publish path stays pulled; own-site content deploy behind the same approve gate), and it **closes the A13 loop** post-B6.7 (GSC snapshots on published posts → horizon cards → next topic). Honesty guardrails recorded: quality-gated cadence (landing says "regularly" until daily is proven), FAQ disclosure stance applies to posts, rankings never claimed (ADR 0006). Slotting: blog v1 (route + seed posts + BlogPosting JSON-LD + llms.txt index + RSS) rides the step-5 landing-v2 moment; the automated loop is B6.6; measurement arms after B6.7. `docs/FRONTEND.md` §2 gains the subsection. **Wave-3 plan is now final for launch next session: step 1 = workspace light-theme flip (lead terminal).** Session-16 totals: 4 docs commits on main, all planning/research — zero code, suite carries 799/3, guard green throughout.
- 2026-07-07 lead: **WAVE-3 STEPS 1+2 MERGED (PR #32, session 17, lead terminal — no lane launches).** Step 1, the light flip: `/app` is now light-first (paper base · navy-ink structure · blue = interactive · **amber narrowed to the signal channel** via a new `--signal` pair; outlier/opportunity badges, needs-you counts and reason-borders re-pointed off `primary`); `.dark` moved off the html root — landing `/` + `/brand` keep dark-cinematic via scoped wrappers, their base hue-shifted toward ink-navy (§8.5 continuity); `tokens-contrast.test.ts` re-pinned +2 signal pairs × both themes. Step 2, the dossier/context spine: every trend card carries 3–5 selectable ready titles + 2–3 angles + a hook (fixture-shaped; live generation = B6.5 + top-up), per-family exits (→Video·→Post·→Page) replace the single promote button, and the handoff is now a **capture id** (`?ctx=`) resolved through the new `/api/intel/context/[captureId]` route — Create renders removable context chips (title/angle/hook/area/keyword/source-text + provenance + heat), pre-fills the working title, pre-picks the family; stale ids degrade to plain Create; promote/dismiss write SYMMETRIC payloads through the one capture door (the dismiss→eval-row shape — durable write still waits on the next contract window, carried follow-up 3 stays open but shape-proven); cadence stamp on Intel (honest: "sweeps every 4h once live polling arms (B6.5)", Sweep-now disabled with the reason). **Layer lesson (found live):** route handlers and RSCs bundle in SEPARATE layers — an RSC read of the in-memory fake-driver store sees a different module instance than the route writers; context resolution therefore lives in a route + client fetch, never an RSC store read. **Founder feedback landed same-session:** (a) heat grading went explicit THERMAL — stale-blue · yellow warm · orange rising · red hot as a filled pill with the band word inside + a within-band magnitude bar, exact score demoted to tooltip (in-pill text AA, adjacent-band CVD ΔE ≥ 13 validated via the dataviz method; hot hue-kept off destructive-red; `--heat-*` tokens, rationale in `heat-grade.tsx`); founder wants this grading pattern integrated system-wide as visuals-over-numbers. (b) **Planned launch pricing** live on landing §3, founder-delegated: Scout $29 / Falcon $79 (featured) / Aerie $199 USD/mo (reasoning in `copy.ts`; honesty pin evolved — real prices from data, fake was-price anchors stay test-forbidden). (c) Dashboard charts/graphs direction: land WITH B6.5's real time-series (trend/search snapshots already accrue history) — honest sparklines beat decorative ones; chart tokens ready. (d) **Domains exist now** (thalon.com.au + thalon.org, founder-registered): lead assessment = **thalon.org primary** (global SaaS — a ccTLD geo-locks Google targeting to AU and caps the A13 SEO/AEO upside), .com.au 301s to it for AU trust + brand protection; NOT launched — vercel.app remains the test target until B6.7. **Verified:** web suite 214 → 233 → root 799 → **818 passed / 3 skipped** (CI full suite green on the PR — the authoritative gate; the 4 known PGlite-boot contention files flake only under local full-pool load, green in isolation), typecheck clean, lint 0 errors (known `_dataDir` warning), guard green per commit, production `next build` green with `/` still static, live browser walks of both themes + the full dossier→exit→chips flow. Ops note ratcheted to memory: a wedged Turbopack persistent cache in `.next-dev` can serve pre-edit CSS across dev-server restarts — purge `.next-dev` when dev styles lag the checkout. **Next per the ratified order: step 3 = B6.5 live drivers** (needs gateway top-up + transcript key [you]); branch GC'd after merge.

- 2026-07-07 lead: **WAVE-3 STEP 3 (B6.5) CORE MERGED (PR #33, session 18, lead terminal — no lane launches): live drivers + sweep poller + the transcript Library.** (1) **Library** (`/app/library`, nav-registered): paste a video URL → B4.8 `ingestVideoUrl` behind the env-selected TranscriptProvider → timed transcript view w/ copy-all + .txt/.csv/.srt exports (client-side builders; timed formats disarm on untimed ingests — never invented timestamps); honest seam readout (provider names + vendor-configured boolean). **apps/web gained its FIRST engine call sites** — ratified reading: SPINE §80 ("parse/authorize → call an engine/judge service → return", the judge-runner precedent since B2.6) is the doctrine; B5.4/B6.2-era "engine-free" meant the MATH stays engine-side, and it still does. Wiring = the judge pattern exactly (dep "*" + tsconfig path + vitest alias). (2) **Turbopack bundling ratchet extended**: five engine prompt-file shells + two profile loaders carried the raw `new URL(rel, import.meta.url)` lookup that fails `next build` (the B1.5/db, B6.1/judge class) — ONE shared resolver (`packages/engine/src/proprietary-dir.ts`, source-relative + cwd-walk) now serves all seven sites; the lesson has one engine home. (3) **TrendSource drivers, official APIs, budgets as config**: Bluesky account feeds poll KEYLESS on the public AppView (live-verified — real posts/counters); **searchPosts is 403 unauthenticated (probed live 2026-07-07)** — the recharter's "Bluesky keyless" now holds for FEEDS only, so query search rides a free app-password session (`BLUESKY_IDENTIFIER`/`BLUESKY_APP_PASSWORD`, loud refusal naming the knobs; [you]: a free Bluesky account + app password arms area-query sweeps). YouTube: search.list→videos.list, `maxSearchesPerSweep` default 1 (the June-2026 own-bucket quota), refuses unkeyed (`YOUTUBE_API_KEY` [you]); account polling = recorded follow-up (Bluesky covers accounts). Registry `getTrendSource` mirrors the transcript/search seams (`TREND_SOURCE`, default fake). Per-sweep request budgets REFUSE over-budget watchlists naming both knobs — never silent truncation. (4) **The poller + armed reads**: `runTrendSweep` = active areas + stored B4.3 watchlists (`listBySource`'s pass-3 promise kept) → `runTrendIntake` → ranked cards persisted as ONE wire-ready bundle at `sweeps/<tenantId>.json` — a documented MUTABLE-POINTER object-store family (latest-wins; orphan-sweep-protected in eval + the key-scheme docblock; events stayed transaction-internal per I4, so the bus temptation was resisted). `/api/intel/trends` swaps its card source at the commented seam (live bundle → demo:false + real stamp; fixture fallback pre-first-sweep); `POST /api/intel/sweep` = Sweep-now (driver refusals surface VERBATIM); cadence stamp armed (Sweep-now enabled from demo state — the first sweep is the invitation); dismiss/promote resolve live cards first through the one capture door; **live cards carry NO dossier until generation arms — the card renders an honest arming note** (dossier went optional on the wire). (5) **Founder live-request served same-session**: paste-a-YouTube-link → transcript works END-TO-END on the dev stack — the founder-suggested MIT `youtube-transcript-api` (jdepoix) runs as a LOCAL SHIM outside the repo (gitignored `.context/tools/transcript-shim.py`) speaking the hosted-vendor contract (`{mediaUrl}`→`{segments}`), so the ToS posture holds: zero platform-scraping code tracked, the engine still only knows `TRANSCRIPT_VENDOR_URL`/`_API_KEY`; dev `.env.local` wired hosted-vendor→shim (3 lines, comment out to return to caption-paste mode; production still wants a commercial key). Proven live: the founder's example video (tZQ9SNw4TYQ, 10:25) → 244 timed segments → copy/CSV/SRT; exports also delivered to `.context/transcripts/`. **Vendor shortlist for the [you] key** (recorded here, never in code): Supadata ($5–19/mo entry, multi-platform incl. TikTok/IG/X + AI fallback — fits the B3.12 roadmap) vs TranscriptAPI (~$5/mo per 1k, YouTube-only, cheapest) — lead leans Supadata when the key is bought; the per-vendor response mapping is a small follow-up either way. (6) **Deliberately deferred, recorded**: live dossier title/angle generation (top-up-gated [you] — building it now = dead code; wire/UI degrade honestly) · YouTube account polling · horizon real read (deploy-gated on GSC at B6.7 per its own seam comment) · scheduled cron cadence (B6.7 deploy; `nextSweepAtMs` is advisory arithmetic until then). **Verified**: per-project sweep contracts 54 · db 40 · platform 21 · judge 41 · eval 16 · engine 408→**425/3** · web 233→**246** (4 known PGlite-contention files green in isolation; 1 board-hygiene scan flake same class, green isolated) · repo-ratchets green; typecheck + lint clean; production `next build` green, `/` static; live walks: URL-only transcript ingest (real embeddings, ~$0.001) + demo→Sweep-now→demo:false w/ real 4h stamp; live keyless Bluesky feed probe. Suite totals ride PR #33 CI as the authoritative gate. Ops note: the Claude-in-Chrome extension lacked localhost site permission this session — UI walks fell back to API-level verification; grant it for future browser walks.
- 2026-07-07 lead: **Session-18 addendum — founder directed PARALLEL workflows for the remaining Sprint-6 work; wave 3.5 proposed as three Mode B lanes, launch go carried by the session-18 resume prompt (exactly these runs).** Lanes (disjoint globs, one web writer): **origination** (B6.6 loop, engine-only — intel context → Page drafts → judge → approve → own-site publish door; Crawl4AI subprocess ingest, license-verify + user-scope install at lane start; zero apps/web edits) · **landing-v2 + blog v1** (apps/web/** — §8 uplift + §9 blog surface; FeatureLoop swap NOT in-lane) · **composition-v2** (engine render/demo + demo-clip specs + eval — engaging-clips.md plan, Kokoro TTS, NO music bed per the MusicGen gate; clips delivered to gitignored .context/renders/). Lead terminal: step-0 assessment of a small contract window (durable blog-post storage/publish door + the carried dismiss→eval-row write door riding together — open/merge/freeze solo FIRST if needed) · dossier half-step when the top-up lands · merge train origination → composition-v2 → landing-v2 (lead train commits: FeatureLoop swap w/ lane-3 clips + one-line integrations). Board rows land at cut time next session per the standing pre-req list in agent_handoff/CURRENT.md.
- 2026-07-07 lead: **SESSION 19 — research & planning on the lead terminal (founder-directed: no agents, no code; docs + runbook + plan amendments only).** Four founder asks served. **(1) Credential how-tos → gitignored runbook `.context/runbooks/keys.md`:** Bluesky app password (Settings → Privacy & Security → App Passwords; never the real password; feeds already keyless, this arms query search only) · YouTube Data API v3 key from the founder's exact console state (Enable API in Library → Create credentials → API key → restrict to the API; **no billing/free-trial and no OAuth consent screen needed** — both console nags are ignorable for a plain key; 10k units/day free, search.list=100 units validates the `maxSearchesPerSweep:1` default) · **LinkedIn posture clarified**: everything is developer-app-gated by design, but *"Share on LinkedIn"* + *"Sign In with OpenID Connect"* are INSTANT self-serve (grant `w_member_social` = post-as-member, exactly what the publisher test account needs) once a Thalon **Company Page** exists and the app is page-associated; posting-as-organization = Community Management API = review-gated (access form + screencast of a WORKING integration + live sign-off) — chicken-and-egg, deliberately deferred until the publisher bucket exists to screencast; X note recorded (free tier ~500 writes/mo suffices for publisher testing; $200/mo Basic is a bucket-time decision). **(2) Library UX feedback ADOPTED as wave-3.5 lane riders** (founder: collapse the transcript view; title over raw URL; tags/category; relevance-to-profile scoring): the `sources.meta` JSON column carries it all ADDITIVELY — no contract window. Mini-contract for the kickoffs (keys named once, both lanes read this line): `meta.title` (YouTube oEmbed — keyless, quota-free, degrade to URL) · `meta.tags` (string[], operator-set) · `meta.areaRelevance` ({areaId, areaName, score, reason}[] — transcript embeddings already exist at ingest; score vs monitored-area descriptions via the B6.4 ranker's embedding path, reason strings preserved). **Lane-1 rider (origination):** ingest-side metadata — oEmbed title fetch in `ingestVideoUrl` + the areaRelevance computation (its glob already touches ingest for Crawl4AI). **Lane-2 rider (landing-v2+blog, the web writer):** Library surface polish — transcript panel collapsed-by-default after ingest, title-first shelf rows w/ URL demoted, tag chips + relevance badge in the thermal-heat grammar, all degrading honestly on pre-rider rows. **(3) Music & sound ladder → `engaging-clips.md` §6** (wave-3.5 no-music-bed scope UNCHANGED; these populate the audio-v2.5 seam): rung 1 = curated Pixabay/Mixkit pack as OPERATOR DATA outside the repo w/ per-track provenance manifest (license verified: commercial sync OK, standalone redistribution prohibited, no indemnification → paid catalog = production upgrade path); rung 2 = open-weights generation is now LICENSE-clean (ACE-Step v1 Apache-2.0 · YuE Apache-2.0 · DiffRhythm Apache-2.0; Stable Audio Open stays gated at $1M-revenue; ACE-Step 1.5 weights = verify-at-adoption) but HARDWARE-blocked locally — **load-bearing find: the dev box GPU is Intel UHD 750 / 1 GB VRAM** (ACE-Step floor ~6 GB) → cloud-GPU spike in the AWS sub-account or future box, behind the seam; rung 3 = deterministic procedural MIDI + FluidSynth subprocess + CC0 soundfont — the only rung that satisfies SPINE-§1 byte-stability, background-bed grade, optional spike; SFX = curated CC0 pack, no model. **(4) Memory updated** (future-tooling-candidates += music-gen survey + GPU constraint). Wave-3.5 cut plan otherwise UNCHANGED — the session-19 resume prompt re-carries the go for exactly the three lanes (now incl. the two riders) + lead inline work.
- 2026-07-07 lead: **Session-19 post-wrap addendum — trend credentials ARMED + LIVE-VERIFIED, and the music decision landed.** (1) Founder executed the runbook same-session: `BLUESKY_IDENTIFIER`/`BLUESKY_APP_PASSWORD` + `YOUTUBE_API_KEY` pasted into `apps/web/.env.local`, one paste mistake lead-fixed (identifier had the app-password *label*; email works — probe resolved handle `steveneam.bsky.social`), **`TREND_SOURCE=bluesky` now active**. Both probed live: Bluesky `createSession` minted a real session; YouTube `videos.list` answered on 1 quota unit (key valid, API enabled, zero billing). **Sweep-now now runs a REAL Bluesky sweep including query search** — the B6.5 [you] items are cleared; YouTube swaps in by flipping the one `TREND_SOURCE` line. (2) **FOUNDER DECISION (music):** use pre-made licensed tracks — `engaging-clips.md` §6 rung 1 (curated pack as operator data w/ provenance manifest) is the ratified audio-v2.5 path; self-generation (rungs 2–3) explicitly deferred to future-better-hardware, recorded in §6 as do-not-re-litigate. Composition-v2 lane scope unchanged (still no music bed). Remaining [you] unlock for wave 3.5: the gateway top-up (dossier half-step + B6.6 judge dogfood).
- 2026-07-07 lead: **SESSION 20 — step-0 contract assessment DONE (verdict: NO window), first REAL Bluesky sweep proven end-to-end, wave-3.5 lanes cut and prepped; launch awaits the founder opening the three terminals.** (1) **Step-0 verdict — the blog needs NO contract window.** Durable blog-post storage already exists: a published post = an approved `web_page` draft (judge-gated, events-audited) + its content-addressed `web-pages/<sha256>.html` artifact, and the frozen `web_page` meta already carries `title`/`description`/`htmlRef`/`deployStatus`/`deployRef`/optional `seo`. The publish door already exists as a seam: B3.15's `DeployTarget` + the `deploy.ts` core caller — the own-site target is a new driver behind an existing seam, zero schema. The blog read model = `posts/<tenantId>.json`, a mutable-pointer object-store family mirroring `sweeps/<tenantId>.json` (PR #33) exactly — slug is deterministic core math at publish, carried in the bundle, no meta change; the bundle shape is pinned as a mini-contract in BOTH the origination and landing-v2 kickoffs (the session-19 meta-key precedent). **The carried dismiss→eval-row door does NOT ride** — it was only ever a window passenger: shipping it windowless would force `origin='manual'` (dishonest — the origin taxonomy separates mechanism-written from human-authored rows, and a new origin value needs a check-constraint migration + the `eval/src/dataset.ts` enum + the export-CLI type, genuine window material). It stays carried to its charter-named home: B6.7's "eval-row refinement" exit-gate item. (2) **First REAL Bluesky sweep proven** (the armed path holds end-to-end): added a real monitored area through `/api/intel/areas`, Sweep-now expanded it to queries and polled **28 real posts via authenticated query search**, the ranker scored them on real embeddings, the bundle persisted, and `/api/intel/trends` served `demo:false` live cards with the real 4h stamp. Zero refusals; the dev DB now carries one active area ("AI video tooling") + the live sweep bundle. (3) **Lanes cut per the session-19 go:** three worktrees prepped (`worktree:setup` green each: junctions · env/vault copies · identity `steveneam@hotmail.com` · eslint v10.6.0 · guard PASS) — `.claude/worktrees/origination` @ `agent/origination/b66-loop` · `.claude/worktrees/composition-v2` @ `agent/render/composition-v2` · `.claude/worktrees/landing-v2` @ `agent/landing/v2-blog`; kickoffs authored in `.context/kickoffs/` and copied into each worktree (setup had copied `.context/` before they existed). Kickoffs carry: both riders w/ the `sources.meta` mini-contract keys named once in lanes 1+2 · the posts-bundle mini-contract in lanes 1+2 · origination's three sanctioned eval-side edits named to BOTH engine lanes (composition-v2 told to expect them at rebase) · Crawl4AI license-verify + user-scope install at lane start · zero-live-spend + stop-and-report dep rules · per-project verification sweep (full-pool `npm test` stalls under 3-lane load). (4) **Lead terminal next:** dossier half-step the moment the top-up lands (`intel.dossier` shell — inventory pin 11→12 + gateway allowlist travel together) · lane monitoring · merge train origination → composition-v2 → landing-v2 w/ lead train commits (FeatureLoop swap with lane-3 clips · arming the blog's live posts-bundle read · one-line env/inventory integrations).
- 2026-07-07 lead: **DOSSIER HALF-STEP LANDED + PROVEN LIVE (session 20, lead terminal — the founder's top-up arrived and the three lane terminals are running).** The sweep bundle's optional `dossier` field now fills for real: **`intel.dossier`** is the 12th guarded shell op (inventory pin 11→12 + gateway-boundary allowlist + SPINE §1 sentence — the three edits travel together per the B5.2/B6.8 precedent), prompt file `trend-dossier.v1.md` (grounded-in-the-item rules: no invented facts, standalone titles, genuinely-distinct angles, spoken-voice hook), shell mirrors the B6.8 expander exactly (gateway `generateObject` / claude-cli dev transport / deterministic fake), core disposes via the ONE bounded repair loop then **G1 denylist over the WHOLE dossier** (a hit drops it entirely with the matched term reported — never censored-but-partial), per-card failures degrade the card honestly and are REPORTED (`dossiersFailed`), a blown tenant budget still propagates as the operational halt it is. **Spend is an explicit ration:** `TREND_DOSSIER_CARDS` (platform env choke point, default 0 = disarmed) — only the top-N ranked cards per sweep generate; cards beyond the ration honestly carry no dossier. **Proven live on the topped-up gateway:** dev env armed at 4, draft tier pointed at gateway sonnet-4.5 for the proof run → real Bluesky sweep (28 polled) → 4 live cards carrying genuinely grounded titles/angles/hooks (spot-verified against the source posts; bundle UTF-8-clean at rest) → `/api/intel/trends` serves them `demo:false`; draft tier reverted to `claude-cli/sonnet` after (dev doctrine), **`MODEL_JUDGE_FINAL` restored to the ratified `anthropic/claude-sonnet-4.5`** (the top-up's other purpose — B6.6 judge dogfood at the train runs the real final tier). Suite: +6 dossier tests (engine trend 76 green), inventory/boundary ratchets green, typecheck + lint clean. **Founder rider relayed mid-flight (landing-v2 kickoff Deliverable D, worktree copy synced):** transcript exports are AGENT FOOD — a new `.md` AI-brief export (title/URL/duration header + transcript merged into sparse-`[mm:ss]`-stamped paragraphs; SRT is ~2.5–3× the tokens for the same words) + copy-all copies the same brief (fixes the founder's squashed-Notepad paste — `toPlainText` joins everything into one line today) + paragraphed `.txt`. **Follow-ups recorded:** dossier content-addressed caching via `llm_cache` (promptVersion × item text — spares regeneration when a card survives consecutive 4h sweeps); sweep route surfacing `dossiersFailed` (one-line, web glob — train candidate).
- 2026-07-07 lead: **Per-platform publish-params research served (founder question, session 20; lead-terminal web research, no agents) → `docs/research/platform-publish-params.md` — the B3.1 publisher bucket's design input; wave 3.5 unchanged.** The founder's worry (every platform's microscopic posting parameters — IG post/story/reel each with hashtags/tag-people/location/AI-label/audience/music/"more options") resolves into a **three-tier split, per-platform data in a registry, never bespoke frontend**: Tier A *generatable content* (captions w/ inline hashtags+mentions, YouTube title/description/`tags[]`, alt text, cover/thumb offset — generation+judge territory; `platformProfileSchema` policies already steer it) · Tier B *operator decisions the OFFICIAL API accepts* (location, tag-people/collaborators, privacy/visibility/reply settings, paid-partnership + branded-content toggles, AI labels, schedule, duet/stitch/comment) as an approve-time schema-driven params panel (the B5.4 form-from-schema precedent; defaults tenant-profile → per-draft override) · Tier C *native-app-only* (licensed music, filters, hide-like-count, fundraisers, stickers) as an **honest posting checklist on the approved draft** — never silently dropped, never faked. **Three pinned findings:** (1) AI-disclosure labels are API-settable on ALL THREE video platforms now (IG `is_ai_generated` · TikTok `is_aigc` · YouTube `status.containsSyntheticMedia`) — Thalon should set them truthfully BY DEFAULT (invariant-grade, the ADR-0006 honesty doctrine applied to publishing); (2) **TikTok makes the approve-time panel a COMPLIANCE requirement** (guidelines: operator must manually pick `privacy_level` with no default, interaction toggles default OFF; TikTok audits the integration's UX before API approval) — "users figure it out themselves" is structurally not an option there; (3) music is the honest no (IG reels API = original audio only; TikTok sound picker native-only) — composing cleanly with the ratified engaging-clips §6 rung-1 decision: the licensed bed bakes INTO the video as original audio, platform catalog music = a checklist item. Architecture sketch recorded in the doc: platform-surface registry in contracts (the B4.2 pattern; IG = FOUR surfaces), draft `meta.publish` block (capability-gated like seoMeta), per-surface Tier-B defaults on platform profiles, ONE schema-driven approve panel + checklist strip, B3.1 adapters map the validated block onto API fields. Open items for the B3.1 charter listed in the doc (IG Business+FB-Login gates, IG 100-posts/24h cap, X free-tier writes, LinkedIn mention/document gaps, story-surface thinness).
- 2026-07-07 lead: **FOUNDER DIRECTION (same session): hashtag/mention intelligence ratified as Tier-A first-class** → `platform-publish-params.md` §2.1. Thalon actively generates hashtags/tags/@'s for exposure (X/LinkedIn/Facebook primarily; video description boxes + YouTube `tags[]` secondarily), fed by the EXISTING intel spine — B6.8 search targets (keywords ARE proto-hashtags, provenance rides along) · B6.4 area vocabulary · promoted trend-card context · `meta.seo` — shaped by per-platform config (counts/placement = tenant config w/ verified-at-bucket-time defaults; the `hashtagPolicy` string upgrades to structured policy), judged like everything else (G1 + grounded-to-intel, reason string per suggestion — no invented bandwagon tags). **Mentions are SUGGESTED, operator-confirmed, never auto-set** (relationship-bearing; LinkedIn = URN annotations w/ member-mention restrictions, IG tag-people = a Tier-B API param distinct from caption @'s). Timing: hashtags are caption text, so the generation-side slice (fanout consumes search targets/areas/trend context) is useful PRE-publisher — copy-paste captions benefit today; the structured shaping + mention-confirm UI land with the B3.1 approve panel.
- 2026-07-07 origination: **B6.6 PAGE LOOP AT REVIEW — engine-only, 6 commits on `agent/origination/b66-loop`, both deliverables + the rider landed; zero apps/web edits, zero live gateway spend, contract untouched.** (1) **Loop entry** (`origination/page-loop.ts`): the wave-3 CreateContext keys mirrored engine-side (`family` pinned `"page"` — mis-routed contexts refuse loudly; web-side extras like captureId stripped so re-promotes replay), deterministic brief composition from the versioned template `proprietary/prompts/page-loop-brief.v1.md` (comment-stripped, absent-token lines dropped whole, byte-stable — test-pinned), brief ingested as a `prompt` source → the EXISTING `runWebPageGeneration`; judge/approve untouched. (2) **Own-site publish door** (`webpage/publish.ts` + `own-site-target.ts` + `posts.ts`): `publishWebPageToSite` flips `deployStatus`/`deployRef` through the EXISTING `deploy.ts`/artifact-stage caller against a thin `own-site` DeployTarget (reports site-relative `/blog/<slug>` — domain-safe across vercel.app→thalon.org), then upserts **`posts/<tenantId>.json`** exactly per the mini-contract `{version:1, tenantId, generatedAtMs, posts:[{slug,title,description,draftId,htmlRef,publishedAtMs,seo?,tags?}]}`; slug = core math (slugify + `-2/-3` collisions, republish keeps slug AND first-publish time; canonical order publishedAtMs-asc/slug); `readPublishedPosts` exported (null pre-first-publish, mirrors `readSweepBundle`); `rebuildPostsBundle` re-derives from `web_page` drafts if the pointer is lost (documented degrades: publishedAtMs≈draft.updatedAt, operator tags gone, seo survives); `PROTECTED_PREFIXES += "posts/"` landed IN the family-creation commit; sanctioned repo fn `drafts.listByFormat` (additive). (3) **Web-ingest driver** (`ingest/web-ingest.ts` + `crawl4ai-driver.ts` + `ingest-web-url.ts`): registry seam mirroring B4.8 — zero-dep `fetch-extract` default + `crawl4ai` behind a python-subprocess contract (`{"content": string}` JSON stdout; parser takes the LAST valid JSON line because crawl4ai's stdout is chatty — verified live); robots.txt + rate limit run in CORE before ANY driver (B2.5 primitives reused; disallowed = loud `RobotsDisallowedError` by construction); content-hash idempotent; raw under the existing `sources/<hash>` family (no new key family beyond `posts/`). **License check (the lane-start gate): Crawl4AI 0.9.0 = Apache-2.0 per PyPI metadata, BUT the repo LICENSE appends a custom attribution rider after END OF TERMS (“This product includes software developed by UncleCode… as part of the Crawl4AI project”) binding distributions/publications/public uses.** Our posture is clean — subprocess-only operator-machine tool (user-scope `pip install --user`, py3.12), never embedded, never redistributed, zero repo deps — the ReactBits-class nuance, recorded here for any future redistribution decision. Install verified live: crawl4ai 0.9.0 + playwright chromium (user cache, no elevation), `python` resolves 3.12.10, and the driver's EXACT subprocess snippet returned clean markdown for example.com (keyless HTTP only). (4) **Rider — Library ingest metadata** in `ingestVideoUrl`, additive meta keys per the session-19 mini-contract: `meta.title` (YouTube-oEmbed seam, keyless/quota-free, URL fallback, never blocks ingest — belt-and-braces catch), `meta.tags` (request `tags` verbatim), `meta.areaRelevance` (`{areaId,areaName,score,reason}[]` — chunk-embedding CENTROID vs active area descriptions through the EXISTING `embedChunks` choke point with intake-matching cache keys, ranker reason grammar preserved verbatim, score-desc; NO new gateway site); pre-rider rows simply lack the keys. (5) **Dogfood**: `eval/src/dogfood-page-loop.ts` + `dogfood:page-loop` script — the whole loop chained (optional grounding URL → generate → judge → `autoApprove`-gated approve (input-carried; actor recorded) → publish); CLI = live gateway drivers, **not run in-lane** (judge-tier spend = lead-side at the train, top-up-gated); the keyless e2e proof rides in engine (`webpage/__tests__/page-loop-e2e.test.ts`). **One out-of-sanction edit, flagged**: `eval/tsconfig.json` gained the `paths` block (apps/web precedent; eval's vitest config already carried the alias half) — in-worktree `tsc` otherwise resolves `@thalon/engine` through the junction to MAIN's sources and can't see lane exports; merge-safe (identical layout in main); **composition-v2 should expect it at rebase** (it needs it too). **Verified in-worktree**: per-project sweep contracts 54 · db 40 · platform 21 · engine 425→**456/3** (68 files) · judge 41 · eval **16** (incl. the posts/-protected sweep test) · web 246 (4 known PGlite-contention files + 1 keyboard-triage flake under full-pool load, ALL green in isolation — zero web files touched by this lane) · repo-ratchets 5; typecheck all workspaces clean; lint 0 errors (known `_dataDir` warning); guard PASS per commit. **Lead-train follow-ups**: object-keys.ts docblock one-liner documenting the `posts/` family (out of lane glob; the full docblock lives in `posts.ts`) · optional `WEB_INGEST_DRIVER` env line ONLY if config-selected web ingest is wanted (NOT needed in-lane — driver name is per-call data, `fetch-extract` default) · arm the blog's live `readPublishedPosts` read in apps/web · the live judge-tier `npm run -w @thalon/eval dogfood:page-loop` when the top-up lands.
- 2026-07-07 composition-v2: **Composition v2 + TTS narration + narrated demo re-cut complete at review on `agent/render/composition-v2` (5 commits, pushed).** (1) **Composition v2 — raises EVERY video, both entry mappings (engaging-clips §5 item 1, all seven points):** the deterministic generator now emits a multi-file Hyperframes project — the root mounts one sub-composition per cue (`data-composition-src`, the B5.1 linter's own advisory; hosts carry their own composition ids, a requirement found by running the REAL `@hyperframes/lint`, which now passes every generated file 0 errors / 0 warnings) and owns the between-scene layer: the curated transition enum (`cut·dissolve·push·push-up·blur·scale·flash`) as parameterized tween recipes; the brand depth stack derived from `identity.style` DATA behind the char policy (gradient base + accent-glow drift + vignette + GSAP-STEPPED film grain — the catalog's wall-clock CSS `infinite` keyframes deliberately NOT vendored, re-expressed as finite `tl.set` steps so the frame clock replays them); per-cue MOTION exercised from data with a varied deterministic fallback (the `smooth` pin is retired; hook punches, cta lands); kinetic karaoke captions (the pill-karaoke grammar re-derived as COMPILE-TIME TS — char-count line packing, no runtime canvas measurement; judged words live escaped in markup, never in script); and a stat count-up block for numeric onScreenText (object-tween/onUpdate — seek-safe, comma-grouped, markup rests at the true value). (2) **Pacing-density rule enforced by construction:** all scheduled visual changes are planned pre-emission; any gap >2.5s (>2s inside the hook cue) gets deterministic accent pulses inserted, then a hard re-assert — a 12s two-word cue fixture pins maxGap ≤2500ms and the pulses materialize as real tweens. (3) **TTS narration (deliverable B):** `NarrationDriver` seam — Kokoro-82M through the pinned 0.7.33 CLI as an injectable subprocess runner (the B4.8 whisper-runner pattern; tests never synthesize); content-addressed artifact cache (key = text × voice × model; `narration.json` written last as the commit marker); word alignment is its own seam — the in-tree default times words against the MEASURED audio duration and records `alignment:"estimated"` honestly; whisper-cpp is absent on this box (doctor), so transcribe-backed exact alignment is the recorded upgrade behind the same `align` dep. Audio reaches the render as an OPTIONAL target-deps bundle — the `RenderTarget` seam and every manifest byte untouched (empty-decoration byte-identity is test-pinned; key-stability suite 9/9 green). **NO music bed:** the `bed` input exists as an honest empty seam, nothing in-tree supplies a file (§6 founder-ratified); SFX accents resolve from operator `SFX_PACK_DIR`, skipping by name when absent — recorded in the outcome, never fatal. (4) **Demo re-cut (deliverable C):** specs gained per-beat `motion`/`transition`/`sfx` (eval-owned schema, contracts untouched; an undirected spec builds a byte-identical pre-v2 manifest, pin-tested); `render:demos` narrates by default (keyless $0 Kokoro; `DEMO_NARRATION=off` opts out) with measured audio driving EVERY cue duration (hook/cta included — the research doc's timestamps-as-source-of-truth idea); three narrated MP4s (h264+AAC, ~17–22s) delivered to gitignored `.context/renders/` for the lead's FeatureLoop train — zero apps/web edits. (5) **Catalog adoption-gate license checks (recorded per repo rule):** studied items `transitions-push/-dissolve/-blur/-scale`, `caption-pill-karaoke`, `caption-kinetic-slam`, `grain-overlay`, `vignette`, `apple-money-count` — all fetched via the pinned `hyperframes add` from raw.githubusercontent.com/heygen-com/hyperframes/main/registry; the repo root LICENSE verified **Apache-2.0** on 2026-07-07 (items carry no per-file headers; the repo license governs); NO snippet bytes vendored — every recipe re-expressed as parameterized emitters with a provenance record per entry (`composition-transitions.ts`); the one verbatim asset is the grain SVG-noise data-URI (same Apache-2.0 registry item, provenance in `composition-project.ts`). (6) **Machine changes, user-scope only (recorded):** `pip install --user kokoro-onnx soundfile` (MIT / BSD-3; Kokoro-82M weights Apache-2.0) — the kickoff's keyless local audio stack; zero repo deps added; the boundary ratchet caught an env read in the first spawn cut — the driver now inherits the environment untouched (telemetry opt-out stays machine/environment config per ADR-0004 §5). (7) **Worktree hazard fixed structurally:** eval's tsc resolved `@thalon/*` through the node_modules junction to MAIN's sources (the B1.2 class — new engine exports "didn't exist"); `eval/tsconfig.json` now carries the same sibling-source paths its vitest config already had. (8) **Verified in the worktree (per-project sweep; CI is the authoritative gate):** contracts 54 · platform 21 · db 40 · judge 41 · eval 22 (+6 new) · engine render suite 99/1-skip with the full engine pass showing only the documented PGlite/worker contention class under 3-lane+render load (all four representative files re-run green isolated, incl. key-stability) · repo-ratchets 5/5 · web PGlite route files green isolated under load · typecheck all workspaces clean · lint 0 errors · guard green per commit. **Real-render proof:** a probe MP4 came out frame-exact (4.000s = 120 frames @30fps) with karaoke highlight, mid-count stat and the depth stack visible in extracted frames; the narrated clips carry AAC narration. (9) **Follow-ups:** (a) clips weigh 17–22MB — film grain is h264-hostile at CRF18; the lead train should re-encode for web delivery (e.g. crf~30 + light denoise) or we lower grain opacity at the swap; (b) installing whisper-cpp arms exact word alignment (seam ready, config not code); (c) local throughput was ~3s/frame single-worker under load — production artifacts stay the Docker/Lambda path (ADR-0004 §7); (d) scene sub-compositions open the B2.5 real-product-footage compositing path (§5 item 4) as B6.6-adjacent scope. Zero live spend; zero new `withGatewayGuard` sites; hyperframes pins untouched at 0.7.33. Handoff note: `agent_handoff/CURRENT.md` untouched (lead-owned); this message is the composition-v2 lane's wrap record.
- 2026-07-07 landing-v2: **Wave-3.5 web lane complete at review on `agent/landing/v2-blog` (4 commits, rebased onto the lane-cut main `8d1ed6d`).** (A) **§8 landing uplift — ZERO new deps; no dependency/license gate to report**: every ReactBits-class moment held up as a CSS-only equivalent (the kickoff's stated preference), so the client-JS budget's "one hero moment" amendment went unspent — hero signature = split-headline word-rise on server-rendered spans + two drifting depth blooms behind the vignette (pure CSS, `/` stays a static shell); honest proof band (4 engineering facts, provenance documented on `STATS` in copy.ts: 800+ tests · $0 self-rendered demos · 6 platforms drafted · 0 posts without a click); old-way/new-way strip (§5-honest lines only); feature-card pull = pointer-tracked CSS-var spotlight on the §2 cards — **`feature-loops.tsx` has ZERO edits; the FeatureLoop seam awaits the lead's train commit untouched**; micro-interactions stopped at 2 (count-up on waitlist position, CTA glare sweep — both reduced-motion-safe, as is every new animation); footer architecture as shared `SiteHeader`/`SiteFooter` (honest links only — no placeholder-page theater); pricing untouched ($29/$79/$199 from TIERS). (B) **§9 blog v1**: `/blog` + `/blog/[slug]` statically prerendered from a tracked content module (`lib/blog/posts.ts`) — 3 seed articles on the AI/social-tooling beat (the approval-gate argument · llms.txt/JSON-LD for answer engines · trend-signal scoring without a black box), **FLAG: the three articles need founder review at the train per the kickoff**; per-post `BlogPosting` JSON-LD from the same post object (A13 no-drift rule); RSS 2.0 at `/blog/rss.xml` (force-static; lastBuildDate = newest post, deliberately not now(), so feed bytes only change with content; XML-escape test-pinned); sitemap auto-extends; llms.txt gains the article index; nav + footer links from `/`; **cadence honesty is EXECUTABLE** (tests pin "regularly", forbid daily-cadence claims and rankings promises across ALL post text) and the FAQ disclosure stance renders on every post. **Posts-bundle read seam built + DISARMED exactly per the mini-contract**: `lib/blog/types.ts` zod-pins the bundle shape verbatim, `lib/blog/live.ts` returns seed-only with the arming steps in its comment, fixture + parse/map tests prove the shape — arming at the train is wiring, not shape work (components are era-blind; engine post BODIES via htmlRef deliberately wait for the armed read). (C) **Library rider (session-19)**: title-first shelf rows w/ URL demoted to secondary (pre-rider rows keep URL identity); tag chips + tags input (comma-parsed, deduped, capped 12) — **tags ride the ingest request through the engine's EXISTING `meta` pass-through in `ingestVideoUrl`, zero engine edits**; relevance badge = top `meta.areaRelevance` area in the thermal grammar (`heat-grade.tsx` gained an additive `detail` prop; reason string in tooltip + aria); transcript segments collapsed-by-default after ingest w/ copy/export still one click (shelf-open = the expand-on-demand click, opens expanded); honest degrade on pre-rider rows test-pinned (no title → URL primary, no chips, no badge — never invented). **Verified in the worktree: web suite 59 files / 278 tests ALL GREEN under 3-lane load** — and the recurring “4 PGlite route files flake under load” got a REAL diagnosis + an executable ratchet instead of another flake note: `openTestDb()` runs in `beforeEach` HOOKS, so the PGlite WASM boot was blowing vitest's 10s hookTimeout default while `testTimeout` had already been raised to 30s for exactly this (symptom: a DIFFERENT single test failing each run, incl. tests that never touch the db) — `hookTimeout: 30_000` now rides `apps/web/vitest.config.ts` with the diagnosis in its comment; proven live: rotating failures before, 0 failures after, same load. Typecheck clean; lint 0 errors (known `_dataDir` warning only); guard green per commit; production `next build` green with `/` STATIC and /blog + 3 posts SSG. Follow-ups for the train: (1) lead arms the live posts-bundle read (steps named in live.ts); (2) founder reviews the 3 seed articles; (3) known non-item: re-ingesting an existing source (same contentHash) keeps its original meta — tags on a duplicate ingest are dropped by the engine's fast path, recorded here so nobody debugs it as a UI bug.
- 2026-07-07 landing-v2: **Addendum — Deliverable D landed (founder mid-flight kickoff rider): the AI-brief transcript export.** The kickoff gained §D after launch (transcripts exist to be FED TO AGENTS — Thalon dogfood + the founder's other build agents — so exports must be token/context-efficient; the old `toPlainText` one-line join was the founder's "squashed in Notepad" report). Shipped, one commit: (1) **`.md` is the headline export** — `toMarkdownBrief` in `lib/library/export.ts`: `# <meta.title>` degrading to URL, honest fact list (source · duration from the LAST REAL cue, timed-only · segment count · ingest date · tags when present), then `## Transcript` as readable paragraphs with ONE sparse `[mm:ss]` stamp each (`[h:mm:ss]` past the hour); merge rule = break on speech gaps >2s, else at sentence boundaries past ~75s, hard at 90s; untimed ingests paragraph on character windows and carry NO stamps/duration (the never-invent-timestamps rule holds); the token rationale (SRT ≈ 2.5–3× tokens for the same words) is preserved in the builder's comment per the kickoff. (2) **Copy-all now copies the same `.md` brief** (button relabelled "Copy brief", tooltip says where it's meant to be pasted) — the clipboard squash is gone at the source. (3) **`.txt` keeps the same paragraph merge** (no stamps, no header); `.csv`/`.srt` byte-identical to before; `.md` leads the button row and is untimed-safe. (4) Pure builders + tests like the existing three (merge rules incl. both window breaks · brief facts · honest degrades · builder-registry pins). Verified same bar: library suites green, typecheck clean, lint 0 errors, guard green, `next build` green with `/` static. Board row stays `review`.
- 2026-07-08 lead: **WAVE-3.5 MERGE TRAIN COMPLETE (session 20, spanning midnight) — PR #34 (intel.dossier) -> #35 (origination B6.6) -> #36 (composition-v2) -> #37 (landing-v2 + train commits) all rebase-merged, CI green each time (one transient red: onnxruntime postinstall ETIMEDOUT on a runner, green on rerun). SPRINT-6 WAVE 3.5 IS ON MAIN; next act = B6.7 deploy.** Train commits carried on the landing car: (a) **FeatureLoop swap** — composition-v2's three narrated clips re-encoded for web (crf30 + hqdn3d denoise + 960px: 17-22MB -> ~300-400KB each, durations/AAC verified, frame spot-checked), reduced-motion-safe `<video>` loops behind the unchanged seam, popout gains `controls` + a QUIET provenance caption (founder direction mid-train: the watermark carries branding; no self-important overlay copy); (b) **blog live read ARMED end-to-end** — new engine export `readPublishedPageHtml` (B4.6 content-address-verified; safe to render BY CONSTRUCTION since `selfContainmentViolations` forbids script/iframe/object/embed + external loads pre-judging, +1 engine test), engine posts render on /blog and /blog/[slug] (bundle-entry lookup -> verified artifact body), index goes per-request (a publish shows without a rebuild; landing `/` stays static, build-verified; B6.7 upgrades feeds/index to revalidate-on-publish); (c) object-keys docblock += posts/ family (origination car). **Live judge-tier dogfood ran TWICE on the topped-up gateway (MODEL_JUDGE_FINAL restored to sonnet-4.5) — and the judge earned its keep both times:** run 1 (built-in brief-only context) blocked "both g3 tiers failed" — correctly: a brief alone is not grounding, every elaborated claim failed verification; run 2 (context.text carrying substantial true source material — the real promote shape) generated a grounded page, **g3_final (sonnet-4.5) PASSED ("no unsupported specifics"), the haiku screen FAILED rhetorical commonplaces ("fluency and accuracy are not the same property") -> tier disagreement -> BLOCKED per invariant I3.** The draft waits in the approve queue with both tiers' reasons — the operator click is the sanctioned unblock, and approving it publishes Thalon's first engine-authored post through the armed door onto /blog. **Follow-ups recorded:** (1) screen-tier strictness on non-factual rhetoric = judge-prompt tuning material w/ eval rows (B6.7's eval-row refinement item — do NOT loosen I3 itself); (2) [you] the three seed blog articles need founder review (titles on the PR/board; read them rendered at /blog); (3) [you] the blocked draft decision (edit/re-judge/approve in the queue); (4) feeds (rss/llms.txt) bake seed posts until B6.7's revalidation story. Worktrees + lane branches all GC'd (main node_modules verified intact); board rows flipped merged.
