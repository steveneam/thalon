# NEEDS-STEVEN — open founder actions (thalon)

> One open founder action per line, `- [YYYY-MM-DD] text`. Ingested by the
> founder dashboard (swordfish note 2026-07-16). Remove lines as they close.

- [2026-07-26] **THALON_DATA_DIR — your (b) was TRIED s79 and REVERTED same session; the half that mattered shipped anyway.** You approved requiring an absolute path with a loud failure. Implemented, it threw on a HEALTHY dev server: `readEnv()` does not see `.env.local` at every call site (the `/api/intel/trends` route resolved the `.data` default and 500'd while ingest saw the right value), so the refusal fired on correct config — your "console and recoverable error" sighting. Reverted within minutes; the workspace is clean. **What DID ship is the actual pollution fix:** hermetic per-worker test data dirs for the engine and eval suites (`tests/setup/hermetic-data-dir.ts` — apps/web already had one for a version of this exact bug), so no test run can ever again write embeddings into the real `.data` behind the shared cache index, and the s79 self-healing miss covers any rows already poisoned. **Remaining, queued not urgent:** land (b) safely, which first means understanding why some route handlers read the env default — an s80+ item — and your s79-close *"follow your recommendation"* confirms it: land (b) safely once the env plumbing is understood, nothing further needed from you.
- [2026-07-26] ✅ **STAGE ARTIFACTS — RULED s79, EXECUTED s80 pre-flight, nothing further needed.** (a) stage artifacts left the `needsYou` derivation — **live count 25 → 10**, and `counts` gained a `staged` field so the queue can SAY what it is not counting rather than silently filtering. Three derivations had to move together (the pulse, the dashboard card's rows, and the Board's waiting column, which takes `Math.max(pulse.needsYou, its own count)` and would otherwise have out-voted the fix and put two numbers on one screen). (c) the Jul-19/25 dogfood archive ran: 48 judge_results, 15 drafts, 15 runs and 6 sources with no remaining reference. Two attempts aborted on FK constraints and rolled back whole; the committed pass deletes a source only when nothing references it. **Your editor fixtures were never in scope and are intact: 3 projects, 11 cuts, 76 takes.** Original ruling kept below for the record. — **RULED s79 close: (a)+(c), the clean sweep** (*"i'll follow your recommendation"*). So: stage artifacts (`direction_doc`/`storyboard`) stop counting toward `needsYou` — their verb is **advance** through their own staged lifecycle, and a queue only counts what you can act on — AND the Jul-19–25 dogfood stage rows get archived. **Execution = s80's pre-flight** (small, lead-direct, verified before the editor work starts); the archive touches only stage DRAFTS, never the video PROJECTS the editor session drives against. Facts that grounded it kept below for the record: the staged pane's pick/edit/advance is wired end-to-end (`/api/staged/[draftId]/*`), the repo's approve/reject is format-agnostic but "approve a storyboard" has no defined meaning against the judge gate, and adding verbs was therefore the worse option.
- [2026-07-26] ✅ **TRANSCRIPTION SCOPE — RULED s79, and it is NOT a Thalon feature.** His words: *"transcription is a separate product/feature, more like an extra feature i wanted for myself … i have no plans to integrate it with the rest of thalon. If i do use it, it would be so i can get transcripts about system/building related stuff to teach you and make you learn about building stuff."* So it is **his own knowledge-ingestion tool**, and the design follows from that: **free and deterministic by default** · **an AI/toggle button NEXT TO Ingest** that initiates the cleaner-transcript path on demand, per ingest, his choice · **no second artifact** — he explicitly declined keeping verbatim-plus-enhanced side by side, because whether to enhance is the user's call at the moment of ingest. My two-artifact invariant is therefore WITHDRAWN: it was premised on the transcript being judge-grounding, which he has ruled it is not. **The one factual wrinkle he should know, not an argument:** today's code does not match "free and deterministic" yet — every ingest chunks and embeds through the METERED gateway, and those chunks land in the same store `topKSimilarChunks` retrieves from, so the feature is currently both wired to grounding and spending on every paste. Making the embedding pass optional is a small change (the embedder is already an injectable dep) and it is the whole of "free and deterministic initially". **Effort, since he asked: SMALL.** The free/deterministic default = one flag + skipping the embed pass (the embedder is already an injectable dep) — half a session; the AI-enhance button beside Ingest = one session. **Both fit AFTER the video-editor session — queued s81+, and his s79-close *"follow your recommendation"* confirms that sequencing.**
- [2026-07-26] ✅ **YOUTUBE RATION — CLOSED s79, and the arithmetic that kept it open was WRONG.** You ruled *"slow the cadence to ~2h"*, choosing the option whose stated goal was "fits the quota". It already fits, so **nothing was changed** — acting on the ruling literally would have made it worse. The live `sweep_schedules` row is **cadence_minutes = 180 (3h) → 8 sweeps/day**; worst case 12 `search.list` × ~100 units (the engine's own figure, `youtube-source.ts:9`) = 1,200/sweep = **9,600 units/day, just inside the 10,000 allowance** — which is why there has been no 429 today (clean sweep 10:31Z: 60 cards / 422 polled / 0 admitted). The "~96 passes/day → 86k units" figure carried from s75 was the **scheduler TICK** (15m, clamped 1–15m, and it only sweeps when a schedule is DUE — the log reads "0 due" on almost every pass), never the sweep cadence. Going to 120m would mean 12 sweeps/day = 10,800–14,400 units and would re-exhaust daily. **Say the word if you want fresher intel anyway** — 120m is one line, but the ration must drop to ~8 searches/sweep to stay inside budget, trading coverage for freshness. **Note the 0 admitted is a separate thing** (fail-closed admission floors, your s72 likes≥500 call), not a quota symptom.
- [2026-07-26] **RATCHET CANDIDATE for the next contract window (from the above):** the engine guards `search.list` calls PER SWEEP (`youtube-source.ts:158` refuses over `maxSearchesPerSweep`) but has **no PER-DAY budget guard** — the quota is a daily bucket, so the only thing standing between the config and a daily wall is arithmetic done by hand in a handoff note, which is exactly what was wrong for two sessions. A per-day unit ledger with a fail-closed refusal is the executable version of that note. Not built mid-lane (window frozen, s79 lanes live).
- [2026-07-28c] **YOUR FOUR CALLS ARE RECORDED AS DECIDED ("I'll go with your recommendations") — the three s82 lanes are APPROVED and launch at the next boot with no re-ask — AND THE DISTRIBUTION CHARTER YOU ASKED FOR IS DRAFTED.** `docs/research/distribution-charter.md` — the complete Postiz plan, not just connectors. The one framing correction worth your eyes: **Postiz's analytics is NOT our Intel.** Intel measures the MARKET (trends, what works for others); their analytics measures OWN-POST performance (impressions/likes/reach per published post) — we have none of that half; `social_publications` records the post id and stops. The charter's headline flying car is the MERGE: own-post metrics feed the B-learn loop so generation shifts toward what OUR audience rewards — Intel says the market, analytics says us, the fan-out steers by both. Nobody in this category closes that loop. **Phases:** D0 = your approved s82 queue lanes · D1 = connector seam (s83 rec; proof = Reddit+Bluesky live, no review wall) · D2 = analytics closed-loop (publication_metrics window + per-connector postAnalytics + the Analytics surface) · D3 = composer parity, judge-gated evergreen (recycling that re-passes the gate — theirs re-posts blind), RSS entering at GENERATION so the judge gates it (theirs skips to the queue), mention autocomplete · D4 = **REDESIGN PHASE 4/5**: four sheets (Analytics · Calendar→Schedule · composer band · Channels), mock→your verdict→exact build, the era's own process · D5 = Thalon-MCP/public API parked on triggers · D6 = explicit rejects (marketplace · cookie-extension posting on ToS grounds · Temporal). **The ratification bundle (charter §5): (1) ratify or amend the charter** (ADR follows, the ADR-0011 precedent); **(2) D4 design-wave GO** — mock the four sheets for your verdict starting after s82 (mocks cost no engine time, can run beside D1); **(3) one honest sequencing note:** D2's dashboards stay thin until posting is routine, and posting volume is gated on YOUR sequence gate + per-platform GOs — the build order (D1→D2) is right either way, but say if that reorders anything; **(4) short-link domain** (D3) = a stealth call, defaulted to seam-with-empty-provider until you rule.
- [2026-07-28b] **POSTIZ DEEP DIG DONE (your ask, same day) + MY RECOMMENDATIONS ON THE FOUR CALLS.** Answer to your question first: **the secret is NOT the auth folder** (that's user login). It is `libraries/nestjs-libraries/src/integrations/`: ONE provider interface (~190 lines) + ONE hardened base class (retry/refresh/typed-refusal fetch, ~290 lines) + ONE registry + **one file per platform** (Reddit = 514 lines, its whole OAuth dance ~90) — and ONE generic connect flow (two endpoints + one dynamic callback page serve all ~36 platforms; state in Redis with a 1h TTL; refresh runs automatically off typed 401s). **The honest part: their "seamless" is one-third hosted-product privilege** — self-hosted Postiz still makes you create your own developer app per platform (their .env.example is a wall of REDDIT_CLIENT_ID/LINKEDIN_CLIENT_ID pairs), so the Meta/LinkedIn/TikTok posting-scope review wall we named in the s70 Nango verdict is untouched by any code, theirs or ours. What code DOES fix: per-platform build cost and the paste-token connect flow — both ours to take. **Plan §4 (new): the CONNECTOR SEAM, recommended as the s83 headline** — one SocialConnector contract + generic connect door + vault-backed tokens + a refresh tick on the sweep scheduler, **proven by adding Reddit + Bluesky the same session** (both have instant developer apps, NO review wall; Bluesky doesn't even use OAuth). Not s82, because it re-shapes the driver layer the queue lane calls through — s82 proves the queue on today's drivers, s83 re-shapes and adds two platforms. On AGPL: studied their interfaces and flows as facts, every line we build is written fresh against our own contracts — nothing of theirs enters the repo. **My recommendations on your four calls: (1) launch all three lanes** (disjoint by construction; if you want only two, defer editor-polish); **(2) keep the delete default** (an approved cut is a judge receipt — deleting it deletes evidence; softening is one line later); **(3) keeper-state preview now**, sheet amendment only after dogfood proves it earns permanent chrome; **(4) confirm the disarmed posture** — the only one consistent with your standing gate. Full dig: `docs/research/s82-PREPLAN.md` §1b + §4.
- [2026-07-28] **s82 PLAN OF RECORD IS WRITTEN — FOUR CALLS ARE YOURS, #1 GATES THE LAUNCH.** Your ask ("plan the next phases … look at Postiz … address medium/lows … maybe parallel workstreams") is planned in `docs/research/s82-PREPLAN.md`, lanes queued on COORDINATION.md. **The Postiz verdict in one line:** it is AGPL-3.0 so we re-implement patterns and never touch their code; most of it we already have or do better (they have NO judge gate — our approve consent + grounding is the moat), but they expose our one half-built loop: **`publish_queue` exists in our DB with no repo, no producer and no consumer** — so the worthwhile take is finishing our own approve→slot→queue→timed-publish spine, plus a per-platform capability matrix (char limits/media rules as data — today a 400-char X body would reach the platform call before anything refuses) and a platform-true preview at Approve. Parked with triggers: their analytics (→ a publication_metrics window when live cadence exists), evergreen re-queue, RSS, public API/MCP. **The four calls:** (1) **Lane GO** — three lanes (editor-verbs = version management slice · editor-polish = the s78 medium/low tail, 10 items · sched-spine = the Postiz take, ships DISARMED, zero live calls) + the small pre-flight contract window (publishQueue repo · videoCuts.remove · capability matrix). (2) **Delete rule default (say if you want it different):** hard-delete the version row and its file; refuses deleting an approved cut, a parent of a living derived cut, or the project's last cut. (3) **Approve preview card:** real sheet amendment (canvas + re-export) or a keeper-state behind existing chrome? Default if silent: keeper-state, sheet stays law. (4) **Confirm the sequence-gate posture for the scheduling lane:** consumer ships disarmed, queue rows sit pending, your per-platform GO + per-post GO unchanged — the lane never fires a platform call. "gogogo" + "launch all three" (or name fewer) starts s82.
- [2026-07-28] ✅ **VIDEO EDITOR — FINISHED to the scope you ratified, s81, zero spend. Nothing needed from you; the next step is your call.** Both gates moved and both were RE-RUN at boot rather than trusted: the **27-job table went 14 works · 2 dead doors · 10 no-affordance → 21 works · ZERO dead doors · 5 no-affordance**, and the render gate against `Videos.dc.html` went **38 drifted / 0 within tolerance → 30 / 9**. Your ratified order was executed as given: slice (d) first (it really was ONE defect — the copilot band's 19px — and fixing it retired the whole cascade), the music swap took the `music-candidates/` route so **no contract window opened**, and the working-copy preview was built as local compute inside your sequence gate. **What you can now do that you could not:** swap a cut's music bed from the project's own seven candidates (a silent cut could never acquire music at all before) · **preview an unsaved edit** — the player could say "your edits are not in this" but never show them · get back to the 16:9 master from a derived cut (it was a decorative span) · find out why a refused control is refused, by pressing it, instead of hovering a disabled button that answers nobody · see **who authored the version you are editing** and whether a derived cut has fallen behind its parent. **Two things worth knowing.** (1) The copilot fix was not cosmetic: "the agent answers with a proposal — never a silent change" was PLACEHOLDER text, so your provenance promise vanished the moment anyone typed, which is the one moment it matters. It is visible text now. (2) Driving the new music verb caught a bug in my own definition of it — it was offering a silent video experiment as a music bed — and the playhead job turned out to be **flaky**, passing and failing on identical code; both are fixed and both are recorded in the harness's bug ledger. **Five jobs remain with no affordance**, and four are one theme (compare two versions · save as a named variant · delete a version · check on a render after coming back), which would make a clean next slice; the fifth is auditioning a take before you swap it. Say the word if you want that, or something else next.
- [2026-07-26] ✅ **VIDEO EDITOR — s80 BUILT THREE SLICES, AND YOU RATIFIED THE s81 ORDER AT THE CLOSE** (*"i'll follow your recommendations"*): slice (d) leads (the copilot band's 19px retires ~20 of the 38 drift rows in one change), the music swap goes via the project's own `music-candidates/` takes so **no contract window opens**, and the working-copy preview is local compute inside the sequence gate. Recorded as settled so s81 executes rather than re-asks. Nothing needed from you. Both gates now RUN and both moved: the 27-job table went **6 works · 3 dead doors · 14 no-affordance → 14 works · 2 dead doors · 10 no-affordance**, and the render gate against `Videos.dc.html` — recorded as "does not match, unquantified" for two sessions — is now a number: **48 sheet classes, 10 missing, 38 drifted** (new tool, `scripts/measure-sheet.mjs`). Merged: the SAFETY CORE (undo/redo + ⌘Z, an unsaved-work guard on every exit incl. the rail, and a player that says which render it is showing), the BLOCKER (timeline blocks answer the keyboard — a keyboard-only operator previously could not edit a caption at all), and the MISSING VERBS (insert/delete a beat, insert/delete a caption). **The gate found that the audit's scattered layout findings are largely ONE defect:** the copilot band renders 60px against the sheet's 79 and shifts every band below it by exactly −19px — about twenty of the 38 drift rows. That is s81's first move. Still open: swap the music track · preview the working copy · the two remaining dead doors · the copilot chips. Your mid-session call ("can make the one with the music the default") is honoured in the drive harness; the PRODUCT still opens `project.cuts[0]` — whatever the query returned first — which is a real defect on the s81 list. Original ruling kept below. — **RULED s79: FULL BUILD-OUT SESSION.** Your call on the three options was (a) the whole thing, not the minimal safety slice and not parked. So the editor gets **its own session** against the whole 27-job table — undo · an unsaved-work guard on every exit (three plain `<Link>`s and the back button currently discard the working copy silently) · insert/delete a beat and a caption · swap the music track the copilot chip already offers · preview the WORKING COPY (today, while dirty, the player shows the PREVIOUS render with only an "unsaved" pill beside it) — plus the 36 confirmed findings and the sheet-fidelity gap (`Videos.dc.html` IS the editor's sheet and the render gate against it does not match, unquantified — first thing to measure). Report: `docs/research/video-editor-audit-s78.md`. **Nothing further needed from you to start it** — it is queued as the next session's headline, and its one blocker (keyboard-dead timeline blocks, so the caption and music inspectors are unreachable by keyboard) rides that session rather than s79 lane 4, since you took the full-build option. The new `scripts/drive-surface.mjs` jobs harness (s79) is the tool it wants: the editor is the surface with 15 of 27 jobs missing, and that table is now something that RUNS.
- [2026-07-26] ✅ **ALL FOUR s77 RULINGS RECORDED — nothing further needed on any of them.** (a) **Approve sort = NEWEST FIRST.** The app already did this; the SHEET was the wrong one (its chip says "Oldest first" over newest-first rows), now annotated as a founder amendment so no future lane "fixes" the app to match it. (b) **Dashboard publish door = ARM.** Queued into the s78 fix pass. (c) **Calendar = your call, delegated to the lead** — decision made and recorded: adopt "+N more" for concurrent events, because the sheet ALREADY uses that treatment in its waiting lane, so it applies the sheet's own vocabulary rather than inventing grammar; clipping a platform name mid-word loses information with no cue that anything was lost. (d) **Post/page generation on Create = GO TO ARMING**, with your condition carried verbatim into the plan: *"we're not posting anything yet until all the walks are verified and fixed next session."* Recorded as a hard sequence gate — the arming may be BUILT in s78, but no publish path is exercised until the 189 findings are verified and the survivors fixed. Generation is also the one item that spends tokens per click, so the gate protects spend as well as quality.
- [2026-07-25] ~~Wave-0 mock verdict~~ **CLOSED s72: GO recorded ("you have my approval for the workspace redesign") — the wave0-astryx lane launched same-session; wave-1 surface lanes follow per ui-overhaul-plan §4.2.**
- [2026-07-18] **Wave 3 checkpoint is ripe** (A+ animation candidates: bloom-transition video · Orchard seasons-tree scroll · Wagtail scroll-dog). [⑯ Ridge & Valley VERDICTED OK by the founder s63 — closed.]
- [2026-07-18] **W-audit results glance** — `docs/research/workspace-audit-s62.md`: 18/20 after same-session fixes. Your three s60 complaints all closed: source-links everywhere (+ thumbnails plumbed), full impeccable pass done, film registered (dev). Plus: board saved views now save for the whole workspace (they were per-browser), calendar phone bug fixed, two more lists bounded.
- [2026-07-19] **⑲ Tsukimi fix round APPROVED by the founder s64 ("its pass and its ok") — CLOSED, no further re-glance.** Visual arc HOLDS at 20 sites (founder: "leave the landing pages at this for now"); the checkpoint tail (A+ family · B-sitegen · THE LANDING) awaits his sequencing whenever.
- [2026-07-18] NEW glances when you have ten minutes: (a) the Sites surface — /app/sites: the whole portfolio as cards + live dossiers.
- [2026-07-18] Phase I glance: the redesigned workspace is LIVE on main (journey spine · dossier launchpad · create handoff · approve consent · calendar month/week · leads board). Staging redeploys off the push — a click-through when you have ten minutes. Verdict notes welcome; fix rounds are cheap.
- [2026-07-18] W-sites console tail (build GO given s61 — straight-to-build, lead-direct): when convenient, create the Dokploy templates-preview service (neutral hostname + edge auth) and set TEMPLATES_PREVIEW_ARMED=true — that gives the workspace Sites surface a live origin (dev reads the local dir meanwhile; the image already builds on every push).
- [2026-07-14] Month-end Higgsfield credit call (balance 706.12 API-verified s64; waves 1–4 ALL SHIPPED — the whole 20-site portfolio has cost ≈24cr of the 730 opening)
- [2026-07-17] B-crm.4 live-send GO (door built+merged, deliberately disarmed): the s28 stealth question — brand-domain outreach reveals the brand pre-launch; accept / neutral domain / wait. Also needs Resend domain setup when you GO.
- [2026-07-19] **Sprint 8 first wave MERGED s64; second wave OPENED s65** (window 2 frozen PR #64 · B-pub.2 driver lane running Mode B). CLOSED from the old arming list: intel keys armed + sweeping on the 4-hour schedule (scheduler now runs persistently on the box) · **gateway top-up CONFIRMED live** (paid-tier generate probe green). **Drivers LANDED s65 (PR #65).** ✅ s69: first live posts DONE on LinkedIn + Facebook (armed on your recorded GO); X pending only its API credits (line above). Future posts stay per-post founder GO. (Platform app-reviews: nothing to chase now — dogfood needs none; the Thalon-level partner-app filings belong to the Integrations charter below.)
- [2026-07-19] **Integrations charter RATIFIED (s65, all five calls as recommended) → ADR 0011.** Nothing more from you on it now; reminder embedded for later: the mode-1 partner-app filings (LinkedIn→Meta→X) need hosted Terms/Privacy pages, so THE LANDING sequences before OAuth-Connect.
- [2026-07-19] **s67 shipped the two loops end-to-end (zero credit spend):** (a) **THE BLOG LOOP** — pillar #1's article is LIVE on dev `/blog` (+ post page + RSS), through the real generate→judge→approve→publish loop; the ADR-0011 proof-of-product moment. Stealth holds: public `/blog` stays staging-side until your launch call. Glance welcome. (b) **THE POST LOOP** — the production caller is wired (drivers → publish door), one LinkedIn post drafted+judged and waiting; **each platform's first live post is still YOUR per-platform GO**: token into `SOCIAL_<P>_ACCESS_TOKEN` + `SOCIAL_<P>_ARMED="true"` + the platform in the tenant's social block (LinkedIn recommended first; checklist `agent_handoff/lanes/WRAP-pub2-drivers.md`). (c) **B-int.0 vault window FROZEN** (PR #66) — nothing needed from you.
- [2026-07-25] **PILLAR #1 VERDICT RECORDED (s70c): the minted cut = mid-quality exemplar, kept but never approved/published. Pillar #1 re-scoped to a WORKSPACE SCREEN RECORDING, gated behind the UI overhaul.** Nothing more from you on the video itself.
- [2026-07-25] ~~Exemplar-admission unlock~~ **CLOSED s72: you said "both" — done same-session.** (b) = `TREND_ADMISSION_CONFIG` env channel wired through scheduler + Sweep-now, Bluesky opted in at likes ≥ 500. (a) turned out deeper than the ask assumed: your YouTube key was ALREADY in the soak env — the real gate was the single-driver `TREND_SOURCE`; the soak now sweeps a comma-list (`youtube,bluesky`), each with full intake + admissions. Interim honesty: the trends surface still shows the LAST listed source's bundle (bluesky — unchanged view); the true multi-platform trends merge is flagged to the B-learn L2 charter slice.
- [2026-07-25] **THE MEME POST IS LIVE ON ALL THREE — LinkedIn + Facebook (s69) + X (s69b, after your credit purchase).** LinkedIn: https://www.linkedin.com/feed/update/urn:li:share:7486713895370256384 (your punchy body + the horse image + the @Anthropic mention, posted as you). Facebook: post `197903966922661_122232823196050754` on the MacTechDish page (image attached). X: https://x.com/i/web/status/2080955412472648006 (body verified = the approved draft, image attached). Glance welcome — nothing left to do on this one. (The `source` app-name check retired: X no longer exposes the posting app via API v2 or the web UI.)
- [2026-07-25] GitHub Actions billing: **renewed by you + verified back s68** — ci-guard and web-image both green; staging auto-redeploy resumed. CLOSED.
- [2026-07-25] **NEW glance: Settings → Integrations is LIVE (B-int.2, s70)** — /app/settings/integrations: a card per destination with honest states (LinkedIn + Facebook read connected-as-you off the real vault; X wears an "env override" badge for its 1.0a posture), guided connect flows, and the **Published view** — all three s69 posts + the s67 blog article in one ledger, each with its way back. Your Nango question is answered in `docs/research/integrations-surface-plan.md` §Mode-1 automation check (TL;DR: Nango = good OAuth plumbing, ELv2 license, does NOT bypass platform app review; the real unlock is pulling B-int.4's callback+refresh forward for YOUR apps, which already hold the scopes). Swordfish answered same-day (FROM-SWORDFISH): they run self-hosted Nango for the P2 drives — proven pattern, they're the portfolio Nango owner per your call, and they offer to mint Thalon its own instance at the B-int.4 kickoff (no new spend; only the hostname choice is yours). Nothing needed from you until B-int.4 sequences.
- [2026-07-25] **s74 wave 1 landed — 8 of 16 sheets are now the real workspace: Approve · Intel (+ Search) · Runs · Library · Create, on top of s73's shell + Dashboard.** Glance welcome on dev 3111; every surface was screenshot-diffed against its sheet before it merged. Your five live rulings are recorded and need no repeat. **Four things the lanes surfaced that are YOURS to sequence, none blocking:** (a) ~~media thumbnails~~ **DIRECTED BY YOU s74 — now B-media.0 + B-media.1 in ROADMAP, planned next session (research-led, mock before code) — nothing needed from you until that plan lands. You also called the rename: Library → Transcription.** Original finding: media thumbnails are missing on Approve, Runs and Create rows — the run/draft feeds carry no media reference to join, and Approve is exactly where you said media-first matters most ("scan-and-decide"); the fix is a draft-media join, a contract-window candidate, and it is the one place the rebuilt surfaces read thinner than the mock. (b) **Runs' Retry is rendered but resting unarmed** — no fan-out replay route exists anywhere; arming it is an API change. (c) **EmptyArt did not re-enter on any rebuilt surface** — three lanes independently found the plates are light-paper art (`mix-blend-multiply`) that reads near-black on the dark workspace; they need re-cutting for dark before the keeper comes back. (d) **`Approve.dc.html` contradicts itself** — its sort chip says "Oldest first" while its own five rows are drawn newest-first; the lane followed the rows plus your s66 ruling, so say the word if the chip was the intent. Also: **Search now exists as a designed surface, not a ported one** (the mock draws that tab but no panel) — if you want it on the canvas as a real sheet, that is a canvas change and a re-export.
- [2026-07-26] ✅ **ALL THREE s76 media-framework calls RULED same session** (remint granted · ffmpeg-into-image GO, now an s77 lane-B item · zero-state path = lead's choice). **ONE GLANCE when convenient: the zero-state proposal** — `docs/research/zero-art/zero-art-proposal.html` (open the file; it's self-contained). Your eight minted subjects redrawn as code (mono stroke SVG in the sheets' grammar) + Calendar/Videos as proofs that new surfaces cost a sketch, not a mint — identical markup renders both dark and paper, which is the whole theme-proof argument. If you prefer the etching look on any plate, your remint GO stands and we mint just those; the glove and the shelves are the two I'd call weakest.
- ✅ **CLOSED s76 — THE LOGO, and I was wrong.** You asked *"Isnt there a logo on the landing page, and a logo on the actual web tab icon too?"* — yes, both, and my "there is no logo" was flatly wrong. I had searched `apps/web/public/` for filenames containing logo/mark/wordmark; Next keeps the tab icon at `app/icon.svg` + `app/favicon.ico` (neither name matches), and the mark itself is `components/brand/marks.tsx` — your Grip II pick from 2026-07-06, aliased as `BrandMark`, already rendered by the landing header/footer, hero, sticky CTA, both icon twins, and previewed at `/brand`. The workspace rail was the ONE surface still drawing the sheet's placeholder gradient tile. It now renders `BrandMark` like everything else, in brand amber via `color` (mark-only token rule intact). **Nothing needed from you.**
- ✅ **CLOSED s76 — B-media.0 VERDICTED: worth doing, targeted at s77.** Your words: *"my verdict on b-media is that its worth doing right? maybe next session"*. The proposal sheet is now portable (its do-not-port bar lifted in the sheets README). **Nothing further is needed from you** — the plan's one open question is answered by your OWN earlier ruling (s75: *"also have placeholder until bmedia ready"*), so Approve/Runs/Create/Dashboard keep honest striped placeholders until B-media proper gives drafts real media; I did not re-ask. One planning fact worth knowing since you like parallel lanes: step 1 is a **contract window** (`meta.posterRef` + the oEmbed `thumbnail_width/height` we already parse and throw away → a derived `orientation`), and windows freeze BEFORE lanes launch — so s77 opens the window first, then fans out. Two other queued window candidates could ride the same freeze rather than wait for another: Runs' unarmed Retry replay route, and Library's "grounds N drafts" count. Original ask kept below for the record. — [2026-07-25] **B-media.0 PLAN + SHEET LANDED (s75) — one verdict and one question are yours.** You asked for "a proper and clever UI/UX with the source thumbnails … see how others do it", planned before code. Both halves are in `docs/research/source-media-plan.md`, and the proposal sheet is on the canvas beside the others: https://claude.ai/design/p/f5d304cb-cd0e-484d-8542-7b6561e1ef30?file=Source+Media.dc.html (also exported to `docs/research/mock-sheets/Source Media.dc.html`, filed under a NEW README section "Proposals — NOT yet verdicted" so no lane ports it before you rule). **The correction worth knowing:** thumbnails were reported as missing everywhere, but two surfaces already resolve real media end-to-end today — Transcription (captured at ingest via YouTube's keyless oEmbed) and Intel (driver-captured). So this is generalising something that works, not building from nothing. **What the sheet asks you to rule on:** five states of one reserved box (two are new — a *broken* poster, which today paints a browser broken-image glyph, and a *loading* state), three sizes on one ratio, portrait media switching from crop to contain (a 9:16 Short cover-cropped loses ~65% of its height and gets decapitated), and **scrub-on-hover being dropped** — not on taste, but because that artifact needs us to own the transcode and our densest surface holds a YouTube URL and one poster. **THE ONE QUESTION:** a draft is text and owns no media until B-media's import door ships, so **Approve keeps striped placeholders for the foreseeable future** — on the very surface where you said media-first matters most. The plan recommends leaving it honest rather than borrowing a thumbnail from the run or the grounding source (that would invent a provenance on the publish-decision surface). Say the word if you'd rather pull B-media's import door forward instead.
- [2026-07-25] **WAVE 2 MERGED (s75) — 14 of 16 sheets are now the real workspace.** Calendar · Board · Leads · Profiles · Sites · Settings/Integrations all rebuilt two-step off their sheets by three parallel lanes, each screenshot-gated by the lead before it stood. Only **Videos** (three sheets, your "re-conception") is left. Glance welcome on dev 3111. **Three things are yours to sequence, none blocking:** (a) **the legacy leads board is now orphaned** — `components/board/leads-board.tsx`, the old saved-view lead pipeline, has no caller: `Board.dc.html` turned out to be the CONTENT pipeline board (drawn under Home), not a leads board, so rebuilt Leads renders its `Board` tab and says so plainly. Both lanes spotted it and neither touched the other's file, correctly. **I did NOT delete it** — it is a working capability with no home in the mock, and that is your call, not mine; the ratchet pins keep it visible instead of rotting quietly. Options: it re-enters behind Leads' own Board tab (the re-entry rule: a keeper returns as a state behind the sheet's chrome), or it goes. (b) **Calendar clips concurrent events**: when three platforms are approved at the same instant, the week column splits three ways and each chip is ~45px, clipping mid-word ("Linked·", "Faceb·"). That is the sheet's own `.ev` rule (`overflow:hidden`, ported byte-true) meeting a density its fixture never had — honest rather than hiding events, but it reads poorly, and a "+N more" treatment like the one the sheet already uses in the waiting lane would be a sheet change, so I left it. (c) **Sites is a FOURTH surface that already resolves real media** (its portfolio preview shots) — worth folding into the B-media.0 verdict above, because it strengthens the case that the thumbnail work is generalising something that already works rather than building from nothing.
