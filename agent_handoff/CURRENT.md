# CURRENT

## Stamp

2026-07-25 (session 70 FULL ARC: s70 B-int.2 → s70b parallel turn → s70c founder-verdict turn, syd4 — 114cr day, balance 584.12) · **SHIPPED THIS SESSION: B-int.2 Integrations surface (cards + guided connect + published view closing the /blog partial) · B-int.3 driver rewire (lane, merged on green 1801-test verify — arming → tenant data, vault-first everywhere, ONE precedence table) · UI-overhaul PHASE 0 (lane, merged — hybrid Astryx adoption: MIT incl. StyleX, passing Next16+Turbopack+TW4 scratch build; five-disease audit; wave map; ALL 7 charter answers on record) · Phase 2c DISCOVERABILITY LENS (`ec07443` — the founder's LinkedIn catch as an executable golden pair) · pillar #1 minted+rendered then founder-VERDICTED mid-quality-exemplar (re-scoped: workspace screen recording after the overhaul) · ALL model seats → `claude-cli/claude-opus-5` (founder directive: strongest tier while building; BYO-AI parked with charter trigger) · setMediaRoot ratchet · env-connected card honesty · landing→workspace link.** s71 = WAVE 0, mock first.

## Resume prompt (session 71, syd4 — "gogogo" boots this)

**Resume · Thalon** — s71: (0) self-check (tmux `thalon` · pg_isready · 8899 + sweeper systemd units (NEVER hand-start) · `dev` window 3111 (recreate: `tmux new-window -t thalon -n dev -c ~/work/thalon/apps/web` + `npm run dev` if gone) · git status + stamp · **DROP `TENANT_DAILY_TOKEN_BUDGET` 7.3M → 2M (fresh UTC day; the raise was one exemplar embed on 07-25)** · note: all three model seats ride `claude-cli/claude-opus-5` now — engine calls are subscription-side and SLOWER; the 180s CLI timeout is the watch item on the first judged fan-out). (1) Opener: peer-mail LIGHT. (2) **WAVE 0 OF THE UI OVERHAUL — and it OPENS WITH A CLAUDE-DESIGN MOCK (founder standing rule: mock → his verdict → only then build).** Charter of record, all 7 answers: dark default (light = the toggle) · theme FROM SCRATCH in Astryx `defineTheme` · keep Geist/Geist Mono · **semantic status colours (green/amber/red; amber reserved for needs-you; thermal scale survives)** · beta accepted (pin + codemods) · videos re-conceived inside wave 2 · **small mono allowlist kept** (timestamps, ids in detail views, judge codes in tooltips). Plan of record: `docs/research/ui-overhaul-plan.md` (§waves; benchmark systems; the five diseases). Design authoring = Fable 5 only (s51 rule). (3) Lead-serial alternate while mock verdicts wait: **Phase 2c generation side** (fan-out declares `meta.targetTerms` from intel keywords + brand topics + subject entities — the lens is live and waiting) + **exemplar-pool arming** (outlier admission knobs; the pool is 5 entries incl. the founder's edited post `362d2fdb`).

▎ ▸ **Read first:** CLAUDE.md → this file → `agent_handoff/ROADMAP.md` (Phase 2b/2c) → `docs/research/ui-overhaul-plan.md` → COORDINATION.md (s70b/s70c closes) → NEEDS-STEVEN.md.
▎ ▸ **State:** main = origin @ `ec07443` · verify green through the B-int.3 merge (1801 tests) + judge suite 66/66 after the lens · lanes CLOSED + worktrees GC'd (only `dev`+`agent` windows) · vault: linkedin+facebook connected, X env-1.0a ("Connected via env" card) · pillar cut at `.context/renders/pillar-one/cuts/one-prompt-v1.mp4` = mid-quality exemplar (verdict in prov.json, never approve) · founder's LinkedIn edit = exemplar `362d2fdb` + golden pair `eval/golden/discoverability-seed.jsonl` (executable).
▎ ▸ **Standing:** stealth · hermes-relay = founder · blanket workspace grant · NEVER filter gate/suite output · ≥40cr mint ping · `npm run verify` = merge gate · Mode B lanes on fresh founder approval · **strongest-tier models while building (cheap tiers need launch-parity proof)** · **redesign waves: claude-design mock BEFORE build** · UI-semantic beats = code-draw never mint · wrap = guard+commit+push+restamp.
▎ ▸ **✅ SAFE TO CLEAR** — nothing in flight; all work committed+pushed; no founder blockers (charter complete; his verdicts all recorded).

## Pointer

CLAUDE.md → this file → `agent_handoff/ROADMAP.md` → `docs/research/ui-overhaul-plan.md` → COORDINATION.md → NEEDS-STEVEN.md.

## Delta (session 70, full arc — detail in the two COORDINATION closes + ROADMAP)

s70: B-int.2 shipped + verified live; Nango answered (swordfish: self-hosted proven, zero-SDK REST, own-instance offer for B-int.4). s70b: founder's parallel directive — pillar minted+rendered (94cr plan exact + 2 retakes; mint doctrine: UI-semantic beats code-drawn, kling fabricates UI text), two lanes launched, X-card honesty fix. s70c: founder verdicts — pillar cut = mid-quality exemplar (pillar re-scoped to post-overhaul screen recording); his LinkedIn edit ingested + made the executable golden pair; SEO/AEO/GEO gap confirmed → discoverability lens SHIPPED same session; blog-mirror doctrine recorded (social posts mirror the blog article; the blog IS the farm); landing→workspace link shipped; model seats → opus-5 (build-phase strongest-tier rule; BYO-AI parked); both lanes merged on green; wave-0 charter completed (7/7 answers) with the mock-first standing rule.

## Next action — s71: wave-0 claude-design MOCK → founder verdict → build; alternate: Phase 2c generation side + exemplar arming.
