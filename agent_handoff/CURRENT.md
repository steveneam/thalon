# CURRENT

## Stamp

2026-07-25 (session 73, syd4 — zero credit spend; STILL the 07-25 UTC ledger day, s72's "past 07-25" assumption was early) · **B-LEARN WAVE 1 COMPLETE END-TO-END IN ONE SESSION: lead froze the L0 window (`8da5df2` — contracts `admission` overrides onto `monitoredAreaConfigSchema` + `trend_admissions` durable-cap ledger, slot claims serialized on the unique (tenant,area,day,slot) index, content-hash replay idempotency, migration 0019 additive) → lane `blearn` launched on the freeze, shipped all three slices, MERGED (`965f978`, post-merge verify green 1924 tests, lane fully closed): knobs = AREA DATA (request-level `areas` map REMOVED loudly) · durable UTC-day cap (racing-sweeps pin the old code could not pass) · per-source sweep bundles + merged score-ordered trends read (the last-listed-source interim CLOSED; `/api/intel/trends` additive, NEW `sources` stamps) · per-area tuning is now durable operator data (PATCH area `config.admission`).** PLUS **EXACT-MOCK REBUILD WAVE 1a SHIPPED (`01f8179`): shell chrome re-trued to the sheets (rail/topbar/tray ported 1:1 from theme.css+Dashboard.dc.html, Astryx shell components DELETED, sheet's own SVG icons; light toggle + s66 Settings door behind byte-true resting chrome) + Dashboard rebuilt from its sheet (tiles/home-grid/published strip, honest "–" states, oldest-first rows w/ excerpt quotes + judge reasons in the err channel, 3-mark day bound) — screenshot-vs-sheet verified in Chrome dark+light at 1440×940; old shell+dashboard components deleted same change; NEW bridge-burndown ratchet seeded 938.** Budget dropped 7.3M→2M + sweeper restarted (today's ledger already spent 7,303,785 — drop costs nothing today, standing cap in place before 00:00Z). **FOUNDER RULINGS (typed live, s73): per-surface TWO-STEP (pure sheet port → verdict → wire) · keepers re-enter ONLY after structure ("adding back the thalon logo and stuff") and must never change the new structure · inventory = `docs/research/old-design-keepers.md` (tracked, with pre-demolition refs; wave-1a deletions live at `8da5df2`) · pace accepted "as long as the plan, rules and ratchet are in place" · AND at close: PARALLEL REBUILD LANES OPENED ("if that rule and logic is followed exact, then parallel workflows should be safe now") — conditions pinned in plan §5 s73-close (mechanical kickoffs · workspace.css read-only for lanes, surface-scoped css · ratchets as contamination alarms · strongest-tier pin · LEAD = screenshot-vs-sheet merge gate); amends DOCTRINE 0 (b).**

## Resume prompt (session 74, syd4 — "gogogo" boots this)

**Resume · Thalon** — s74: (0) self-check (tmux `thalon` · pg_isready · 8899+sweeper units · `dev` 3111 · git status + stamp). (1) **Morning check (parked s73, fires now):** `.context/logs/sweeper.log` first post-00:00Z pass — should read `swept — … admitted` (multi-source youtube+bluesky, budget 2M fresh ledger, AND slice-3 per-source bundles now persist). Failing? read the reason verbatim — five layers already peeled (quoting → ration → chunking → budget → merged-read); note the one-time cutover grace: admissions made pre-ledger the same UTC day don't count against that day's cap once (WRAP-blearn deploy note). (2) **THE HEADLINE — FIRST PARALLEL REBUILD WAVE (founder opened lanes s73 close; plan §5 s73-close conditions BINDING):** author kickoffs for `approve-rebuild` + `intel-rebuild` (each cites its sheet + its `old-design-keepers.md` rows + the two-step; surface-scoped css, workspace.css READ-ONLY; strongest-tier model) → **get the founder's named GO for the two launches (per-launch approval rule still holds)** → prep worktrees + launch-lane.sh → LEAD merge-gates each behind its own screenshot-vs-sheet diff + full post-merge verify. Intel step 2 consumes the new merged trends read (`sources` stamps landed s73). Lead stays free for the next window/small-surface ports while lanes run. (3) **Recorded s73, do NOT re-ask:** two-step + keepers-after-structure + parallel-lanes-opened rulings · pace accepted · blearn merged+closed · budget drop done. Founder unlock asks still open in NEEDS-STEVEN (YouTube soak key / likes-500 floor — now settable durably per area).

▎ ▸ **Read first:** CLAUDE.md → this file → `docs/research/old-design-keepers.md` → mock-sheets README → ui-overhaul-plan §5 (s73 execution rules) → COORDINATION.md → NEEDS-STEVEN.md.
▎ ▸ **State:** main = origin `965f978` · verify green ×3 today (1924 tests; suite +16 over s72) · NO open worktrees/lanes · budget 2M (active, restarted) · sweeper ticking, refusing till 00:00Z (ledger spent) · bridge-burndown pin 938 · mock canvas `f5d304cb` = 16 sheets in repo · balance 584.12.
▎ ▸ **Standing:** stealth · hermes-relay = founder · blanket workspace grant · NEVER filter live gate/suite output · ≥40cr mint ping · verify-on-merged-main = THE gate · design rebuilds: parallel lanes ALLOWED under plan §5 s73-close conditions (strongest-tier pin, per-launch GO, lead = screenshot-vs-sheet merge gate) · wrap = guard+commit+push+restamp.
▎ ▸ **✅ SAFE TO CLEAR** — nothing in flight; all committed+pushed; first sweeper-green lands on its own at 00:00Z.

## Pointer

CLAUDE.md → this file → `docs/research/old-design-keepers.md` → `docs/research/mock-sheets/README.md` → `docs/research/ui-overhaul-plan.md` §5/§4.2 → COORDINATION.md → NEEDS-STEVEN.md.

## Delta (session 73)

Boot: budget 7.3M→2M + sweeper restart (same-UTC-day catch re-checked: today's ledger already fully spent, so the drop was free; s72's morning-check parked to s74 — 00:00Z hadn't happened yet). Window: B-learn L0 frozen direct-to-main behind full verify (COPY_ORDER completeness ratchet caught the new table — working as designed; cap+1 claim-retry bound; PGlite transaction serialization verified before trusting the interleaved-claims pin). Lane: launched via launch-lane.sh on the ff'd worktree, three slices committed clean, wrap read, rebased onto the moved main, merged `--no-ff`, post-merge verify green, branch+worktree+window closed, board rows updated. Rebuild: workspace.css = the sheets' own CSS ported with every var aliased to built theme tokens (light rides light-dark() free); rail/topbar/tray/shell + Dashboard components rewritten from sheet markup; three screenshot rounds fixed real-data honesty defects (grid min-width-0, bounded rows, mark bound, en-GB date); un-rebuilt Approve verified rendering on the bridge inside the new chrome; mono pins burned for deleted files; selected-row ratchet notes the sheet's `.row.sel`; bridge-burndown ratchet authored + seeded. Founder (typed live): keepers strategy question → answered per-surface two-step over a big-bang worktree (double-verification + long-branch drift vs lanes + test big-bang); he ratified two-step + keepers-after-structure + named the logo; inventory doc written + plan §5 execution rules added.

## Next action — s74: sweeper first-green morning check · first parallel rebuild wave (approve + intel kickoffs → founder's named GO → launch + merge-gate).
