# CURRENT

## Stamp

2026-07-26 (session 79, syd4 — zero credit spend, OPUS 5). **THE VERIFY-AND-FIX PASS PART 2 OF 2 — DONE, BOTH LANES MERGED — AND THEN THE FOUNDER PASTED A YOUTUBE LINK AND FOUND TRANSCRIPTION WAS DEAD.** Final verify on merged main: **2392 passed / 9 skipped, 0 lint errors** (2293 at the s78 close). Working tree clean, everything pushed, both worktrees/branches/tmux windows GC'd.

**THE LESSON OF THE SESSION: the s78 lesson got a tool, and the tool immediately paid for itself twice — then cost me twice.** s78 ended by naming the highest-value change available: *"`fe-check` has no interaction driver — it reads code and refutes claims, so it cannot see a capability that is absent rather than wrong."* That is now `scripts/drive-surface.mjs` — `--inventory <route>` lists what a surface OFFERS, `--jobs <surface>` prints a JOBS TABLE scored **works · dead-door · no-affordance · undriven**. The third verdict is the point (pass/fail cannot say *"nothing here offers this"* — the column the calendar hid in); the fourth I added mid-session after catching my own harness printing ✓ for a job it never exercised. `fe-check`'s G1 phase now DRIVES before reading: its prompt used to end *"verify every claim against the code"*, which is how the gate whose whole purpose is finding what ISN'T there was verifying against the one thing that cannot show absence.

**IT FOUND TWO THINGS NO READING PASS COULD.** (1) **The calendar's empty planner stated the wrong fact.** `PlanPicker`'s own docstring promised the distinction — *"'nothing approved yet' and 'everything is already planned' are different facts and the operator's next move differs"* — and the code rendered ONE hardcoded line for both. On live data it was false: five drafts approved, every plannable one already holding a slot, so it told the founder to go approve something he had already approved. **No reading audit catches that, because the docstring describes the correct behaviour and the code reads as consistent with its own stated intent.** (2) **THE FOUNDER'S OWN WALK: video ingest was permanently broken.** He pasted a YouTube URL and asked whether transcription still worked. It did not — and the redesign was not the cause.

**THE INGEST ROOT CAUSE IS A SPLIT BRAIN, AND ITS SHAPE MATTERS MORE THAN ITS FIX.** `llm_cache` (Postgres) holds a POINTER; the object store holds the truth. `THALON_DATA_DIR` is **relative** (`.data`) and the local store root is `path.resolve(THALON_DATA_DIR, "objects")` — **so the root follows the process's working directory.** The dev server (cwd `apps/web`) had 71 embeddings; every test, script and eval run from the repo root had 704. One shared cache index, two different stores. The old code **threw** on a dangling pointer, and since the row survives a throw, ingest was broken **forever** for that chunk. Fixed (`93a9972`): a pointer whose target is gone is exactly what a cache MISS is — re-embed, upsert, heal, and report the divergence via `onCacheDangling` rather than swallow it. **Proven end-to-end in a real browser afterwards: 61 segments / 00:03:31, provenance link, .md/.txt/.csv/.srt export.** The relative data dir itself is NOT fixed — it is a platform change with deploy blast-radius (a container whose workdir differs from the build's gets an empty store with a full index) and is a founder call in NEEDS-STEVEN.

**MAIN WAS RED BEFORE ANY LANE LAUNCHED, and both broken guards were mine from s78.** Found by smoke-testing the lane worktrees instead of trusting them (`d5c46c4`). (a) **main-RED #5:** `ecee263` shipped the two s79 kickoffs, each ending by telling its lane to write its own wrap — which the s78 dead-link guard flagged. Committed after the last green verify, same disease as #1–#4. Exempted SAME-SLUG ONLY, with its own pin, because widening it to "kickoffs are exempt" would un-guard the cross-lane citations most likely to rot. (b) **The s78 worktree ratchet false-red inside every worktree, including the two lanes it exists to protect** — `expect(isWorktreeRoot(REPO)).toBe(false)` encoded "this suite runs in the main checkout", so both lanes would have opened on a red verify pointing at the guard built to keep them honest, and the obvious way to green it is to DELETE the line. `--git-common-dir` now resolves the real main checkout from anywhere, and in a lane the difference becomes extra coverage instead of a failure. (c) **A real flake on the merge gate:** intel's j/k test, one failure at ~291s suite time, green in isolation every time — `useListKeys` attaches its listener in a PASSIVE effect and a DOM-based find can win that race, so the press landed with no listener and was dropped. It was the suite's ONLY raw `fireEvent.keyDown(window, …)`; the nine other surfaces use `await user.keyboard(…)`, which is why only this one flaked.

**BOTH LANES: 11 findings each, 22 SURVIVED, 0 REFUTED — and the gate still paid for itself, because 12 of the 22 changed shape.** Lane 3 (`44e3e00`, dashboard · transcription · sites): 33 refuters, 31 voted real, and it **drove all three surfaces before fixing** — D2 measured at 743px down a 419px box with `scrollTop` still 0. **Its planned D3 fix was refused by its own verifiers**: feeding the pill from `rows.length` would trade one visible disagreement for two invisible ones, since the topbar chip and rail badge render the pulse's number on the same screen. It ships *"21 of 25 shown — the oldest wait in the queue →"* instead. **State the bound, don't chase the number.** Lane 4 (`5f3b1ee`, approve · create · intel · videos): ran its gate INLINE and DROVE the live app rather than fanning out reading-only refuters, correctly noting the boot approval covered the lane and not a ~33-agent fan-out inside it. **A2 was much worse than reported** — 11 of 13 waiting drafts have no decision verb anywhere (founder call, homed). **C3 was wider** — a failed profile read painted *"Denylist · empty · grounding · every gate on"* against a real profile carrying six terms, telling the operator they had no term protection.

**MY HARNESS HAD FIVE WRONG SELECTORS, THREE OF THEM FALSE PASSES, AND IT CONTRADICTED A LANE AND LOST.** Recorded in full in `docs/research/jobs-table-s79.md` because it is the failure mode that file warns about, happening to that file. The worst: it reported Approve's grounding-tier finding as WORKING while lane 4 measured it surviving — the lane was right. The job clicked the first row of any kind (a *waiting* draft, where no gate has failed and the tiers cannot be seen to disagree) and read `body.innerText` for lines starting "Grounding", picking up correctly-labelled text from elsewhere. Driven properly, the panel's own `.reason-gate` column read `Denylist / Grounding / Grounding`. **A harness that measures the wrong element does not merely miss a defect — it manufactures a contradiction with someone who measured correctly, and the more confident output wins that argument.** Two others demanded EQUALITY of counts the lanes had correctly fixed by *stating the gap*, which would have graded the right fix as broken and pushed toward the worse one. **A gate that insists on the wrong remedy is worse than a gate that misses the defect.**

**THE JOBS TABLE, BEFORE AND AFTER:** 14 works · 9 dead doors · 2 no-affordance → **28 works · 1 undriven · ZERO dead doors · ZERO no-affordance** across 8 surfaces (transcription reached 4/4 only after the founder's own ingest left a row on the shelf — the UNDRIVEN verdict was right that it was untestable, not missing). The calendar's four jobs pin the s78 fixes and all pass. Render measured too: no clipped or unreachable content and no horizontal scroll on any of 8 routes, both themes; 14 screenshots shot and read.

**A NUL BYTE MADE A 12KB TEST FILE BINARY TO GIT, AND IT MERGED THAT WAY.** Lane 3 caught it in its own commit after I had already merged (`git show --stat` on your own work, rather than assuming a `.tsx` file is text): `new File([" "], "clip.mp4")` carried `0x00` where the space should have been, so git filed the whole file as `Bin 0 -> 12667 bytes` — no reviewable diff, then or ever. Cherry-picked (`872c957`) after verifying blob identity rather than force-deleting the branch. **Second occurrence on record (s72 had one), so it is now a class with a guard:** `tests/no-nul-in-source.test.ts`, revert-checked, and it names the offending byte and its context. Nothing else would have caught it — not lint, not typecheck, not vitest, and not a human reading a diff that did not exist.

**AND I BROKE MY OWN RULE, TWICE, BOTH CAUGHT.** I chained a commit behind `vitest … | grep` — the shell saw *grep's* status (it found matches), not the suite's, and a red main reached origin. The standing rule was spelled *"never pipe the suite through `tail`"*, which is exactly why reaching for `grep` felt safe; it now reads **gate on the exit code, never pipe it into anything**. Then the final merged-main verify caught 4 lint **errors** — browser globals in the new drop-behaviour test — because I had linted `scripts/` *before* adding it and not after. Both are the gate working.

## Resume prompt (session 80, syd4 — "gogogo" boots this)

**Resume · Thalon** — s80 = **the 139 mediums+lows, re-read against the FIXED code**, or the **video editor full build-out** if the founder sequences that first (he has already ruled its scope).

**Do these in this order:**

0. **Self-check** — tmux `thalon` · `pg_isready` · both user units · dev 3111 ·
   `git status` + this stamp.
1. **Restate the three open founder calls** (all in NEEDS-STEVEN, none blocking):
   stage-artifact counting · the `THALON_DATA_DIR` anchor · transcription's
   free-and-deterministic default.
2. **Re-read the 139 mediums+lows against the fixed code, not the s77 text.**
   22 blocker+highs have landed since they were written; a medium whose
   surface was rebuilt twice may be stale, already closed, or now wrong.
   `docs/research/workspace-audit-findings-s77.md` is still the work list.
3. **DRIVE, don't read.** `node scripts/drive-surface.mjs --jobs all` is the
   baseline; extend the job set as you go. The harness is the cheapest way to
   tell a live finding from a stale one, and `--inventory` is how the
   no-affordance column gets earned. **Its own failure mode is documented in
   `docs/research/jobs-table-s79.md` — read that before trusting a verdict.**

**Explicitly NOT this session:** any publish path (the sequence gate below).

▎ ▸ **Read first:** CLAUDE.md → this file → `docs/research/jobs-table-s79.md` (the harness AND its five self-inflicted bugs) → `docs/research/workspace-audit-findings-s77.md` → `.claude/skills/thalon-check/SKILL.md` → `agent_handoff/lanes/WRAP-s79-lane3.md` + `WRAP-s79-lane4.md` → COORDINATION.md → NEEDS-STEVEN.md.
▎ ▸ **THE VIDEO EDITOR IS SCOPED AND WAITING: a FULL BUILD-OUT session, his ruling s79.** `docs/research/video-editor-audit-s78.md` — 36 findings, and the real number is **15 of 27 operator jobs with NO affordance at all** (no undo · no unsaved-work guard on any exit · no add/remove of a beat or caption · no music swap · no preview of the working copy while dirty). Its one blocker (keyboard-dead timeline blocks) rides that session, not s80's list. **It is the surface the jobs harness pays off on most** — the whole 27-job table is exactly what it needs, and that table now RUNS.
▎ ▸ **The two founder rulings that close old asks:** the **YouTube ration is CLOSED and the arithmetic that kept it open was WRONG** — the live cadence is 180 min (8 sweeps/day ≈ 9,600 units against 10,000), so it already fits; the "~96 passes/day" figure was the scheduler TICK, and acting on the ruling literally would have caused the wall it was meant to prevent. **Transcription is NOT a Thalon feature** — his own knowledge-ingestion tool, free and deterministic by default with an AI button beside Ingest on demand, no second artifact (my two-artifact invariant is withdrawn; it was premised on grounding, which he has ruled out).
▎ ▸ **State:** main = origin, all pushed · verify **2392 passed / 9 skipped, 0 lint errors** · budget 2M · balance 584.12 · **zero spend s79** · both s79 worktrees/branches/tmux windows GC'd · the dev transcript shim is a hand-started `.context/tools/transcript-shim.py` on 127.0.0.1:8787 and is **NOT** a unit, so it dies with the session.
▎ ▸ ⛔ **THE SEQUENCE GATE, unchanged:** *"we're not posting anything yet until all the walks are verified and fixed."* s79 exercised no publish path and armed nothing; the Intel→Create arming shipped DISARMED. Posting still needs a fresh per-platform GO.
▎ ▸ **Standing:** stealth · hermes-relay = founder · blanket workspace grant · **every lane/subagent launch needs fresh founder approval** · **GATE ON THE SUITE'S EXIT CODE — never pipe it into anything** (`| tail` hid a failure for three sessions; `| grep` pushed a red main at s79, because the rule was spelled for `tail`) · **vitest does NOT typecheck, and does not lint either** (four catches on record, two this session) · verify-on-merged-main = THE gate, and **measure the rendered surface too** · **DRIVE the surface before calling it fixed** · a LANE CANNOT SCREENSHOT **OR DRIVE** ITS OWN WORK (both enforced) · **never run a full verify while lane fan-outs are live** · REDESIGN ERA MODEL = OPUS 5 · wrap = verify+commit+push+restamp.
▎ ▸ **✅ SAFE TO CLEAR** — nothing in flight; tree clean and in sync with origin; both lanes merged and GC'd; tmux back to `dev` + `agent`.

## Pointer

CLAUDE.md → this file → `docs/research/jobs-table-s79.md` → `docs/research/workspace-audit-findings-s77.md` → `docs/research/video-editor-audit-s78.md` → `.claude/skills/thalon-check/SKILL.md` → COORDINATION.md → NEEDS-STEVEN.md.

## Delta (session 78)

s78 was part 1 of the same pass: the keyed-by-entity sweep shipped first, lead-direct (7 instances, one spelling, killing an Approve data-corruption blocker a session before its own lane), then lanes 1+2 verified 23 findings adversarially and fixed the 20 survivors. The founder then USED the calendar and found a whole missing capability — nothing in the product could create a plan — which s78 built and s79 turned into executable jobs. The 15th surface (the video editor) was walked and produced the JOBS table shape this session generalised into a tool.

## Next action — s80 (BOOT ON OPUS 5): self-check · restate the three open founder calls · then either the video-editor FULL BUILD-OUT (scoped and approved, his sequencing call) or the 139 mediums+lows re-read against the fixed code — driving each surface, never reading it.
