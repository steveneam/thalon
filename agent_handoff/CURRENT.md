## Stamp

2026-07-19 (session 64 close, syd4 — the founder-live marathon: ⑲ fix round → doctrine → Sprint 8 opened AND its three buckets MERGED) · **SPRINT 8 "ARM IT LIVE" FIRST WAVE COMPLETE IN ONE SESSION:** contract window 0016 frozen (PR #60: entitlements seam + social ledger + sweep schedules) → three lanes built + merged (#61 B-pub.1 publisher seam · #62 B-arm.1 sweep scheduler · #63 B-vid.7 one-prompt auto-run), every merge on a full local root-suite gate (Actions still billing-dead) · db-dump door retired · ⑲ Tsukimi fix round shipped earlier same session (reference-guided minting = standing doctrine) · Mode B ratcheted as the lane-launch default (this launch ran Mode A on the founder's one-time OK) · balance 706.12.

## Resume prompt (session 65, syd4 — "gogogo" boots this)

**Resume · Thalon** — s65: (0) self-check (tmux · pg_isready · 8899 relaunch if down · git status + stamp · balance ≈706.12). (1) Opener: peer-mail = LIGHT (swordfish's major work all closed — cutover/step-8/resize done; only two dormant non-blocking tails remain: film-import queued THEIR side · basicauth/DB_DUMP_TOKEN rotation = founder-gated console pass, code side already done; plus a read FYI on live-comms tooling, no action). Founder calls: which track to sequence · gateway top-up · credit call · live-send GO. **(⑲ Tsukimi APPROVED s64 — closed. Intel ARMED + flowing. Social creds for X/LinkedIn/FB/IG COLLECTED + validated.)** (2) Main line: **Sprint-8 second wave** — the next contract window (frozen gaps: brand_profiles social block · publishable capability · SOCIAL_* env keys · sweep.schedule_failed event · sweepSchedules listAll · entitlement feature keys) THEN B-pub.2 publisher drivers wired to the collected creds (LinkedIn→X→Meta, first post per-platform GO) · B-arm.2 intel soak · B-vid.8 pillar #1 (needs gateway top-up). OR Track B: the Integrations charter. Every lane = Mode B via scripts/launch-lane.sh. (3) Parked: visual arc HOLDS at 20 sites (A+ family · B-sitegen · THE LANDING await sequencing); Integrations/entitlements UI = charter candidate. **Full map: `docs/research/social-transition-plan.md`.**

▎ ▸ **Read first:** CLAUDE.md → this file → COORDINATION.md (s64 records incl. lane close) → NEEDS-STEVEN.md.
▎ ▸ **State:** main = origin @ s64 close (no PRs, no worktrees, no lanes) · migrations through **0016**, contract frozen · engine: publisher seam (no drivers, structurally unarmed) + sweep scheduler (schedules default disabled) + one-prompt video door (zero-spend, planned takes) all LIVE on main · portfolio 20 sites (⑲ fix round APPROVED s64) · box 16 GiB/6 vCPU · balance 706.12.
▎ ▸ **Standing:** stealth · hermes-relay = founder · blanket workspace grant · get_cost preflight · reference-guided mints (ref must BE the target) · pre-plan mandatory · Fable-direct design · Mode B lanes · wrap = guard+commit+push+local verify.
▎ ▸ **✅ SAFE TO CLEAR** — no PRs, worktrees, lanes, or vendor jobs in flight; guard green at HEAD.

## Pointer

CLAUDE.md → this file → COORDINATION.md (s64 lane-close record) → NEEDS-STEVEN.md → docs/research/arming-plan-sprint8.md.

## Delta (session 64 — one founder-live marathon)

Three arcs in one session: (1) the ⑲ Tsukimi fix round off the founder's typed verdict, which made REFERENCE-GUIDED MINTING standing doctrine (ref must BE the target; enhancer caption beats prompt). (2) Doctrine rounds: price-register corollary (fine dining = stillness), tier-gating clarified into the ENTITLEMENTS SEAM, Mode B ratcheted as the lane default after the founder flagged the Mode-A launch. (3) Sprint 8 opened on the founder's GO and its FIRST WAVE fully merged: window 0016 frozen, then three lanes built in parallel worktrees and merged one by one on full local root-suite gates (#61 #62 #63); vid7 stopped pre-commit and the lead finished it (no agent resume). Also: db-dump door retired; swordfish cutover choreography closed; syd4 resized 16 GiB/6 vCPU.

## Next action — the s65 PLAN (founder asked for it; lanes launch only on his opener approval)

▶▶ **THE CONSOLIDATED MAP: `docs/research/social-transition-plan.md`** (dogfood → Thalon launch → client product; one capability, two scopes; Thalon = tenant zero). Read it first — it links the arming plan, the Integrations plan, and this file into one arc, and its final section IS the s65 start.

**Opener:** self-check (tmux · pg · 8899 · balance ≈706.12) · peer-mail (swordfish tails: film-import their queue · founder-gated basicauth+DB_DUMP_TOKEN console pass) · founder calls: which track first · gateway top-up. (⑲ Tsukimi APPROVED s64 — closed, no re-glance.)

**Big state change this session: the publish loop can now be DOGFOODED end-to-end.** Intel is armed and flowing (bluesky, frontier-AI watchlist, 4-hourly). Social creds for **X · LinkedIn · Facebook · Instagram** are collected + read-only-validated in `.context/social-logins.md` (NOT wired, NOT armed). So s65 has two tracks the founder sequences:

**Track A — CLOSE THE PUBLISH LOOP (creds ready; highest proof-of-product value):**
1. **Next contract window FIRST** (lead, serial) — the frozen gaps: `brand_profiles` social config block · `publishable` draft-format capability · `SOCIAL_*`/platform env keys in platform env schema · `sweep.schedule_failed` event door · `sweepSchedules.listAll` · entitlement feature keys as needed.
2. **B-pub.2 publisher DRIVERS** wired to the validated creds — LinkedIn member-post first, then X, then Meta (FB page + IG). Per-platform arming; **first dogfood post on each stays behind a per-platform founder GO** (a single reviewed test, then delete).
3. **B-arm.2 intel soak** — already armed + flowing; run it, eval rows from dismiss/promote.
4. **B-vid.8 pillar #1 dogfood** — real render spend, needs the gateway top-up.

**Track B — INTEGRATIONS SURFACE CHARTER (founder's explicit "do it next session"):** charter from `docs/research/integrations-surface-plan.md` — Settings→Integrations so tenants connect their own accounts; the **per-tenant encrypted credential vault is the central design decision** (we have none; arming is env-based self-only); OAuth-Connect (Thalon-owns-the-partner-app) vs guided-manual modes; ties the entitlements seam + the social config block. The s64 dev-app slog IS the dogfood proof this feature matters.

**Every lane = Mode B via `scripts/launch-lane.sh`, named in the approval ask.**

**Founder-side between sessions (all in `.context/`, none blocking):** rotate the account passwords in `social-logins.md` · Threads + TikTok apps whenever (exact click-paths saved in `.context/developer-apps.md`; slots in the sheet) · gateway top-up call. **NB on reviews (correcting an s64 overstatement): the DOGFOOD needs NO platform reviews** — LinkedIn app is DONE + validated for personal-profile posting (`w_member_social`; the Community Management review is OPTIONAL, only for company-page posting), Meta posts to own accounts in dev mode, so no filing is urgent. The only review that gates real *public* posting is TikTok's — and it can't be filed until hosted Terms/Privacy pages + a demo video exist (both downstream). So: nothing review-related for the founder to chase now.
