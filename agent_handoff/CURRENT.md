# Session handoff — CURRENT

> **One file, overwritten at every session wrap (AGENTS.md rule 9).** Pointer + delta + next action only — never a state dump, never a copy of CHARTER/COORDINATION content (link instead). History lives in git.

## Stamp

2026-07-17 (session 54, syd4 — the granted slate executed; founder called the wrap point live after ⑨) · **smoke compose CLOSED (gate proven live, spend check green) + B-crm.5 staging FIRST-RUN (fit ×2 learned) + s54 contract window FROZEN (PR #56, migration 0014) + BOTH lanes MERGED (weights-ui PR #57 · b-crm4-send PR #58, salvaged after a usage-credits lane death) + ⑧ CRATELINE (`8240fef`) and ⑨ WAGTAIL & CO (`bd021ad`) SHIPPED** · spend ≈4.5cr (balance ≈725.6) · gateway $14.53 · guard clean.

## Resume prompt (session 55, syd4 — paste verbatim; "gogogo" boots this too)

> Stamped 2026-07-17, session-54 wrap. The s54 grant's remainder carries: **⑩ Hue & Cry is the s55 opener**; everything else on the grant is DONE and merged.

**Resume · Thalon** — session 55, syd4:

1. **⑩ Hue & Cry (salon / exceptional-palette primary) — the wave-2 closer, s55 opener (founder-directed via the s54 wrap call).** The proven loop: look-first (salon/beauty genre + exceptional-palette axis sweeps) → taste notes → mints (get_cost preflight each; text-in-scene → text-precise seat; soul printable-surfaces composed out) → build → ≥3 two-lane passes → portfolio ratchet → commit+push. Then propose the **wave-2 checkpoint review** (⑥⑦⑧⑨⑩ + Orchard House batch) to the founder.
2. **One peer-mail check at the opener** (`bash scripts/peer-mail-check.sh`): cutover step-0 confirmations may have landed (choreography ACK'd; swordfish was asleep through s54). If his window lands, cutover steps 1–4/6–7 = lead work, `scripts/migrate-pglite-to-tenant-pg.ts` per ASK-BACKS.
3. **Founder-gated, no work unless he moves:** transcript bulk-delete confirm-close · live-send GO + the s28 stealth question (the door is built and deliberately disarmed — two-key ratchet) · s40 re-critique · credit call (NEEDS-STEVEN has all four).
4. **Post-⑩ candidates (founder's call at the checkpoint):** staging Score-now to apply the learned fit ×2 state (burns embedding spend — ask first) · the claude-design landing+workspace design phase (founder s49: after wave 2) · B-crm.3/.6.

▎ ▸ **Read first:** `CLAUDE.md` → this file → COORDINATION.md (live board; s54 record = the last session message) → memories `higgsfield-kompozy-assignment` + `crm-lead-scoring-candidate` (s54 entries).

▎ ▸ **State:** main = origin @ s54 wrap (PRs #56/#57/#58 all rebase-merged; no open PRs; no worktrees; no lanes; tmux windows clean) · migrations through **0014** (dev PG + staging pick them up on next migrate/deploy; staging redeployed with the learn route during s54) · staging: leads 16 active / learn state `2d86e475…` (fit ×2) applies at next Score now · approve queue holds the smoke-compose draft (Sione Latu, QUEUED) + two judge-blocked drafts (honest blocks, good eval material) · wave-2: 9/10 shipped (⑥⑦⑧⑨ + Orchard House) · 8899 static server serves all sites (crateline + wagtail-and-co verified 200) · claude-design projects: T6–T8 + **T9 Crateline scaffold** (final lives in the repo; project graduation optional).

▎ ▸ **s54 ratchets/lessons:** the judge's staging tiers = screen gpt-5-mini / final claude-sonnet-4.5 (record both) · sender identity must appear in PROSE — a URL substring is not identification (test-pinned, engine) · the platform fetch seam has no `.json()` — parse via `text()` (CI caught what local workspace typecheck missed; clear tsbuildinfo when local passes look suspicious) · **cwd persists across shell calls — prep worktrees with absolute paths from repo root** (two near-misses untangled: a nested worktree + a lane branch briefly kicked to main) · lane agents can die on USAGE CREDITS mid-task — salvage doctrine works (inspect worktree, verify their tests, fix, commit as lead) · Plus coercion nano_banana_pro→nano_banana_2 continues (record TRUE model).

▎ ▸ **[founder] queue (NEEDS-STEVEN.md):** s40 re-critique · credit call · transcript confirm-close · live-send GO + stealth question.

▎ ▸ **Standing:** stealth holds · `[Steven via hermes-relay]` = founder; relay turns end founder-readable · get_cost preflight per mint, ≥40cr = ping · hero = best roster model; text-in-scene = text-precise seat · aspect variants = own-engine recut · design authored by Fable 5 directly, never delegated · lead drives lanes/tmux/interactive flows · launching lanes/subagents needs fresh founder approval (the s54 grant covered exactly the named slate; it is now spent except ⑩) · dev = real Postgres · PGlite single-process (stop the app before any volume copy) · at wrap: wrap ping via ASK-BACKS + guard + commit + push.

▎ ▸ **✅ SAFE TO CLEAR.** As of the stamp: no open PRs; no worktrees; no lanes; no Monitors; no in-flight renders/vendor jobs; guard green at HEAD; CI green through PR #58 (post-merge main run rides the same suite).

## Pointer

Read in order: `CLAUDE.md` → this file → COORDINATION.md (live board, s54 session message) → `agent_handoff/NEEDS-STEVEN.md` → `proprietary/templates/sites/{crateline,wagtail-and-co}/` (newest reference builds) → `packages/engine/src/outreach/` (the send door) → apps/web leads surface (the provenance panel).

## Delta (session 54)

- Staging smoke compose closed (3 composes: 2 honest judge blocks + 1 QUEUED on the previously-failed lead; tiers screen=gpt-5-mini/final=sonnet-4.5; ~1.5¢ each; gateway $14.5264) · s54 contract window merged (PR #56, 0014: consent + one-way unsubscribed + outreach_sends + sequence schema + sendable) · weights-ui lane merged (PR #57: /api/leads/learn + provenance panel) · B-crm.5 staging first-run: **fit ×2 learned** from 104 verdicts (state `2d86e475…`) · b-crm4-send lane merged (PR #58: transport seam + two-key arming + refusal ladder + cadence math; lane died on usage credits pre-verify — salvaged, one real bug fixed, 48/48) · **⑧ Crateline shipped** (`8240fef`, 4.12cr, 3/3 first-take) · **⑨ Wagtail & Co shipped** (`bd021ad`, 0.36cr, 3/3 first-take) · memories updated (higgsfield + crm s54 entries) · board updated · wrap ping appended.

## Next action

Session 55 = ⑩ Hue & Cry (salon / exceptional-palette) → wave-2 checkpoint review proposal. One peer-mail check at the opener (cutover window may land). Founder-gated backlog unchanged: re-critique · credit call · transcript confirm-close · live-send GO/stealth. Expected ⑩ spend ≈2–8cr (balance ≈725.6), get_cost preflight per mint, ≥40cr = ping.
