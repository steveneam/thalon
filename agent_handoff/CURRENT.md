# CURRENT

## Stamp

2026-07-25 (session 74, syd4 — zero credit spend; still the 07-25 UTC ledger day, s73 wrapped ~17:00Z and s74 ran straight on from it) · **THE FIRST PARALLEL REBUILD WAVE LANDED — FOUR SURFACES IN ONE SESSION.** Three lanes launched at the boot on the founder's Opus-5 pin (`861ae2f`, worktrees ff'd first) and all three merged behind the lead's own screenshot-vs-sheet diff + a full verify on merged main: **approve-rebuild** (`146f4d0` — a/r/e keys + bulk bar + toast + per-gate judge provenance behind byte-true chrome; TWO judge-semantics corrections: cadence BLOCKS so it wears the blocking ✗, and the SEO gate is `seo_aeo` not `seo_lens`) · **intel-rebuild** (`bc9d4b4` — capture doors + demo/cadence honesty folded into the sheet's own stamp band + the merged multi-source per-source stamps, B-learn slice 3 consumed; it MEASURED the port rect-by-rect and fixed two drifts rather than shipping them; bridge 938→843) · **runslib-rebuild** (`34f2adf` — Runs + Library, both two-step; Runs' Retry rendered at full fidelity but RESTING UNARMED with its reason in the title because no replay route exists). **The lead rebuilt Create itself** two-step (`7ddb43f` port → `a22fdf7` wired) and then, on the founder's live direction, **redesigned Intel's Search tab** (`c5067da`) — the one surface with no sheet — and **re-cut its horizon cards to lead with end-point significance** (`3e8c0ae`). Merged-main verify GREEN: exit 0, **1951 passed / 9 skipped**, 0 lint errors. Pushed `04e6e41` + the follow-ups. **THE WAVE'S REAL PRIZE IS A NEW EXECUTABLE RATCHET:** the intel lane raised that per-surface stylesheets are globally scoped, a sweep of all 16 sheets proved they REUSE class names with different values (`.prompt-box` Create≠Sites, `.split` Approve≠Leads, `.reason` Intel≠Leads, `.today` Calendar≠Dashboard, `.on` across four), and it is now README rule 6 + `surface-css-scope.test.ts` — a defect class that would have shown up as "some other surface looks subtly wrong" is now a red suite. **FOUNDER RULINGS (typed live, s74):** no primary discoverability chip = OK · no run thumbnails = OK · per-field context pruning RESTORED as a behaviour behind the sheet's pick chip (his deferral to the lead's taste), which generalises the re-entry rule — **a keeper comes back as a state behind the sheet's chrome, never as extra chrome** · Search needed a consistent redesign · horizon metrics are too technical for the general user.

## Resume prompt (session 75, syd4 — "gogogo" boots this)

**Resume · Thalon** — s75: (0) self-check (tmux `thalon` · pg_isready · 8899+sweeper units · `dev` 3111 · git status + stamp). (1) **Morning check (parked TWICE now, s73→s74→s75; it finally fires):** `.context/logs/sweeper.log` first post-00:00Z pass — should read `swept — … admitted` (multi-source youtube+bluesky, fresh 2M ledger, per-source bundles persisting). Failing? read the reason verbatim — five layers already peeled; note the one-time cutover grace in WRAP-blearn. (2) **THE HEADLINE — LAUNCH WAVE 2, three lanes, no re-ask** (founder s74: "rinse and repeat … for the next session parallel workflow redesign"): `planner-rebuild` (Calendar → Board) · `crm-rebuild` (Leads → Profiles) · `estate-rebuild` (Sites → Settings/Integrations). Kickoffs are written and carry this session's three hard-won lessons (surface-CSS scoping as a merge-gate check · never pipe verify through `tail` · sequence your verify against the other lanes — four concurrent suites on 6 vCPU drove load to 34 and turned a 4-minute suite into 14). Launch each: `LANE_CLAUDE_ARGS="--model claude-opus-5" scripts/launch-lane.sh <name> .claude/worktrees/<name> agent_handoff/KICKOFF-<name>.md` — ff each worktree to origin/main first if main moved. **That wave finishes every sheet except VIDEOS, which stays LEAD-DIRECT** (three sheets, and the founder called it a re-conception, not a port). (3) **Lead's own queue while the lanes run:** the per-field pruning panel behind Create's pick chip (founder-ruled, unbuilt) · then Videos. (4) **Do NOT re-ask:** the five s74 rulings above are recorded.

▎ ▸ **Read first:** CLAUDE.md → this file → `docs/research/old-design-keepers.md` → mock-sheets README (**rule 6 is new**) → ui-overhaul-plan §5 → COORDINATION.md → NEEDS-STEVEN.md.
▎ ▸ **State:** main = origin, all pushed · merged-main verify green (1951 tests) · **8 of 16 sheets now rebuilt** (shell+Dashboard s73; Approve · Intel · Runs · Library · Create s74, plus Search designed) · bridge burn-down 938 → **~700s and falling**, Intel now carries ZERO bridged tokens but heat-grade · three lane worktrees CLOSED and branches deleted · wave-2 worktrees prepped · budget 2M · balance 584.12.
▎ ▸ **Standing:** stealth · hermes-relay = founder · blanket workspace grant · NEVER filter live gate/suite output (and never pipe it through `tail`) · ≥40cr mint ping · verify-on-merged-main = THE gate · parallel rebuild lanes ALLOWED under plan §5 s73-close + the s74 additions · **REDESIGN ERA MODEL = OPUS 5 (lanes + lead) until structural completion** · wrap = guard+commit+push+restamp.
▎ ▸ **✅ SAFE TO CLEAR** — nothing in flight; all committed+pushed; every lane closed.

## Pointer

CLAUDE.md → this file → `docs/research/old-design-keepers.md` → `docs/research/mock-sheets/README.md` (rule 6) → `docs/research/ui-overhaul-plan.md` §5 → COORDINATION.md → NEEDS-STEVEN.md.

## Delta (session 74)

Boot: three prepped worktrees ff'd to `861ae2f` and launched on the Opus-5 pin inside ~4 minutes; all three took their kickoffs and ran ~45 minutes each. Lead found and fixed a contract defect immediately: the sheets README still said "surface rebuilds are not delegated to lanes" while three lanes were reading it as their contract — amended to the s73-close ruling (a lane may port; the lead is the GATE). Lead then rebuilt **Create** two-step against `Create.dc.html`, screenshot-diffed dark+light at 1440×940, and shipped three honest deviations rather than fabricating fixture values (no primary discoverability chip — the subject entity is derived at generation; no run thumbnails — `FeedRun` has no media ref to join; the Video row states what is true instead of "~40s · 8 beats"). All three founder-ruled OK or delegated. **The CSS-scoping find**: the intel lane raised it as a question, a 16-sheet sweep confirmed it as a live defect (Create's `.prompt-box` would have been silently restyled by Sites), and it became README rule 6, an executable ratchet, a fix to Create/runs/library, and a merge-gate line in all three wave-2 kickoffs. Merge gates: each lane rebased-free (merged `--no-ff`), pin-file conflicts resolved by the lead, runs.css/library.css scoped at the gate, every surface rendered on dev 3111 and diffed against its sheet before the merge stood. Founder mid-session: Search redesigned (no sheet exists — designed in the sheets' language, `horizon-card.tsx` + `demo-banner.tsx` folded in and deleted), then re-cut so the horizon card leads with a verdict and hides position/CTR/snapshots + the ranker's verbatim strings behind one disclosure. Post-wave cleanup: `platformLabel` had grown THREE copies across the lanes — consolidated to `lib/workspace/format.ts`.

## Next action — s75 (BOOT ON OPUS 5): sweeper morning check (parked twice) · LAUNCH wave 2 (planner · crm · estate; no re-ask) · lead merge-gates each on screenshot-vs-sheet + full verify · lead's own queue = Create's pruning panel, then VIDEOS (lead-direct, the re-conception).
