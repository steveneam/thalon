# CURRENT

## Stamp

2026-07-25 (session 75, syd4 — zero credit spend; still the 07-25 UTC ledger day, s74 ran into the evening and s75 straight on from it) · **WAVE 2 LANDED — THE WORKSPACE IS NOW 14 OF 16 SHEETS.** Three lanes launched at the boot on the founder's standing s74 approval ("rinse and repeat … for the next session parallel workflow redesign"), worktrees ff'd first, and all three merged behind the lead's own screenshot-vs-sheet gate + a full verify on merged main: **planner-rebuild** (Calendar + Board, `938db1b`) · **crm-rebuild** (Leads + Profiles, `4f90e6a`) · **estate-rebuild** (Sites + Settings/Integrations, `656e61c`). Merged-main verify GREEN: exit 0, **2027 passed / 9 skipped**, 0 lint errors. Pushed `1792f46`. **Only VIDEOS is left** (three sheets — the founder called it a re-conception, not a port, and it stays LEAD-DIRECT). **The lead's own queue ran beside the lanes and three of four items shipped:** **B-media.0** planned research-first with a proposal sheet and NO code (`8bbe352`), **B-media.1** Library→Transcription shipped (`1ba43bd`), **Create's per-field pruning panel** built (`11877aa`). **THE CORRECTION THAT MATTERS:** thumbnails were recorded at s74 as missing everywhere, but reading the code showed **four surfaces already resolve real media end-to-end** — Transcription (keyless YouTube oEmbed captured at ingest, B6.6 rider), Intel (driver-captured), Sites (portfolio previews), and the striped fallback the sheets specify is already shipping. B-media.0 is therefore GENERALISING something that works, and the resolved `<img>` is already copy-pasted twice, so a shared `<SourceThumb>` is owed regardless. **The one cheap find:** `video-title.ts` parses the oEmbed reply for `thumbnail_url` and **discards `thumbnail_width`/`height` from that same response** — no extra call, no quota, and it is the only thing standing between us and the portrait-vs-crop decision.

## Resume prompt (session 76, syd4 — "gogogo" boots this)

**Resume · Thalon** — s76: (0) self-check (tmux `thalon` · pg_isready · 8899+sweeper units · `dev` 3111 · git status + stamp). (1) **Morning check — it can finally fire** (parked s73→s74→s75 because every one of those sessions ran inside the SAME 07-25 UTC day): read `.context/logs/sweeper.log` for the first post-00:00Z pass. **s75 diagnosed why it will still fail without a decision:** `YOUTUBE_MAX_SEARCHES_PER_SWEEP` was raised 1→12 to unblock the multi-source soak, which lets ~9 `search.list` calls through per sweep; the engine's own guard prices each at ~100 units, YouTube's standard allowance is 10k units/day, and the scheduler ticks every 15m — ~900 × ~96 ≈ 86k against 10k. It re-exhausts daily until the ration changes. The founder's coverage-vs-cadence call is in NEEDS-STEVEN (slow the tick / cut the ration / request a quota raise); if he has answered, apply it, and note the tenant token budget also sat at 2M against 7.3M spent. (2) **THE HEADLINE — VIDEOS, LEAD-DIRECT** (`Videos.dc.html` · `Videos Overview.dc.html` · `Video Dossier.dc.html`): the last three sheets and the one the founder called a **re-conception, not a port**, so it does NOT go to a lane. Read the three sheets + `old-design-keepers.md` first; the editor/EDL keepers are the densest re-entry set in the repo. (3) **Then the lead's remaining queue:** (a) act on the founder's B-media.0 verdict when it lands — the plan, the join map and the five open calls are `docs/research/source-media-plan.md` + `mock-sheets/Source Media.dc.html`; the contract-window candidates are `meta.posterRef` and oEmbed `thumbnail_width/height` → a derived `orientation`; (b) his call on the orphaned `components/board/leads-board.tsx`. (4) **Do NOT re-ask:** the s74 rulings stand; wave 2 is merged and its lanes are closed.

▎ ▸ **Read first:** CLAUDE.md → this file → `docs/research/mock-sheets/README.md` (**two new sections: "Founder amendments" and "Proposals — NOT yet verdicted"**) → `docs/research/source-media-plan.md` → `old-design-keepers.md` → ui-overhaul-plan §5 → NEEDS-STEVEN.md.
▎ ▸ **State:** main = origin `1792f46`, all pushed · merged-main verify green (**2027 tests**) · **14 of 16 sheets rebuilt** (shell+Dashboard s73; Approve · Intel · Runs · Transcription · Create s74 + Search designed; Calendar · Board · Leads · Profiles · Sites · Integrations s75) · all three lane worktrees removed, branches deleted, tmux windows killed · budget 2M · balance 584.12 · **zero spend s75**.
▎ ▸ **Standing:** stealth · hermes-relay = founder · blanket workspace grant · NEVER filter live gate/suite output (and never pipe it through `tail` — write it to a file instead, as s75 did) · ≥40cr mint ping · verify-on-merged-main = THE gate · **REDESIGN ERA MODEL = OPUS 5** · wrap = guard+commit+push+restamp.
▎ ▸ **✅ SAFE TO CLEAR** — nothing in flight; all committed+pushed; every lane closed.

## Pointer

CLAUDE.md → this file → `docs/research/mock-sheets/README.md` → `docs/research/source-media-plan.md` → `docs/research/old-design-keepers.md` → `docs/research/ui-overhaul-plan.md` §5 → COORDINATION.md → NEEDS-STEVEN.md.

## Delta (session 75)

Boot: three prepped worktrees ff'd to `1046534` and launched on the Opus-5 pin inside ~3 minutes; all three ran ~50 minutes and wrapped themselves. **Merge gate:** planner and crm merged clean; estate hit three conflicts, all resolved deliberately — the instructive one was `selected-row.test.ts`, where each lane had removed **its own** entry from the legacy-selection pin, so the correct merge removes **both** (taking either side alone would have silently re-pinned a deleted file). Two more conflicts were the lead's own B-media.1 prose landing in files estate rebuilt or deleted. Then all six surfaces were rendered on dev 3111 and diffed against their sheets before the merge stood.

**What the gate surfaced, all three homed in NEEDS-STEVEN rather than decided unilaterally:** (a) `components/board/leads-board.tsx` is **orphaned** — `Board.dc.html` is the CONTENT pipeline board drawn under Home, not a leads board, so rebuilt Leads renders its `Board` tab and says so plainly. Both lanes found it independently and each correctly declined to touch the other's file, escalating it. **The lead did not delete it**: a working capability with no home in the mock is the founder's call, and the ratchet pins keep it visible instead of letting it rot. (b) Calendar clips concurrent same-instant events to ~45px chips — the sheet's own `overflow:hidden` meeting a density its fixture never had; honest rather than hiding events, but a "+N more" fix would be a sheet change. (c) Sites turned out to be a fourth surface already resolving real media.

**Lead's own work, beside the lanes.** B-media.0 was planned research-first per the founder's "see how others do it": the findings that actually bore on a decision were that `.thumb-sm` (64×40) is **exactly** Mux's 256×160 storyboard-tile ratio, that a 16:9 poster cover-cropped into it loses only ~10% of its width (inside the central-80% safe area creators already design for) while a **9:16 Short loses ~65% of its height and gets decapitated**, and that **hover-scrub is out on a structural ground, not taste** — the storyboard artifact requires owning the transcode, and our densest surface holds a YouTube URL and one poster. The proposal sheet is filed under a NEW README section "Proposals — NOT yet verdicted (do not port)" so no future lane ports it before the founder rules. B-media.1 renamed the surface only — `lib/library/`, `/api/library` and `LibrarySourceRow` keep their names because they serve the generic `sources` shelf — with `/app/library` kept permanently as a redirect, and the divergence recorded under another new README section, "Founder amendments — the sheets are WRONG here on purpose". Create's pruning panel restores the keeper as a state behind the sheet's own chip: the chip text becomes the disclosure, pruning reaches **generation** (not just display) through one `pruneContext()` that every downstream consumer already reads, and dropping a field is reversible while dropping the whole chip is not — so the chip renders off the UNPRUNED context and cannot vanish mid-prune.

**Discipline note carried forward:** the lead ran its own full verify while three lane suites were live and drove load to 13. It finished clean, but the s74 sequencing lesson applies to the lead too, not just the lanes — the crm lane, which had that lesson in its kickoff, explicitly waited out its siblings before its full suite and was right to.

## Next action — s76 (BOOT ON OPUS 5): sweeper morning check (now genuinely a new UTC day — apply the founder's quota call if he has made it) · **VIDEOS, lead-direct, the last three sheets and a re-conception** · then the B-media.0 verdict and the orphaned-leads-board call.
