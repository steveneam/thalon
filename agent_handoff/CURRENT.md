# Session handoff — CURRENT

> **One file, overwritten at every session wrap (AGENTS.md rule 9).** Pointer + delta + next action only — never a state dump, never a copy of CHARTER/COORDINATION content (link instead). History lives in git.

## Stamp

2026-07-14 (session 32, syd4) · **L-SET WIRED (`f04e404`) + TWO FOUNDER FIXES (`8517ab3` hero-ambient, `5b3bea7` beacons close) + HEADLESS CHROME INSTALLED — WAVE-1 PLAN CUT.** Step 1 of the mint order is closed end-to-end: pinned originals → `BRAND_ASSETS` manifest (provenance-linked) → `scripts/export-brand-assets.ts` (hash-verified, deterministic, 57KB total webp) → six placements on `/` (hero dusk · features currents · steps unfolding · trust lantern · pricing grain · first-light horizon close — the dusk→dawn arc bookends the scroll). ≥3 two-lane passes run; suite **1133/3/0**; build 44/44 static; SSR-HTML verified (six `data-brand` layers); guard clean; auto-deploy fired. L3a + L6 + B1 stay pinned-but-unwired (popout backdrops died on object-cover videos; OG swap + blog auto-hero are their own slices). **Wave-1 axis draws recorded in COORDINATION session-32 message** (5 verticals, primaries unique, neutral fictional names).

## Resume prompt (session 33, syd4 — paste verbatim; "gogogo" boots this too)

> Stamped 2026-07-14, session-32 wrap (incl. founder fix round + browser install).

**Resume · Thalon** — session 33, syd4 — **mint-week step 2: build wave-1 template page ① (tech/SaaS "Loopwell", primary axis data/instrument) through the factory meta-prompt.**

▎ ▸ **Read first:** `CLAUDE.md` → this file → COORDINATION.md session-32 message (the wave-1 plan) → `proprietary/templates/meta-prompt.md` + `iteration-pass-checklist.md` → `proprietary/prompts/b7.2-shot-list.md` (ledger).

▎ ▸ **State:** main = origin @ `5b3bea7` (+ wrap docs) · suite **1133/3/0** · guard clean · staging = the founder-tuned landing (hero-ambient + beacons close, BOTH edge-verified + screenshot-confirmed) · credits **~1007.8**/`plus` (video pool untouched) · pinned assets = 12 in syd4 `.data/objects/` (single copy — don't clean) · **headless chrome LIVE on syd4**: binary `~/.cache/ms-playwright/chromium-1228/chrome-linux64/chrome` (NOT the installer log's headless_shell path), playwright-core + edge Basic pair screenshots staging · leads triage + →Email dogfood + first LIVE compose spend check still pending · impeccable re-critique QUEUED on founder go (assessment agents need a fresh launch approval).

▎ ▸ **Session-33 plan (founder-confirmed: move to next tasks):** **(1) Scaffolding decision first** — where template code lives + how neutral previews deploy (Phase-3 default: tracked generic code under `proprietary/templates/<vertical>/`, client instantiations stay data; static single-page each; deploy through the established channel under neutral names — pick the mechanism ONCE, it carries all ~25). **(2) Build ① Loopwell** by instantiating the meta-prompt verbatim (axis: data/instrument + exceptional-palette; stakes line; `/guide` route; per-page mints pinned via `pin-mint.ts` at mint time; ≥3 two-lane passes; SSR + staging verify). **(3) If time: ② TrueBore** (physics — the grocer precedent at `.context/clients/pujusfresh` is the reference). Sequential by default; **any lane fan-out needs fresh founder go**. Interleave when blocked: dashboard v3 (`workspace-ux-v2.md` §10) or P0.4/P0.5 (AssetSource window 2 + driver skeleton — still not started).

▎ ▸ **Next checkpoint (B-crm.3–6) additions unchanged:** Postgres migration BEFORE triage/eval rows accumulate (then RLS ratchet) · B-crm.5 shortlist + `matchTerm` word-boundary · B-crm.3 enrichment provider · B-crm.4 Resend timing · approve/reject undo contract change.

▎ ▸ **[founder] queue:** **go/no-go on the impeccable re-critique agents** · triage the staged leads queue · dogfood the →Email exit · swordfish rotation when the `FROM-SWORDFISH` note lands · month-end downgrade decision rides the §6 credit re-assess.

▎ ▸ **Standing:** stealth holds (template previews = neutral names, no real domain) · founder `.env.local` edits arrive BOM+CRLF — normalize · `[Steven via hermes-relay]` = founder; relay turns end founder-readable · any lane/subagent launch needs fresh founder go · `.env.tenant-pg` syd2-only, never tracked · Dokploy key stays CI-secret-only · detector hook fires on UI edits; the hero `text-[3.4rem]` finding = the documented Display step, intentional, unsuppressed.

▎ ▸ **✅ SAFE TO CLEAR.** As of the stamp: all commits pushed (`f04e404` · `8517ab3` · `5b3bea7` + wrap docs); both fix deploys green and screenshot-verified; no open PRs; no worktrees; no mid-edit state; no dev servers; suite/build/guard green at HEAD.

## Pointer

Read in order: `CLAUDE.md` → this file → `COORDINATION.md` (Sprint-7 board + session-31/32 messages) → `proprietary/templates/` + `proprietary/prompts/b7.2-shot-list.md` → visual-uplift proposal §Session-29/30 addenda.

## Delta (session 32)

- **Landing wiring (`f04e404`)**: `BRAND_ASSETS` manifest + export ratchet + six placements + tests (brand-assets existence pins; landing backdrop decorative-contract pin). Plain `<img>` deliberate (pre-optimized exact-size webp; 6 accepted warnings, 0 errors). Pass 2 caught the pointer-events leak; pass 3's ambition item = the dusk→dawn narrative arc.
- **Wave-1 plan**: 5 verticals with unique primary axes + neutral demo names (COORDINATION s32). Wave-2 primaries reserved.
- Honest kills: popout backdrops (videos are object-cover — nothing shows behind), L6 OG swap deferred (opengraph-image.tsx is its own decision), B1 blog wiring = product-feature slice, not a paste.
- **Founder fix round (same day):** hero sails read as a floating flame → L1v2 ambient (`8517ab3`); closing streak "did nothing" → L5v2 beacons under the publishing-everywhere CTA, presence raised (`5b3bea7`). Superseded originals stay pinned. Inspiration pass (Higgsfield/Motionsites/2026 roundups) shaped the close: the final CTA owns the page's focal glow.
- **Headless chrome installed on syd4 (founder-approved)** and proven with staging screenshots; memory + COORDINATION updated. Dribbble/Pinterest text-fetches stay empty (JS galleries) — browse them with the real browser from now on; email-only login is impossible (no password/OAuth), so gated boards still come via founder screenshots.

## Next action

Session 33: **template scaffolding decision → build Loopwell (①) through the factory meta-prompt, browser-verified locally → TrueBore (②) if time.** Founder at their pace: re-critique go/no-go · leads triage · →Email dogfood · rotation swap.
