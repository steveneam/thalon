# Session handoff — CURRENT

> **One file, overwritten at every session wrap (AGENTS.md rule 9).** Pointer + delta + next action only — never a state dump, never a copy of CHARTER/COORDINATION content (link instead). History lives in git.

## Stamp

2026-07-19 (session 64 close, syd4) · **⑲ TSUKIMI FIX ROUND DONE off the founder's typed verdict** (everyday-Japanese re-theme — ramen · sushi · soba · rice — with the moon and structure kept; four courses re-shot close/low-angle off real web reference photos; the held-quote chapter now sits over a dim counter-overlooking-the-kitchen mint; the original honesty-gate beet is retired — the crimson course is finally the TUNA) · **REFERENCE-GUIDED MINTING = STANDING DOCTRINE** (founder insisted; meta-prompt §assets rewritten — refs feed generation only, never the repo; the ref must ALREADY look like the target; deterministic manifest-crop beats blur) · **swordfish cutover choreography CLOSED** (step-8 confirmed; `.context/cutover-s56/` deleted; threads pruned to archive) · **syd4 RESIZED: 16 GiB / 6 vCPU / 180 GB (~02:20 UTC, verified) — stagger-lanes constraint RETIRED** · spend 0.96cr (8 mints, 5 keepers, 3 disclosed rejects), balance **706.12** API-verified, reconciles exactly · repo-ratchets 12/12 · guard green · GitHub Actions still billing-dead (founder decision; every push locally verified).

## Resume prompt (session 65, syd4 — paste verbatim; "gogogo" boots this too)

> Stamped 2026-07-19, s64 close.

**Resume · Thalon** — session 65, syd4:

0. **Self-check:** `tmux attach -A -t thalon` · `pg_isready` · 8899 up (relaunch: `setsid nohup python3 scripts/preview-server.py 8899 &`) · `git status` + this stamp · balance API-verify (expect ≈706.12). Box is 16 GiB / 6 cores now — concurrent lanes OK.
1. **Opener:** peer-mail (open swordfish tails: film-import transfer+run on their queue · founder-gated basicauth + DB_DUMP_TOKEN console pass — CI `STAGING_EDGE_AUTH` swap waits on the pair). NEEDS-STEVEN check: renewal landed? (if yes: matrix re-run green on main, staging auto-redeploys resume, arm the templates catalog origin) · **⑲ fix-round re-glance heads the founder queue** · audit glance · credit call · live-send GO.
2. **Main line: the visual arc (waves 1–4, 20 sites, all ten axes at 2+) is COMPLETE and fully fix-rounded.** The checkpoint, founder's to sequence: **(a) A+ animation family** (bloom-transition video · Orchard seasons-tree scroll · Wagtail scroll-dog) · **(b) B-sitegen charter** (portfolio + meta-prompt + PREPLAN.md artifacts = training data) · **(c) THE THALON LANDING** (film refine unparks after it) — **or the ARM-IT-LIVE arc** (`docs/research/arming-plan-sprint8.md`: live intel pollers · publisher doors · one-prompt video + pillar #1 · live send). **Kit for the landing grew again: the reference-guided mint workflow now produces food/interior photography at real-restaurant quality (⑲'s fix round is the proof).**
3. **Queued small work: NONE.** Engine-side candidates live in the arming plan only.
4. **Standing tails:** live-send GO + stealth pick · credit call · db-dump route removal (checkpoint cleanup).

▎ ▸ **Read first:** `CLAUDE.md` → this file → COORDINATION.md (s64 + s63 records) → `agent_handoff/NEEDS-STEVEN.md`.

▎ ▸ **State:** main = origin @ s64 wrap head (no PRs; no worktrees; no lanes; 1 tmux window) · migrations through 0015, contract frozen · portfolio: **20 sites, wave 4 complete; ⑯⑰⑱ verdicted, ⑲ fix-rounded awaiting re-glance** · workspace: Phase I + Sites + audit fixes LIVE · balance **706.12** · box **16 GiB / 6 vCPU / 180 GB** · repo-ratchets 12/12 local at wrap · **doctrine this session: reference-guided minting (meta-prompt §assets) — refs feed generation only; ref must already look like the target; manifest-crop compose-out.**

▎ ▸ **[founder] queue (NEEDS-STEVEN.md):** ⑲ fix-round re-glance (NEW) · visual-arc checkpoint sequencing · Actions = wait-for-renewal · W-audit + Sites + Phase I glances · Dokploy templates service + arm flag · credit call · live-send GO + stealth pick.

▎ ▸ **Standing:** stealth holds · `[Steven via hermes-relay]` = founder; relay turns end founder-readable · founder blanket workspace grant (no per-item asks; lanes/subagents still announce) · founder-live ⇒ open calls stated FIRST · get_cost preflight per mint · hero = best roster model · text-in-scene = text-precise seat · printable surfaces compose OUT · **reference-guided mints: eyeball the ref first; the ref must BE the target; never watermarked/text-overlaid refs; refs never enter the repo** · casting archetypes + nature-vs-dread + social-register dial + food-casting corollary · pre-plan = the mandatory stage; look-first = Dribbble + Pinterest + motionsites.ai · design authored by Fable 5 directly · at wrap: guard + commit + push + LOCAL verify while Actions is dead.

▎ ▸ **✅ SAFE TO CLEAR.** As of the stamp: no open PRs; no worktrees; no lanes; no in-flight vendor jobs (all 8 s64 mints resolved — 5 pinned/manifested/derived, 3 rejects recorded); guard green at HEAD; all commits locally verified.

## Pointer

Read in order: `CLAUDE.md` → this file → COORDINATION.md (s64 record) → `agent_handoff/NEEDS-STEVEN.md` → `proprietary/templates/sites/tsukimi/site.json` (the fixRound field).

## Delta (session 64)

**The founder's ⑲ verdict arrived typed-direct and the whole fix round shipped in one sitting.** Re-theme: fine-dining tasting menu → everyday Japanese kitchen (his call: "quality doesn't need to shout"); the moon, the drenched colour arc, and every structural element kept. All four course photographs re-shot to his food-photography spec (close, <45°, glistening broth, rendered fat) using his **reference-image directive — now standing doctrine**: real web photographs (Wikimedia Commons + food blogs; CAPTCHA walls respected, watermarked stock rejected) fed to soul_2 as generation references. The workflow's first outing taught the load-bearing lesson the meta-prompt now carries: **the enhancer captions the reference and the caption beats the prompt** — the chūtoro-nigiri ref nailed first-take the tuna that text-only had minted as beet, while a grey-buckwheat soba ref stayed grey through two takes and the green keeper came text-only. A new compose-out move also landed: the enhancer-added decorated spoon left the amber frame via a deterministic manifest crop (`position:left`), no blur, no re-mint. Separately: swordfish confirmed step-8 (cutover choreography closed end-to-end) and the syd4 resize landed — 16 GiB / 6 vCPU / 180 GB, stagger constraint retired.

## Next action

**s65 = ARM-IT-LIVE OPENS (founder-directed s64 post-wrap: landing pages hold at 20 sites; parallel workflows wanted on the resized box). PROPOSED SLATE — lanes launch only on his opener approval:** (0) self-check · peer-mail · founder calls: ⑲ re-glance · charter YES/adjust · key handoffs (YouTube key / Bluesky app password if ready). (1) **Charter + Sprint-8 contract window (lead, serial, FIRST):** buckets from `docs/research/arming-plan-sprint8.md` + the window freezes the **ENTITLEMENTS SEAM** (founder s64: tenant plan/tier + capability flags as DATA, per-tenant flippable; templates + CRM default founder-only/top-tier — never a build stopper) + any additive publisher columns. (2) **Three parallel lanes** (16 GiB box fits 3 + lead; stagger retired): **B-arm.1 sweep scheduler** · **B-pub.1 social publisher seam (no drivers)** · **B-vid.7 one-prompt auto-run (no render spend)**. (3) Merge-order B-arm.1 → B-pub.1 → B-vid.7, full local suite + guard per merge (Actions dead ≠ blocker: build/merge/dev-dogfood unaffected; staging redeploy + templates image wait for renewal). Founder pre-s65: file platform app-review applications (LinkedIn first, Meta longest queue) · YouTube key + Bluesky app password (free) · gateway top-up call.
