# Session handoff — CURRENT

> **One file, overwritten at every session wrap (AGENTS.md rule 9).** Pointer + delta + next action only — never a state dump, never a copy of CHARTER/COORDINATION content (link instead). History lives in git.

## Stamp

2026-07-18 (session 62 FULL close, syd4 — founder live in the second half: ⑯ verdict + fix round, the HOUSE STYLE stated+ratcheted (nature = supporting cast, meta-prompt §casting (9) + memory), Hartline re-pass done on his grant) · **W-AUDIT COMPLETE, all five items** ((a) dev-done/staging-filed s61 · (b) Source-Link sweep + thumbnails `d6c3935` · (c)+(d) full impeccable + s40 re-critique `32f24a7`, 18/20, report `docs/research/workspace-audit-s62.md` · (e) storage-story + saved-views door WIRED `d7d17aa`) · **⑯ RIDGE & VALLEY SHIPPED (`0f03b1e`) — WAVE 3 COMPLETE, 17 sites; first site under the pre-plan-mandatory loop, PREPLAN.md = new tracked artifact class** · main-was-RED fixed on arrival (`65248db`, verdict-pinned-as-data test) · spend 0.72cr, balance **710.08 API-verified** (reconciles exactly; second-half spend 0.48cr) · six commits, all pushed, all locally verified (suite 1564 + guard) · guard clean at every commit.

## Resume prompt (session 63, syd4 — paste verbatim; "gogogo" boots this too)

> Stamped 2026-07-18, s62 close. **The box auto-reboots 18:30 UTC 2026-07-18** — after it, tmux + Postgres auto-start but 8899 does NOT (relaunch below). GitHub Actions stays billing-dead BY FOUNDER DECISION until the monthly renewal — verify every push locally (suite + guard); staging + templates images can't rebuild until then.

**Resume · Thalon** — session 63, syd4:

0. **Self-check:** `tmux attach -A -t thalon` · `pg_isready` · 8899 up (relaunch: `setsid nohup python3 scripts/preview-server.py 8899 &`) · `git status` + this stamp · balance API-verify (expect ≈710.08).
1. **Opener:** peer-mail — swordfish tails: step-8 dump confirm (was due post-15:00 UTC 07-18) · rotated basicauth pair (CI `STAGING_EDGE_AUTH` swap queued on arrival) · FILM-IMPORT confirm (closes W-audit (a) staging half). Check NEEDS-STEVEN: GitHub renewal landed? (if yes: re-run the matrix green on main, staging auto-redeploys resume, arm the templates catalog origin) · **⑯ re-glance (fix round done: mountain-mirror hero, merged instrument, tarp link) + audit-report glance** · credit call · live-send GO.
2. **Main line: WAVE 4, FOUNDER-DIRECTED (s62 live): ⑰ café (novel-typography × soft-organic, urban nature) · ⑱ science-consumables distributor (editorial-print × data-instrument, micrograph nature) · ⑲ fine Asian restaurant (exceptional-palette × cinematic; Onice↔Joji poles, taste notes in the s62 wave-4 message).** Build on the pre-plan-mandatory loop, one at a time, nature doctrine (§casting 9) as named input; name candidates in the message (Fern & Crumb · Strata Scientific · Tsukimi rec'd). Checkpoint remainder: A+ animation family · B-sitegen charter · the Thalon landing (post-wave-4; film unparks after it).
3. **Queued small work (no gate needed, workspace grant):** audit follow-ups from the report — form-field id/name harden micro-pass · text-[11px] typeset micro-pass · toast z-scale. **Engine-side look needed (report finding): fanout_runs stays `pending` after drafts judge — transition missing or the word is wrong.**
4. **Standing tails:** live-send GO + stealth pick · credit call · db-dump route removal (checkpoint cleanup) · Phase R was folded into W-audit and is now DONE.

▎ ▸ **Read first:** `CLAUDE.md` → this file → COORDINATION.md (the s62 message) → `agent_handoff/NEEDS-STEVEN.md`.

▎ ▸ **State:** main = origin @ `0f03b1e` (no PRs; no worktrees; no lanes; 1 tmux window) · migrations through 0015, contract frozen · portfolio: **17 sites (16 verdicted + ⑯ awaiting)** · workspace: Phase I + Sites + the s62 audit fixes + saved-views door LIVE (`/api/views`) · 8899 up until the 18:30 reboot · balance 710.56 · suite 1564/1564 local · throwaway 3111 killed.

▎ ▸ **[founder] queue (NEEDS-STEVEN.md):** ⑯ verdict + audit glance (NEW) · Actions = wait-for-renewal · Sites + Phase I glances · Dokploy templates service + arm flag · credit call · live-send GO + stealth pick.

▎ ▸ **Standing:** stealth holds · `[Steven via hermes-relay]` = founder; relay turns end founder-readable · founder blanket workspace grant (no per-item asks; lanes/subagents still announce) · founder-live ⇒ open calls stated FIRST · get_cost preflight per mint · hero = best roster model · text-in-scene = text-precise seat · printable surfaces compose OUT (now incl. bucket bodies/gift boxes/arrangement cards/wrap collars/vehicle glass · machine bodies · vintage livery · worn fabric→KNIT · grade stamps · breaker faces; count-anchor) · casting archetypes + nature-vs-dread · **the pre-plan is the mandatory stage (meta-prompt §How-to step 3); claude-design = optional mock tool** · design authored by Fable 5 directly · at wrap: guard + commit + push + LOCAL verify while Actions is dead.

▎ ▸ **✅ SAFE TO CLEAR.** As of the stamp: no open PRs; no worktrees; no lanes; no in-flight renders/vendor jobs (all six ⑯ mints pinned); 3111 killed, 8899 untouched; guard green at HEAD; all commits locally verified; the 18:30 reboot is expected and survivable.

## Pointer

Read in order: `CLAUDE.md` → this file → COORDINATION.md (s62 message) → `agent_handoff/NEEDS-STEVEN.md`.

## Delta (session 62)

**The pre-confirmed W-audit ran end-to-end and closed.** Along the way: main was RED on arrival (the ⑭ verdict flip broke a test that pinned verdict data; fixed + made structurally impossible to recur) · the Source-Link Rule now holds at every representation with thumbnails plumbed ingest→UI · the one client-machine system-of-record (board views in localStorage) was found by the storage sweep and wired to the tenant-wide saved_views store the same session (a Phase-I window door) · the full impeccable pass scored the workspace 18/20 after fixes (calendar-390 bleed, sub-AA placeholders, hard-coded judge green → `--ok` token, two unbounded lists) with the re-critique clean on all five list recipes + Four-Verbs. **Then ⑯ Ridge & Valley closed wave 3** — the first site through the pre-plan-mandatory loop, 6/6 first-take mints at 0.72cr (the compose-out playbook now pre-empts the pseudo-text class entirely when a site keeps all in-scene text code-drawn), and `PREPLAN.md` became a tracked artifact class feeding B-sitegen.

## Next action

**s63 opener: self-check (8899 relaunch post-reboot) → peer-mail (step-8 + basicauth + film-import tails) → founder calls stated first if he's live (⑯ verdict · audit glance · renewal check · credit call · live-send) → then either the wave-3 checkpoint work his verdicts open, or the queued audit follow-ups (harden/typeset micro-passes, runs-status engine look) under the standing grant.**
