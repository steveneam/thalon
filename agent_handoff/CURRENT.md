# CURRENT

## Stamp

2026-08-03 close of session 98 (syd4 — **zero credits; the FIRST LIVE
CREATE-ORIGINATED POST is on Bluesky**). Boot was the founder's "gogogo. and
Go for the dogfood too" — both GOs executed the same session: **post door
ARMED** (`ba850c0`) → **B-create.5 dogfood END-TO-END** → **the judge
verdict-order fix, live-proven** (`abf145e`) → **published + verified**
(`at://…/3ms6ep6a3sm23`, `steveneam.bsky.social`, 11:16Z). Wrap verify on
main: **exit 0, 3310 passed / 9 skipped**.

## WHAT SHIPPED (the board §s98 carries the full records)

**Arming** — `createDoor("post")` flipped on the founder's recorded GO; the
one seam opened route + surfaces + Intel exits together (the s77
dead-primary case healed live). `page` STAYS SHUT; its refusal is still
executable in the route test. **Dogfood** — real intel card → capture →
wizard → Bluesky-only plan → spend statement stated BEFORE Generate
(claude-cli/opus-5 on his subscription; zero credits; the plan's own
preflight agreed: "0 credits · 1 metered call") → generate → judge →
Composer → Approve → queue row → `run-publish-queue.ts --once` armed
per-run → 1 due / 1 published / 0 failed. **The judge fix** — g3_screen
recorded all-claims-supported + verdict:fail 3× on the same body (its own
notes arguing pass); root cause: `verdict` FIRST in the output shape =
the model rules before weighing; fixed verdict-LAST in both transports;
**live golden:g3 34/34 including g3-016 (the s69 red pin, now closed) +
new g3-017** (the dogfood's universalized-imperative class).

## Resume prompt (session 99, syd4)

**Resume · Thalon** — the video arc has ONE item left; the Create loop is
live end-to-end. Candidates, no fixed order (re-test at the opener, rule 11):

1. **V9 — the video arc's LAST item:** the render-gate pass across the four
   surfaces (screenshot-vs-sheet at 1440×940 + the refused/empty/loading
   state matrix; drift is a defect). fe-check {mode:"built"} per surface is
   the harness shape. Fold in the s98 dogfood's two UX observations:
   wizard platform exclusion is one-way within a session; the Composer
   verdict strip hides WHICH hard gate failed behind "details ▸".
2. **UX programme** — remaining un-passed rows in
   `docs/research/ux-refinement-program.md`.
3. **Simplify candidate** (s96): plat-mark paths in THREE surface copies
   (approve/analytics/schedule) → `components/media`.
4. **More Create dogfood** — the door is armed and proven; each run is
   subscription-only until media enters. Page generation still needs its
   OWN founder word.

▎ ▸ **The one FLAGGED contract-window ask (s96, unchanged):** the Dossier's
drawn "Restore brings it back" needs a cut retire/restore column — raise at
the next window with the founder.
▎ ▸ **Token rail:** the founder's standing 2M/day pin in `.env.local` is
UNTOUCHED and it FIRED correctly mid-golden-lap s98; the finish ran under a
per-process 4M raise (s68 precedent). A fresh day resets the ledger — no
action needed.
▎ ▸ **Design work = Fable 5 direct, never delegated** (standing s51); every
lane/subagent launch needs fresh founder approval; Mode B default.
▎ ▸ **Traps worth keeping:** `next dev` at `localhost:3111` (`npm run dev`),
started/stopped in-session — s98 left it STOPPED · scripts need `set -a;
source apps/web/.env.local; set +a` · **chrome-devtools `fill` does NOT
reach React controlled inputs in this app** (two stale submissions s98) —
type via keyboard events or use the surface's own API wire · one workspace
`.data` root since `68d9873`, NEVER re-pin · chip platform words live in
aria-labels · `placeColumn` returns `{placed, overflow}` · sheet-verbatim
CSS = impeccable findings intentional (DOCTRINE 0) · vitest doesn't
typecheck (`npx tsc --noEmit -p apps/web`) · canvas writes need
`finalize_plan` (UUID `f5d304cb-cd0e-484d-8542-7b6561e1ef30`) · editor
render tests: primary button matches `/^Rendering…/`.
▎ ▸ **⛔ SEQUENCE GATE, current truth:** **post = ARMED** (founder GO s98,
`ba850c0`); **page still 409s** at `POST /api/create` until his word;
bluesky = the one platform granted for live testing (s83 grant, exercised
s83 + s98); queue consumer's key rests EMPTY — arming is per-run; youtube
CANNOT arm. **Two live posts total, both bluesky, both under the grant.
Zero credits ever spent.**
▎ ▸ **Standing:** stealth · hermes-relay = founder · GATE ON EXIT CODE ·
verify-on-main = THE gate · rules 10/11/12 · platform logins live durably in
`.context` · no AGPL embedded · wrap = verify+commit+push+restamp.
▎ ▸ **State:** main = origin (this wrap) · staging rolls s93–s98 with the
next auto-deploy · four social channels connected · dev PG live · 8899
preview + sweeper user units keep running — NEVER hand-start the sweeper.
▎ ▸ **✅ SAFE TO CLEAR** — nothing in flight; no lanes; tree clean and
pushed; s99 boots on "gogogo" alone (this file + COORDINATION §s98 carry
the whole state).

0. **Self-check** — tmux `thalon` · `pg_isready` · both user units (needs
   `XDG_RUNTIME_DIR=/run/user/$(id -u)`) · `git status` + this stamp · `npm run
   doctor` (data-root row must read ONE store) · `bash
   ~/work/swordfish/provisioning/checks/needs-steven-hygiene.sh`.

## Pointer

CLAUDE.md → this file → COORDINATION.md (§s98) → `docs/video-arc/spec.md`
(V9) → `docs/research/ux-refinement-program.md` →
`docs/research/mock-sheets/README.md` → agent_handoff/NEEDS-STEVEN.md →
`docs/research/prior-art-portal-automation-s84.md` (BEFORE ANY PORTAL WORK).

## Delta (session 98)

The founder's one-line boot carried two GOs and the session spent them in
order: the arming flip (its own green + commit), then the dogfood — which
did exactly what dogfood exists to do: the first real run walked every
surface, and the judge's screen tier failed a fully-grounded draft three
times WHILE ITS OWN NOTES ARGUED PASS. Root cause was structural, not
judgment: verdict-first output shape = ruling before weighing. The fix +
golden g3-017 + the 34/34 live lap (closing the s69 red pin g3-016 as a
side effect) landed as one boundary, the unchanged draft re-judged clean,
and the queue consumer put it on Bluesky under the standing grant. What
remains of the video arc: V9 alone.
