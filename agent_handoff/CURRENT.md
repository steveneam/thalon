# CURRENT

## Stamp

2026-07-26 (session 77, syd4 — zero credit spend, OPUS 5 as the founder directed). **THE s77 WINDOW IS FROZEN AND LANE A'S CORE IS SHIPPED**, both direct-to-main on green verifies. Plan of record executed in order: §9 step 1 (the window, because windows freeze before lanes launch), then lane A lead-direct. Verify on merged main: **2153 passed / 9 skipped, 0 lint errors** — up from the s76 baseline of 2101.

**The window (`13163d4`) is CONTRACTS-ONLY, zero SQL migrations**, exactly as the planning pass ground-truthed (both meta columns were already jsonb). `media.ts` models `MediaRef` as a union on WHERE THE BYTES LIVE, because that is what decides trust, failure mode and serving path. **Three departures from the plan's sketch, each closing a hole a type could close for free:** the ext list is SPLIT into image and audio families (one flat list let a poster be an mp3); audio is STORED-ONLY (you cannot license bytes you do not hold, and hot-linking would make a render's legal footing depend on a stranger's uptime); and every schema is `strictObject`, which is how invariant 3 stops being a sentence — an envelope carrying `orientation` is REFUSED at the door. The Retry rider rode the same freeze. Library's "grounds N drafts" count did NOT, on purpose: `groundingSourceIds` lives in `drafts.meta` jsonb, so it is a containment query needing a repo method and an index decision — real work, not a shape to freeze, and padding it in would have been dishonest about what a window is.

**Lane A (`a70a53c`):** the resolver is one pure function per entity over the current row — which is what "media changes dynamically" actually reduces to, with no refresh mechanism and no cache to invalidate. Chains are DATA in one table and a test walks them, so the Approve ruling (a draft never wears its run's or its grounding source's image) is now executable rather than remembered. `<SourceThumb>` renders the five verdicted states and kills the bug the sheet named: all three shipped `<img>` blocks had no `onError`, so a dead poster painted a browser broken-image glyph — the one state that reads as a defect rather than an honesty. **It renders the SHARED `.thumb-sm`/`.thumb-md`, never the sheet's own `.tb`** — four surfaces override that box legitimately (Approve's margin, the editor's 118×68, the video dossier's 52×33, the site dossier's 148×92) and `.tb` would have flattened all four. `workspace.css` untouched.

**THREE CORRECTIONS ON THE RECORD, all mine, all forced by a gate rather than noticed by me.** (1) The intel suite caught that requiring `capturedAt` made a card DROP a thumbnail it genuinely held — hiding real media to protect a field nobody reads is the wrong trade, so the contract was amended **while it was still the owner's and no lane had consumed it**: absent now means "we did not record when", which is a fact where a fabricated time would not be. (2) The css-scope ratchet correctly refused `source-thumb.css` — a shared component is not a surface and cannot be surface-scoped, and dumping its internals into `workspace.css` would push component guts into the shell contract. It gained a SECOND category with an equally strong rule: a declared shared-component sheet must carry its own namespace on every selector. Same guarantee, different mechanism, still checked. (3) Typecheck caught `failureReason(response: Response)` while every test stayed green — **vitest does not typecheck**, which is the standing reason verify-on-main is the gate.

**THE SWEEPER'S EIGHTH LAYER IS PEELED, and it is the one that would have misled us.** It had logged the same eight words for ~18 hours because the call site read `response.status` and threw the body away — so "read the reason VERBATIM" was impossible by construction. It now carries the API's own words, and the answer is instructive: the code is **`rateLimitExceeded`** — which reads as *transient, just retry* — but the message says the limit is **"Search Queries PER DAY"**. A daily cap wearing a rate-limit label. Per-day quotas reset at Pacific midnight, so it self-heals; the backoff (30→60→120m, working exactly as designed) would have delayed the first clean sweep to ~09:51Z, so the scheduler was restarted onto the fix and should sweep clean from ~07:17Z. **This is hard evidence for the founder's still-open coverage-vs-cadence ration call** — the per-day search cap is precisely what `YOUTUBE_MAX_SEARCHES_PER_SWEEP` 1→12 spends, so it re-exhausts daily until he picks.

## Resume prompt (session 78, syd4 — "gogogo" boots this)

**Resume · Thalon** — s78: (0) self-check (tmux `thalon` · pg_isready · 8899+sweeper units · `dev` 3111 · git status + stamp). (1) **Sweeper: it should now be sweeping clean.** If it is still 429 past ~08:00Z, the log finally names the reason itself — read it and act on THAT, not on the status. (2) **THE HEADLINE — lane B, which is PREPPED and needs only his fresh launch approval** (standing rule; the plan states it explicitly): ingest width/height capture · poster derivation + the backfill door for existing takes · **ffmpeg into `Dockerfile.web` (his GO is on record — DO it, don't re-ask)** · then B-audio.1's three pieces (bed source → audition → mux), which is ARMING A SEAM, not a green field: the editor already carries the cue end-to-end as data and the engine already has `CompositionAudio`, but `bed` is an admittedly empty seam and the composition is commented "today's silent composition". (3) **Lead-direct remainder:** Sites' dossier mint strip adopts `SourceThumb` (the first surface that can actually reach `broken`, so the new state gets a live caller), and `lg` either earns a home or is cut — an unused size is a size that drifts. (4) **Do NOT re-ask:** B-media.0 verdicted · placeholders = his s75 ruling · ffmpeg = GO · zero-state path = code-drawn, proposal shipped and awaiting only his glance (not a gate).

▎ ▸ **Read first:** CLAUDE.md → this file → `docs/research/media-framework-plan.md` (§9 = the build order; §8 = the audio pieces) → COORDINATION.md §Sprint 9/s77 → NEEDS-STEVEN.md.
▎ ▸ **State:** main = origin, all pushed · window FROZEN, lane A core SHIPPED · verify 2153 green · budget 2M · balance 584.12 · **zero spend s77**.
▎ ▸ **Standing:** stealth · hermes-relay = founder · blanket workspace grant · **every lane launch needs fresh founder approval** · NEVER filter live gate/suite output (**never pipe it through `tail`** — write to a file and read it) · ≥40cr mint ping · verify-on-merged-main = THE gate · REDESIGN ERA MODEL = OPUS 5 · wrap = guard+commit+push+restamp.
▎ ▸ **✅ SAFE TO CLEAR** — nothing in flight; all committed+pushed; no lane running.

## Pointer

CLAUDE.md → this file → `docs/research/media-framework-plan.md` → `docs/research/mock-sheets/README.md` → `docs/research/source-media-plan.md` → COORDINATION.md → NEEDS-STEVEN.md.

## Delta (session 76)

**Wave 3 merged — all 16 sheets became the real workspace**, and the founder's five-item polish pass shipped on top of it. Two corrections went on the record that session: the `127.0.0.1`-vs-`localhost` cross-origin block (a healthy app reads as dead while curl still returns 200), and "there is no logo", which was flatly wrong — **a filename grep is not an existence proof; search by ROLE.** The screenshot gate became a script that runs (`scripts/shoot-surface.mjs`), and it is what caught the first bug. Second half, on Fable 5 at the founder's direction: the media framework was planned as ONE framework (`docs/research/media-framework-plan.md`) after he verdicted the thumbnail work as needing a sustainable schema; his three calls were ruled and executed same-session (remint granted-then-superseded, ffmpeg-in-image GO, zero-state = code-drawn with `docs/research/zero-art/zero-art-proposal.html` shipped as the evidence).

## Delta (session 75)


**Wave 2.** Three prepped worktrees launched inside ~3 minutes and ran ~50 minutes each. Merge gate: two merged clean; `estate-rebuild` hit three conflicts, all resolved deliberately. The instructive one was `selected-row.test.ts`, where each lane had removed **its own** entry from the legacy-selection pin — taking either side alone would have silently re-pinned a deleted file, so the correct merge removes both. All six surfaces were then rendered on dev 3111 and diffed against their sheets before the merge stood.

**What the gate surfaced, homed rather than decided unilaterally:** `components/board/leads-board.tsx` was left **orphaned** — `Board.dc.html` is the CONTENT pipeline board drawn under Home, not a leads board, so rebuilt Leads rendered its Board tab and said so plainly. Both lanes found it independently and each correctly declined to touch the other's file. The lead did not delete a working capability on its own authority; **the founder then ruled it comes back**, and step 1 shipped the same session. Also flagged: Calendar clips concurrent same-instant events to ~45px chips (the sheet's own `overflow:hidden` meeting a density its fixture never had), and Sites turned out to be a fourth surface already resolving real media.

**B-media.0, planned research-first** per the founder's "see how others do it". The findings that actually bore on a decision: `.thumb-sm` (64×40) is **exactly** Mux's 256×160 storyboard-tile ratio; a 16:9 poster cover-cropped into it loses ~10% of its width, inside the central-80% safe area creators already design for; a **9:16 Short loses ~65% of its height and gets decapitated**; and hover-scrub is out on structural grounds, not taste — the storyboard artifact requires owning the transcode, and our densest surface holds a YouTube URL and one poster. Reading the code also corrected the premise: **four surfaces already resolve real media**, so this generalises something that works. And `video-title.ts` parses the oEmbed reply for `thumbnail_url` while **discarding `thumbnail_width`/`height` from that same response** — free, and the only thing blocking the portrait decision.

**B-media.1** renamed the surface only (`lib/library/`, `/api/library`, `LibrarySourceRow` keep their names — they serve the generic `sources` shelf), with `/app/library` kept permanently as a redirect and the divergence recorded under a new README section, *Founder amendments — the sheets are WRONG here on purpose*.

**Create's pruning panel** restores the keeper as a state behind the sheet's own chip: the chip text becomes the disclosure, pruning reaches **generation** (not just display) through one `pruneContext()` every downstream consumer already reads, and dropping a field is reversible while dropping the whole chip is not — so the chip renders off the UNPRUNED context and cannot vanish mid-prune.

**Discipline note carried forward.** The lead ran its own full verify while three lane suites were live and drove load to 13. It finished clean, but the s74 sequencing lesson applies to the lead too — the crm lane, which had that lesson in its kickoff, explicitly waited out its siblings and was right to. Both wave-3 kickoffs carry it.

**The founder's Sites findings, diagnosed at the close (both confirmed, neither a guess).** *"there thumbnails seem broken"* — all 20 load ON THE BOX, because `lib/sites/provider.ts` hardcodes `DEV_PREVIEW_ORIGIN = "http://127.0.0.1:8899"` and both the card images and the dossier iframe resolve against it. From any other machine `127.0.0.1` is the VIEWER's loopback, so every one of them breaks. Not a rendering bug, and the images are not missing. `SITES_PREVIEW_ORIGIN` overrides it, but the real fix is same-origin serving (or catalog-time poster capture, which would also feed B-media.0) — the lane picks one and says why. *"clicking on one of them seem to take me to the old design"* — exactly right: `site-dossier.tsx` has not been touched since s61 and still carries 19 bridged tokens (it is pinned at that number). It has NO sheet, so the precedent is Intel's Search tab: DESIGN it in the sheets' language rather than port it. All three jobs are the `sites-deepen` kickoff.

## Next action — s76 (BOOT ON OPUS 5): sweeper morning check (backoff should be visible in the log; quota resets ~07:00Z, PACIFIC midnight) · **LAUNCH WAVE 3 — `videos-rebuild` + `leadboard-wire` + `sites-deepen`, which finishes all 16 sheets and closes the founder's Sites findings** · lead merge-gates each on screenshot-vs-sheet + verify on merged main · lead's own queue = the B-media.0 verdict, the Calendar clipping call, EmptyArt's dark re-cut.
