# Session handoff — CURRENT

> **One file, overwritten at every session wrap (AGENTS.md rule 9).** Pointer + delta + next action only — never a state dump, never a copy of CHARTER/COORDINATION content (link instead). History lives in git.

## Stamp

2026-07-18 (session 59, syd4 — founder live) · **PHASE D DESIGN-COMPLETE + ⑫ SHIPPED, 0.72cr: wave-1 taste pass VERDICTED (founder: "the 3 sites look good, definitely improved") · the four owed Phase D designs shipped (Intel Dossier · Create Handoff · Approve Consent · Calendar Week — all 9 survey Qs answered/flagged; icon grammar decided) — THE WORKSPACE DESIGN CHECKPOINT IS RIPE · ⑫ Pearl & Rowe SHIPPED (the dental studio: static FDI chart instrument + scroll check-up pass + retained-53 wink; 3 soul_2 keepers / 6 takes; the printable-surface lesson proven on wall frames AND instrument handles)** · peer-mail clean TWICE (step-8 dump confirm + basicauth pair BOTH still pending → s60 opener) · balance 717.64 API-verified · guard clean · founder called the wrap after ⑫.

## Resume prompt (session 60, syd4 — paste verbatim; "gogogo" boots this too)

> Stamped 2026-07-18, session-59 wrap. **s60 = wave-3 ⑬ First Crack, then ⑭ Sparkwright (lead design work, standing approval — no gates).** Founder verdicts pending: ⑫ on 8899 + THE DESIGN CHECKPOINT (all seven Phase D designs; gates Phase I).

**Resume · Thalon** — session 60, syd4:

0. **Quick self-check (box 8GiB + 6GB swap):** `tmux attach -A -t thalon` · `pg_isready` · 8899 up (`curl -s localhost:8899/ >/dev/null`; relaunch: `setsid nohup python3 scripts/preview-server.py 8899 &`) · `git status` + this stamp.
1. **Opener:** peer-mail (`bash scripts/peer-mail-check.sh`) — still-open tails: swordfish **step-8 nightly-dump confirm** (on confirm delete `.context/cutover-s56/`) · **rotated basicauth pair** (→ swap `STAGING_EDGE_AUTH`, update `.context/staging-secrets-from-swordfish.md`, five-route probe). Fold in founder verdicts if given (⑫ · the design checkpoint — on verdict, cut the Phase I lane map for approval).
2. **Lead spine — ⑬ First Crack** (specialty roaster; data-instrument primary 2nd exercise × cinematic secondary; roast curves / cupping scores / first-crack timelines ARE the vertical's language): full meta-prompt treatment — look-first sweep → instruments → mints w/ get_cost preflight (soul_2 0.12cr/take; printable surfaces OUT of frame — proven on wall frames + instrument handles s59; bags/labels are the roaster trap) → ≥3 two-lane passes → ratchet 6/6 → /guide. Then **⑭ Sparkwright** (electrician; high-quality-3d × otherworldly: the dark CSS-3D house gaining light circuit-by-circuit with scroll).
3. **If the design checkpoint verdicts land:** absorb feedback into the seven designs (claude-design project "Thalon workspace — Phase D") → propose the Phase I lane map (W-spine lead / W-intel / W-create / W-boards, disjoint by construction) — **each lane launch = fresh founder approval**. Checkpoint also decides: manifest model/credits fields · headlamp.webp cleanup · Q9 recurrence + Q4 undo flags.
4. **Then:** ⑮ Stem & Vow + ⑯ Ridge & Valley → wave-3 checkpoint (+ meta-prompt ratchet candidates fold in: printable-surfaces-out-of-frame · count-anchor · text-seat 2cr estimating) → Phase I → Phase R = s40 re-critique. A+ bar standing over everything.

▎ ▸ **Read first:** `CLAUDE.md` → this file → COORDINATION.md (queue row 1 + s59 messages) → `proprietary/templates/meta-prompt.md` → memory `higgsfield-kompozy-assignment`.

▎ ▸ **State:** main = origin @ s59 wrap (no PRs; no worktrees; no lanes; 1 tmux window) · migrations through 0014 · staging LIVE on tenant-pg (two open tails above) · portfolio: **13 sites — 12 verdicted + ⑫ awaiting verdict**; wave-1 refresh approved s59 · ratchet 6/6 · **Phase D: ALL SEVEN designs done, checkpoint-ripe** (`docs/research/workspace-phase-d-designs.md` = decision ledger) · 8899 = `scripts/preview-server.py` (no-cache) · balance 717.64 · box 8GiB (STAGGER any parallel launches).

▎ ▸ **[founder] queue (NEEDS-STEVEN.md):** ⑫ Pearl & Rowe verdict · THE WORKSPACE DESIGN CHECKPOINT (gates Phase I) · credit call · live-send GO + stealth pick · (s40 re-critique rides Phase R).

▎ ▸ **Standing:** stealth holds · `[Steven via hermes-relay]` = founder; relay turns end founder-readable · get_cost preflight per mint, ≥40cr ping · hero = best roster model · text-in-scene = text-precise seat · **soul-2 letters EVERY printable surface (walls, frames, instrument handles, boxes, chests) — compose them OUT of frame; s59 proved one recomposition beats arguing twice** · aspect variants = own-engine recut · design authored by Fable 5 directly (design subagents = Fable-pinned + fresh approval) · lead drives lanes/tmux/interactive flows · close wrapped lane windows at batch review (capture pane first) · at wrap: guard + commit + push (+ ASK-BACKS ping only when the founder isn't live).

▎ ▸ **✅ SAFE TO CLEAR.** As of the stamp: no open PRs; no worktrees; no lanes; no in-flight renders/vendor jobs; guard green at HEAD; full matrix rides the push CI.

## Pointer

Read in order: `CLAUDE.md` → this file → COORDINATION.md (queue + s59 messages) → `agent_handoff/NEEDS-STEVEN.md`.

## Delta (session 59, full)

Founder "go" at the opener → self-check green, peer-mail clean (both tails stand) → **wave-1 taste pass VERDICTED live** ("look good, definitely improved" — row closed, board logged) → **Phase D session 2**: four designs authored + verify-looped in claude-design (dossier card-as-launchpad w/ per-family exits + honest "suggested" pre-pick · Create handoff w/ six removable typed context chips + profile-marked settings + One-prompt|Advanced · Approve consent w/ verbatim judge reasons + lineage deep-links + fail-closed blocked inset · Calendar week w/ one shared time→position mapping after fresh-eyes caught label drift); icon grammar decided (Spine rail set canonical); record doc → checkpoint-ripe; NEEDS-STEVEN row upgraded to THE DESIGN CHECKPOINT (`4fc3bf2`) → **⑫ Pearl & Rowe**: 3-angle look-first sweep (genre default refused on the record; boutique-spa suite register + chart-not-smile insight) → Literata/Atkinson vendored (OFL) → the FDI chart built STATIC with JS enhancement (the no-JS fault caught in pass 2 and rearchitected) + role-group a11y fix + narrow-screen records + quadrant midline + pass-end wink surfacing → mints 3/6 keepers 0.72cr (hero t2 after frame-pseudo-text reject; visit t1 w/ honest alt correction; tray t3 after two handle-lettering rejects — handles cropped OUT) → suite 6/6, guard clean, committed → founder called the wrap. Balance 717.64 API-verified.

## Next action

**s60 opener: self-check → peer-mail (both tails) → fold in founder verdicts (⑫ + design checkpoint; on checkpoint verdict, cut the Phase I lane map for approval) → ⑬ First Crack → ⑭ Sparkwright.**
