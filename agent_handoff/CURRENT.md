# Session handoff — CURRENT

> **One file, overwritten at every session wrap (AGENTS.md rule 9).** Pointer + delta + next action only — never a state dump, never a copy of CHARTER/COORDINATION content (link instead). History lives in git.

## Stamp

2026-07-18 (session 61 close, syd4 — founder checked in mid-session ×3, no gates opened) · **⑭ SPARKWRIGHT SHIPPED (`c798a28`)** — 15 sites; the re-energize scroll scene (CSS-3D wireframe house + switchboard on one clock, RCD TEST, C7 MYSTERY wink); 4 keepers / 9 takes 1.08cr, balance 714.28 API-verified · **NEW meta-prompt ratchet (same change): WORN FABRIC is a printable surface — knit garments resist invented branding** (5-take portrait record: camera-stare · chest badge · brooch · sleeve stripes → waffle-knit clean) · export chain gained a deterministic `position` crop field · suite 11/11 · guard clean.

## Resume prompt (session 62, syd4 — paste verbatim; "gogogo" boots this too)

> Stamped 2026-07-18, session-61 close. **The box auto-reboots 18:30 UTC 2026-07-18 (kernel patch)** — after it, tmux + Postgres auto-start but the 8899 preview server does NOT (relaunch below). All main-line work is founder-gated on NEEDS-STEVEN; wave 3 continues on standing approval.

**Resume · Thalon** — session 62, syd4:

0. **Self-check:** `tmux attach -A -t thalon` · `pg_isready` · 8899 up (`curl -s localhost:8899/ >/dev/null`; relaunch: `setsid nohup python3 scripts/preview-server.py 8899 &`) · `git status` + this stamp. (If the 18:30 reboot happened, 8899 WILL need the relaunch.)
1. **Opener:** peer-mail (`bash scripts/peer-mail-check.sh`) — open tails: swordfish step-8 nightly-dump confirm (on confirm delete `.context/cutover-s56/`) · rotated basicauth pair (→ CI `STAGING_EDGE_AUTH` swap + five-route probe). Check NEEDS-STEVEN verdicts: Phase I glance · ⑭ glance · **Phase-I contract window approval** · W-sites pick (design-first vs straight-to-build) · **⑭ retrofit question (rides the glance row): claude-design project for Sparkwright, or accept as-built?**. Sequencing per founder: W-sites → W-audit.
2. **Main line (as verdicts allow):** the Phase-I contract window (planned-slot store + scheduledFor · lead stage field · captures list read · capture-id on drafts · saved-views store — `/contract-window`, founder approval) · **W-sites** (plan: `docs/research/sites-surface-plan.md`; founder picks the mode + arms the templates preview service from his console) · **W-audit AFTER W-sites** (scope in queue row 1c: film registration first (a) · Source-Link sweep + thumbnails (b) · full /impeccable (c) · s40 re-critique (d) · storage-story audit (e)).
3. **Interleave (standing approval, no gate):** **⑮ Stem & Vow** (florist/weddings; cinematic-imagery 2nd × soft-organic; the casting lesson's biggest workout) then **⑯ Ridge & Valley** (roofing; brutalist-raw 2nd × cinematic). **BOTH run the FULL per-template loop — look-first → claude-design scaffold w/ named slots → alive final in the project → repo landing (meta-prompt §How-to step 3, RESTORED s61 after the founder caught the s53–54 drift).** Mind the fabric-knit casting corollary on every human mint.
4. **Standing tails:** live-send GO + stealth pick · month-end credit call (both NEEDS-STEVEN, founder-paced) · checkpoint row 4 candidates incl. B-sitegen.

▎ ▸ **Read first:** `CLAUDE.md` → this file → COORDINATION.md (s61 message = ⑭ record + founder Q&A) → `agent_handoff/NEEDS-STEVEN.md`.

▎ ▸ **State:** main = origin @ s61 close (⑭ + ratchet + board docs; no PRs; no worktrees; no lanes; 1 tmux window) · migrations through 0014, contract untouched · staging LIVE on tenant-pg, auto-redeploys on push · portfolio: **15 sites, 14 verdicted + ⑭ awaiting glance** · workspace: Phase I live, honest gaps = the queued contract window · 8899 preview up (dies at the 18:30 reboot) · balance 714.28 · repo suite 11/11.

▎ ▸ **[founder] queue (NEEDS-STEVEN.md):** Phase I glance · ⑭ glance + retrofit call · contract-window approval · W-sites pick · W-audit launch confirm (after W-sites) · credit call · live-send GO + stealth pick.

▎ ▸ **Standing:** stealth holds · `[Steven via hermes-relay]` = founder; relay turns end founder-readable · get_cost preflight per mint, ≥40cr ping · hero = best roster model · text-in-scene = text-precise seat · printable surfaces compose OUT (machine bodies · vintage livery · **NEW: worn fabric — cast humans in plain KNIT, never negative-prompt the shirt**) · casting archetypes + nature-vs-dread (§casting 7+8) · aspect variants = own-engine recut · design authored by Fable 5 directly · lead drives lanes/tmux · every lane/subagent launch needs fresh founder approval · at wrap: guard + commit + push.

▎ ▸ **✅ SAFE TO CLEAR.** As of the stamp: no open PRs; no worktrees; no lanes; no in-flight renders/vendor jobs; guard green at HEAD; full matrix rides the push CI; the 18:30 reboot is expected and survivable.

## Pointer

Read in order: `CLAUDE.md` → this file → COORDINATION.md (s61 message) → `agent_handoff/NEEDS-STEVEN.md`.

## Delta (session 61)

**⑭ Sparkwright, one session:** stamped opener ran clean (reboot not yet fired; peer-mail clean; zero new verdicts → the interleave was the only ungated work) → look-first sweep (genre defaults refused on the record; the trade's own plan grammar = the unused goldmine) → the re-energize concept built as one self-contained page (house + switchboard on one scroll clock; lay twin runs BOTH directions; markup ships energized, JS takes it dark; noscript folds the track) → 3 two-lane passes with real catches (mirrored floor-plan tags, roofless model, RCD trip honesty, mobile flow restructure) → suite/guard/console green → pushed. Mints: 4 keepers / 9 takes / 1.08cr — the 5-take portrait produced the session's ratchet (worn fabric = printable surface; knit resists branding), landed in the meta-prompt in the same change, plus a deterministic `position` crop field in the export chain. Founder checked in three times mid-session: got the full plan readout, and a claude-design answer that was WRONG and founder-corrected: the per-template loop (s51, ⑥/⑦) includes claude-design for mock AND final assembly; it silently drifted out at s53–54 and ⑪–⑭ copied the drift. Corrected on the board + memory; the loop is ratcheted into meta-prompt §How-to step 3 and stands from ⑮. No gated work touched.

## Next action

**s62 opener: self-check (8899 relaunch if the 18:30 reboot fired) → peer-mail → founder verdicts (Phase I · ⑭ + retrofit call · contract window · W-sites pick) → build as gated; interleave ⑮ Stem & Vow on standing approval — FULL loop (claude-design scaffold first).**
