# CURRENT

## Stamp

2026-07-26 (session 76, syd4 — zero credit spend). **WAVE 3 MERGED: ALL 16 SHEETS ARE NOW THE REAL WORKSPACE**, and the founder's five-item polish pass shipped on top of it. Three lanes (`videos-rebuild` · `leadboard-wire` · `sites-deepen`) launched inside four minutes, ran ~30–70m, and merged with **zero conflicts**; merged-main verify GREEN (**2101 passed / 9 skipped, 0 lint errors**). His two Sites findings are CLOSED: previews now serve same-origin (all 20 load off-box) and the dossier is rebuilt in the sheets' language. **The founder's polish pass, all five done same session:** Leads' score provenance compressed the way Intel's was (14 wrapped lines → 4) · Needs-you and the week card now agree at 512px · week↔day holds that height and the day view is a real time axis with the calendar's own red now-line · the mark points at the landing · **and the real Grip II mark replaced the rail's placeholder gradient tile**. Also retired two dead components (`workspace/bulk-bar.tsx` deleted; `HeatGrade`'s component removed, its `heatBand` kept) — **two more ratchet pins burned to zero**, exactly as the mono-ratchet comment had predicted and left to the lead.

**TWO CORRECTIONS ON THE RECORD, both mine, both caught by the founder or by a gate.** (1) I claimed the workspace didn't populate on dev. WRONG — I was loading `127.0.0.1` while Next serves `localhost`, and it blocks `/_next/*` cross-origin, so client chunks never load, React never hydrates, and every surface paints its SSR loading state forever *while curl still returns 200*. Fixed at the root: `allowedDevOrigins` + a `THALON_DEV_ORIGINS` env hook. (2) I claimed there was no logo in the repo. FLATLY WRONG — the founder asked "isn't there a logo on the landing page, and on the tab icon too?" and there is: `app/icon.svg` + `favicon.ico`, and his Grip II pick in `components/brand/marks.tsx`, already on the landing header/footer, hero, sticky CTA and `/brand`. My search looked only in `public/` for names containing logo/mark/wordmark. **The lesson worth keeping: a filename grep is not an existence proof — search by ROLE (what renders the brand?) not by hoped-for filename.**

**NEW THIS SESSION — the screenshot-vs-sheet gate is now a script that RUNS** (`scripts/shoot-surface.mjs`, AGENTS.md rule 8). Three waves and ~20 surfaces had been gated by hand. It shoots the app route and its sheet at the SAME geometry, parsed from `mock-sheets/theme.css`'s own `.screen` rule so it can never drift, in both themes — and it **refuses to call a shot green until the surface is ready**, which is precisely what caught the 127.0.0.1 bug: its first run captured "Reading the runs…" under a "No tenant" topbar and would have exited 0.

**s76 SECOND HALF (Fable 5, founder-directed for the planning):** the founder verdicted the thumbnail work as needing a *sustainable* schema+framework ("dynamically change based on the sources and content"), so the s77 plan was re-planned as ONE framework and written to `docs/research/media-framework-plan.md`: the `MediaRef` contract (external|stored × captured/derived/operator), the pure per-entity resolver with declared no-borrowing chains (the Approve ruling made executable), two serving doors, dynamism = three write moments, `<SourceThumb>` off the verdicted sheet, the zero-state layer kept as a SEPARATE question, and B-audio.1's bed as the same stored-ref shape. Ground-truthed in code: both meta columns are jsonb so **the whole window is contracts-only, zero SQL migrations**; `video-title.ts:43` really does discard the oEmbed dims; **ffmpeg is on the box but NOT in Dockerfile.web** (derivation gates on binary presence; founder call filed). Three small founder calls filed in NEEDS-STEVEN (plate remints ≤1.5cr · ffmpeg-in-image · zero-state sheets); none blocks the start.

**s76 CLOSE — the three rulings landed and were executed same turn:** remint GRANTED then superseded by his own "your choice" delegation — path chosen = **code-drawn token-native zero-state art**, and the evidence SHIPPED: `docs/research/zero-art/zero-art-proposal.html` (ten mono-stroke plates — his eight subjects + Calendar/Videos as new-surface proofs; identical markup renders dark AND paper, which IS the theme-proof argument; glove + shelves flagged weakest, refine-or-mint per-plate under the standing GO). **ffmpeg into Dockerfile.web = GO** — now an s77 lane-B DO item, binary-presence gate stays as defense in depth. Canvas-sheet question resolved by construction (code plates paste into a dc.html verbatim). **Box hygiene done at his ask:** wave-3 tmux windows killed, worktrees removed, merged lane branches deleted — thalon session is back to `dev` + `agent`; the box's other tmux sessions are OTHER agents' homes, untouched (two of their names are guard tokens — never name them in tracked files, which the guard just proved by catching this very sentence's first draft).

## Resume prompt (session 77, syd4 — "gogogo" boots this)

**Resume · Thalon** — s77 (**IMPLEMENTATION — the founder switches the model BACK TO OPUS 5 for this session; the plan was cut on Fable 5**): (0) self-check (tmux `thalon` · pg_isready · 8899+sweeper units · `dev` 3111 · git status + stamp). (1) **Sweeper morning read — the first one that can actually come back clean.** The s76 backoff is live and verified in the log; YouTube's quota resets at **Pacific** midnight (~07:00Z), so expect a clean `swept — … admitted`. Still 429 past ~08:00Z → read the reason VERBATIM; seven layers peeled, the eighth will be new. (2) **THE HEADLINE — the two builds, in order, per the founder's "have it all planned and ready": ① B-media.0 (he VERDICTED it worth doing) — the CONTRACT WINDOW FIRST, because windows freeze before lanes launch: `meta.posterRef` + oEmbed `thumbnail_width/height` → derived `orientation`, and ride Runs' Retry replay route + Library's "grounds N drafts" on the SAME freeze; then `<SourceThumb>` and migrate the FOUR surfaces already resolving real media. ② B-audio.1 — sound/music in the video editor (founder-directed s76), which is ARMING A SEAM, not a green field: the editor already has `AudioCue`/`music-align`/`patchMusic`/waveform, the engine already has `CompositionAudio`/`RenderAudioBundle`/narration — but `bed` is an admittedly empty seam and the composition is commented "today's silent composition", so nothing is audible and nothing is muxed.** Full plan with the three audio pieces = ROADMAP §S77 PLAN. (3) **Do NOT re-ask:** B-media.0 verdicted · placeholders = his s75 ruling · logo done · remint granted (held as per-plate fallback) · ffmpeg-in-image = GO (DO it, lane B) · zero-state path = code-drawn, proposal shipped awaiting only his GLANCE (not a gate).

▎ ▸ **Read first:** CLAUDE.md → this file → **`docs/research/media-framework-plan.md` (THE plan of record for s77 — §9 is the build order)** → ROADMAP §S77 PLAN (background) → `docs/research/source-media-plan.md` (UI treatment) → mock-sheets README → NEEDS-STEVEN.md → COORDINATION.md.
▎ ▸ **State:** main = origin, all pushed · **16 of 16 sheets rebuilt — the rebuild era's surface work is COMPLETE** · wave-3 branches merged (worktrees still on disk, safe to GC) · gate script live · budget 2M · balance 584.12 · **zero spend s76**.
▎ ▸ **Standing:** stealth · hermes-relay = founder · blanket workspace grant · NEVER filter live gate/suite output (**and never pipe it through `tail`** — write to a file and read it) · ≥40cr mint ping · verify-on-merged-main = THE gate · **REDESIGN ERA MODEL = OPUS 5** · wrap = guard+commit+push+restamp.
▎ ▸ **✅ SAFE TO CLEAR** — nothing in flight; all committed+pushed; every lane closed and merged.

## Pointer

CLAUDE.md → this file → `docs/research/mock-sheets/README.md` → `docs/research/source-media-plan.md` → `docs/research/old-design-keepers.md` → `docs/research/ui-overhaul-plan.md` §5 → COORDINATION.md → NEEDS-STEVEN.md.

## Delta (session 75)

**Wave 2.** Three prepped worktrees launched inside ~3 minutes and ran ~50 minutes each. Merge gate: two merged clean; `estate-rebuild` hit three conflicts, all resolved deliberately. The instructive one was `selected-row.test.ts`, where each lane had removed **its own** entry from the legacy-selection pin — taking either side alone would have silently re-pinned a deleted file, so the correct merge removes both. All six surfaces were then rendered on dev 3111 and diffed against their sheets before the merge stood.

**What the gate surfaced, homed rather than decided unilaterally:** `components/board/leads-board.tsx` was left **orphaned** — `Board.dc.html` is the CONTENT pipeline board drawn under Home, not a leads board, so rebuilt Leads rendered its Board tab and said so plainly. Both lanes found it independently and each correctly declined to touch the other's file. The lead did not delete a working capability on its own authority; **the founder then ruled it comes back**, and step 1 shipped the same session. Also flagged: Calendar clips concurrent same-instant events to ~45px chips (the sheet's own `overflow:hidden` meeting a density its fixture never had), and Sites turned out to be a fourth surface already resolving real media.

**B-media.0, planned research-first** per the founder's "see how others do it". The findings that actually bore on a decision: `.thumb-sm` (64×40) is **exactly** Mux's 256×160 storyboard-tile ratio; a 16:9 poster cover-cropped into it loses ~10% of its width, inside the central-80% safe area creators already design for; a **9:16 Short loses ~65% of its height and gets decapitated**; and hover-scrub is out on structural grounds, not taste — the storyboard artifact requires owning the transcode, and our densest surface holds a YouTube URL and one poster. Reading the code also corrected the premise: **four surfaces already resolve real media**, so this generalises something that works. And `video-title.ts` parses the oEmbed reply for `thumbnail_url` while **discarding `thumbnail_width`/`height` from that same response** — free, and the only thing blocking the portrait decision.

**B-media.1** renamed the surface only (`lib/library/`, `/api/library`, `LibrarySourceRow` keep their names — they serve the generic `sources` shelf), with `/app/library` kept permanently as a redirect and the divergence recorded under a new README section, *Founder amendments — the sheets are WRONG here on purpose*.

**Create's pruning panel** restores the keeper as a state behind the sheet's own chip: the chip text becomes the disclosure, pruning reaches **generation** (not just display) through one `pruneContext()` every downstream consumer already reads, and dropping a field is reversible while dropping the whole chip is not — so the chip renders off the UNPRUNED context and cannot vanish mid-prune.

**Discipline note carried forward.** The lead ran its own full verify while three lane suites were live and drove load to 13. It finished clean, but the s74 sequencing lesson applies to the lead too — the crm lane, which had that lesson in its kickoff, explicitly waited out its siblings and was right to. Both wave-3 kickoffs carry it.

**The founder's Sites findings, diagnosed at the close (both confirmed, neither a guess).** *"there thumbnails seem broken"* — all 20 load ON THE BOX, because `lib/sites/provider.ts` hardcodes `DEV_PREVIEW_ORIGIN = "http://127.0.0.1:8899"` and both the card images and the dossier iframe resolve against it. From any other machine `127.0.0.1` is the VIEWER's loopback, so every one of them breaks. Not a rendering bug, and the images are not missing. `SITES_PREVIEW_ORIGIN` overrides it, but the real fix is same-origin serving (or catalog-time poster capture, which would also feed B-media.0) — the lane picks one and says why. *"clicking on one of them seem to take me to the old design"* — exactly right: `site-dossier.tsx` has not been touched since s61 and still carries 19 bridged tokens (it is pinned at that number). It has NO sheet, so the precedent is Intel's Search tab: DESIGN it in the sheets' language rather than port it. All three jobs are the `sites-deepen` kickoff.

## Next action — s76 (BOOT ON OPUS 5): sweeper morning check (backoff should be visible in the log; quota resets ~07:00Z, PACIFIC midnight) · **LAUNCH WAVE 3 — `videos-rebuild` + `leadboard-wire` + `sites-deepen`, which finishes all 16 sheets and closes the founder's Sites findings** · lead merge-gates each on screenshot-vs-sheet + verify on merged main · lead's own queue = the B-media.0 verdict, the Calendar clipping call, EmptyArt's dark re-cut.
