# Session handoff — CURRENT

> **One file, overwritten at every session wrap (AGENTS.md rule 9).** Pointer + delta + next action only — never a state dump, never a copy of CHARTER/COORDINATION content (link instead). History lives in git.

## Stamp

2026-07-17 (session 52, syd4, full-reign while founder on break) · **WAVE-2 ⑦ VANCE & ALDER SHIPPED** (legal / editorial-print; 5 mints / 4 keepers / 1 reject-with-reasons; portfolio ratchet 5/5) · **→Email FIRST LIVE COMPOSE queued for founder review** (real gateway spend metered; staging seat gap found) · **v7/v8 forensic restore COMPLETE** (WAL-graft + committing-XID-burn out of the torn PGlite dir; v8 replay-verified) · **1:1 v3 RENDERED** (first agent-reframed cut watchable) · leads triage slice prepped · spend **2.48cr** (balance ≈738.1) · guard clean.

## Resume prompt (session 53, syd4 — paste verbatim; "gogogo" boots this too)

> Stamped 2026-07-17, session-52 wrap.

**Resume · Thalon** — session 53, syd4 — **OPENER = founder review round (he returns from break):** (1) present **⑦ Vance & Alder IN MOTION** — `proprietary/templates/sites/vance-alder/` served locally, or the claude-design final "Vance & Alder — commercial law landing (T7)" v2; collect the taste verdict (sequential rule: ⑧ starts only after this review). (2) His one-email review: the composed outreach email WAITS in the dev approve queue (dev server `npm run dev -w @thalon/web`, port 3111, /app — lead "Sione Latu"). (3) His 10-min leads triage off `.context/leads-dogfood/triage-slice-s52.md`. (4) Staging MODEL_DRAFT seat fix (Dokploy console, see NEEDS-STEVEN). Then on his go: **⑧ Crateline (logistics / brutalist primary)** — the proven loop (look-first → claude-design rough scaffold w/ named slots → mint to slots on the routed seats → graduate SAME project to final IN MOTION → self-contained repo build + /guide → ratchet + guard → commit) → ⑨ Wagtail & Co (pets/soft-organic) → ⑩ Hue & Cry (salon/exceptional-palette) → wave-2 checkpoint at slate end.

▎ ▸ **Read first:** `CLAUDE.md` → this file → **`bash scripts/peer-mail-check.sh`** (NEW MAIL → read `agent_handoff/FROM-SWORDFISH.md` → `--ack`) → COORDINATION.md tail (s52 message: taste notes ⑦ + the full session record) → memory `higgsfield-kompozy-assignment` (s52 ledger).

▎ ▸ **State:** main = origin @ s52 wrap commit · wave-2 = ⑥ ⑦ shipped, ⑧ ⑨ ⑩ pending (SEQUENTIAL, founder review between each) · dev Postgres = FULL film history (v6/v7/v8 + scored v1 approved + receipts restored with meta.restored provenance; 1:1 v1/v2/v3 all rendered; 20 dogfood leads + profile v1 + the queued outreach draft) · dev server STOPPED, port lane 3111 · dev MODEL_DRAFT back on claude-cli/sonnet (the dogfood seat-swap recipe: one line in apps/web/.env.local) · torn PGlite dir preserved at `apps/web/.data/pg.damaged-original-2026-07-17` (keep; the s49 music-align eval row is the one true loss, unrecoverable) · claude-design project T7 holds scaffold v1 + alive final v2.

▎ ▸ **s52 ratchets/lessons:** meta-prompt — soul-class invents garbled text UNPROMPTED on printable surfaces; compose them out of frame, never negative-prompt (⑦ desk plate reject) · PGlite forensics — burn XIDs with COMMITTING transactions, never aborting ones (aborts trample clog + poison hint bits); WAL-graft pattern in dev-db memory · compose door: the brief = pruned chips + direction ONLY, chips are not auto-included (judge rightly refuses ungrounded names) · usage_ledger cost_estimate=0 finding · direct psql to dev Postgres works now (s51 hang not reproducible).

▎ ▸ **[founder] queue:** NEEDS-STEVEN.md current (email review · staging seat · leads triage · re-critique · transcript bulk-delete · month-end credit call).

▎ ▸ **Standing:** stealth holds · `[Steven via hermes-relay]` = founder; relay turns end founder-readable · get_cost preflight per mint, ≥40cr = founder ping · declined_preset_id · aspect variants = own-engine recut NEVER vendor reframe · hero slots = best roster image model; text-in-scene = text-precise seat · templates judged IN MOTION · agent/lane launches need fresh founder approval; launch mechanics = lead's (`scripts/launch-lane.sh`) · lead self-drives interactive flows + tmux · dev = real Postgres (PGlite = tests/fallback only; never SIGKILL a server holding a DB).

▎ ▸ **✅ SAFE TO CLEAR.** As of the stamp: no open PRs; no worktrees; no lanes; no Monitors; no in-flight renders or vendor jobs; dev server stopped; guard green at HEAD; wrap ping appended (founder Telegram fires ≤10 min via swordfish-peer-mail.timer).

## Pointer

Read in order: `CLAUDE.md` → this file → COORDINATION.md s52 message → `proprietary/templates/meta-prompt.md` (§Assets: the new printable-surface rule) → `proprietary/templates/sites/vance-alder/` (the ⑦ build) → `agent_handoff/NEEDS-STEVEN.md`.

## Delta (session 52)

- **⑦ Vance & Alder:** look-first (legal genre-default catalogued and avoided; editorial/print + legal-document grammar) → claude-design scaffold v1 → mints (hero nano banana pro 2cr/2k, engraved nameplate perfect first take; 3 soul_2 plates 0.12cr ea; 1 reject: unprompted garbled spine text + a scales statue) → alive final v2 (running head + progress rule, develop-in plates, drawn leaders, refusing redactions, inked signatures; 390px + no-JS + reduced-motion complete) → pinned assets → self-contained repo build (OFL fonts vendored, 272KB derived webps, honest /guide) → ratchet 5/5 + meta-prompt amendment.
- **→Email dogfood:** staging blocked (MODEL_DRAFT json_schema gap → NEEDS-STEVEN); dev on real gateway: compose #1 judge-blocked (invented backstory caught by sonnet-4.5 final — the gate proven), compose #2 with chips queued for founder review; both anthropic seats metered; cost_estimate=0 finding; seat reverted.
- **v7/v8 restore:** recovered from the torn PGlite dir (the restic snapshot predated them — both memories corrected); WAL-graft + committing-XID-burn; v8 replay-verified; rows + receipts + 18 events restored into dev Postgres; eval-row loss documented.
- **1:1 v3:** rendered through the product render door (2m51s, 1080×1080); draft→rendered.
- **Leads:** staging fresh scoring pass (120 scored, 0 real waitlist signups yet); triage slice note written (gitignored).

## Next action

Session 53 — founder review round first (⑦ in motion · the one email · leads triage · staging seat), then **⑧ Crateline (logistics/brutalist)** on his go, sequential through ⑨ ⑩ to the wave-2 checkpoint. Backlog on deck: transcript bulk-delete, s40 re-critique (launch needs approval), staging tenant-pg migration + RLS ratchet, B-crm.4/5 (provider Resend), month-end credit call, B6.7 domain launch, post-wave-2 design phase.
