# CURRENT

## Stamp

2026-07-25/26 (session 75, syd4 — zero credit spend; the session crossed the UTC boundary, so the sweeper's "new day" check is finally live for s76) · **WAVE 2 LANDED AND WAVE 3 IS PREPPED.** Three lanes merged behind the lead's screenshot-vs-sheet gate + a full verify on merged main — **Calendar · Board · Leads · Profiles · Sites · Settings/Integrations** — putting the workspace at **14 of 16 sheets**. Merged-main verify GREEN: **2027 passed / 9 skipped**, 0 lint errors. **The lead's own queue ran beside the lanes:** B-media.0 planned research-first with a proposal sheet and NO code · B-media.1 Library→Transcription shipped · Create's per-field pruning panel built. **Then the founder ruled on all four open items and each was executed the same session:** (1) the leads board is BUILT on the mock's column grammar as **step 1, structure only** (`5408090`), with step 2 prepped as a lane; (2) **thumbnails stay placeholders until B-media is ready** — his call, now written into both lane kickoffs so no lane "helpfully" invents them; (3) the sweeper is sized to **the maximum free YouTube tier**; (4) **Videos is designed exactly as mocked**, which is precisely what makes it delegable to a lane. **THE CORRECTION THAT MATTERS MOST:** the s75 wrap's first diagnosis of the intel soak was WRONG and is now fixed in code. The steady state was never over quota — the schedule's cadence is 240 minutes, not 15; the 15 is the scheduler's *poll tick*. The real defect is that a FAILED sweep deliberately leaves `last_sweep_at` untouched so the tenant stays due, and the scheduler then retries at tick rate, re-spending ~900 quota units each time until the day's 10,000 is gone — after which the 429 is itself a failure, so it retries all day. **Fixed:** geometric backoff on consecutive failing passes (capped at the schedule's own cadence), cadence 240→180m (8 sweeps/day = 7,200 of 10,000), and `tests/sweep-backoff.test.ts` pins both the backoff shape and the quota arithmetic so the lesson RUNS.

## Resume prompt (session 76, syd4 — "gogogo" boots this)

**Resume · Thalon** — s76: (0) self-check (tmux `thalon` · pg_isready · 8899+sweeper units · `dev` 3111 · git status + stamp). (1) **Morning check — it can finally fire, and now it means something.** Read `.context/logs/sweeper.log`. Note **YouTube's quota resets at midnight PACIFIC (~07:00Z), not midnight UTC** — that is why 07-26 still read 429 at 02:00Z. Expect: the backoff line (`backing off — N consecutive failing pass(es), next tick in Nm`) while the quota is still out, then a clean `swept — … admitted` after the reset, and the failure counter resetting itself. If it is still failing after ~08:00Z, read the reason VERBATIM; six layers have been peeled and the seventh will be a new one. (2) **THE HEADLINE — LAUNCH WAVE 3, two lanes, no re-ask** (founder s75: *"prepare for the parallel workflow next session"*): `videos-rebuild` (the last three sheets — Overview → Dossier → editor) and `leadboard-wire` (leads board step 2 + delete the orphaned legacy board). Both worktrees exist at `5408090`, symlinked and smoke-tested. Launch each: `LANE_CLAUDE_ARGS="--model claude-opus-5" scripts/launch-lane.sh <name> .claude/worktrees/<name> agent_handoff/KICKOFF-<name>.md` — ff each worktree to origin/main first if main moved. **That wave finishes all 16 sheets.** (3) **Lead's own queue while they run:** (a) act on the founder's **B-media.0 verdict** if it has landed — plan + join map in `docs/research/source-media-plan.md`, sheet at `mock-sheets/Source Media.dc.html`, contract-window candidates are `meta.posterRef` and capturing oEmbed `thumbnail_width/height` → a derived `orientation`; (b) the **Calendar concurrent-event clipping** call (NEEDS-STEVEN); (c) **EmptyArt** still has not re-entered any rebuilt surface — its plates are light-paper art that reads near-black on dark and need re-cutting. (4) **Do NOT re-ask:** the s74 rulings AND the four s75 rulings above are recorded.

▎ ▸ **Read first:** CLAUDE.md → this file → `docs/research/mock-sheets/README.md` (**two new sections: "Founder amendments" and "Proposals — NOT yet verdicted"**) → `docs/research/source-media-plan.md` → `old-design-keepers.md` → ui-overhaul-plan §5 → COORDINATION.md → NEEDS-STEVEN.md.
▎ ▸ **State:** main = origin, all pushed · **14 of 16 sheets rebuilt** (shell+Dashboard s73; Approve · Intel · Runs · Transcription · Create s74 + Search designed; Calendar · Board · Leads · Profiles · Sites · Integrations s75) · leads board at step 1 (structure, unwired, honest about it) · wave-2 lanes closed and branches deleted · **wave-3 worktrees prepped** (`videos-rebuild`, `leadboard-wire`) · sweeper backoff live and verified in the log · budget 2M · balance 584.12 · **zero spend s75**.
▎ ▸ **Standing:** stealth · hermes-relay = founder · blanket workspace grant · NEVER filter live gate/suite output (**and never pipe it through `tail`** — write to a file and read it) · ≥40cr mint ping · verify-on-merged-main = THE gate · **REDESIGN ERA MODEL = OPUS 5** · wrap = guard+commit+push+restamp.
▎ ▸ **✅ SAFE TO CLEAR** — nothing in flight; all committed+pushed; every lane closed; wave 3 prepped and waiting.

## Pointer

CLAUDE.md → this file → `docs/research/mock-sheets/README.md` → `docs/research/source-media-plan.md` → `docs/research/old-design-keepers.md` → `docs/research/ui-overhaul-plan.md` §5 → COORDINATION.md → NEEDS-STEVEN.md.

## Delta (session 75)

**Wave 2.** Three prepped worktrees launched inside ~3 minutes and ran ~50 minutes each. Merge gate: two merged clean; `estate-rebuild` hit three conflicts, all resolved deliberately. The instructive one was `selected-row.test.ts`, where each lane had removed **its own** entry from the legacy-selection pin — taking either side alone would have silently re-pinned a deleted file, so the correct merge removes both. All six surfaces were then rendered on dev 3111 and diffed against their sheets before the merge stood.

**What the gate surfaced, homed rather than decided unilaterally:** `components/board/leads-board.tsx` was left **orphaned** — `Board.dc.html` is the CONTENT pipeline board drawn under Home, not a leads board, so rebuilt Leads rendered its Board tab and said so plainly. Both lanes found it independently and each correctly declined to touch the other's file. The lead did not delete a working capability on its own authority; **the founder then ruled it comes back**, and step 1 shipped the same session. Also flagged: Calendar clips concurrent same-instant events to ~45px chips (the sheet's own `overflow:hidden` meeting a density its fixture never had), and Sites turned out to be a fourth surface already resolving real media.

**B-media.0, planned research-first** per the founder's "see how others do it". The findings that actually bore on a decision: `.thumb-sm` (64×40) is **exactly** Mux's 256×160 storyboard-tile ratio; a 16:9 poster cover-cropped into it loses ~10% of its width, inside the central-80% safe area creators already design for; a **9:16 Short loses ~65% of its height and gets decapitated**; and hover-scrub is out on structural grounds, not taste — the storyboard artifact requires owning the transcode, and our densest surface holds a YouTube URL and one poster. Reading the code also corrected the premise: **four surfaces already resolve real media**, so this generalises something that works. And `video-title.ts` parses the oEmbed reply for `thumbnail_url` while **discarding `thumbnail_width`/`height` from that same response** — free, and the only thing blocking the portrait decision.

**B-media.1** renamed the surface only (`lib/library/`, `/api/library`, `LibrarySourceRow` keep their names — they serve the generic `sources` shelf), with `/app/library` kept permanently as a redirect and the divergence recorded under a new README section, *Founder amendments — the sheets are WRONG here on purpose*.

**Create's pruning panel** restores the keeper as a state behind the sheet's own chip: the chip text becomes the disclosure, pruning reaches **generation** (not just display) through one `pruneContext()` every downstream consumer already reads, and dropping a field is reversible while dropping the whole chip is not — so the chip renders off the UNPRUNED context and cannot vanish mid-prune.

**Discipline note carried forward.** The lead ran its own full verify while three lane suites were live and drove load to 13. It finished clean, but the s74 sequencing lesson applies to the lead too — the crm lane, which had that lesson in its kickoff, explicitly waited out its siblings and was right to. Both wave-3 kickoffs carry it.

## Next action — s76 (BOOT ON OPUS 5): sweeper morning check (backoff should be visible in the log; quota resets ~07:00Z, PACIFIC midnight) · **LAUNCH WAVE 3 — `videos-rebuild` + `leadboard-wire`, which finishes all 16 sheets** · lead merge-gates each on screenshot-vs-sheet + verify on merged main · lead's own queue = the B-media.0 verdict, the Calendar clipping call, EmptyArt's dark re-cut.
