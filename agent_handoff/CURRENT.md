# Session handoff — CURRENT

> **One file, overwritten at every session wrap (AGENTS.md rule 9).** Pointer + delta + next action only — never a state dump, never a copy of CHARTER/COORDINATION content (link instead). History lives in git.

## Stamp

2026-07-18 (session 61 FULL close, syd4 — founder live throughout: verdicts, corrections, grants, product direction) · **FIVE ships in one session:** ⑭ Sparkwright (`c798a28`) + its picture-card fix round (`3a30b7d`) · repo hygiene pass (`aca0f62`) · **Phase-I contract window MERGED (PR #59, migration 0015 — contract RE-FROZEN)** · **W-SITES SHIPPED (`84473f8`) — the portfolio is IN the workspace** · claude-design loop correction ratcheted (meta-prompt §How-to step 3) · worn-fabric printable-surface corollary ratcheted · B-sitegen input-side direction recorded (sites-plan §6) · spend 2.40cr, balance 712.96 (API verify at next opener) · **GitHub Actions billing DOWN (founder row)** · guard clean at every commit.

## Resume prompt (session 62, syd4 — paste verbatim; "gogogo" boots this too)

> Stamped 2026-07-18, s61 full close. **The box auto-reboots 18:30 UTC 2026-07-18** — after it, tmux + Postgres auto-start but 8899 does NOT (relaunch below). **W-AUDIT IS PRE-CONFIRMED to auto-launch this session** — no ask needed. GitHub Actions is billing-dead BY FOUNDER DECISION until the monthly renewal (no top-up) — verify every push locally (suite + guard); staging + templates images cannot rebuild until then.

**Resume · Thalon** — session 62, syd4:

0. **Self-check:** `tmux attach -A -t thalon` · `pg_isready` · 8899 up (relaunch: `setsid nohup python3 scripts/preview-server.py 8899 &`) · `git status` + this stamp · balance API-verify (expect ≈712.96).
1. **Opener:** peer-mail — open tails: swordfish step-8 dump confirm · basicauth pair. Check NEEDS-STEVEN: has the GitHub monthly renewal landed? (if yes: confirm the full matrix re-ran green on main + staging auto-redeploys resume) · Phase I glance · Sites/⑭-fix glances · W-sites console tail (Dokploy service + TEMPLATES_PREVIEW_ARMED) · credit call · live-send GO.
2. **Main line (pre-confirmed): W-AUDIT** — (a) **DONE-DIAGNOSED s61: dev has the film registered + playable (58 takes/8 cuts, media route 200); staging import filed with swordfish (ASK-BACKS s61) — close on their confirm** · (b) Source-Link Rule sweep + thumbnails (intel rising rows, library sources.uri surfaced, lineage everywhere) · (c) full `/impeccable audit` over every workspace surface · (d) the s40 re-critique · (e) storage-story audit → FEATURE-MAP. The Phase-I window's five doors are OPEN for any UI the audit fixes (planned slots, lead stage, captures read, capture lineage, saved views — repos merged, UI wiring = fair game under the founder's blanket workspace grant).
3. **Interleave:** **⑮ Stem & Vow — loop stages 1–2 DONE s61** (look-first taste notes on the board; claude-design scaffold v1 render-verified in project "Stem & Vow — wedding florist (T15)": the 72-hour countdown, cold open, focus-pull grammar, halation arc, bloom triptych, +1-stem wink). **⑮ SHIPPED s61 (`53857f2`, 16 sites — the FIRST full-loop site; awaiting verdict). s62 interleave = ⑯ Ridge & Valley (roofing; brutalist-raw 2nd × cinematic; Colorbond/terracotta/storm palette) on the same full loop.** Mind worn-fabric/knit + grade-stamps + count-anchor (flowers AND tools) on every mint.
4. **Standing tails:** B-sitegen charter candidate (checkpoint row 4 — input side expanded s61: URL-DNA · template tier · metadata block · capture autopopulate; sites-plan §6) · live-send GO + stealth pick · credit call.

▎ ▸ **Read first:** `CLAUDE.md` → this file → COORDINATION.md (the TWO s61 messages) → `agent_handoff/NEEDS-STEVEN.md`.

▎ ▸ **State:** main = origin @ s61 close (`84473f8` tip + wrap docs; no PRs; no worktrees; no lanes; 1 tmux window) · **migrations through 0015, contract frozen** (dev PG applies on next app open; staging applies when Actions billing returns and the image builds) · portfolio: 16 sites (14 approved + ⑭ fix awaiting + ⑮ awaiting) · workspace: Phase I + **Sites surface live** (gallery + dossier; dev reads local dir; staging needs the founder's Dokploy service) · 8899 up (dies at the 18:30 reboot) · balance 712.96 · repo suite 1554/1554 local.

▎ ▸ **[founder] queue (NEEDS-STEVEN.md):** (Actions = wait-for-renewal, decided) · Sites + ⑭-fix glances · Phase I glance · W-sites console tail (Dokploy + arm flag) · credit call · live-send GO + stealth pick.

▎ ▸ **Standing:** stealth holds · `[Steven via hermes-relay]` = founder; relay turns end founder-readable · **founder blanket grant (s61): workspace-scope work needs NO per-item approval — he reviews by pass; lanes/subagents still announce** · **when the founder appears live, state the open founder-calls FIRST** · get_cost preflight per mint · hero = best roster model · text-in-scene = text-precise seat · printable surfaces compose OUT (machine bodies · vintage livery · worn fabric→KNIT · timber GRADE STAMPS · breaker faces; count-anchor probes/tools too) · casting archetypes + nature-vs-dread · per-template loop = claude-design scaffold + alive final (§How-to step 3) · design authored by Fable 5 directly · at wrap: guard + commit + push (+ LOCAL verify while Actions is dead).

▎ ▸ **✅ SAFE TO CLEAR.** As of the stamp: no open PRs; no worktrees; no lanes; no in-flight renders/vendor jobs; throwaway 3111 dev server killed (8899 untouched); guard green at HEAD; CI matrix pending Actions billing (all commits locally verified); the 18:30 reboot is expected and survivable.

## Pointer

Read in order: `CLAUDE.md` → this file → COORDINATION.md (both s61 messages) → `agent_handoff/NEEDS-STEVEN.md`.

## Delta (session 61, full)

**First half:** ⑭ Sparkwright shipped (re-energize scene; 4 keepers / 9 takes 1.08cr; worn-fabric ratchet; export `position` field) + the claude-design loop correction (founder-caught: the per-template loop includes claude-design for mock AND assembly; drifted s53–54; ratcheted into §How-to step 3). **Second half (founder live — verdicts + grants in one round):** ⑭ "overall good" → picture-card fix round shipped (1.32cr; grade-stamp + breaker-face + three-probe lessons; two disclosed blurs) · "repo is a mess" → subagent audit → hygiene pass shipped · three gates opened by AskUserQuestion (W-sites straight-to-build · contract window GO · W-audit auto-launch) + a blanket workspace grant ("anything in the workspace is approved, just do it") · contract window built + merged (billing-dead CI → full local verify + admin bypass, s50 precedent) · W-sites built + browser-passed + shipped · B-sitegen input-side product direction recorded (sites-plan §6). Process lesson banked in ledger-discipline memory: founder-live ⇒ open calls stated first.

## Next action

**s62 opener: self-check (8899 relaunch post-reboot; billing check) → peer-mail (watch for the swordfish FILM-IMPORT confirm — closes W-audit (a)) → W-audit (b)–(e) (source-link sweep + thumbnails · full /impeccable · s40 re-critique · storage-story) → ⑮ stage 3: the ALIVE final in claude-design (wire focus pulls + halation), then mints (8 slots ≈1.1–1.3cr) + repo landing. Film is DEV-registered already (verified s61); staging rides the swordfish ask.**
