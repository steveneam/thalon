# CURRENT

## Stamp

2026-08-02 (session 95, syd4 — **zero credit spend, zero posts, zero founder
hands**). The s94-stamped plan's **Phase 1 + 2 ran exactly as written**: the
four-sheet design block drawn lead-direct (Fable 5), synced to canvas
`f5d304cb` (zero parity debt), and the **ONE W3 verdict ask sent** (wrap
ping, a single texted word). Every surface opened with a fresh Mobbin pull —
the s94 MCP error did not recur; every banked reference set HELD (the week
pull re-surfaced the banked Later screen itself top-ranked), with one genuine
addition (Resend's revert confirm) and one question closed negative (month-
cell media: nobody draws it honestly → thumbs stay week+agenda). Verify on
main at the wrap: **exit 0, 3265 passed / 9 skipped, 357 files** — the
literal `npm run verify`, single-lane, no dev server.

## WHAT WAS DRAWN (all four on canvas + in mock-sheets/, headers carry the records)

**VIDEOS OVERVIEW:** state pill ONTO the thumb (VEED) with the s94
opaque-backing adaptation drawn INTO the sheet; "no preview yet" in words on
the composing card; family line = takes · cuts · platforms from the
project's own rows (Riverside — the first mechanical mapping contradicted
the Dossier's 9-takes fixture, caught in review); kind token (ClickUp); the
still sheds its lying 0:00; Loom metrics triplet = REJECT-until-D2.

**VIDEO DOSSIER — V1 DRAWN:** the five no-affordance jobs get homes, four as
ONE version rail — crumb selector (Synthesia) · marked/named cuts
("✓ Founder pick") + ☆ Mark door (Adobe) · published pill · compare radios
armed (AI Studio) · delete confirm OPEN in the Fibery/Resend register
("takes, credits and judge verdicts stay in Cut history; Restore brings it
back exactly") · the Takes audition band (play doors; auditioning shows
scrub + Swap-into-cut). First render CRUSHED the strip (pill wrapped
vertical, door chips clipped) — the browser pass caught it; chips slimmed.

**VIDEOS EDITOR p1:** frame-thumbnail blocks (stripes per rule 2, ring
selection) + kind-coloured tracks with sheet-local tokens (NOT ok/warn) +
kind dots + credit badges at metered verbs (Recut ⚡9 · Retake ⚡12 ·
Propose ⚡1; EDL-only verbs unbadged). Script-first (V3) deliberately
undrawn — pass-2 gate.

**SCHEDULE S1–S4:** S1 was ALREADY drawn s86 (the ledger's PENDING rows were
stale — flipped); density ruling closed NEGATIVE (thumbs week+agenda only).
S2 = the month-mark grammar RECORDED in the amendment block. S3 DRAWN — the
Thu 11:00 concurrent cluster (cap 2 + count-door; first placement sat under
the sheet's own open modal — moved; census → Published · 6). S4 done:
`Calendar.dc.html` → `archive/`, and TWO ratchets fired on the move (spec
path citation + the archive-citation ratchet, which bans even the literal
filename in live code) — spec repointed, three code comments reworded.
Build gaps named for Phase 3: CalEvent has no media (serializer widening) ·
`placeColumn` splits concurrent events with NO cap.

## Resume prompt (session 96, syd4)

**Resume · Thalon** — s95 drew the four sheets and asked the W3 verdict.
**ONE thing is awaited: his texted word on W3.**

1. **If W3 is approved (or arrives mid-session): Phase 3 builds, in order:**
   (a) editor p1 deltas (V2 thumbs/track-colour + V4 credit badges; re-baseline
   the render-gate screenshot after); (b) Overview/Dossier deltas + V1's five
   affordances — **Decision-5 pre-check FIRST**: named-variant + delete/restore
   must come from `videoProjects`/`videoTakes`/`videoCuts` rows
   (`packages/db/src/schema/video.ts`, repos beside it) — a missing column is a
   contract-window ask, flagged, never built around; cuts routes exist
   (`apps/web/src/app/api/videos/[projectId]/cuts/…`); (c) Schedule deltas —
   event-media read widening `(new, serializer-only)` on
   `fetchPlan`/`fetchQueueRows` + the `placeColumn` cap (2 per instant +
   count-door; the model's "split evenly" comment is SUPERSEDED, see the
   sheet header). Lanes possible at (b)/(c) — each launch needs fresh
   approval; Mode B default.
2. **If he names changes:** apply to the sheets first (canvas + local),
   re-ask only if the change is structural.
3. **Interleavable, its own gate:** B-create.5 dogfood (first real Create
   run, bluesky) — needs a spend statement + his GO. Phase 4 (V3
   script-first gate · pass-3 states) queues behind Phase 3.

**Read first:** CLAUDE.md → this file → COORDINATION §s95 →
`docs/video-arc/spec.md` (§s95 plan + Phase-1 record) →
`docs/schedule-refinement/spec.md` (§execution record) → the four sheet
headers in `docs/research/mock-sheets/` → runway §9.

0. **Self-check** — tmux `thalon` · `pg_isready` · both user units (needs
   `XDG_RUNTIME_DIR=/run/user/$(id -u)`) · `git status` + this stamp · `npm run
   doctor` · `bash ~/work/swordfish/provisioning/checks/needs-steven-hygiene.sh`.

▎ ▸ **⛔ HIS s90 CONSTRAINT: no console visits this week** (dated 2026-08-01 —
re-test, don't assume expired). Console batch stays PARKED on NEEDS-STEVEN.
▎ ▸ **Design work = Fable 5 direct, never delegated** (standing s51). Sheet
changes on a W3 revision are the lead's own hands.
▎ ▸ **The wizard's Generate spends when clicked** — door-gated, but a LIVE
click is a real run. **Zero credits spent to date.**
▎ ▸ **The s90 windows stay frozen** — s95 touched neither packages/db nor
packages/contracts (two comment-only test/code edits aside, no behavior);
Phase 3's Decision-5 flag path is the only sanctioned opener.
▎ ▸ **impeccable hook notes:** schedule.css findings (font-size ×3 + one
scrim oklch) = pre-existing SHEET-VERBATIM ports (DOCTRINE 0), intentional,
not suppressed — same classification as s94's sites.css.
▎ ▸ **Traps worth keeping:** the ARCHIVE ratchet bans the literal filename of
an archived sheet in live code — reword history comments, don't cite
(`tests/repo-hygiene.test.ts`) · spec-ground-truth re-checks every cited
path after ANY file move · Bash cwd PERSISTS (`git -C`/absolute paths) ·
the RAIL hard-codes label lists · alpha-tint pills illegible over photos
(opaque composite = the pattern, now IN the Overview sheet) · vitest doesn't
typecheck (`npx tsc --noEmit -p apps/web`) · Mobbin MCP errors are
transient — retry in-session · canvas writes need `finalize_plan` first
(plan_token), full UUID `f5d304cb-cd0e-484d-8542-7b6561e1ef30`.
▎ ▸ **⛔ SEQUENCE GATE unchanged AND executable:** post/page generation 409s
at `POST /api/create` until his GO (flip `lib/create/families.ts`); bluesky
armed for testing on his recorded words; queue consumer's key EMPTY; youtube
CANNOT arm. **Nothing posted. Zero credits spent.**
▎ ▸ **Standing:** stealth · hermes-relay = founder · every lane/subagent
launch needs fresh founder approval · GATE ON EXIT CODE · verify-on-main =
THE gate · rules 10/11/12 · platform logins live durably in `.context` · no
AGPL embedded · wrap = verify+commit+push+restamp.
▎ ▸ **State:** main = origin (this wrap) · staging rolls s93–s95 with the
next auto-deploy · four social channels connected · dev PG live · edge auth
creds = `.context/staging-secrets-from-swordfish.md` · 8899 preview +
sweeper user units keep running — NEVER hand-start the sweeper.
▎ ▸ **✅ SAFE TO CLEAR** — nothing in flight; no lanes; tree clean and
pushed; s96 boots on "gogogo" alone (this file + the two specs + the four
sheet headers carry the whole state; the only open item is his W3 word).

## Pointer

CLAUDE.md → this file → COORDINATION.md (§s95) → `docs/video-arc/spec.md` →
`docs/schedule-refinement/spec.md` → `docs/research/ux-refinement-program.md`
→ `docs/research/mock-sheets/README.md` → NEEDS-STEVEN.md (PARKED header) →
`docs/research/prior-art-portal-automation-s84.md` (BEFORE ANY PORTAL WORK).

## Delta (session 94)

s94 stamped the plan; s95 spent it exactly — and the browser pass earned its
keep again (the Dossier strip crush; the S3 cluster hidden under the sheet's
own modal), while TWO executable ratchets caught the archive move's loose
ends the same hour they were made (the spec citation + the live-code
filename ban). The re-grounding habit paid a third time: the ledger's
§Schedule PENDING rows were stale against the sheet's own s86 pass-1 record
— the sheet had already taken Later's thumbs; only the BUILD lags. What
remains: his W3 word → Phase 3 builds → dogfood → Phase 4.
