# Session handoff — CURRENT

> **One file, overwritten at every session wrap (AGENTS.md rule 9).** Pointer + delta + next action only — never a state dump, never a copy of CHARTER/COORDINATION content (link instead). History lives in git.

## Stamp

2026-07-17 (session 52, syd4 — wrap after a LIVE founder review round) · **⑦ Vance & Alder SHIPPED + founder-approved (contrast darkened ×2 to .92 ink)** · **first outreach email APPROVED through the product door; prompt ratcheted v2→v3 (walkthrough close + web-survey rules)** · **outreach survey filed incl. AU Spam Act send-path gates** · **triage: 8 pinned / 96 dismissed / 16 active (104 lead_triage eval rows banked)** · v7/v8 restore + 1:1 v3 render + →Email dogfood all done earlier in-session · spend 2.48cr (balance ≈738.1) · guard clean.

## Resume prompt (session 53, syd4 — paste verbatim; "gogogo" boots this too)

> Stamped 2026-07-17, session-52 wrap. **FOUNDER BATCH-APPROVED THIS WHOLE SLATE LIVE, including "divide the work and workflow in parallel as you see fit" — parallel lane launches for the named items below are PRE-AUTHORIZED (that grant covers exactly this slate; anything new = ask).**

**Resume · Thalon** — session 53, syd4 — three tracks:

**LEAD (design is never delegated — Fable 5 authors directly):**
1. **ORCHARD HOUSE** — the founder-directed seasons-tree site (vertical: food / seasonal kitchen, his food/cafés preference; concept in COORDINATION s52-close). Hero tree right-of-center; scroll drives autumn→winter→spring→summer; tree/leaf state changes per season (hybrid: minted painterly backdrops + code-drawn physics leaves — leaves FALL in autumn, snow settles in winter, blossom pops, canopy fills). The proven loop: look-first → claude-design scaffold w/ named slots → mints (hero best-model; text-precise for any text-in-scene; printable surfaces composed out of frame) → graduate SAME project IN MOTION → self-contained `sites/orchard-house/` + /guide → ratchet+guard+commit. **Founder review, then:**
2. **⑧ CRATELINE** (logistics / brutalist primary) — same loop, then review.
3. **Fillers between mint/render waits:** staging smoke compose the moment the env lands (watch FROM-SWORDFISH; seat = openai/gpt-5-mini, dev-verified) · **transcript bulk-delete pass (founder GO)** — his duplicate transcript rows via the library bulk-delete door · **phone scripts (founder ask)**: one call-script template ratcheted from `docs/research/outreach-email-patterns.md` (calling beats email for local SMBs) + 8 instantiations for the pinned leads → gitignored `.context/leads-dogfood/call-scripts-s53.md` · **B-crm.4 back-half PROPOSAL** (doc only, build waits): Resend send path w/ the AU Spam Act door invariants (consent-basis field, sender ID, unsubscribe) + cadence design (4–7 touches D0/3/10/17, ≤50/batch, Wed sends).

**LANE A (worktree via `scripts/launch-lane.sh`, pre-authorized): B-crm.5 scoring learn loop** — consume the 104 banked lead_triage eval rows: Beta-posterior + Wilson lower-bound weight updates per `docs/research/lead-scoring-algorithms.md`; additive only; **migration number RESERVED = 0012**; eval rows as test fixtures; lead reviews + merges.

**LANE B (worktree, pre-authorized): RLS ratchet + tenant-pg migration prep** — packages/db RLS policies + tenancy-isolation tests + the staging PGlite→syd2-tenant-PG data-migration script (creds `.context`/`.env.tenant-pg`, s28 memory); **migration number RESERVED = 0013**; the staging DATABASE_URL flip is env-door-gated (swordfish/console) — prep TO the door, file the ASK-BACKS coordination note; lead merges.

▎ ▸ **Read first:** `CLAUDE.md` → this file → **`bash scripts/peer-mail-check.sh`** (NEW MAIL → `agent_handoff/FROM-SWORDFISH.md` → `--ack`; expect the staging env-edit reply) → COORDINATION.md tail (s52 messages: verdict record + division of work) → memories `higgsfield-kompozy-assignment` + `crm-lead-scoring-candidate` (s52 entries).

▎ ▸ **State:** main = origin @ s52-close commits · wave-2: ⑥ ⑦ shipped+approved; ⑧ next; ⑨ ⑩ pending · Orchard House = founder-inserted site (otherworldly-animation primary / cinematic secondary) · outreach prompt = **v3** · staging: 8 pinned / 16 active leads; env edit PENDING at swordfish (ask filed; API base = deploy.swordfish.cfd; box Dokploy key is pre-rotation — env stays console-only) · dev Postgres carries full film history + queued-approved email + one blocked test draft (dogfood record) · dev server STOPPED, lane 3111 · static server 8899 serves `sites/` (inherited, useful for reviews) · claude-design projects: T6 Houselights, T7 Vance & Alder.

▎ ▸ **s52 ratchets/lessons (full list in COORDINATION):** soul-class invents text unprompted → compose printable surfaces out of frame (meta-prompt) · committing-XID-burn never aborting (PGlite forensics) · compose brief = chips + direction ONLY · AU Spam Act invariants recorded for B-crm.4 · migration-number pre-assignment for parallel lanes (0012/0013 reserved) · founder standing: template multiplication within a vertical = FOOD/CAFÉS first.

▎ ▸ **[founder] queue (NEEDS-STEVEN.md):** staging env two-liner (if swordfish hasn't landed it) · s40 re-critique launch approval · month-end credit call · B6.7 domains (all NOT in the s53 grant).

▎ ▸ **Standing:** stealth holds · `[Steven via hermes-relay]` = founder; relay turns end founder-readable · get_cost preflight per mint, ≥40cr = ping · hero = best roster model; text-in-scene = text-precise seat · templates judged IN MOTION · aspect variants = own-engine recut · lead drives lanes/tmux/interactive flows · dev = real Postgres · **at wrap: "message me when you're done" = append `## Wrap ping for the founder (s53)` to ASK-BACKS-FOR-SWORDFISH.md + guard + commit + push (Telegram ≤10 min).**

▎ ▸ **✅ SAFE TO CLEAR.** As of the stamp: no open PRs; no worktrees; no lanes; no Monitors; no in-flight renders/vendor jobs; dev server stopped; guard green at HEAD; CI green through the wave-2 ⑦ run (later doc/prompt commits ride the same checks).

## Pointer

Read in order: `CLAUDE.md` → this file → COORDINATION.md s52 messages (esp. the verdict record + division) → `docs/research/outreach-email-patterns.md` → `proprietary/templates/meta-prompt.md` → `proprietary/templates/sites/vance-alder/` + `houselights/` (reference builds) → `docs/research/lead-scoring-algorithms.md` (Lane A shape).

## Delta (session 52 — see COORDINATION for the full record)

- ⑦ shipped + founder-approved (two contrast rounds live) · first email approved (actor-attributed) · prompt v2 (walkthrough close) → v3 (web-survey rules) · outreach survey doc incl. AU Spam Act gates · triage applied (8 pins / 96 dismissed; Riley unpin→re-pin on founder correction) · staging seats verdict = openai/gpt-5-mini (dev-verified; env change with swordfish) · v7/v8 forensic restore · 1:1 v3 rendered · leads slice → founder reviewed live.

## Next action

Session 53 = the three tracks above, in parallel per the founder's grant. Open with peer-mail check (staging env reply likely) → prep both lane worktrees + kickoffs → launch lanes → lead starts the Orchard House look-first study. **At wrap: Telegram ping (the founder asked again explicitly).** Founder-gated backlog unchanged: re-critique, credit call, B6.7 domains, ⑨ ⑩, post-wave-2 design phase.
