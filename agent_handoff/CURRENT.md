# Session handoff — CURRENT

> **One file, overwritten at every session wrap (AGENTS.md rule 9).** Pointer + delta + next action only — never a state dump, never a copy of CHARTER/COORDINATION content (link instead). History lives in git.

## Stamp

2026-07-14 (session 34, syd4) · **WAVE-1 ② TRUEBORE PLUMBING SHIPPED, BROWSER-VERIFIED LOCALLY (`6d47002`).** First single-file site on the s33 scaffolding: physics & interaction (drag-to-close valve stops a live leak; headline surrenders; Matter.js parts bin of six minted sprites with impact dust) on brutalist concrete/ink/safety-yellow — blue only ever means water. 3 two-lane passes in `/guide`, verified 390/768/1024/1440 + reduced-motion + no-script + keyboard. Spend 6.72cr (ledger §s34 in b7.2-shot-list; **vendor background-remover = 1.0cr/image, budget it for sprite work**). Suite **1138/3/0** · lint 0 errors · guard clean.

## Resume prompt (session 35, syd4 — paste verbatim; "gogogo" boots this too)

> Stamped 2026-07-14, session-34 wrap.

**Resume · Thalon** — session 35, syd4 — **mint-week step 2 continues: template ③ Ember & Rye (wood-fired neighborhood restaurant; primary cinematic imagery, secondary editorial/print) through the factory meta-prompt on the s33 scaffolding.**

▎ ▸ **Read first:** `CLAUDE.md` → this file → COORDINATION.md session-32/33/34 messages → `proprietary/templates/README.md` + `meta-prompt.md` + `iteration-pass-checklist.md`.

▎ ▸ **State:** main = origin @ `6d47002` · suite **1138/3/0** · guard clean · credits **1001.12**/`plus` (video pool untouched) · pinned assets now 25 in syd4 `.data/objects/` (single copy — don't clean) · Loopwell + TrueBore verified locally (preview service PARKED as outreach-only, founder call) · headless chrome LIVE (`~/.cache/ms-playwright/chromium-1228/chrome-linux64/chrome`; scroll at human speed before shots — IO races and rAF-frozen screenshots mislead) · leads triage + →Email dogfood + first LIVE compose spend check still pending · impeccable re-critique still QUEUED on founder go.

▎ ▸ **Session-35 plan:** **③ Ember & Rye via the meta-prompt verbatim** — the model-roster site: cinematic stills are the page (soul/seedance class per slot via `models_explore`; nano banana for any text/menu-precise graphics; video-class stays a deliberate draw — batch-estimate before any [VID] mint). Editorial/print secondary (grid-broken magazine layout, long-form confidence). Single-file default; ≥3 two-lane passes; local browser-verify; mint → pin → manifest → export chain per contract (cutouts only if sprites needed — remember 1.0cr each). **(2) If time: ④ Northpace** (otherworldly animation + novel type). Sequential lead-inline default; **any lane/subagent fan-out needs fresh founder go**. Interleave when blocked: dashboard v3 (`workspace-ux-v2.md` §10) or P0.4/P0.5.

▎ ▸ **[founder] queue:** go/no-go on the impeccable re-critique agents · triage the staged leads queue · dogfood the →Email exit · month-end downgrade decision rides the §6 credit re-assess · preview-gallery service stays parked until outreach wants links.

▎ ▸ **Next checkpoint (B-crm.3–6) additions unchanged:** Postgres migration BEFORE triage/eval rows accumulate (then RLS ratchet) · B-crm.5 shortlist + `matchTerm` word-boundary · B-crm.3 enrichment provider · B-crm.4 Resend timing · approve/reject undo contract change.

▎ ▸ **Standing:** stealth holds (neutral names, no real domain; template pages disclose fictional+AI on `/guide`) · founder `.env.local` edits arrive BOM+CRLF — normalize · `[Steven via hermes-relay]` = founder; relay turns end founder-readable · `.env.tenant-pg` syd2-only · Dokploy keys CI-secret-only · template sites are impeccable-ignored (`.impeccable/config.json`) · physics engines: matter-js is the 2D standard (vendored, eslint-ignored under `js/vendor/`); Jolt parked for ⑤ Hartline's 3D axis, Rapier2D parked as 2D upgrade.

▎ ▸ **✅ SAFE TO CLEAR.** As of the stamp: all session-34 commits pushed; CI green expected (guard/test/eval/infra + templates-image build+smoke, deploy dormant by design); no open PRs; no worktrees; no dev servers (session http server stopped); no mid-edit state; suite/lint/guard green at HEAD.

## Pointer

Read in order: `CLAUDE.md` → this file → `COORDINATION.md` (Sprint-7 board + session-32/33/34 messages) → `proprietary/templates/README.md` + `meta-prompt.md` + `iteration-pass-checklist.md` → `proprietary/prompts/b7.2-shot-list.md` (ledgers).

## Delta (session 34)

- **② TrueBore shipped** (`6d47002`): single-file trades site, physics axis — live leak + drag/keyboard valve with honest demo gauge, surrendering headline, Matter.js parts bin (6 minted sprites, material-truth masses, hit-tested drag, impact dust), scroll-filled protocol pipe, sample-labeled rate card. Blue = water only.
- **Asset-chain precedent for sprites:** mint → pin original → vendor `remove_background` (**1.0cr/image**) → pin cutout with `derived_from_content_hash` → manifest points at cutout → alpha-webp export. Content boxes measured (sharp trim) and baked into the site's physics metadata.
- **Scaffolding amendment:** eslint ignores `proprietary/templates/sites/**/js/vendor/**` (vendored MIT libs aren't style-linted; contract's vendor rule made executable-compatible).
- **Founder Q&A recorded:** engine = liabru/matter-js; JoltPhysics = parked for the 3D-axis site (⑤), Rapier2D = parked 2D upgrade path; neither fits a single-file 2D page today.
- **Verify lesson:** IntersectionObserver-gated systems don't boot under fast programmatic scrolls and rAF doesn't tick during fullPage screenshots — probe with human-speed scrolls + linger before calling something broken.

## Next action

Session 35: **③ Ember & Rye through the factory prompt (cinematic imagery — the model-roster site) → ④ Northpace if time.** Founder at their pace: impeccable re-critique go/no-go · leads triage · →Email dogfood · month-end credit re-assess.
