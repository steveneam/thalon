# CURRENT

## Stamp

2026-08-03 close of session 97 (syd4 — **zero credit spend, zero posts, zero
founder console visits**). Three boundaries, each committed on its own green:
**the `.data` split-brain CLOSED as a workspace ratchet** (`68d9873`) →
**the V3 script-first gate DECIDED — HOLD** (`0731366`) → **V7 render
honesty BUILT** (`1ddcf2c`). Wrap verify on main: **exit 0, 3309 passed / 9
skipped**.

## WHAT SHIPPED (the board §s97 + the spec carry the full records)

**One-root ratchet** — relative `THALON_DATA_DIR` anchors at the WORKSPACE
root (`packages/platform/src/env.ts`, walk-up to `.git`/workspaces
`package.json`; absolute values pass through — Docker `/data`, test
tmpdirs). Three stores merged into repo `.data`; one divergent embedding
dropped on purpose (the s79 heal path re-embeds it as a miss); both stale
pins retired (eval `useWebAppDataDir` + the backfill script's). Ratchets:
`data-root.test.ts` on every verify + doctor's `data-root` row (it found a
THIRD stray store on its first run). **V3 gate** — script-first NOT adopted;
both criteria answered no (record: `docs/video-arc/spec.md` §Phase 4a);
`beats→scene re-render` is NOT built; revisit trigger = the re-brief door
dominating dogfood usage. **V7** — live `Rendering… 1m 04s` from recorded
clocks; done names its real duration; failed names how long it ran;
`recordRender` takes measured `elapsedMs` onto the rendered event.

## Resume prompt (session 98, syd4)

**Resume · Thalon** — Phase 4 is DECIDED+HALF-BUILT; nothing awaits a
verdict. Candidates, no fixed order (re-test at the opener, rule 11):

1. **V9 — the video arc's LAST item:** the render-gate pass across the four
   surfaces (screenshot-vs-sheet at 1440×940 + the refused/empty/loading
   state matrix; drift is a defect). fe-check {mode:"built"} per surface is
   the harness shape.
2. **B-create.5 dogfood** (first real Create run, bluesky) — its own gate:
   needs a spend statement + his GO before any Generate click.
3. **UX programme** — remaining un-passed rows in
   `docs/research/ux-refinement-program.md`.
4. **Simplify candidate** (noted s96): plat-mark paths in THREE surface
   copies (approve/analytics/schedule) → `components/media`.

▎ ▸ **The one FLAGGED contract-window ask (s96, unchanged):** the Dossier's
drawn "Restore brings it back" needs a cut retire/restore column — NOT built
around (s90 freeze); raise at the next window with the founder.
▎ ▸ **His s90 no-console-visits week is OUT OF DATE (dated 2026-08-01) —
re-test at the opener; the console batch stays PARKED on NEEDS-STEVEN.**
▎ ▸ **Design work = Fable 5 direct, never delegated** (standing s51); every
lane/subagent launch needs fresh founder approval; Mode B default.
▎ ▸ **The wizard's Generate spends when clicked** — door-gated; **zero
credits spent to date** (all renders local).
▎ ▸ **Traps worth keeping:** `next dev` at `localhost:3111` (`npm run dev`),
started/stopped in-session · scripts still need `set -a; source
apps/web/.env.local; set +a` for env, but **the store-root pin is RETIRED —
one workspace `.data` root since `68d9873`; NEVER re-pin** (doctor's
data-root row flags any regrown stray) · chip platform words live in
aria-labels — test queries go by role+name · `placeColumn` returns
`{placed, overflow}` · sheet-verbatim CSS = impeccable findings intentional
(DOCTRINE 0) · vitest doesn't typecheck (`npx tsc --noEmit -p apps/web`) ·
canvas writes need `finalize_plan` (UUID
`f5d304cb-cd0e-484d-8542-7b6561e1ef30`) · editor render tests: the primary
button's name is now `Rendering… <elapsed>` — match by `/^Rendering…/`.
▎ ▸ **⛔ SEQUENCE GATE unchanged AND executable:** post/page generation 409s
at `POST /api/create` until his GO (flip `lib/create/families.ts`); bluesky
armed for testing on his recorded words; queue consumer's key EMPTY; youtube
CANNOT arm. **Nothing posted. Zero credits spent.**
▎ ▸ **Standing:** stealth · hermes-relay = founder · GATE ON EXIT CODE ·
verify-on-main = THE gate · rules 10/11/12 · platform logins live durably in
`.context` · no AGPL embedded · wrap = verify+commit+push+restamp.
▎ ▸ **State:** main = origin (this wrap) · staging rolls s93–s97 with the
next auto-deploy · four social channels connected · dev PG live · 8899
preview + sweeper user units keep running — NEVER hand-start the sweeper.
▎ ▸ **✅ SAFE TO CLEAR** — nothing in flight; no lanes; tree clean and
pushed; s98 boots on "gogogo" alone (this file + COORDINATION §s97 + the
video-arc spec carry the whole state).

0. **Self-check** — tmux `thalon` · `pg_isready` · both user units (needs
   `XDG_RUNTIME_DIR=/run/user/$(id -u)`) · `git status` + this stamp · `npm run
   doctor` (data-root row must read ONE store) · `bash
   ~/work/swordfish/provisioning/checks/needs-steven-hygiene.sh`.

## Pointer

CLAUDE.md → this file → COORDINATION.md (§s97) → `docs/video-arc/spec.md`
(§Phase 4a + V7's record) → `docs/research/ux-refinement-program.md` →
`docs/research/mock-sheets/README.md` → NEEDS-STEVEN.md (PARKED header) →
`docs/research/prior-art-portal-automation-s84.md` (BEFORE ANY PORTAL WORK).

## Delta (session 97)

The s96 handoff's three lead-actionable candidates all closed in one
session, in the order the evidence demanded: the split-brain first (it
could have corrupted any later script run — and its doctor row caught a
third store nobody knew about), then the V3 decision on grounded criteria
(HOLD: the timeline work had already fixed everything script-first promised,
and the metered path stays secondary by credit discipline), then V7 as the
natural follow-on (the honesty the decision session found missing — no
elapsed recorded anywhere — became the build). What remains of the video
arc: V9 alone. B-create.5 stays its own gate.
