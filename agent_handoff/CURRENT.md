# CURRENT

## Stamp

2026-07-19 (session 65 close, syd4 — the second-wave session: window 2 → B-pub.2 drivers merged → Integrations charter drafted live with the founder) · **Sprint-8 second wave: contract window 2 FROZEN (PR #64, migration 0017 — social+outreach profile columns, `publishable` capability, SOCIAL_* env pairs, sweep failure door + listAll, `social_publishing` key; the s54 outreach persistence gap found NEVER-CLOSED and closed) · B-pub.2 drivers MERGED (PR #65, first Mode B lane via launch-lane.sh; four drivers DISARMED, zero-live) · Integrations charter DRAFTED w/ the founder's destinations addition (website/blog = optional first-class destination; blog = first live destination candidate) · intel soak REVIVED (was silently dead — persistent `sweeper` tmux window, 4-hourly, ticking) · gateway paid path VERIFIED live · guard incident (lead's own token leak) caught + history rewritten + pre-commit hook ratchet installed · main-was-RED #2 (boundary ratchet) fixed** · balance 706.12 (zero spend s65).

## Resume prompt (session 67, syd4 — "gogogo" boots this)

**Resume · Thalon** — s67: (0) self-check (tmux `thalon` · pg_isready · 8899 · `sweeper` window · `dev` window on port 3111 (relaunch any that died — memory machine-migration) · git status + stamp · balance ≈706.12 · Fable plan resets Jul 24 — pace). (1) Opener: peer-mail LIGHT (swordfish tails: film-import their queue · basicauth pair · systemd-units ask). NO founder calls pending at boot — everything below is pre-approved; his GOs arrive inline (per-platform test posts · the mint plan). (2) **Work straight down THE s67 SLATE in ## Next action below — blog loop → post loop → (deferred) mint plan → B-int.0 window → workspace fixes → eval rows.** Charter = RATIFIED (ADR 0011). Profile v2 ACTIVE (endorsed facts). Approve area rebuilt + verified. Parked: visual arc at 20 sites; the landing gates OAuth-Connect filings.

▎ ▸ **Read first:** CLAUDE.md → this file (the slate at the bottom IS the plan) → COORDINATION.md (s65/s66 records) → NEEDS-STEVEN.md.
▎ ▸ **State:** main = origin @ s66 close (no PRs, no worktrees, no lanes) · migrations through 0017 (live on dev pg) · engine: seam + four drivers DISARMED (no production caller — item 2 builds it) · one-prompt chain for pillar #1 planned in the DB (mint/render deferred by founder call) · sweep scheduler ticking · portfolio 20 sites · balance 706.12.
▎ ▸ **Standing:** stealth · hermes-relay = founder · blanket workspace grant · Mode B lanes on fresh approval · pre-commit guard hook (box-level, untracked — reinstall on fresh clones) · **`npm run verify` = the merge gate** · ≥40cr mint ping · wrap = guard+commit+push+local verify.
▎ ▸ **✅ SAFE TO CLEAR** — nothing in flight; guard green at HEAD; sweeper/dev/8899 are persistent processes, leave them running.

## Pointer

CLAUDE.md → this file → COORDINATION.md (s65 close records) → NEEDS-STEVEN.md → `docs/proposals/2026-07-19-integrations-surface.md` → `WRAP-pub2-drivers.md`.

## Delta (session 65)

Window 2 frozen (PR #64): both profile-config persistence gaps closed (`social` NEW + `outreach` — the s54 gap had never been closed, meaning the send door could not arm for any real tenant), `publishable` capability, ten SOCIAL_* env pairs, sweep `markFailed`/`listAll`, `social_publishing` entitlement key; migration 0017 verified live on dev pg. B-pub.2 merged (PR #65): linkedin/x/facebook drivers + instagram typed text-only refusal + `productionSocialDrivers` assembly, all disarmed, +20 engine tests; three honest gaps reported (refusal wording fixed same session; Little-Format call + version re-verify ride pre-live). Integrations charter drafted (Track B) and amended LIVE with the founder's destinations addition + optionality nuance. Ops: intel soak found dead → persistent scheduler started (swept the overdue tenant immediately); gateway top-up verified with a paid generate probe; stale `.next/types` (retired db-dump route) purged. Discipline: main-was-RED #2 found+fixed (boundary ratchet, comment literal); the lead's own guard incident (client-company tokens in a tracked record, exit code swallowed by `| tail`) — caught, history rewritten under a briefly-lifted protection flag, **pre-commit guard hook installed (lanes inherit it)**.

## Next action — s66 CONTINUED (the session rolled straight on: charter RATIFIED → ADR 0011; approve area rebuilt; pillar #1 opened)

**s66-so-far (founder live):** charter RATIFIED (ADR 0011, all five calls) · **Approve area rebuilt to the founder's direction** (`af9ec4f`, live-verified: newest-first default + sort/filter + exact stamps) · **pillar #1 through the one-prompt door FOUR times — every stop honest**: two correct screen-judge catches (invented build-in-public claim · invented URL; final tier leniently passed both = two eval-row candidates), one real staged-pipeline bug FOUND+FIXED (1-based scene numbering in the scenes/polish prompts vs 0-based sceneIndex — SCENE_INDEXING_RULE in direction/shell/generator.ts), one both-tier catch of an ungrounded meta-claim. MODEL_DRAFT=sonnet-4.5 on dev env now (top-up purpose). Dev server = tmux `dev` (port 3111) · sweeper ticking.

▶▶ **THE s67 SLATE (consolidated at the founder's ask — every item below founder-approved s66; "gogogo" works straight down this list):**

1. **THE BLOG LOOP end-to-end** — pillar #1's ARTICLE as a `web_page` draft through generate→judge→approve→the existing deploy door→`/blog`+RSS on dev (zero credit spend; the ADR-0011 proof-of-product moment). Grounding is ready: profile v2 ACTIVE with the 6 endorsed product facts.
2. **THE POST LOOP** — the production publish caller wiring `productionSocialDrivers` into the publish door, then ONE reviewed test post per platform behind his per-platform GO (LinkedIn first; his token + `ARMED` flag each time; checklist = `WRAP-pub2-drivers.md` incl. the Little-Format call + version-pin re-verify).
3. **MINT/RENDER DEFERRED BY FOUNDER CALL** ("after the post and blog stuff is proven end to end") — only after 1+2: the per-take mint COST PLAN (get_cost each; ≥40cr pings) → his GO → mints + render. The s66 video chain (project/planned takes/EDL/cut) sits safely in the DB meanwhile.
4. **B-int.0 — the Integrations contract window** (charter RATIFIED, ADR 0011): `tenant_credentials` vault table + repos + contracts service/DESTINATIONS registry + card-state vocabulary. Lead-serial; independent of 1–3, interleave as capacity allows. (Founder asked s66: the normal-user unlock = this vault + B-int.2 surface + B-int.3 rewire; drivers/entitlements/protocol already account-agnostic and merged.)
5. **Workspace fixes (founder-flagged s66):** (a) staged-flow detail FAILS TO LOAD for one-prompt storyboard drafts in Approve — operator can't inspect blocked pillar chains; (b) **Settings discoverability** — it EXISTS (`/app/settings`, gear at the rail's foot, icon-only) but the founder couldn't find it: keep the foot gear, ALSO add Settings to the account/avatar menu, review foot-icon prominence; Integrations lands under Settings at B-int.2.
6. **The two judge eval rows** (final-tier leniency: passed the topics-grounded build-in-public claim · passed the invented URL — both correctly refused by the screen tier).

**Founder-side, none blocking:** per-platform live-post GOs when item 2 presents each test post · mint-plan GO at item 3 · swordfish systemd-units ask whenever. Standing: ≥40cr ping · stealth (public /blog stays staging-side until the launch call) · Mode B lanes on fresh approval · `npm run verify` = the merge gate.
