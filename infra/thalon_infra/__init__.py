"""Thalon infrastructure — CDK-Python stacks (B0.5+).

Stacks live in this package, one module per stack, all named Thalon*.
Config comes from cdk.json context / environment via config.py — never
hard-coded account ids or secrets.
"""
