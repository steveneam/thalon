# Archived sheets — reference material, not spec

A sheet lands here when it is SUPERSEDED and **no live surface cites it as its
port source** (grep before moving — `Calendar.dc.html` stayed in the parent dir
for exactly that reason until the Schedule rename rebuild retires it). Archived
sheets are reference/history only: no pass, no verdict, no render gate, no
coverage-ledger row. Founder ruling s87: old workspace design goes to archives
and is not passed through code or verification anymore.

- `Calendar.dc.html` — moved s95. Its hold clause ("stays that code's spec
  until the rebuild lands") expired when Schedule shipped s86 at
  `/app/schedule`; no live surface cites it (the shipped `/app/calendar` it
  specced was deleted at the s86 rebuild). The "+N more" call its README
  exception recorded is now DRAWN in `../Schedule.dc.html` (s95 · S3).
