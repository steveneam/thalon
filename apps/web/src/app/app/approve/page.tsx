import { ApproveQueue } from "@/components/approve/approve-queue";

/** The approve queue inside the workspace shell (B6.2 restyle — the classic 3-zone flow, test-pinned). */
export default function WorkspaceApprovePage() {
  return <ApproveQueue />;
}
