import { Transcription } from "@/components/transcription/transcription";
import "@/components/transcription/transcription.css";

/**
 * Transcription: the exact-mock rebuild of Library.dc.html (DOCTRINE 0) — the
 * grounding shelf. Renamed from "Library" on the founder's s74 direction
 * ("we should really rename it to Transcription"), since the surface serves
 * video_transcript sources only. This is a DELIBERATE DIVERGENCE from every
 * sheet's rail, recorded as a founder amendment in the mock-sheets README —
 * a future lane must not "correct" it back.
 */
export default function TranscriptionPage() {
  return <Transcription />;
}
