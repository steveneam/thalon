import { Runs } from "@/components/runs/runs";
import "@/components/runs/runs.css";

/** Runs: the exact-mock rebuild of Runs.dc.html (DOCTRINE 0) — history, status, receipts. */
export default function RunsPage() {
  return <Runs />;
}
