import { VideosOverview } from "@/components/videos/videos";

/**
 * Videos: the exact-mock rebuild of Videos Overview.dc.html (DOCTRINE 0) —
 * the project grid, every family folded behind one card. STEP 1 is the
 * sheet's own placeholder content; step 2 puts the project read behind
 * these bands.
 */
export default function VideosPage() {
  return <VideosOverview />;
}
