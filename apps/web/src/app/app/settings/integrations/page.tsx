import { Integrations } from "@/components/settings/integrations";

/**
 * Settings → Integrations: the exact-mock rebuild of Integrations.dc.html
 * (DOCTRINE 0) — every destination in one of the engine's honest states.
 * STEP 1 is the sheet's own placeholder content; step 2 puts the engine's
 * card derivation behind these bands.
 */
export default function IntegrationsPage() {
  return <Integrations />;
}
