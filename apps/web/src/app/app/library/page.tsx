import { Library } from "@/components/library/library";
import "@/components/library/library.css";

/**
 * Library: the exact-mock rebuild of Library.dc.html at its s90 W2 amendment
 * (DOCTRINE 0) — the ONE grounding shelf, per the founder's §5.3 ruling
 * ("ONE Library surface; transcription becomes an ingest kind + a filter,
 * not a second route"). This retires the s74 rename: `/app/transcription`
 * is deleted with this rebuild, and the s74 note's own boundary — "revisit
 * the name only if the surface's source kinds widen beyond transcripts" —
 * is exactly what §5.3 did.
 */
export default function LibraryPage() {
  return <Library />;
}
